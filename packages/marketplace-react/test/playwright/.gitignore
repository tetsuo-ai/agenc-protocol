# Playwright deps + run artifacts.
# package-lock.json is intentionally committed for reproducible CI installs.
node_modules/
test-results/
playwright-report/
.cache/

# Runtime harness context written by global-setup — carries a generated buyer
# SECRET KEY. Never commit it.
.playwright-sandbox.json
