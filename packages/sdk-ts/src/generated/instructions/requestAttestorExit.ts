/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const REQUEST_ATTESTOR_EXIT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([94, 3, 20, 231, 175, 66, 101, 44]);

export function getRequestAttestorExitDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REQUEST_ATTESTOR_EXIT_DISCRIMINATOR,
  );
}

export type RequestAttestorExitInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountModerationAttestor extends string | AccountMeta<string> = string,
  TAccountAttestor extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountModerationAttestor extends string
        ? WritableAccount<TAccountModerationAttestor>
        : TAccountModerationAttestor,
      TAccountAttestor extends string
        ? ReadonlySignerAccount<TAccountAttestor> &
            AccountSignerMeta<TAccountAttestor>
        : TAccountAttestor,
      ...TRemainingAccounts,
    ]
  >;

export type RequestAttestorExitInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RequestAttestorExitInstructionDataArgs = {};

export function getRequestAttestorExitInstructionDataEncoder(): FixedSizeEncoder<RequestAttestorExitInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REQUEST_ATTESTOR_EXIT_DISCRIMINATOR,
    }),
  );
}

export function getRequestAttestorExitInstructionDataDecoder(): FixedSizeDecoder<RequestAttestorExitInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRequestAttestorExitInstructionDataCodec(): FixedSizeCodec<
  RequestAttestorExitInstructionDataArgs,
  RequestAttestorExitInstructionData
> {
  return combineCodec(
    getRequestAttestorExitInstructionDataEncoder(),
    getRequestAttestorExitInstructionDataDecoder(),
  );
}

export type RequestAttestorExitInput<
  TAccountModerationAttestor extends string = string,
  TAccountAttestor extends string = string,
> = {
  /**
   * Roster entry to exit. Seeded by its own stored `attestor` (canonical PDA).
   * The exit path is for SELF-REGISTERED entries only (`assigned_by == attestor`):
   * a deputized entry (`assigned_by == authority`, bond 0, rent paid by the
   * authority) is authority-managed and comes off the roster via
   * `revoke_moderation_attestor` (rent → authority), NOT here. Without this scope a
   * deputy could self-remove outside the authority's control and `close = attestor`
   * would redirect the authority-funded rent to the deputy (adversarial finding).
   */
  moderationAttestor: Address<TAccountModerationAttestor>;
  /** Only the attestor itself may start its exit. */
  attestor: TransactionSigner<TAccountAttestor>;
};

export function getRequestAttestorExitInstruction<
  TAccountModerationAttestor extends string,
  TAccountAttestor extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RequestAttestorExitInput<TAccountModerationAttestor, TAccountAttestor>,
  config?: { programAddress?: TProgramAddress },
): RequestAttestorExitInstruction<
  TProgramAddress,
  TAccountModerationAttestor,
  TAccountAttestor
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    moderationAttestor: {
      value: input.moderationAttestor ?? null,
      isWritable: true,
    },
    attestor: { value: input.attestor ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("moderationAttestor", accounts.moderationAttestor),
      getAccountMeta("attestor", accounts.attestor),
    ],
    data: getRequestAttestorExitInstructionDataEncoder().encode({}),
    programAddress,
  } as RequestAttestorExitInstruction<
    TProgramAddress,
    TAccountModerationAttestor,
    TAccountAttestor
  >);
}

export type ParsedRequestAttestorExitInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Roster entry to exit. Seeded by its own stored `attestor` (canonical PDA).
     * The exit path is for SELF-REGISTERED entries only (`assigned_by == attestor`):
     * a deputized entry (`assigned_by == authority`, bond 0, rent paid by the
     * authority) is authority-managed and comes off the roster via
     * `revoke_moderation_attestor` (rent → authority), NOT here. Without this scope a
     * deputy could self-remove outside the authority's control and `close = attestor`
     * would redirect the authority-funded rent to the deputy (adversarial finding).
     */
    moderationAttestor: TAccountMetas[0];
    /** Only the attestor itself may start its exit. */
    attestor: TAccountMetas[1];
  };
  data: RequestAttestorExitInstructionData;
};

export function parseRequestAttestorExitInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRequestAttestorExitInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      moderationAttestor: getNextAccount(),
      attestor: getNextAccount(),
    },
    data: getRequestAttestorExitInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
