/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBytesDecoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findModerationConfigPda, findTaskModerationPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const RECORD_TASK_MODERATION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([156, 205, 56, 24, 136, 114, 93, 160]);

export function getRecordTaskModerationDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(RECORD_TASK_MODERATION_DISCRIMINATOR);
}

export type RecordTaskModerationInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountModerationConfig extends string | AccountMeta<string> = string,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountModerator extends string | AccountMeta<string> = string,
  TAccountTaskModeration extends string | AccountMeta<string> = string,
  TAccountModerationAttestor extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountModerationConfig extends string
        ? ReadonlyAccount<TAccountModerationConfig>
        : TAccountModerationConfig,
      TAccountTask extends string
        ? ReadonlyAccount<TAccountTask>
        : TAccountTask,
      TAccountModerator extends string
        ? WritableSignerAccount<TAccountModerator> &
            AccountSignerMeta<TAccountModerator>
        : TAccountModerator,
      TAccountTaskModeration extends string
        ? WritableAccount<TAccountTaskModeration>
        : TAccountTaskModeration,
      TAccountModerationAttestor extends string
        ? ReadonlyAccount<TAccountModerationAttestor>
        : TAccountModerationAttestor,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RecordTaskModerationInstructionData = {
  discriminator: ReadonlyUint8Array;
  jobSpecHash: ReadonlyUint8Array;
  status: number;
  riskScore: number;
  categoryMask: bigint;
  policyHash: ReadonlyUint8Array;
  scannerHash: ReadonlyUint8Array;
  expiresAt: bigint;
};

export type RecordTaskModerationInstructionDataArgs = {
  jobSpecHash: ReadonlyUint8Array;
  status: number;
  riskScore: number;
  categoryMask: number | bigint;
  policyHash: ReadonlyUint8Array;
  scannerHash: ReadonlyUint8Array;
  expiresAt: number | bigint;
};

export function getRecordTaskModerationInstructionDataEncoder(): FixedSizeEncoder<RecordTaskModerationInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["jobSpecHash", getFixedBytesEncoder(32, "jobSpecHash")],
      ["status", getU8Encoder()],
      ["riskScore", getU8Encoder()],
      ["categoryMask", getU64Encoder()],
      ["policyHash", getFixedBytesEncoder(32, "policyHash")],
      ["scannerHash", getFixedBytesEncoder(32, "scannerHash")],
      ["expiresAt", getI64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: RECORD_TASK_MODERATION_DISCRIMINATOR,
    }),
  );
}

export function getRecordTaskModerationInstructionDataDecoder(): FixedSizeDecoder<RecordTaskModerationInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["jobSpecHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["status", getU8Decoder()],
    ["riskScore", getU8Decoder()],
    ["categoryMask", getU64Decoder()],
    ["policyHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["scannerHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["expiresAt", getI64Decoder()],
  ]);
}

export function getRecordTaskModerationInstructionDataCodec(): FixedSizeCodec<
  RecordTaskModerationInstructionDataArgs,
  RecordTaskModerationInstructionData
> {
  return combineCodec(
    getRecordTaskModerationInstructionDataEncoder(),
    getRecordTaskModerationInstructionDataDecoder(),
  );
}

export type RecordTaskModerationAsyncInput<
  TAccountModerationConfig extends string = string,
  TAccountTask extends string = string,
  TAccountModerator extends string = string,
  TAccountTaskModeration extends string = string,
  TAccountModerationAttestor extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  moderationConfig?: Address<TAccountModerationConfig>;
  task: Address<TAccountTask>;
  /**
   * The recording signer. Authorization is checked in the handler (NOT as an account
   * constraint here) so the registered-attestor OR global-authority branch can be
   * evaluated. In the canary build there is no attestor account, so the handler falls
   * back to the global-authority-only check — the canary surface stays frozen.
   * Declared BEFORE `task_moderation` in the full build so the v2 seed can reference
   * it (an IDL account-order change for this batch's regenerated clients).
   */
  moderator: TransactionSigner<TAccountModerator>;
  /**
   * P1.2 §4.3 — v2 MODERATOR-KEYED record: each attestor owns an exclusive slot, so
   * `init_if_needed` is self-re-review only. No attestor can overwrite another's
   * verdict (flip a trusted BLOCKED→CLEAN or grief CLEAN→BLOCKED); a trusted
   * attestor's BLOCKED verdict is un-erasable evidence. Post-upgrade, records are
   * written ONLY under v2 seeds — legacy `["task_moderation", …]` PDAs are frozen.
   */
  taskModeration?: Address<TAccountTaskModeration>;
  /**
   * OPTIONAL (P6.8): a registered moderation-attestor roster entry. When supplied (and
   * `moderator == moderation_attestor.attestor`), authorizes a non-global-authority
   * attestor to record. Bound to `["moderation_attestor", moderator]` — Anchor enforces
   * the canonical PDA, so a forged or mismatched entry fails account resolution; a
   * REVOKED attestor's PDA is closed and fails to load (cannot attest). Full-surface
   * only — gated so the frozen canary account list for `record_task_moderation` is
   * unchanged.
   */
  moderationAttestor?: Address<TAccountModerationAttestor>;
  systemProgram?: Address<TAccountSystemProgram>;
  jobSpecHash: RecordTaskModerationInstructionDataArgs["jobSpecHash"];
  status: RecordTaskModerationInstructionDataArgs["status"];
  riskScore: RecordTaskModerationInstructionDataArgs["riskScore"];
  categoryMask: RecordTaskModerationInstructionDataArgs["categoryMask"];
  policyHash: RecordTaskModerationInstructionDataArgs["policyHash"];
  scannerHash: RecordTaskModerationInstructionDataArgs["scannerHash"];
  expiresAt: RecordTaskModerationInstructionDataArgs["expiresAt"];
};

export async function getRecordTaskModerationInstructionAsync<
  TAccountModerationConfig extends string,
  TAccountTask extends string,
  TAccountModerator extends string,
  TAccountTaskModeration extends string,
  TAccountModerationAttestor extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RecordTaskModerationAsyncInput<
    TAccountModerationConfig,
    TAccountTask,
    TAccountModerator,
    TAccountTaskModeration,
    TAccountModerationAttestor,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RecordTaskModerationInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountTask,
    TAccountModerator,
    TAccountTaskModeration,
    TAccountModerationAttestor,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    moderationConfig: {
      value: input.moderationConfig ?? null,
      isWritable: false,
    },
    task: { value: input.task ?? null, isWritable: false },
    moderator: { value: input.moderator ?? null, isWritable: true },
    taskModeration: { value: input.taskModeration ?? null, isWritable: true },
    moderationAttestor: {
      value: input.moderationAttestor ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.moderationConfig.value) {
    accounts.moderationConfig.value = await findModerationConfigPda();
  }
  if (!accounts.taskModeration.value) {
    accounts.taskModeration.value = await findTaskModerationPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
      jobSpecHash: getNonNullResolvedInstructionInput(
        "jobSpecHash",
        args.jobSpecHash,
      ),
      moderator: getAddressFromResolvedInstructionAccount(
        "moderator",
        accounts.moderator.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("moderationConfig", accounts.moderationConfig),
      getAccountMeta("task", accounts.task),
      getAccountMeta("moderator", accounts.moderator),
      getAccountMeta("taskModeration", accounts.taskModeration),
      getAccountMeta("moderationAttestor", accounts.moderationAttestor),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRecordTaskModerationInstructionDataEncoder().encode(
      args as RecordTaskModerationInstructionDataArgs,
    ),
    programAddress,
  } as RecordTaskModerationInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountTask,
    TAccountModerator,
    TAccountTaskModeration,
    TAccountModerationAttestor,
    TAccountSystemProgram
  >);
}

export type RecordTaskModerationInput<
  TAccountModerationConfig extends string = string,
  TAccountTask extends string = string,
  TAccountModerator extends string = string,
  TAccountTaskModeration extends string = string,
  TAccountModerationAttestor extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  moderationConfig: Address<TAccountModerationConfig>;
  task: Address<TAccountTask>;
  /**
   * The recording signer. Authorization is checked in the handler (NOT as an account
   * constraint here) so the registered-attestor OR global-authority branch can be
   * evaluated. In the canary build there is no attestor account, so the handler falls
   * back to the global-authority-only check — the canary surface stays frozen.
   * Declared BEFORE `task_moderation` in the full build so the v2 seed can reference
   * it (an IDL account-order change for this batch's regenerated clients).
   */
  moderator: TransactionSigner<TAccountModerator>;
  /**
   * P1.2 §4.3 — v2 MODERATOR-KEYED record: each attestor owns an exclusive slot, so
   * `init_if_needed` is self-re-review only. No attestor can overwrite another's
   * verdict (flip a trusted BLOCKED→CLEAN or grief CLEAN→BLOCKED); a trusted
   * attestor's BLOCKED verdict is un-erasable evidence. Post-upgrade, records are
   * written ONLY under v2 seeds — legacy `["task_moderation", …]` PDAs are frozen.
   */
  taskModeration: Address<TAccountTaskModeration>;
  /**
   * OPTIONAL (P6.8): a registered moderation-attestor roster entry. When supplied (and
   * `moderator == moderation_attestor.attestor`), authorizes a non-global-authority
   * attestor to record. Bound to `["moderation_attestor", moderator]` — Anchor enforces
   * the canonical PDA, so a forged or mismatched entry fails account resolution; a
   * REVOKED attestor's PDA is closed and fails to load (cannot attest). Full-surface
   * only — gated so the frozen canary account list for `record_task_moderation` is
   * unchanged.
   */
  moderationAttestor?: Address<TAccountModerationAttestor>;
  systemProgram?: Address<TAccountSystemProgram>;
  jobSpecHash: RecordTaskModerationInstructionDataArgs["jobSpecHash"];
  status: RecordTaskModerationInstructionDataArgs["status"];
  riskScore: RecordTaskModerationInstructionDataArgs["riskScore"];
  categoryMask: RecordTaskModerationInstructionDataArgs["categoryMask"];
  policyHash: RecordTaskModerationInstructionDataArgs["policyHash"];
  scannerHash: RecordTaskModerationInstructionDataArgs["scannerHash"];
  expiresAt: RecordTaskModerationInstructionDataArgs["expiresAt"];
};

export function getRecordTaskModerationInstruction<
  TAccountModerationConfig extends string,
  TAccountTask extends string,
  TAccountModerator extends string,
  TAccountTaskModeration extends string,
  TAccountModerationAttestor extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RecordTaskModerationInput<
    TAccountModerationConfig,
    TAccountTask,
    TAccountModerator,
    TAccountTaskModeration,
    TAccountModerationAttestor,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RecordTaskModerationInstruction<
  TProgramAddress,
  TAccountModerationConfig,
  TAccountTask,
  TAccountModerator,
  TAccountTaskModeration,
  TAccountModerationAttestor,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    moderationConfig: {
      value: input.moderationConfig ?? null,
      isWritable: false,
    },
    task: { value: input.task ?? null, isWritable: false },
    moderator: { value: input.moderator ?? null, isWritable: true },
    taskModeration: { value: input.taskModeration ?? null, isWritable: true },
    moderationAttestor: {
      value: input.moderationAttestor ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("moderationConfig", accounts.moderationConfig),
      getAccountMeta("task", accounts.task),
      getAccountMeta("moderator", accounts.moderator),
      getAccountMeta("taskModeration", accounts.taskModeration),
      getAccountMeta("moderationAttestor", accounts.moderationAttestor),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRecordTaskModerationInstructionDataEncoder().encode(
      args as RecordTaskModerationInstructionDataArgs,
    ),
    programAddress,
  } as RecordTaskModerationInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountTask,
    TAccountModerator,
    TAccountTaskModeration,
    TAccountModerationAttestor,
    TAccountSystemProgram
  >);
}

export type ParsedRecordTaskModerationInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    moderationConfig: TAccountMetas[0];
    task: TAccountMetas[1];
    /**
     * The recording signer. Authorization is checked in the handler (NOT as an account
     * constraint here) so the registered-attestor OR global-authority branch can be
     * evaluated. In the canary build there is no attestor account, so the handler falls
     * back to the global-authority-only check — the canary surface stays frozen.
     * Declared BEFORE `task_moderation` in the full build so the v2 seed can reference
     * it (an IDL account-order change for this batch's regenerated clients).
     */
    moderator: TAccountMetas[2];
    /**
     * P1.2 §4.3 — v2 MODERATOR-KEYED record: each attestor owns an exclusive slot, so
     * `init_if_needed` is self-re-review only. No attestor can overwrite another's
     * verdict (flip a trusted BLOCKED→CLEAN or grief CLEAN→BLOCKED); a trusted
     * attestor's BLOCKED verdict is un-erasable evidence. Post-upgrade, records are
     * written ONLY under v2 seeds — legacy `["task_moderation", …]` PDAs are frozen.
     */
    taskModeration: TAccountMetas[3];
    /**
     * OPTIONAL (P6.8): a registered moderation-attestor roster entry. When supplied (and
     * `moderator == moderation_attestor.attestor`), authorizes a non-global-authority
     * attestor to record. Bound to `["moderation_attestor", moderator]` — Anchor enforces
     * the canonical PDA, so a forged or mismatched entry fails account resolution; a
     * REVOKED attestor's PDA is closed and fails to load (cannot attest). Full-surface
     * only — gated so the frozen canary account list for `record_task_moderation` is
     * unchanged.
     */
    moderationAttestor?: TAccountMetas[4] | undefined;
    systemProgram: TAccountMetas[5];
  };
  data: RecordTaskModerationInstructionData;
};

export function parseRecordTaskModerationInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRecordTaskModerationInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === AGENC_COORDINATION_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      moderationConfig: getNextAccount(),
      task: getNextAccount(),
      moderator: getNextAccount(),
      taskModeration: getNextAccount(),
      moderationAttestor: getNextOptionalAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRecordTaskModerationInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
