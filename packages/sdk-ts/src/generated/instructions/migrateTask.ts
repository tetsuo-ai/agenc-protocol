/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const MIGRATE_TASK_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  114, 41, 111, 76, 14, 117, 128, 54,
]);

export function getMigrateTaskDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(MIGRATE_TASK_DISCRIMINATOR);
}

export type MigrateTaskInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountTask extends string
        ? WritableAccount<TAccountTask>
        : TAccountTask,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type MigrateTaskInstructionData = {
  discriminator: ReadonlyUint8Array;
  dryRun: boolean;
};

export type MigrateTaskInstructionDataArgs = { dryRun: boolean };

export function getMigrateTaskInstructionDataEncoder(): FixedSizeEncoder<MigrateTaskInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["dryRun", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: MIGRATE_TASK_DISCRIMINATOR }),
  );
}

export function getMigrateTaskInstructionDataDecoder(): FixedSizeDecoder<MigrateTaskInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["dryRun", getBooleanDecoder()],
  ]);
}

export function getMigrateTaskInstructionDataCodec(): FixedSizeCodec<
  MigrateTaskInstructionDataArgs,
  MigrateTaskInstructionData
> {
  return combineCodec(
    getMigrateTaskInstructionDataEncoder(),
    getMigrateTaskInstructionDataDecoder(),
  );
}

export type MigrateTaskInput<
  TAccountProtocolConfig extends string = string,
  TAccountTask extends string = string,
  TAccountPayer extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * PDA, and a real ProtocolConfig via a size-tolerant try_deserialize). MUST be raw
   * — a typed `Account<ProtocolConfig>` would reject the 349B PRE-`migrate_protocol`
   * config (the struct is now 351B) before the handler runs, hard-coupling the task
   * sweep to `migrate_protocol` having already grown the config. The size-tolerant
   * hand-decode in the handler reads the multisig gate from BOTH the 349B and 351B
   * layouts, so the two migrations are order-independent. Mirrors `MigrateProtocol`.
   */
  protocolConfig: Address<TAccountProtocolConfig>;
  /**
   * try_deserialize). MUST be raw — a typed `Account<Task>` would reject the 382B
   * pre-migration account before the handler runs, making migration impossible.
   */
  task: Address<TAccountTask>;
  /**
   * Funds the rent top-up for the growth (up to +84 bytes from a 382B legacy task,
   * or +34 from a 432B Batch-2 task).
   */
  payer: TransactionSigner<TAccountPayer>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  dryRun: MigrateTaskInstructionDataArgs["dryRun"];
};

export function getMigrateTaskInstruction<
  TAccountProtocolConfig extends string,
  TAccountTask extends string,
  TAccountPayer extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: MigrateTaskInput<
    TAccountProtocolConfig,
    TAccountTask,
    TAccountPayer,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): MigrateTaskInstruction<
  TProgramAddress,
  TAccountProtocolConfig,
  TAccountTask,
  TAccountPayer,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    task: { value: input.task ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("task", accounts.task),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getMigrateTaskInstructionDataEncoder().encode(
      args as MigrateTaskInstructionDataArgs,
    ),
    programAddress,
  } as MigrateTaskInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountTask,
    TAccountPayer,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedMigrateTaskInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * PDA, and a real ProtocolConfig via a size-tolerant try_deserialize). MUST be raw
     * — a typed `Account<ProtocolConfig>` would reject the 349B PRE-`migrate_protocol`
     * config (the struct is now 351B) before the handler runs, hard-coupling the task
     * sweep to `migrate_protocol` having already grown the config. The size-tolerant
     * hand-decode in the handler reads the multisig gate from BOTH the 349B and 351B
     * layouts, so the two migrations are order-independent. Mirrors `MigrateProtocol`.
     */
    protocolConfig: TAccountMetas[0];
    /**
     * try_deserialize). MUST be raw — a typed `Account<Task>` would reject the 382B
     * pre-migration account before the handler runs, making migration impossible.
     */
    task: TAccountMetas[1];
    /**
     * Funds the rent top-up for the growth (up to +84 bytes from a 382B legacy task,
     * or +34 from a 432B Batch-2 task).
     */
    payer: TAccountMetas[2];
    authority: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: MigrateTaskInstructionData;
};

export function parseMigrateTaskInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedMigrateTaskInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      protocolConfig: getNextAccount(),
      task: getNextAccount(),
      payer: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getMigrateTaskInstructionDataDecoder().decode(instruction.data),
  };
}
