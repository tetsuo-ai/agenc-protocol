/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProtocolConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_MIN_VERSION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([149, 215, 23, 120, 114, 69, 110, 37]);

export function getUpdateMinVersionDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(UPDATE_MIN_VERSION_DISCRIMINATOR);
}

export type UpdateMinVersionInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProtocolConfig extends string
        ? WritableAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateMinVersionInstructionData = {
  discriminator: ReadonlyUint8Array;
  newMinVersion: number;
};

export type UpdateMinVersionInstructionDataArgs = { newMinVersion: number };

export function getUpdateMinVersionInstructionDataEncoder(): FixedSizeEncoder<UpdateMinVersionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["newMinVersion", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_MIN_VERSION_DISCRIMINATOR }),
  );
}

export function getUpdateMinVersionInstructionDataDecoder(): FixedSizeDecoder<UpdateMinVersionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newMinVersion", getU8Decoder()],
  ]);
}

export function getUpdateMinVersionInstructionDataCodec(): FixedSizeCodec<
  UpdateMinVersionInstructionDataArgs,
  UpdateMinVersionInstructionData
> {
  return combineCodec(
    getUpdateMinVersionInstructionDataEncoder(),
    getUpdateMinVersionInstructionDataDecoder(),
  );
}

export type UpdateMinVersionAsyncInput<
  TAccountProtocolConfig extends string = string,
  TAccountAuthority extends string = string,
> = {
  protocolConfig?: Address<TAccountProtocolConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  newMinVersion: UpdateMinVersionInstructionDataArgs["newMinVersion"];
};

export async function getUpdateMinVersionInstructionAsync<
  TAccountProtocolConfig extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: UpdateMinVersionAsyncInput<TAccountProtocolConfig, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateMinVersionInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUpdateMinVersionInstructionDataEncoder().encode(
      args as UpdateMinVersionInstructionDataArgs,
    ),
    programAddress,
  } as UpdateMinVersionInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountAuthority
  >);
}

export type UpdateMinVersionInput<
  TAccountProtocolConfig extends string = string,
  TAccountAuthority extends string = string,
> = {
  protocolConfig: Address<TAccountProtocolConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  newMinVersion: UpdateMinVersionInstructionDataArgs["newMinVersion"];
};

export function getUpdateMinVersionInstruction<
  TAccountProtocolConfig extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: UpdateMinVersionInput<TAccountProtocolConfig, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): UpdateMinVersionInstruction<
  TProgramAddress,
  TAccountProtocolConfig,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUpdateMinVersionInstructionDataEncoder().encode(
      args as UpdateMinVersionInstructionDataArgs,
    ),
    programAddress,
  } as UpdateMinVersionInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountAuthority
  >);
}

export type ParsedUpdateMinVersionInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    protocolConfig: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: UpdateMinVersionInstructionData;
};

export function parseUpdateMinVersionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateMinVersionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { protocolConfig: getNextAccount(), authority: getNextAccount() },
    data: getUpdateMinVersionInstructionDataDecoder().decode(instruction.data),
  };
}
