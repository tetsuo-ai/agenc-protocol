/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProtocolConfigPda, findTaskValidationConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const SETTLE_DISPUTE_CLAIM_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([139, 155, 103, 84, 64, 11, 148, 6]);

export function getSettleDisputeClaimDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(SETTLE_DISPUTE_CLAIM_DISCRIMINATOR);
}

export type SettleDisputeClaimInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountDispute extends string | AccountMeta<string> = string,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountClaim extends string | AccountMeta<string> = string,
  TAccountWorker extends string | AccountMeta<string> = string,
  TAccountTaskSubmission extends string | AccountMeta<string> = string,
  TAccountTaskValidationConfig extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountDispute extends string
        ? WritableAccount<TAccountDispute>
        : TAccountDispute,
      TAccountTask extends string
        ? WritableAccount<TAccountTask>
        : TAccountTask,
      TAccountClaim extends string
        ? WritableAccount<TAccountClaim>
        : TAccountClaim,
      TAccountWorker extends string
        ? WritableAccount<TAccountWorker>
        : TAccountWorker,
      TAccountTaskSubmission extends string
        ? WritableAccount<TAccountTaskSubmission>
        : TAccountTaskSubmission,
      TAccountTaskValidationConfig extends string
        ? WritableAccount<TAccountTaskValidationConfig>
        : TAccountTaskValidationConfig,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type SettleDisputeClaimInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type SettleDisputeClaimInstructionDataArgs = {};

export function getSettleDisputeClaimInstructionDataEncoder(): FixedSizeEncoder<SettleDisputeClaimInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
    ]),
    (value) => ({
      ...value,
      discriminator: SETTLE_DISPUTE_CLAIM_DISCRIMINATOR,
    }),
  );
}

export function getSettleDisputeClaimInstructionDataDecoder(): FixedSizeDecoder<SettleDisputeClaimInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSettleDisputeClaimInstructionDataCodec(): FixedSizeCodec<
  SettleDisputeClaimInstructionDataArgs,
  SettleDisputeClaimInstructionData
> {
  return combineCodec(
    getSettleDisputeClaimInstructionDataEncoder(),
    getSettleDisputeClaimInstructionDataDecoder(),
  );
}

export type SettleDisputeClaimAsyncInput<
  TAccountProtocolConfig extends string = string,
  TAccountDispute extends string = string,
  TAccountTask extends string = string,
  TAccountClaim extends string = string,
  TAccountWorker extends string = string,
  TAccountTaskSubmission extends string = string,
  TAccountTaskValidationConfig extends string = string,
  TAccountAuthority extends string = string,
> = {
  protocolConfig?: Address<TAccountProtocolConfig>;
  dispute: Address<TAccountDispute>;
  task: Address<TAccountTask>;
  /**
   * and binding to `worker` are all enforced by the shared bundle
   * validation in the handler (identical to the retired monolithic path).
   */
  claim: Address<TAccountClaim>;
  /** the shared bundle validation; receives the closed claim's rent. */
  worker: Address<TAccountWorker>;
  /**
   * swept with counter conservation; the exact empty system-owned PDA
   * proves absence. Non-skippable evidence, as on the retired path.
   */
  taskSubmission: Address<TAccountTaskSubmission>;
  taskValidationConfig?: Address<TAccountTaskValidationConfig>;
  authority: TransactionSigner<TAccountAuthority>;
};

export async function getSettleDisputeClaimInstructionAsync<
  TAccountProtocolConfig extends string,
  TAccountDispute extends string,
  TAccountTask extends string,
  TAccountClaim extends string,
  TAccountWorker extends string,
  TAccountTaskSubmission extends string,
  TAccountTaskValidationConfig extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: SettleDisputeClaimAsyncInput<
    TAccountProtocolConfig,
    TAccountDispute,
    TAccountTask,
    TAccountClaim,
    TAccountWorker,
    TAccountTaskSubmission,
    TAccountTaskValidationConfig,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SettleDisputeClaimInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountDispute,
    TAccountTask,
    TAccountClaim,
    TAccountWorker,
    TAccountTaskSubmission,
    TAccountTaskValidationConfig,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    dispute: { value: input.dispute ?? null, isWritable: true },
    task: { value: input.task ?? null, isWritable: true },
    claim: { value: input.claim ?? null, isWritable: true },
    worker: { value: input.worker ?? null, isWritable: true },
    taskSubmission: { value: input.taskSubmission ?? null, isWritable: true },
    taskValidationConfig: {
      value: input.taskValidationConfig ?? null,
      isWritable: true,
    },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.taskValidationConfig.value) {
    accounts.taskValidationConfig.value = await findTaskValidationConfigPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("dispute", accounts.dispute),
      getAccountMeta("task", accounts.task),
      getAccountMeta("claim", accounts.claim),
      getAccountMeta("worker", accounts.worker),
      getAccountMeta("taskSubmission", accounts.taskSubmission),
      getAccountMeta("taskValidationConfig", accounts.taskValidationConfig),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getSettleDisputeClaimInstructionDataEncoder().encode({}),
    programAddress,
  } as SettleDisputeClaimInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountDispute,
    TAccountTask,
    TAccountClaim,
    TAccountWorker,
    TAccountTaskSubmission,
    TAccountTaskValidationConfig,
    TAccountAuthority
  >);
}

export type SettleDisputeClaimInput<
  TAccountProtocolConfig extends string = string,
  TAccountDispute extends string = string,
  TAccountTask extends string = string,
  TAccountClaim extends string = string,
  TAccountWorker extends string = string,
  TAccountTaskSubmission extends string = string,
  TAccountTaskValidationConfig extends string = string,
  TAccountAuthority extends string = string,
> = {
  protocolConfig: Address<TAccountProtocolConfig>;
  dispute: Address<TAccountDispute>;
  task: Address<TAccountTask>;
  /**
   * and binding to `worker` are all enforced by the shared bundle
   * validation in the handler (identical to the retired monolithic path).
   */
  claim: Address<TAccountClaim>;
  /** the shared bundle validation; receives the closed claim's rent. */
  worker: Address<TAccountWorker>;
  /**
   * swept with counter conservation; the exact empty system-owned PDA
   * proves absence. Non-skippable evidence, as on the retired path.
   */
  taskSubmission: Address<TAccountTaskSubmission>;
  taskValidationConfig?: Address<TAccountTaskValidationConfig>;
  authority: TransactionSigner<TAccountAuthority>;
};

export function getSettleDisputeClaimInstruction<
  TAccountProtocolConfig extends string,
  TAccountDispute extends string,
  TAccountTask extends string,
  TAccountClaim extends string,
  TAccountWorker extends string,
  TAccountTaskSubmission extends string,
  TAccountTaskValidationConfig extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: SettleDisputeClaimInput<
    TAccountProtocolConfig,
    TAccountDispute,
    TAccountTask,
    TAccountClaim,
    TAccountWorker,
    TAccountTaskSubmission,
    TAccountTaskValidationConfig,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): SettleDisputeClaimInstruction<
  TProgramAddress,
  TAccountProtocolConfig,
  TAccountDispute,
  TAccountTask,
  TAccountClaim,
  TAccountWorker,
  TAccountTaskSubmission,
  TAccountTaskValidationConfig,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    dispute: { value: input.dispute ?? null, isWritable: true },
    task: { value: input.task ?? null, isWritable: true },
    claim: { value: input.claim ?? null, isWritable: true },
    worker: { value: input.worker ?? null, isWritable: true },
    taskSubmission: { value: input.taskSubmission ?? null, isWritable: true },
    taskValidationConfig: {
      value: input.taskValidationConfig ?? null,
      isWritable: true,
    },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("dispute", accounts.dispute),
      getAccountMeta("task", accounts.task),
      getAccountMeta("claim", accounts.claim),
      getAccountMeta("worker", accounts.worker),
      getAccountMeta("taskSubmission", accounts.taskSubmission),
      getAccountMeta("taskValidationConfig", accounts.taskValidationConfig),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getSettleDisputeClaimInstructionDataEncoder().encode({}),
    programAddress,
  } as SettleDisputeClaimInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountDispute,
    TAccountTask,
    TAccountClaim,
    TAccountWorker,
    TAccountTaskSubmission,
    TAccountTaskValidationConfig,
    TAccountAuthority
  >);
}

export type ParsedSettleDisputeClaimInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    protocolConfig: TAccountMetas[0];
    dispute: TAccountMetas[1];
    task: TAccountMetas[2];
    /**
     * and binding to `worker` are all enforced by the shared bundle
     * validation in the handler (identical to the retired monolithic path).
     */
    claim: TAccountMetas[3];
    /** the shared bundle validation; receives the closed claim's rent. */
    worker: TAccountMetas[4];
    /**
     * swept with counter conservation; the exact empty system-owned PDA
     * proves absence. Non-skippable evidence, as on the retired path.
     */
    taskSubmission: TAccountMetas[5];
    taskValidationConfig?: TAccountMetas[6] | undefined;
    authority: TAccountMetas[7];
  };
  data: SettleDisputeClaimInstructionData;
};

export function parseSettleDisputeClaimInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSettleDisputeClaimInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 8,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === AGENC_COORDINATION_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      protocolConfig: getNextAccount(),
      dispute: getNextAccount(),
      task: getNextAccount(),
      claim: getNextAccount(),
      worker: getNextAccount(),
      taskSubmission: getNextAccount(),
      taskValidationConfig: getNextOptionalAccount(),
      authority: getNextAccount(),
    },
    data: getSettleDisputeClaimInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
