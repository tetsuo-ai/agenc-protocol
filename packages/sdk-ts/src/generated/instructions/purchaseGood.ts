/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProtocolConfigPda, findSaleReceiptPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const PURCHASE_GOOD_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  29, 214, 83, 30, 102, 121, 47, 92,
]);

export function getPurchaseGoodDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(PURCHASE_GOOD_DISCRIMINATOR);
}

export type PurchaseGoodInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountGood extends string | AccountMeta<string> = string,
  TAccountSaleReceipt extends string | AccountMeta<string> = string,
  TAccountSellerAgent extends string | AccountMeta<string> = string,
  TAccountSellerWallet extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountModerationBlock extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountOperatorWallet extends string | AccountMeta<string> = string,
  TAccountPriceMint extends string | AccountMeta<string> = string,
  TAccountBuyerTokenAccount extends string | AccountMeta<string> = string,
  TAccountSellerTokenAccount extends string | AccountMeta<string> = string,
  TAccountTreasuryTokenAccount extends string | AccountMeta<string> = string,
  TAccountOperatorTokenAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGood extends string
        ? WritableAccount<TAccountGood>
        : TAccountGood,
      TAccountSaleReceipt extends string
        ? WritableAccount<TAccountSaleReceipt>
        : TAccountSaleReceipt,
      TAccountSellerAgent extends string
        ? ReadonlyAccount<TAccountSellerAgent>
        : TAccountSellerAgent,
      TAccountSellerWallet extends string
        ? WritableAccount<TAccountSellerWallet>
        : TAccountSellerWallet,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountModerationBlock extends string
        ? ReadonlyAccount<TAccountModerationBlock>
        : TAccountModerationBlock,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountOperatorWallet extends string
        ? WritableAccount<TAccountOperatorWallet>
        : TAccountOperatorWallet,
      TAccountPriceMint extends string
        ? ReadonlyAccount<TAccountPriceMint>
        : TAccountPriceMint,
      TAccountBuyerTokenAccount extends string
        ? WritableAccount<TAccountBuyerTokenAccount>
        : TAccountBuyerTokenAccount,
      TAccountSellerTokenAccount extends string
        ? WritableAccount<TAccountSellerTokenAccount>
        : TAccountSellerTokenAccount,
      TAccountTreasuryTokenAccount extends string
        ? WritableAccount<TAccountTreasuryTokenAccount>
        : TAccountTreasuryTokenAccount,
      TAccountOperatorTokenAccount extends string
        ? WritableAccount<TAccountOperatorTokenAccount>
        : TAccountOperatorTokenAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PurchaseGoodInstructionData = {
  discriminator: ReadonlyUint8Array;
  expectedSerial: bigint;
  expectedPrice: bigint;
  expectedMetadataHash: ReadonlyUint8Array;
};

export type PurchaseGoodInstructionDataArgs = {
  expectedSerial: number | bigint;
  expectedPrice: number | bigint;
  expectedMetadataHash: ReadonlyUint8Array;
};

export function getPurchaseGoodInstructionDataEncoder(): FixedSizeEncoder<PurchaseGoodInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["expectedSerial", getU64Encoder()],
      ["expectedPrice", getU64Encoder()],
      [
        "expectedMetadataHash",
        getFixedBytesEncoder(32, "expectedMetadataHash"),
      ],
    ]),
    (value) => ({ ...value, discriminator: PURCHASE_GOOD_DISCRIMINATOR }),
  );
}

export function getPurchaseGoodInstructionDataDecoder(): FixedSizeDecoder<PurchaseGoodInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["expectedSerial", getU64Decoder()],
    ["expectedPrice", getU64Decoder()],
    ["expectedMetadataHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getPurchaseGoodInstructionDataCodec(): FixedSizeCodec<
  PurchaseGoodInstructionDataArgs,
  PurchaseGoodInstructionData
> {
  return combineCodec(
    getPurchaseGoodInstructionDataEncoder(),
    getPurchaseGoodInstructionDataDecoder(),
  );
}

export type PurchaseGoodAsyncInput<
  TAccountGood extends string = string,
  TAccountSaleReceipt extends string = string,
  TAccountSellerAgent extends string = string,
  TAccountSellerWallet extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountTreasury extends string = string,
  TAccountModerationBlock extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountOperatorWallet extends string = string,
  TAccountPriceMint extends string = string,
  TAccountBuyerTokenAccount extends string = string,
  TAccountSellerTokenAccount extends string = string,
  TAccountTreasuryTokenAccount extends string = string,
  TAccountOperatorTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  good: Address<TAccountGood>;
  /**
   * One receipt per sold UNIT: seeded on the serial passed as an argument.
   * The `expected_serial == good.sold_count` gate in the handler is
   * LOAD-BEARING — without it a buyer could mint a receipt at an arbitrary
   * future serial and corrupt the provenance namespace.
   */
  saleReceipt?: Address<TAccountSaleReceipt>;
  /**
   * Seller's agent registration — carried only to enforce the seller's
   * agent-level STATUS (a suspended seller stops selling). The PAYEE is NOT
   * sourced from this account (see AC-2): it is pinned to the listing's
   * snapshotted `seller_authority`, so re-registering a deregistered agent_id
   * cannot redirect payouts.
   */
  sellerAgent: Address<TAccountSellerAgent>;
  /** NOT the current `seller_agent.authority`. */
  sellerWallet: Address<TAccountSellerWallet>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  treasury: Address<TAccountTreasury>;
  /**
   * The moderation BLOCK floor over the listing's CURRENT `metadata_hash` —
   * checked at every sale, so a post-listing block (or a blocked hash swapped
   * in via update) stops purchases immediately.
   *
   */
  moderationBlock: Address<TAccountModerationBlock>;
  /** The BUYER — a bare wallet signer; no agent registration required. */
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  /**
   * the listing carries an operator leg (validated in the handler — Anchor
   * optional-account constraints don't run when the account is absent).
   */
  operatorWallet?: Address<TAccountOperatorWallet>;
  priceMint?: Address<TAccountPriceMint>;
  buyerTokenAccount?: Address<TAccountBuyerTokenAccount>;
  sellerTokenAccount?: Address<TAccountSellerTokenAccount>;
  treasuryTokenAccount?: Address<TAccountTreasuryTokenAccount>;
  operatorTokenAccount?: Address<TAccountOperatorTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
  expectedSerial: PurchaseGoodInstructionDataArgs["expectedSerial"];
  expectedPrice: PurchaseGoodInstructionDataArgs["expectedPrice"];
  expectedMetadataHash: PurchaseGoodInstructionDataArgs["expectedMetadataHash"];
};

export async function getPurchaseGoodInstructionAsync<
  TAccountGood extends string,
  TAccountSaleReceipt extends string,
  TAccountSellerAgent extends string,
  TAccountSellerWallet extends string,
  TAccountProtocolConfig extends string,
  TAccountTreasury extends string,
  TAccountModerationBlock extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TAccountOperatorWallet extends string,
  TAccountPriceMint extends string,
  TAccountBuyerTokenAccount extends string,
  TAccountSellerTokenAccount extends string,
  TAccountTreasuryTokenAccount extends string,
  TAccountOperatorTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: PurchaseGoodAsyncInput<
    TAccountGood,
    TAccountSaleReceipt,
    TAccountSellerAgent,
    TAccountSellerWallet,
    TAccountProtocolConfig,
    TAccountTreasury,
    TAccountModerationBlock,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountOperatorWallet,
    TAccountPriceMint,
    TAccountBuyerTokenAccount,
    TAccountSellerTokenAccount,
    TAccountTreasuryTokenAccount,
    TAccountOperatorTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PurchaseGoodInstruction<
    TProgramAddress,
    TAccountGood,
    TAccountSaleReceipt,
    TAccountSellerAgent,
    TAccountSellerWallet,
    TAccountProtocolConfig,
    TAccountTreasury,
    TAccountModerationBlock,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountOperatorWallet,
    TAccountPriceMint,
    TAccountBuyerTokenAccount,
    TAccountSellerTokenAccount,
    TAccountTreasuryTokenAccount,
    TAccountOperatorTokenAccount,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    good: { value: input.good ?? null, isWritable: true },
    saleReceipt: { value: input.saleReceipt ?? null, isWritable: true },
    sellerAgent: { value: input.sellerAgent ?? null, isWritable: false },
    sellerWallet: { value: input.sellerWallet ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    treasury: { value: input.treasury ?? null, isWritable: true },
    moderationBlock: {
      value: input.moderationBlock ?? null,
      isWritable: false,
    },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    operatorWallet: { value: input.operatorWallet ?? null, isWritable: true },
    priceMint: { value: input.priceMint ?? null, isWritable: false },
    buyerTokenAccount: {
      value: input.buyerTokenAccount ?? null,
      isWritable: true,
    },
    sellerTokenAccount: {
      value: input.sellerTokenAccount ?? null,
      isWritable: true,
    },
    treasuryTokenAccount: {
      value: input.treasuryTokenAccount ?? null,
      isWritable: true,
    },
    operatorTokenAccount: {
      value: input.operatorTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.saleReceipt.value) {
    accounts.saleReceipt.value = await findSaleReceiptPda({
      good: getAddressFromResolvedInstructionAccount(
        "good",
        accounts.good.value,
      ),
      expectedSerial: getNonNullResolvedInstructionInput(
        "expectedSerial",
        args.expectedSerial,
      ),
    });
  }
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("good", accounts.good),
      getAccountMeta("saleReceipt", accounts.saleReceipt),
      getAccountMeta("sellerAgent", accounts.sellerAgent),
      getAccountMeta("sellerWallet", accounts.sellerWallet),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("moderationBlock", accounts.moderationBlock),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("operatorWallet", accounts.operatorWallet),
      getAccountMeta("priceMint", accounts.priceMint),
      getAccountMeta("buyerTokenAccount", accounts.buyerTokenAccount),
      getAccountMeta("sellerTokenAccount", accounts.sellerTokenAccount),
      getAccountMeta("treasuryTokenAccount", accounts.treasuryTokenAccount),
      getAccountMeta("operatorTokenAccount", accounts.operatorTokenAccount),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getPurchaseGoodInstructionDataEncoder().encode(
      args as PurchaseGoodInstructionDataArgs,
    ),
    programAddress,
  } as PurchaseGoodInstruction<
    TProgramAddress,
    TAccountGood,
    TAccountSaleReceipt,
    TAccountSellerAgent,
    TAccountSellerWallet,
    TAccountProtocolConfig,
    TAccountTreasury,
    TAccountModerationBlock,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountOperatorWallet,
    TAccountPriceMint,
    TAccountBuyerTokenAccount,
    TAccountSellerTokenAccount,
    TAccountTreasuryTokenAccount,
    TAccountOperatorTokenAccount,
    TAccountTokenProgram
  >);
}

export type PurchaseGoodInput<
  TAccountGood extends string = string,
  TAccountSaleReceipt extends string = string,
  TAccountSellerAgent extends string = string,
  TAccountSellerWallet extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountTreasury extends string = string,
  TAccountModerationBlock extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountOperatorWallet extends string = string,
  TAccountPriceMint extends string = string,
  TAccountBuyerTokenAccount extends string = string,
  TAccountSellerTokenAccount extends string = string,
  TAccountTreasuryTokenAccount extends string = string,
  TAccountOperatorTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  good: Address<TAccountGood>;
  /**
   * One receipt per sold UNIT: seeded on the serial passed as an argument.
   * The `expected_serial == good.sold_count` gate in the handler is
   * LOAD-BEARING — without it a buyer could mint a receipt at an arbitrary
   * future serial and corrupt the provenance namespace.
   */
  saleReceipt: Address<TAccountSaleReceipt>;
  /**
   * Seller's agent registration — carried only to enforce the seller's
   * agent-level STATUS (a suspended seller stops selling). The PAYEE is NOT
   * sourced from this account (see AC-2): it is pinned to the listing's
   * snapshotted `seller_authority`, so re-registering a deregistered agent_id
   * cannot redirect payouts.
   */
  sellerAgent: Address<TAccountSellerAgent>;
  /** NOT the current `seller_agent.authority`. */
  sellerWallet: Address<TAccountSellerWallet>;
  protocolConfig: Address<TAccountProtocolConfig>;
  treasury: Address<TAccountTreasury>;
  /**
   * The moderation BLOCK floor over the listing's CURRENT `metadata_hash` —
   * checked at every sale, so a post-listing block (or a blocked hash swapped
   * in via update) stops purchases immediately.
   *
   */
  moderationBlock: Address<TAccountModerationBlock>;
  /** The BUYER — a bare wallet signer; no agent registration required. */
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  /**
   * the listing carries an operator leg (validated in the handler — Anchor
   * optional-account constraints don't run when the account is absent).
   */
  operatorWallet?: Address<TAccountOperatorWallet>;
  priceMint?: Address<TAccountPriceMint>;
  buyerTokenAccount?: Address<TAccountBuyerTokenAccount>;
  sellerTokenAccount?: Address<TAccountSellerTokenAccount>;
  treasuryTokenAccount?: Address<TAccountTreasuryTokenAccount>;
  operatorTokenAccount?: Address<TAccountOperatorTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
  expectedSerial: PurchaseGoodInstructionDataArgs["expectedSerial"];
  expectedPrice: PurchaseGoodInstructionDataArgs["expectedPrice"];
  expectedMetadataHash: PurchaseGoodInstructionDataArgs["expectedMetadataHash"];
};

export function getPurchaseGoodInstruction<
  TAccountGood extends string,
  TAccountSaleReceipt extends string,
  TAccountSellerAgent extends string,
  TAccountSellerWallet extends string,
  TAccountProtocolConfig extends string,
  TAccountTreasury extends string,
  TAccountModerationBlock extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TAccountOperatorWallet extends string,
  TAccountPriceMint extends string,
  TAccountBuyerTokenAccount extends string,
  TAccountSellerTokenAccount extends string,
  TAccountTreasuryTokenAccount extends string,
  TAccountOperatorTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: PurchaseGoodInput<
    TAccountGood,
    TAccountSaleReceipt,
    TAccountSellerAgent,
    TAccountSellerWallet,
    TAccountProtocolConfig,
    TAccountTreasury,
    TAccountModerationBlock,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountOperatorWallet,
    TAccountPriceMint,
    TAccountBuyerTokenAccount,
    TAccountSellerTokenAccount,
    TAccountTreasuryTokenAccount,
    TAccountOperatorTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PurchaseGoodInstruction<
  TProgramAddress,
  TAccountGood,
  TAccountSaleReceipt,
  TAccountSellerAgent,
  TAccountSellerWallet,
  TAccountProtocolConfig,
  TAccountTreasury,
  TAccountModerationBlock,
  TAccountAuthority,
  TAccountSystemProgram,
  TAccountOperatorWallet,
  TAccountPriceMint,
  TAccountBuyerTokenAccount,
  TAccountSellerTokenAccount,
  TAccountTreasuryTokenAccount,
  TAccountOperatorTokenAccount,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    good: { value: input.good ?? null, isWritable: true },
    saleReceipt: { value: input.saleReceipt ?? null, isWritable: true },
    sellerAgent: { value: input.sellerAgent ?? null, isWritable: false },
    sellerWallet: { value: input.sellerWallet ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    treasury: { value: input.treasury ?? null, isWritable: true },
    moderationBlock: {
      value: input.moderationBlock ?? null,
      isWritable: false,
    },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    operatorWallet: { value: input.operatorWallet ?? null, isWritable: true },
    priceMint: { value: input.priceMint ?? null, isWritable: false },
    buyerTokenAccount: {
      value: input.buyerTokenAccount ?? null,
      isWritable: true,
    },
    sellerTokenAccount: {
      value: input.sellerTokenAccount ?? null,
      isWritable: true,
    },
    treasuryTokenAccount: {
      value: input.treasuryTokenAccount ?? null,
      isWritable: true,
    },
    operatorTokenAccount: {
      value: input.operatorTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("good", accounts.good),
      getAccountMeta("saleReceipt", accounts.saleReceipt),
      getAccountMeta("sellerAgent", accounts.sellerAgent),
      getAccountMeta("sellerWallet", accounts.sellerWallet),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("moderationBlock", accounts.moderationBlock),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("operatorWallet", accounts.operatorWallet),
      getAccountMeta("priceMint", accounts.priceMint),
      getAccountMeta("buyerTokenAccount", accounts.buyerTokenAccount),
      getAccountMeta("sellerTokenAccount", accounts.sellerTokenAccount),
      getAccountMeta("treasuryTokenAccount", accounts.treasuryTokenAccount),
      getAccountMeta("operatorTokenAccount", accounts.operatorTokenAccount),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getPurchaseGoodInstructionDataEncoder().encode(
      args as PurchaseGoodInstructionDataArgs,
    ),
    programAddress,
  } as PurchaseGoodInstruction<
    TProgramAddress,
    TAccountGood,
    TAccountSaleReceipt,
    TAccountSellerAgent,
    TAccountSellerWallet,
    TAccountProtocolConfig,
    TAccountTreasury,
    TAccountModerationBlock,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountOperatorWallet,
    TAccountPriceMint,
    TAccountBuyerTokenAccount,
    TAccountSellerTokenAccount,
    TAccountTreasuryTokenAccount,
    TAccountOperatorTokenAccount,
    TAccountTokenProgram
  >);
}

export type ParsedPurchaseGoodInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    good: TAccountMetas[0];
    /**
     * One receipt per sold UNIT: seeded on the serial passed as an argument.
     * The `expected_serial == good.sold_count` gate in the handler is
     * LOAD-BEARING — without it a buyer could mint a receipt at an arbitrary
     * future serial and corrupt the provenance namespace.
     */
    saleReceipt: TAccountMetas[1];
    /**
     * Seller's agent registration — carried only to enforce the seller's
     * agent-level STATUS (a suspended seller stops selling). The PAYEE is NOT
     * sourced from this account (see AC-2): it is pinned to the listing's
     * snapshotted `seller_authority`, so re-registering a deregistered agent_id
     * cannot redirect payouts.
     */
    sellerAgent: TAccountMetas[2];
    /** NOT the current `seller_agent.authority`. */
    sellerWallet: TAccountMetas[3];
    protocolConfig: TAccountMetas[4];
    treasury: TAccountMetas[5];
    /**
     * The moderation BLOCK floor over the listing's CURRENT `metadata_hash` —
     * checked at every sale, so a post-listing block (or a blocked hash swapped
     * in via update) stops purchases immediately.
     *
     */
    moderationBlock: TAccountMetas[6];
    /** The BUYER — a bare wallet signer; no agent registration required. */
    authority: TAccountMetas[7];
    systemProgram: TAccountMetas[8];
    /**
     * the listing carries an operator leg (validated in the handler — Anchor
     * optional-account constraints don't run when the account is absent).
     */
    operatorWallet?: TAccountMetas[9] | undefined;
    priceMint?: TAccountMetas[10] | undefined;
    buyerTokenAccount?: TAccountMetas[11] | undefined;
    sellerTokenAccount?: TAccountMetas[12] | undefined;
    treasuryTokenAccount?: TAccountMetas[13] | undefined;
    operatorTokenAccount?: TAccountMetas[14] | undefined;
    tokenProgram?: TAccountMetas[15] | undefined;
  };
  data: PurchaseGoodInstructionData;
};

export function parsePurchaseGoodInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPurchaseGoodInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 16) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 16,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === AGENC_COORDINATION_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      good: getNextAccount(),
      saleReceipt: getNextAccount(),
      sellerAgent: getNextAccount(),
      sellerWallet: getNextAccount(),
      protocolConfig: getNextAccount(),
      treasury: getNextAccount(),
      moderationBlock: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
      operatorWallet: getNextOptionalAccount(),
      priceMint: getNextOptionalAccount(),
      buyerTokenAccount: getNextOptionalAccount(),
      sellerTokenAccount: getNextOptionalAccount(),
      treasuryTokenAccount: getNextOptionalAccount(),
      operatorTokenAccount: getNextOptionalAccount(),
      tokenProgram: getNextOptionalAccount(),
    },
    data: getPurchaseGoodInstructionDataDecoder().decode(instruction.data),
  };
}
