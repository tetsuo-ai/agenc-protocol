/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const RECLAIM_ORPHAN_TASK_CHILD_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([91, 115, 255, 0, 240, 58, 74, 223]);

export function getReclaimOrphanTaskChildDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECLAIM_ORPHAN_TASK_CHILD_DISCRIMINATOR,
  );
}

export type ReclaimOrphanTaskChildInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountChild extends string | AccountMeta<string> = string,
  TAccountParentTask extends string | AccountMeta<string> = string,
  TAccountWorkerAgent extends string | AccountMeta<string> = string,
  TAccountRentRecipient extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountChild extends string
        ? WritableAccount<TAccountChild>
        : TAccountChild,
      TAccountParentTask extends string
        ? ReadonlyAccount<TAccountParentTask>
        : TAccountParentTask,
      TAccountWorkerAgent extends string
        ? ReadonlyAccount<TAccountWorkerAgent>
        : TAccountWorkerAgent,
      TAccountRentRecipient extends string
        ? WritableAccount<TAccountRentRecipient>
        : TAccountRentRecipient,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type ReclaimOrphanTaskChildInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ReclaimOrphanTaskChildInstructionDataArgs = {};

export function getReclaimOrphanTaskChildInstructionDataEncoder(): FixedSizeEncoder<ReclaimOrphanTaskChildInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: RECLAIM_ORPHAN_TASK_CHILD_DISCRIMINATOR,
    }),
  );
}

export function getReclaimOrphanTaskChildInstructionDataDecoder(): FixedSizeDecoder<ReclaimOrphanTaskChildInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getReclaimOrphanTaskChildInstructionDataCodec(): FixedSizeCodec<
  ReclaimOrphanTaskChildInstructionDataArgs,
  ReclaimOrphanTaskChildInstructionData
> {
  return combineCodec(
    getReclaimOrphanTaskChildInstructionDataEncoder(),
    getReclaimOrphanTaskChildInstructionDataDecoder(),
  );
}

export type ReclaimOrphanTaskChildInput<
  TAccountChild extends string = string,
  TAccountParentTask extends string = string,
  TAccountWorkerAgent extends string = string,
  TAccountRentRecipient extends string = string,
  TAccountAuthority extends string = string,
> = {
  /** validated in the handler before any lamports move. */
  child: Address<TAccountChild>;
  /**
   * children, `TaskSubmission` for validation votes) and be a provably
   * absent system-owned, zero-data account. The field name is ABI-stable.
   */
  parentTask: Address<TAccountParentTask>;
  /** worker AgentRegistration and is fully deserialized/canonically verified. */
  workerAgent: Address<TAccountWorkerAgent>;
  /** writable. The cranker can never choose the rent destination. */
  rentRecipient: Address<TAccountRentRecipient>;
  /**
   * Permissionless cranker. Its signature provides transaction accountability
   * but grants no authority over the rent destination.
   */
  authority: TransactionSigner<TAccountAuthority>;
};

export function getReclaimOrphanTaskChildInstruction<
  TAccountChild extends string,
  TAccountParentTask extends string,
  TAccountWorkerAgent extends string,
  TAccountRentRecipient extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: ReclaimOrphanTaskChildInput<
    TAccountChild,
    TAccountParentTask,
    TAccountWorkerAgent,
    TAccountRentRecipient,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): ReclaimOrphanTaskChildInstruction<
  TProgramAddress,
  TAccountChild,
  TAccountParentTask,
  TAccountWorkerAgent,
  TAccountRentRecipient,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    child: { value: input.child ?? null, isWritable: true },
    parentTask: { value: input.parentTask ?? null, isWritable: false },
    workerAgent: { value: input.workerAgent ?? null, isWritable: false },
    rentRecipient: { value: input.rentRecipient ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("child", accounts.child),
      getAccountMeta("parentTask", accounts.parentTask),
      getAccountMeta("workerAgent", accounts.workerAgent),
      getAccountMeta("rentRecipient", accounts.rentRecipient),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getReclaimOrphanTaskChildInstructionDataEncoder().encode({}),
    programAddress,
  } as ReclaimOrphanTaskChildInstruction<
    TProgramAddress,
    TAccountChild,
    TAccountParentTask,
    TAccountWorkerAgent,
    TAccountRentRecipient,
    TAccountAuthority
  >);
}

export type ParsedReclaimOrphanTaskChildInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** validated in the handler before any lamports move. */
    child: TAccountMetas[0];
    /**
     * children, `TaskSubmission` for validation votes) and be a provably
     * absent system-owned, zero-data account. The field name is ABI-stable.
     */
    parentTask: TAccountMetas[1];
    /** worker AgentRegistration and is fully deserialized/canonically verified. */
    workerAgent: TAccountMetas[2];
    /** writable. The cranker can never choose the rent destination. */
    rentRecipient: TAccountMetas[3];
    /**
     * Permissionless cranker. Its signature provides transaction accountability
     * but grants no authority over the rent destination.
     */
    authority: TAccountMetas[4];
  };
  data: ReclaimOrphanTaskChildInstructionData;
};

export function parseReclaimOrphanTaskChildInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedReclaimOrphanTaskChildInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      child: getNextAccount(),
      parentTask: getNextAccount(),
      workerAgent: getNextAccount(),
      rentRecipient: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getReclaimOrphanTaskChildInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
