/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProtocolConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const REVOKE_DISPUTE_RESOLVER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([155, 111, 4, 156, 192, 155, 89, 134]);

export function getRevokeDisputeResolverDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(REVOKE_DISPUTE_RESOLVER_DISCRIMINATOR);
}

export type RevokeDisputeResolverInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountDisputeResolver extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountDisputeResolver extends string
        ? WritableAccount<TAccountDisputeResolver>
        : TAccountDisputeResolver,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type RevokeDisputeResolverInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RevokeDisputeResolverInstructionDataArgs = {};

export function getRevokeDisputeResolverInstructionDataEncoder(): FixedSizeEncoder<RevokeDisputeResolverInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
    ]),
    (value) => ({
      ...value,
      discriminator: REVOKE_DISPUTE_RESOLVER_DISCRIMINATOR,
    }),
  );
}

export function getRevokeDisputeResolverInstructionDataDecoder(): FixedSizeDecoder<RevokeDisputeResolverInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRevokeDisputeResolverInstructionDataCodec(): FixedSizeCodec<
  RevokeDisputeResolverInstructionDataArgs,
  RevokeDisputeResolverInstructionData
> {
  return combineCodec(
    getRevokeDisputeResolverInstructionDataEncoder(),
    getRevokeDisputeResolverInstructionDataDecoder(),
  );
}

export type RevokeDisputeResolverAsyncInput<
  TAccountProtocolConfig extends string = string,
  TAccountDisputeResolver extends string = string,
  TAccountAuthority extends string = string,
> = {
  protocolConfig?: Address<TAccountProtocolConfig>;
  /**
   * Roster entry to remove. Seeded by its own stored `resolver`, so the canonical PDA
   * is enforced; `close = authority` returns the rent to the protocol authority.
   */
  disputeResolver: Address<TAccountDisputeResolver>;
  /**
   * Must be the protocol authority; configured M-of-N approval arrives through
   * remaining accounts.
   */
  authority: TransactionSigner<TAccountAuthority>;
};

export async function getRevokeDisputeResolverInstructionAsync<
  TAccountProtocolConfig extends string,
  TAccountDisputeResolver extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RevokeDisputeResolverAsyncInput<
    TAccountProtocolConfig,
    TAccountDisputeResolver,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RevokeDisputeResolverInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountDisputeResolver,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    disputeResolver: { value: input.disputeResolver ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("disputeResolver", accounts.disputeResolver),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getRevokeDisputeResolverInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokeDisputeResolverInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountDisputeResolver,
    TAccountAuthority
  >);
}

export type RevokeDisputeResolverInput<
  TAccountProtocolConfig extends string = string,
  TAccountDisputeResolver extends string = string,
  TAccountAuthority extends string = string,
> = {
  protocolConfig: Address<TAccountProtocolConfig>;
  /**
   * Roster entry to remove. Seeded by its own stored `resolver`, so the canonical PDA
   * is enforced; `close = authority` returns the rent to the protocol authority.
   */
  disputeResolver: Address<TAccountDisputeResolver>;
  /**
   * Must be the protocol authority; configured M-of-N approval arrives through
   * remaining accounts.
   */
  authority: TransactionSigner<TAccountAuthority>;
};

export function getRevokeDisputeResolverInstruction<
  TAccountProtocolConfig extends string,
  TAccountDisputeResolver extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RevokeDisputeResolverInput<
    TAccountProtocolConfig,
    TAccountDisputeResolver,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): RevokeDisputeResolverInstruction<
  TProgramAddress,
  TAccountProtocolConfig,
  TAccountDisputeResolver,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    disputeResolver: { value: input.disputeResolver ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("disputeResolver", accounts.disputeResolver),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getRevokeDisputeResolverInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokeDisputeResolverInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountDisputeResolver,
    TAccountAuthority
  >);
}

export type ParsedRevokeDisputeResolverInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    protocolConfig: TAccountMetas[0];
    /**
     * Roster entry to remove. Seeded by its own stored `resolver`, so the canonical PDA
     * is enforced; `close = authority` returns the rent to the protocol authority.
     */
    disputeResolver: TAccountMetas[1];
    /**
     * Must be the protocol authority; configured M-of-N approval arrives through
     * remaining accounts.
     */
    authority: TAccountMetas[2];
  };
  data: RevokeDisputeResolverInstructionData;
};

export function parseRevokeDisputeResolverInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevokeDisputeResolverInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      protocolConfig: getNextAccount(),
      disputeResolver: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getRevokeDisputeResolverInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
