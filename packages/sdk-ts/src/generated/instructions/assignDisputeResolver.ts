/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findDisputeResolverPda, findProtocolConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const ASSIGN_DISPUTE_RESOLVER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([64, 252, 49, 0, 45, 0, 58, 14]);

export function getAssignDisputeResolverDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ASSIGN_DISPUTE_RESOLVER_DISCRIMINATOR,
  );
}

export type AssignDisputeResolverInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountDisputeResolver extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountDisputeResolver extends string
        ? WritableAccount<TAccountDisputeResolver>
        : TAccountDisputeResolver,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AssignDisputeResolverInstructionData = {
  discriminator: ReadonlyUint8Array;
  resolver: Address;
};

export type AssignDisputeResolverInstructionDataArgs = { resolver: Address };

export function getAssignDisputeResolverInstructionDataEncoder(): FixedSizeEncoder<AssignDisputeResolverInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["resolver", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: ASSIGN_DISPUTE_RESOLVER_DISCRIMINATOR,
    }),
  );
}

export function getAssignDisputeResolverInstructionDataDecoder(): FixedSizeDecoder<AssignDisputeResolverInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["resolver", getAddressDecoder()],
  ]);
}

export function getAssignDisputeResolverInstructionDataCodec(): FixedSizeCodec<
  AssignDisputeResolverInstructionDataArgs,
  AssignDisputeResolverInstructionData
> {
  return combineCodec(
    getAssignDisputeResolverInstructionDataEncoder(),
    getAssignDisputeResolverInstructionDataDecoder(),
  );
}

export type AssignDisputeResolverAsyncInput<
  TAccountProtocolConfig extends string = string,
  TAccountDisputeResolver extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  protocolConfig?: Address<TAccountProtocolConfig>;
  /** Roster entry for `resolver`. `init` ⇒ assigning an already-assigned wallet fails. */
  disputeResolver?: Address<TAccountDisputeResolver>;
  /**
   * Must be the protocol authority (the roster proposal is authority-bound;
   * configured M-of-N approval arrives through remaining accounts).
   */
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  resolver: AssignDisputeResolverInstructionDataArgs["resolver"];
};

export async function getAssignDisputeResolverInstructionAsync<
  TAccountProtocolConfig extends string,
  TAccountDisputeResolver extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: AssignDisputeResolverAsyncInput<
    TAccountProtocolConfig,
    TAccountDisputeResolver,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AssignDisputeResolverInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountDisputeResolver,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    disputeResolver: { value: input.disputeResolver ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.disputeResolver.value) {
    accounts.disputeResolver.value = await findDisputeResolverPda({
      resolver: getNonNullResolvedInstructionInput("resolver", args.resolver),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("disputeResolver", accounts.disputeResolver),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAssignDisputeResolverInstructionDataEncoder().encode(
      args as AssignDisputeResolverInstructionDataArgs,
    ),
    programAddress,
  } as AssignDisputeResolverInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountDisputeResolver,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type AssignDisputeResolverInput<
  TAccountProtocolConfig extends string = string,
  TAccountDisputeResolver extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  protocolConfig: Address<TAccountProtocolConfig>;
  /** Roster entry for `resolver`. `init` ⇒ assigning an already-assigned wallet fails. */
  disputeResolver: Address<TAccountDisputeResolver>;
  /**
   * Must be the protocol authority (the roster proposal is authority-bound;
   * configured M-of-N approval arrives through remaining accounts).
   */
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  resolver: AssignDisputeResolverInstructionDataArgs["resolver"];
};

export function getAssignDisputeResolverInstruction<
  TAccountProtocolConfig extends string,
  TAccountDisputeResolver extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: AssignDisputeResolverInput<
    TAccountProtocolConfig,
    TAccountDisputeResolver,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AssignDisputeResolverInstruction<
  TProgramAddress,
  TAccountProtocolConfig,
  TAccountDisputeResolver,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    disputeResolver: { value: input.disputeResolver ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("disputeResolver", accounts.disputeResolver),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAssignDisputeResolverInstructionDataEncoder().encode(
      args as AssignDisputeResolverInstructionDataArgs,
    ),
    programAddress,
  } as AssignDisputeResolverInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountDisputeResolver,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedAssignDisputeResolverInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    protocolConfig: TAccountMetas[0];
    /** Roster entry for `resolver`. `init` ⇒ assigning an already-assigned wallet fails. */
    disputeResolver: TAccountMetas[1];
    /**
     * Must be the protocol authority (the roster proposal is authority-bound;
     * configured M-of-N approval arrives through remaining accounts).
     */
    authority: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: AssignDisputeResolverInstructionData;
};

export function parseAssignDisputeResolverInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAssignDisputeResolverInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      protocolConfig: getNextAccount(),
      disputeResolver: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAssignDisputeResolverInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
