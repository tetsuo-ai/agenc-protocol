/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";

import {
  getBorshStringDecoder,
  getBorshStringEncoder,
} from "../codecs/borshString";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAgentVerificationPda, findModerationConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const RECORD_AGENT_VERIFICATION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([185, 60, 213, 195, 106, 238, 222, 56]);

export function getRecordAgentVerificationDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECORD_AGENT_VERIFICATION_DISCRIMINATOR,
  );
}

export type RecordAgentVerificationInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountModerationConfig extends string | AccountMeta<string> = string,
  TAccountAgent extends string | AccountMeta<string> = string,
  TAccountAgentVerification extends string | AccountMeta<string> = string,
  TAccountAttestor extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountModerationConfig extends string
        ? ReadonlyAccount<TAccountModerationConfig>
        : TAccountModerationConfig,
      TAccountAgent extends string
        ? ReadonlyAccount<TAccountAgent>
        : TAccountAgent,
      TAccountAgentVerification extends string
        ? WritableAccount<TAccountAgentVerification>
        : TAccountAgentVerification,
      TAccountAttestor extends string
        ? WritableSignerAccount<TAccountAttestor> &
            AccountSignerMeta<TAccountAttestor>
        : TAccountAttestor,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RecordAgentVerificationInstructionData = {
  discriminator: ReadonlyUint8Array;
  verifiedDomain: string;
  method: number;
  expiresAt: bigint;
};

export type RecordAgentVerificationInstructionDataArgs = {
  verifiedDomain: string;
  method: number;
  expiresAt: number | bigint;
};

export function getRecordAgentVerificationInstructionDataEncoder(): Encoder<RecordAgentVerificationInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      [
        "verifiedDomain",
        addEncoderSizePrefix(getBorshStringEncoder(), getU32Encoder()),
      ],
      ["method", getU8Encoder()],
      ["expiresAt", getI64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: RECORD_AGENT_VERIFICATION_DISCRIMINATOR,
    }),
  );
}

export function getRecordAgentVerificationInstructionDataDecoder(): Decoder<RecordAgentVerificationInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    [
      "verifiedDomain",
      addDecoderSizePrefix(getBorshStringDecoder(), getU32Decoder()),
    ],
    ["method", getU8Decoder()],
    ["expiresAt", getI64Decoder()],
  ]);
}

export function getRecordAgentVerificationInstructionDataCodec(): Codec<
  RecordAgentVerificationInstructionDataArgs,
  RecordAgentVerificationInstructionData
> {
  return combineCodec(
    getRecordAgentVerificationInstructionDataEncoder(),
    getRecordAgentVerificationInstructionDataDecoder(),
  );
}

export type RecordAgentVerificationAsyncInput<
  TAccountModerationConfig extends string = string,
  TAccountAgent extends string = string,
  TAccountAgentVerification extends string = string,
  TAccountAttestor extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  moderationConfig?: Address<TAccountModerationConfig>;
  /** The agent being verified, pinned to its canonical `["agent", agent_id]` PDA. */
  agent: Address<TAccountAgent>;
  /**
   * Domain-verification attestation. `init_if_needed` so re-verification overwrites the
   * same PDA in place. Keyed only by `agent` (one current verification per agent).
   */
  agentVerification?: Address<TAccountAgentVerification>;
  /**
   * The recording signer. P1.2 §4.6: must be the GLOBAL moderation authority —
   * the roster no longer authorizes domain verification (decoupled from the
   * permissionless open roster; checked in the handler).
   */
  attestor: TransactionSigner<TAccountAttestor>;
  systemProgram?: Address<TAccountSystemProgram>;
  verifiedDomain: RecordAgentVerificationInstructionDataArgs["verifiedDomain"];
  method: RecordAgentVerificationInstructionDataArgs["method"];
  expiresAt: RecordAgentVerificationInstructionDataArgs["expiresAt"];
};

export async function getRecordAgentVerificationInstructionAsync<
  TAccountModerationConfig extends string,
  TAccountAgent extends string,
  TAccountAgentVerification extends string,
  TAccountAttestor extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RecordAgentVerificationAsyncInput<
    TAccountModerationConfig,
    TAccountAgent,
    TAccountAgentVerification,
    TAccountAttestor,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RecordAgentVerificationInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountAgent,
    TAccountAgentVerification,
    TAccountAttestor,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    moderationConfig: {
      value: input.moderationConfig ?? null,
      isWritable: false,
    },
    agent: { value: input.agent ?? null, isWritable: false },
    agentVerification: {
      value: input.agentVerification ?? null,
      isWritable: true,
    },
    attestor: { value: input.attestor ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.moderationConfig.value) {
    accounts.moderationConfig.value = await findModerationConfigPda();
  }
  if (!accounts.agentVerification.value) {
    accounts.agentVerification.value = await findAgentVerificationPda({
      agent: getAddressFromResolvedInstructionAccount(
        "agent",
        accounts.agent.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("moderationConfig", accounts.moderationConfig),
      getAccountMeta("agent", accounts.agent),
      getAccountMeta("agentVerification", accounts.agentVerification),
      getAccountMeta("attestor", accounts.attestor),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRecordAgentVerificationInstructionDataEncoder().encode(
      args as RecordAgentVerificationInstructionDataArgs,
    ),
    programAddress,
  } as RecordAgentVerificationInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountAgent,
    TAccountAgentVerification,
    TAccountAttestor,
    TAccountSystemProgram
  >);
}

export type RecordAgentVerificationInput<
  TAccountModerationConfig extends string = string,
  TAccountAgent extends string = string,
  TAccountAgentVerification extends string = string,
  TAccountAttestor extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  moderationConfig: Address<TAccountModerationConfig>;
  /** The agent being verified, pinned to its canonical `["agent", agent_id]` PDA. */
  agent: Address<TAccountAgent>;
  /**
   * Domain-verification attestation. `init_if_needed` so re-verification overwrites the
   * same PDA in place. Keyed only by `agent` (one current verification per agent).
   */
  agentVerification: Address<TAccountAgentVerification>;
  /**
   * The recording signer. P1.2 §4.6: must be the GLOBAL moderation authority —
   * the roster no longer authorizes domain verification (decoupled from the
   * permissionless open roster; checked in the handler).
   */
  attestor: TransactionSigner<TAccountAttestor>;
  systemProgram?: Address<TAccountSystemProgram>;
  verifiedDomain: RecordAgentVerificationInstructionDataArgs["verifiedDomain"];
  method: RecordAgentVerificationInstructionDataArgs["method"];
  expiresAt: RecordAgentVerificationInstructionDataArgs["expiresAt"];
};

export function getRecordAgentVerificationInstruction<
  TAccountModerationConfig extends string,
  TAccountAgent extends string,
  TAccountAgentVerification extends string,
  TAccountAttestor extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RecordAgentVerificationInput<
    TAccountModerationConfig,
    TAccountAgent,
    TAccountAgentVerification,
    TAccountAttestor,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RecordAgentVerificationInstruction<
  TProgramAddress,
  TAccountModerationConfig,
  TAccountAgent,
  TAccountAgentVerification,
  TAccountAttestor,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    moderationConfig: {
      value: input.moderationConfig ?? null,
      isWritable: false,
    },
    agent: { value: input.agent ?? null, isWritable: false },
    agentVerification: {
      value: input.agentVerification ?? null,
      isWritable: true,
    },
    attestor: { value: input.attestor ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("moderationConfig", accounts.moderationConfig),
      getAccountMeta("agent", accounts.agent),
      getAccountMeta("agentVerification", accounts.agentVerification),
      getAccountMeta("attestor", accounts.attestor),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRecordAgentVerificationInstructionDataEncoder().encode(
      args as RecordAgentVerificationInstructionDataArgs,
    ),
    programAddress,
  } as RecordAgentVerificationInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountAgent,
    TAccountAgentVerification,
    TAccountAttestor,
    TAccountSystemProgram
  >);
}

export type ParsedRecordAgentVerificationInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    moderationConfig: TAccountMetas[0];
    /** The agent being verified, pinned to its canonical `["agent", agent_id]` PDA. */
    agent: TAccountMetas[1];
    /**
     * Domain-verification attestation. `init_if_needed` so re-verification overwrites the
     * same PDA in place. Keyed only by `agent` (one current verification per agent).
     */
    agentVerification: TAccountMetas[2];
    /**
     * The recording signer. P1.2 §4.6: must be the GLOBAL moderation authority —
     * the roster no longer authorizes domain verification (decoupled from the
     * permissionless open roster; checked in the handler).
     */
    attestor: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: RecordAgentVerificationInstructionData;
};

export function parseRecordAgentVerificationInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRecordAgentVerificationInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      moderationConfig: getNextAccount(),
      agent: getNextAccount(),
      agentVerification: getNextAccount(),
      attestor: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRecordAgentVerificationInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
