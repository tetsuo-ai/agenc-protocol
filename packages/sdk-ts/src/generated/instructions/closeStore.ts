/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findStorePda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_STORE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  87, 18, 213, 192, 63, 136, 65, 205,
]);

export function getCloseStoreDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(CLOSE_STORE_DISCRIMINATOR);
}

export type CloseStoreInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountStore extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountStore extends string
        ? WritableAccount<TAccountStore>
        : TAccountStore,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type CloseStoreInstructionData = { discriminator: ReadonlyUint8Array };

export type CloseStoreInstructionDataArgs = {};

export function getCloseStoreInstructionDataEncoder(): FixedSizeEncoder<CloseStoreInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
    ]),
    (value) => ({ ...value, discriminator: CLOSE_STORE_DISCRIMINATOR }),
  );
}

export function getCloseStoreInstructionDataDecoder(): FixedSizeDecoder<CloseStoreInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseStoreInstructionDataCodec(): FixedSizeCodec<
  CloseStoreInstructionDataArgs,
  CloseStoreInstructionData
> {
  return combineCodec(
    getCloseStoreInstructionDataEncoder(),
    getCloseStoreInstructionDataDecoder(),
  );
}

export type CloseStoreAsyncInput<
  TAccountStore extends string = string,
  TAccountOwner extends string = string,
> = {
  /**
   * `close = owner` refunds rent + the bond (held as excess lamports on the
   * PDA) to the owner in one step — never confiscatable, owner-only.
   */
  store?: Address<TAccountStore>;
  owner: TransactionSigner<TAccountOwner>;
};

export async function getCloseStoreInstructionAsync<
  TAccountStore extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: CloseStoreAsyncInput<TAccountStore, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseStoreInstruction<TProgramAddress, TAccountStore, TAccountOwner>
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    store: { value: input.store ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.store.value) {
    accounts.store.value = await findStorePda({
      owner: getAddressFromResolvedInstructionAccount(
        "owner",
        accounts.owner.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("store", accounts.store),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getCloseStoreInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseStoreInstruction<TProgramAddress, TAccountStore, TAccountOwner>);
}

export type CloseStoreInput<
  TAccountStore extends string = string,
  TAccountOwner extends string = string,
> = {
  /**
   * `close = owner` refunds rent + the bond (held as excess lamports on the
   * PDA) to the owner in one step — never confiscatable, owner-only.
   */
  store: Address<TAccountStore>;
  owner: TransactionSigner<TAccountOwner>;
};

export function getCloseStoreInstruction<
  TAccountStore extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: CloseStoreInput<TAccountStore, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): CloseStoreInstruction<TProgramAddress, TAccountStore, TAccountOwner> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    store: { value: input.store ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("store", accounts.store),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getCloseStoreInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseStoreInstruction<TProgramAddress, TAccountStore, TAccountOwner>);
}

export type ParsedCloseStoreInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * `close = owner` refunds rent + the bond (held as excess lamports on the
     * PDA) to the owner in one step — never confiscatable, owner-only.
     */
    store: TAccountMetas[0];
    owner: TAccountMetas[1];
  };
  data: CloseStoreInstructionData;
};

export function parseCloseStoreInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseStoreInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { store: getNextAccount(), owner: getNextAccount() },
    data: getCloseStoreInstructionDataDecoder().decode(instruction.data),
  };
}
