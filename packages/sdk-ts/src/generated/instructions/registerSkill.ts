/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProtocolConfigPda, findSkillPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const REGISTER_SKILL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  166, 249, 255, 189, 192, 197, 102, 2,
]);

export function getRegisterSkillDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(REGISTER_SKILL_DISCRIMINATOR);
}

export type RegisterSkillInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountSkill extends string | AccountMeta<string> = string,
  TAccountAuthor extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSkill extends string
        ? WritableAccount<TAccountSkill>
        : TAccountSkill,
      TAccountAuthor extends string
        ? ReadonlyAccount<TAccountAuthor>
        : TAccountAuthor,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterSkillInstructionData = {
  discriminator: ReadonlyUint8Array;
  skillId: ReadonlyUint8Array;
  name: ReadonlyUint8Array;
  contentHash: ReadonlyUint8Array;
  price: bigint;
  priceMint: Option<Address>;
  tags: ReadonlyUint8Array;
};

export type RegisterSkillInstructionDataArgs = {
  skillId: ReadonlyUint8Array;
  name: ReadonlyUint8Array;
  contentHash: ReadonlyUint8Array;
  price: number | bigint;
  priceMint: OptionOrNullable<Address>;
  tags: ReadonlyUint8Array;
};

export function getRegisterSkillInstructionDataEncoder(): Encoder<RegisterSkillInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["skillId", getFixedBytesEncoder(32, "skillId")],
      ["name", getFixedBytesEncoder(32, "name")],
      ["contentHash", getFixedBytesEncoder(32, "contentHash")],
      ["price", getU64Encoder()],
      ["priceMint", getOptionEncoder(getAddressEncoder())],
      ["tags", getFixedBytesEncoder(64, "tags")],
    ]),
    (value) => ({ ...value, discriminator: REGISTER_SKILL_DISCRIMINATOR }),
  );
}

export function getRegisterSkillInstructionDataDecoder(): Decoder<RegisterSkillInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["skillId", fixDecoderSize(getBytesDecoder(), 32)],
    ["name", fixDecoderSize(getBytesDecoder(), 32)],
    ["contentHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["price", getU64Decoder()],
    ["priceMint", getOptionDecoder(getAddressDecoder())],
    ["tags", fixDecoderSize(getBytesDecoder(), 64)],
  ]);
}

export function getRegisterSkillInstructionDataCodec(): Codec<
  RegisterSkillInstructionDataArgs,
  RegisterSkillInstructionData
> {
  return combineCodec(
    getRegisterSkillInstructionDataEncoder(),
    getRegisterSkillInstructionDataDecoder(),
  );
}

export type RegisterSkillAsyncInput<
  TAccountSkill extends string = string,
  TAccountAuthor extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  skill?: Address<TAccountSkill>;
  author: Address<TAccountAuthor>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  skillId: RegisterSkillInstructionDataArgs["skillId"];
  name: RegisterSkillInstructionDataArgs["name"];
  contentHash: RegisterSkillInstructionDataArgs["contentHash"];
  price: RegisterSkillInstructionDataArgs["price"];
  priceMint: RegisterSkillInstructionDataArgs["priceMint"];
  tags: RegisterSkillInstructionDataArgs["tags"];
};

export async function getRegisterSkillInstructionAsync<
  TAccountSkill extends string,
  TAccountAuthor extends string,
  TAccountProtocolConfig extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RegisterSkillAsyncInput<
    TAccountSkill,
    TAccountAuthor,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterSkillInstruction<
    TProgramAddress,
    TAccountSkill,
    TAccountAuthor,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    skill: { value: input.skill ?? null, isWritable: true },
    author: { value: input.author ?? null, isWritable: false },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.skill.value) {
    accounts.skill.value = await findSkillPda({
      author: getAddressFromResolvedInstructionAccount(
        "author",
        accounts.author.value,
      ),
      skillId: getNonNullResolvedInstructionInput("skillId", args.skillId),
    });
  }
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("skill", accounts.skill),
      getAccountMeta("author", accounts.author),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterSkillInstructionDataEncoder().encode(
      args as RegisterSkillInstructionDataArgs,
    ),
    programAddress,
  } as RegisterSkillInstruction<
    TProgramAddress,
    TAccountSkill,
    TAccountAuthor,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type RegisterSkillInput<
  TAccountSkill extends string = string,
  TAccountAuthor extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  skill: Address<TAccountSkill>;
  author: Address<TAccountAuthor>;
  protocolConfig: Address<TAccountProtocolConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  skillId: RegisterSkillInstructionDataArgs["skillId"];
  name: RegisterSkillInstructionDataArgs["name"];
  contentHash: RegisterSkillInstructionDataArgs["contentHash"];
  price: RegisterSkillInstructionDataArgs["price"];
  priceMint: RegisterSkillInstructionDataArgs["priceMint"];
  tags: RegisterSkillInstructionDataArgs["tags"];
};

export function getRegisterSkillInstruction<
  TAccountSkill extends string,
  TAccountAuthor extends string,
  TAccountProtocolConfig extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RegisterSkillInput<
    TAccountSkill,
    TAccountAuthor,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterSkillInstruction<
  TProgramAddress,
  TAccountSkill,
  TAccountAuthor,
  TAccountProtocolConfig,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    skill: { value: input.skill ?? null, isWritable: true },
    author: { value: input.author ?? null, isWritable: false },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("skill", accounts.skill),
      getAccountMeta("author", accounts.author),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterSkillInstructionDataEncoder().encode(
      args as RegisterSkillInstructionDataArgs,
    ),
    programAddress,
  } as RegisterSkillInstruction<
    TProgramAddress,
    TAccountSkill,
    TAccountAuthor,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterSkillInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    skill: TAccountMetas[0];
    author: TAccountMetas[1];
    protocolConfig: TAccountMetas[2];
    authority: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: RegisterSkillInstructionData;
};

export function parseRegisterSkillInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterSkillInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      skill: getNextAccount(),
      author: getNextAccount(),
      protocolConfig: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterSkillInstructionDataDecoder().decode(instruction.data),
  };
}
