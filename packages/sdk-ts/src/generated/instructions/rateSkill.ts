/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findProtocolConfigPda,
  findRateSkillPurchaseRecordPda,
  findRatingAccountPda,
} from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const RATE_SKILL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  44, 124, 30, 253, 90, 244, 174, 75,
]);

export function getRateSkillDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(RATE_SKILL_DISCRIMINATOR);
}

export type RateSkillInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountSkill extends string | AccountMeta<string> = string,
  TAccountRatingAccount extends string | AccountMeta<string> = string,
  TAccountRater extends string | AccountMeta<string> = string,
  TAccountPurchaseRecord extends string | AccountMeta<string> = string,
  TAccountAuthorAgent extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSkill extends string
        ? WritableAccount<TAccountSkill>
        : TAccountSkill,
      TAccountRatingAccount extends string
        ? WritableAccount<TAccountRatingAccount>
        : TAccountRatingAccount,
      TAccountRater extends string
        ? ReadonlyAccount<TAccountRater>
        : TAccountRater,
      TAccountPurchaseRecord extends string
        ? ReadonlyAccount<TAccountPurchaseRecord>
        : TAccountPurchaseRecord,
      TAccountAuthorAgent extends string
        ? ReadonlyAccount<TAccountAuthorAgent>
        : TAccountAuthorAgent,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RateSkillInstructionData = {
  discriminator: ReadonlyUint8Array;
  rating: number;
  reviewHash: Option<ReadonlyUint8Array>;
};

export type RateSkillInstructionDataArgs = {
  rating: number;
  reviewHash: OptionOrNullable<ReadonlyUint8Array>;
};

export function getRateSkillInstructionDataEncoder(): Encoder<RateSkillInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["rating", getU8Encoder()],
      ["reviewHash", getOptionEncoder(fixEncoderSize(getBytesEncoder(), 32))],
    ]),
    (value) => ({ ...value, discriminator: RATE_SKILL_DISCRIMINATOR }),
  );
}

export function getRateSkillInstructionDataDecoder(): Decoder<RateSkillInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["rating", getU8Decoder()],
    ["reviewHash", getOptionDecoder(fixDecoderSize(getBytesDecoder(), 32))],
  ]);
}

export function getRateSkillInstructionDataCodec(): Codec<
  RateSkillInstructionDataArgs,
  RateSkillInstructionData
> {
  return combineCodec(
    getRateSkillInstructionDataEncoder(),
    getRateSkillInstructionDataDecoder(),
  );
}

export type RateSkillAsyncInput<
  TAccountSkill extends string = string,
  TAccountRatingAccount extends string = string,
  TAccountRater extends string = string,
  TAccountPurchaseRecord extends string = string,
  TAccountAuthorAgent extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  skill: Address<TAccountSkill>;
  ratingAccount?: Address<TAccountRatingAccount>;
  rater: Address<TAccountRater>;
  purchaseRecord?: Address<TAccountPurchaseRecord>;
  /**
   * Skill author's durable registration. Required so a legacy purchase made
   * through another agent controlled by the same wallet cannot self-rate.
   */
  authorAgent: Address<TAccountAuthorAgent>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  rating: RateSkillInstructionDataArgs["rating"];
  reviewHash: RateSkillInstructionDataArgs["reviewHash"];
};

export async function getRateSkillInstructionAsync<
  TAccountSkill extends string,
  TAccountRatingAccount extends string,
  TAccountRater extends string,
  TAccountPurchaseRecord extends string,
  TAccountAuthorAgent extends string,
  TAccountProtocolConfig extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RateSkillAsyncInput<
    TAccountSkill,
    TAccountRatingAccount,
    TAccountRater,
    TAccountPurchaseRecord,
    TAccountAuthorAgent,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RateSkillInstruction<
    TProgramAddress,
    TAccountSkill,
    TAccountRatingAccount,
    TAccountRater,
    TAccountPurchaseRecord,
    TAccountAuthorAgent,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    skill: { value: input.skill ?? null, isWritable: true },
    ratingAccount: { value: input.ratingAccount ?? null, isWritable: true },
    rater: { value: input.rater ?? null, isWritable: false },
    purchaseRecord: { value: input.purchaseRecord ?? null, isWritable: false },
    authorAgent: { value: input.authorAgent ?? null, isWritable: false },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.ratingAccount.value) {
    accounts.ratingAccount.value = await findRatingAccountPda({
      skill: getAddressFromResolvedInstructionAccount(
        "skill",
        accounts.skill.value,
      ),
      rater: getAddressFromResolvedInstructionAccount(
        "rater",
        accounts.rater.value,
      ),
    });
  }
  if (!accounts.purchaseRecord.value) {
    accounts.purchaseRecord.value = await findRateSkillPurchaseRecordPda({
      skill: getAddressFromResolvedInstructionAccount(
        "skill",
        accounts.skill.value,
      ),
      rater: getAddressFromResolvedInstructionAccount(
        "rater",
        accounts.rater.value,
      ),
    });
  }
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("skill", accounts.skill),
      getAccountMeta("ratingAccount", accounts.ratingAccount),
      getAccountMeta("rater", accounts.rater),
      getAccountMeta("purchaseRecord", accounts.purchaseRecord),
      getAccountMeta("authorAgent", accounts.authorAgent),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRateSkillInstructionDataEncoder().encode(
      args as RateSkillInstructionDataArgs,
    ),
    programAddress,
  } as RateSkillInstruction<
    TProgramAddress,
    TAccountSkill,
    TAccountRatingAccount,
    TAccountRater,
    TAccountPurchaseRecord,
    TAccountAuthorAgent,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type RateSkillInput<
  TAccountSkill extends string = string,
  TAccountRatingAccount extends string = string,
  TAccountRater extends string = string,
  TAccountPurchaseRecord extends string = string,
  TAccountAuthorAgent extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  skill: Address<TAccountSkill>;
  ratingAccount: Address<TAccountRatingAccount>;
  rater: Address<TAccountRater>;
  purchaseRecord: Address<TAccountPurchaseRecord>;
  /**
   * Skill author's durable registration. Required so a legacy purchase made
   * through another agent controlled by the same wallet cannot self-rate.
   */
  authorAgent: Address<TAccountAuthorAgent>;
  protocolConfig: Address<TAccountProtocolConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  rating: RateSkillInstructionDataArgs["rating"];
  reviewHash: RateSkillInstructionDataArgs["reviewHash"];
};

export function getRateSkillInstruction<
  TAccountSkill extends string,
  TAccountRatingAccount extends string,
  TAccountRater extends string,
  TAccountPurchaseRecord extends string,
  TAccountAuthorAgent extends string,
  TAccountProtocolConfig extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RateSkillInput<
    TAccountSkill,
    TAccountRatingAccount,
    TAccountRater,
    TAccountPurchaseRecord,
    TAccountAuthorAgent,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RateSkillInstruction<
  TProgramAddress,
  TAccountSkill,
  TAccountRatingAccount,
  TAccountRater,
  TAccountPurchaseRecord,
  TAccountAuthorAgent,
  TAccountProtocolConfig,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    skill: { value: input.skill ?? null, isWritable: true },
    ratingAccount: { value: input.ratingAccount ?? null, isWritable: true },
    rater: { value: input.rater ?? null, isWritable: false },
    purchaseRecord: { value: input.purchaseRecord ?? null, isWritable: false },
    authorAgent: { value: input.authorAgent ?? null, isWritable: false },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("skill", accounts.skill),
      getAccountMeta("ratingAccount", accounts.ratingAccount),
      getAccountMeta("rater", accounts.rater),
      getAccountMeta("purchaseRecord", accounts.purchaseRecord),
      getAccountMeta("authorAgent", accounts.authorAgent),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRateSkillInstructionDataEncoder().encode(
      args as RateSkillInstructionDataArgs,
    ),
    programAddress,
  } as RateSkillInstruction<
    TProgramAddress,
    TAccountSkill,
    TAccountRatingAccount,
    TAccountRater,
    TAccountPurchaseRecord,
    TAccountAuthorAgent,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedRateSkillInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    skill: TAccountMetas[0];
    ratingAccount: TAccountMetas[1];
    rater: TAccountMetas[2];
    purchaseRecord: TAccountMetas[3];
    /**
     * Skill author's durable registration. Required so a legacy purchase made
     * through another agent controlled by the same wallet cannot self-rate.
     */
    authorAgent: TAccountMetas[4];
    protocolConfig: TAccountMetas[5];
    authority: TAccountMetas[6];
    systemProgram: TAccountMetas[7];
  };
  data: RateSkillInstructionData;
};

export function parseRateSkillInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRateSkillInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 8,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      skill: getNextAccount(),
      ratingAccount: getNextAccount(),
      rater: getNextAccount(),
      purchaseRecord: getNextAccount(),
      authorAgent: getNextAccount(),
      protocolConfig: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRateSkillInstructionDataDecoder().decode(instruction.data),
  };
}
