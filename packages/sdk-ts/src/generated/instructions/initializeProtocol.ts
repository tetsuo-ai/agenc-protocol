/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProtocolConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_PROTOCOL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([188, 233, 252, 106, 134, 146, 202, 91]);

export function getInitializeProtocolDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(INITIALIZE_PROTOCOL_DISCRIMINATOR);
}

export type InitializeProtocolInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSecondSigner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProtocolConfig extends string
        ? WritableAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountTreasury extends string
        ? ReadonlySignerAccount<TAccountTreasury> &
            AccountSignerMeta<TAccountTreasury>
        : TAccountTreasury,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSecondSigner extends string
        ? ReadonlySignerAccount<TAccountSecondSigner> &
            AccountSignerMeta<TAccountSecondSigner>
        : TAccountSecondSigner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeProtocolInstructionData = {
  discriminator: ReadonlyUint8Array;
  disputeThreshold: number;
  protocolFeeBps: number;
  minStake: bigint;
  minStakeForDispute: bigint;
  multisigThreshold: number;
  multisigOwners: Array<Address>;
};

export type InitializeProtocolInstructionDataArgs = {
  disputeThreshold: number;
  protocolFeeBps: number;
  minStake: number | bigint;
  minStakeForDispute: number | bigint;
  multisigThreshold: number;
  multisigOwners: Array<Address>;
};

export function getInitializeProtocolInstructionDataEncoder(): Encoder<InitializeProtocolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["disputeThreshold", getU8Encoder()],
      ["protocolFeeBps", getU16Encoder()],
      ["minStake", getU64Encoder()],
      ["minStakeForDispute", getU64Encoder()],
      ["multisigThreshold", getU8Encoder()],
      ["multisigOwners", getArrayEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_PROTOCOL_DISCRIMINATOR }),
  );
}

export function getInitializeProtocolInstructionDataDecoder(): Decoder<InitializeProtocolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["disputeThreshold", getU8Decoder()],
    ["protocolFeeBps", getU16Decoder()],
    ["minStake", getU64Decoder()],
    ["minStakeForDispute", getU64Decoder()],
    ["multisigThreshold", getU8Decoder()],
    ["multisigOwners", getArrayDecoder(getAddressDecoder())],
  ]);
}

export function getInitializeProtocolInstructionDataCodec(): Codec<
  InitializeProtocolInstructionDataArgs,
  InitializeProtocolInstructionData
> {
  return combineCodec(
    getInitializeProtocolInstructionDataEncoder(),
    getInitializeProtocolInstructionDataDecoder(),
  );
}

export type InitializeProtocolAsyncInput<
  TAccountProtocolConfig extends string = string,
  TAccountTreasury extends string = string,
  TAccountAuthority extends string = string,
  TAccountSecondSigner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  protocolConfig?: Address<TAccountProtocolConfig>;
  /**
   * Treasury account to receive protocol fees. Production clients must request
   * the custody key's signature in generated account metas.
   */
  treasury: TransactionSigner<TAccountTreasury>;
  authority: TransactionSigner<TAccountAuthority>;
  /**
   * Second multisig signer required at initialization to prevent single-party setup.
   * Must be different from authority and must be in multisig_owners.
   * This ensures at least two parties are involved in protocol initialization (fix #556).
   */
  secondSigner: TransactionSigner<TAccountSecondSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  disputeThreshold: InitializeProtocolInstructionDataArgs["disputeThreshold"];
  protocolFeeBps: InitializeProtocolInstructionDataArgs["protocolFeeBps"];
  minStake: InitializeProtocolInstructionDataArgs["minStake"];
  minStakeForDispute: InitializeProtocolInstructionDataArgs["minStakeForDispute"];
  multisigThreshold: InitializeProtocolInstructionDataArgs["multisigThreshold"];
  multisigOwners: InitializeProtocolInstructionDataArgs["multisigOwners"];
};

export async function getInitializeProtocolInstructionAsync<
  TAccountProtocolConfig extends string,
  TAccountTreasury extends string,
  TAccountAuthority extends string,
  TAccountSecondSigner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: InitializeProtocolAsyncInput<
    TAccountProtocolConfig,
    TAccountTreasury,
    TAccountAuthority,
    TAccountSecondSigner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeProtocolInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountTreasury,
    TAccountAuthority,
    TAccountSecondSigner,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    secondSigner: { value: input.secondSigner ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("secondSigner", accounts.secondSigner),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeProtocolInstructionDataEncoder().encode(
      args as InitializeProtocolInstructionDataArgs,
    ),
    programAddress,
  } as InitializeProtocolInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountTreasury,
    TAccountAuthority,
    TAccountSecondSigner,
    TAccountSystemProgram
  >);
}

export type InitializeProtocolInput<
  TAccountProtocolConfig extends string = string,
  TAccountTreasury extends string = string,
  TAccountAuthority extends string = string,
  TAccountSecondSigner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  protocolConfig: Address<TAccountProtocolConfig>;
  /**
   * Treasury account to receive protocol fees. Production clients must request
   * the custody key's signature in generated account metas.
   */
  treasury: TransactionSigner<TAccountTreasury>;
  authority: TransactionSigner<TAccountAuthority>;
  /**
   * Second multisig signer required at initialization to prevent single-party setup.
   * Must be different from authority and must be in multisig_owners.
   * This ensures at least two parties are involved in protocol initialization (fix #556).
   */
  secondSigner: TransactionSigner<TAccountSecondSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  disputeThreshold: InitializeProtocolInstructionDataArgs["disputeThreshold"];
  protocolFeeBps: InitializeProtocolInstructionDataArgs["protocolFeeBps"];
  minStake: InitializeProtocolInstructionDataArgs["minStake"];
  minStakeForDispute: InitializeProtocolInstructionDataArgs["minStakeForDispute"];
  multisigThreshold: InitializeProtocolInstructionDataArgs["multisigThreshold"];
  multisigOwners: InitializeProtocolInstructionDataArgs["multisigOwners"];
};

export function getInitializeProtocolInstruction<
  TAccountProtocolConfig extends string,
  TAccountTreasury extends string,
  TAccountAuthority extends string,
  TAccountSecondSigner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: InitializeProtocolInput<
    TAccountProtocolConfig,
    TAccountTreasury,
    TAccountAuthority,
    TAccountSecondSigner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeProtocolInstruction<
  TProgramAddress,
  TAccountProtocolConfig,
  TAccountTreasury,
  TAccountAuthority,
  TAccountSecondSigner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    secondSigner: { value: input.secondSigner ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("secondSigner", accounts.secondSigner),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeProtocolInstructionDataEncoder().encode(
      args as InitializeProtocolInstructionDataArgs,
    ),
    programAddress,
  } as InitializeProtocolInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountTreasury,
    TAccountAuthority,
    TAccountSecondSigner,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeProtocolInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    protocolConfig: TAccountMetas[0];
    /**
     * Treasury account to receive protocol fees. Production clients must request
     * the custody key's signature in generated account metas.
     */
    treasury: TAccountMetas[1];
    authority: TAccountMetas[2];
    /**
     * Second multisig signer required at initialization to prevent single-party setup.
     * Must be different from authority and must be in multisig_owners.
     * This ensures at least two parties are involved in protocol initialization (fix #556).
     */
    secondSigner: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: InitializeProtocolInstructionData;
};

export function parseInitializeProtocolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeProtocolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      protocolConfig: getNextAccount(),
      treasury: getNextAccount(),
      authority: getNextAccount(),
      secondSigner: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeProtocolInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
