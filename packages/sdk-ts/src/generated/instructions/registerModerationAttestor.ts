/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findModerationAttestorPda, findProtocolConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const REGISTER_MODERATION_ATTESTOR_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([118, 194, 62, 4, 232, 60, 199, 142]);

export function getRegisterModerationAttestorDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(
    REGISTER_MODERATION_ATTESTOR_DISCRIMINATOR,
  );
}

export type RegisterModerationAttestorInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountModerationAttestor extends string | AccountMeta<string> = string,
  TAccountAttestor extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountModerationAttestor extends string
        ? WritableAccount<TAccountModerationAttestor>
        : TAccountModerationAttestor,
      TAccountAttestor extends string
        ? WritableSignerAccount<TAccountAttestor> &
            AccountSignerMeta<TAccountAttestor>
        : TAccountAttestor,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterModerationAttestorInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RegisterModerationAttestorInstructionDataArgs = {};

export function getRegisterModerationAttestorInstructionDataEncoder(): FixedSizeEncoder<RegisterModerationAttestorInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
    ]),
    (value) => ({
      ...value,
      discriminator: REGISTER_MODERATION_ATTESTOR_DISCRIMINATOR,
    }),
  );
}

export function getRegisterModerationAttestorInstructionDataDecoder(): FixedSizeDecoder<RegisterModerationAttestorInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRegisterModerationAttestorInstructionDataCodec(): FixedSizeCodec<
  RegisterModerationAttestorInstructionDataArgs,
  RegisterModerationAttestorInstructionData
> {
  return combineCodec(
    getRegisterModerationAttestorInstructionDataEncoder(),
    getRegisterModerationAttestorInstructionDataDecoder(),
  );
}

export type RegisterModerationAttestorAsyncInput<
  TAccountModerationAttestor extends string = string,
  TAccountAttestor extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Roster entry for the self-registering signer. `init` ⇒ registering an
   * already-rostered wallet fails (the desired "already registered" signal), and a
   * re-register after exit re-inits a fresh entry.
   */
  moderationAttestor?: Address<TAccountModerationAttestor>;
  /**
   * The self-registering wallet. No authority constraint — this is the
   * permissionless path. It pays rent AND the registration bond.
   */
  attestor: TransactionSigner<TAccountAttestor>;
  /**
   * Emergency entry-control. A paused or version-incompatible protocol must
   * not accept a fresh seven-day moderation bond while ordinary marketplace
   * entry is disabled. Exit instructions intentionally do not carry this
   * pause gate, so existing attestors can always recover their bond.
   */
  protocolConfig?: Address<TAccountProtocolConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getRegisterModerationAttestorInstructionAsync<
  TAccountModerationAttestor extends string,
  TAccountAttestor extends string,
  TAccountProtocolConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RegisterModerationAttestorAsyncInput<
    TAccountModerationAttestor,
    TAccountAttestor,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterModerationAttestorInstruction<
    TProgramAddress,
    TAccountModerationAttestor,
    TAccountAttestor,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    moderationAttestor: {
      value: input.moderationAttestor ?? null,
      isWritable: true,
    },
    attestor: { value: input.attestor ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.moderationAttestor.value) {
    accounts.moderationAttestor.value = await findModerationAttestorPda({
      attestor: getAddressFromResolvedInstructionAccount(
        "attestor",
        accounts.attestor.value,
      ),
    });
  }
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("moderationAttestor", accounts.moderationAttestor),
      getAccountMeta("attestor", accounts.attestor),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterModerationAttestorInstructionDataEncoder().encode({}),
    programAddress,
  } as RegisterModerationAttestorInstruction<
    TProgramAddress,
    TAccountModerationAttestor,
    TAccountAttestor,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >);
}

export type RegisterModerationAttestorInput<
  TAccountModerationAttestor extends string = string,
  TAccountAttestor extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Roster entry for the self-registering signer. `init` ⇒ registering an
   * already-rostered wallet fails (the desired "already registered" signal), and a
   * re-register after exit re-inits a fresh entry.
   */
  moderationAttestor: Address<TAccountModerationAttestor>;
  /**
   * The self-registering wallet. No authority constraint — this is the
   * permissionless path. It pays rent AND the registration bond.
   */
  attestor: TransactionSigner<TAccountAttestor>;
  /**
   * Emergency entry-control. A paused or version-incompatible protocol must
   * not accept a fresh seven-day moderation bond while ordinary marketplace
   * entry is disabled. Exit instructions intentionally do not carry this
   * pause gate, so existing attestors can always recover their bond.
   */
  protocolConfig: Address<TAccountProtocolConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getRegisterModerationAttestorInstruction<
  TAccountModerationAttestor extends string,
  TAccountAttestor extends string,
  TAccountProtocolConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RegisterModerationAttestorInput<
    TAccountModerationAttestor,
    TAccountAttestor,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterModerationAttestorInstruction<
  TProgramAddress,
  TAccountModerationAttestor,
  TAccountAttestor,
  TAccountProtocolConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    moderationAttestor: {
      value: input.moderationAttestor ?? null,
      isWritable: true,
    },
    attestor: { value: input.attestor ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("moderationAttestor", accounts.moderationAttestor),
      getAccountMeta("attestor", accounts.attestor),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterModerationAttestorInstructionDataEncoder().encode({}),
    programAddress,
  } as RegisterModerationAttestorInstruction<
    TProgramAddress,
    TAccountModerationAttestor,
    TAccountAttestor,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterModerationAttestorInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Roster entry for the self-registering signer. `init` ⇒ registering an
     * already-rostered wallet fails (the desired "already registered" signal), and a
     * re-register after exit re-inits a fresh entry.
     */
    moderationAttestor: TAccountMetas[0];
    /**
     * The self-registering wallet. No authority constraint — this is the
     * permissionless path. It pays rent AND the registration bond.
     */
    attestor: TAccountMetas[1];
    /**
     * Emergency entry-control. A paused or version-incompatible protocol must
     * not accept a fresh seven-day moderation bond while ordinary marketplace
     * entry is disabled. Exit instructions intentionally do not carry this
     * pause gate, so existing attestors can always recover their bond.
     */
    protocolConfig: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: RegisterModerationAttestorInstructionData;
};

export function parseRegisterModerationAttestorInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterModerationAttestorInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      moderationAttestor: getNextAccount(),
      attestor: getNextAccount(),
      protocolConfig: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterModerationAttestorInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
