/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findBidBookPda, findBidPda, findProtocolConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const DEMOTE_INELIGIBLE_BEST_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([51, 92, 169, 234, 241, 36, 97, 216]);

export function getDemoteIneligibleBestDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(DEMOTE_INELIGIBLE_BEST_DISCRIMINATOR);
}

export type DemoteIneligibleBestInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountBidBook extends string | AccountMeta<string> = string,
  TAccountBid extends string | AccountMeta<string> = string,
  TAccountBidder extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountTask extends string
        ? ReadonlyAccount<TAccountTask>
        : TAccountTask,
      TAccountBidBook extends string
        ? WritableAccount<TAccountBidBook>
        : TAccountBidBook,
      TAccountBid extends string ? ReadonlyAccount<TAccountBid> : TAccountBid,
      TAccountBidder extends string
        ? ReadonlyAccount<TAccountBidder>
        : TAccountBidder,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type DemoteIneligibleBestInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type DemoteIneligibleBestInstructionDataArgs = {};

export function getDemoteIneligibleBestInstructionDataEncoder(): FixedSizeEncoder<DemoteIneligibleBestInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
    ]),
    (value) => ({
      ...value,
      discriminator: DEMOTE_INELIGIBLE_BEST_DISCRIMINATOR,
    }),
  );
}

export function getDemoteIneligibleBestInstructionDataDecoder(): FixedSizeDecoder<DemoteIneligibleBestInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getDemoteIneligibleBestInstructionDataCodec(): FixedSizeCodec<
  DemoteIneligibleBestInstructionDataArgs,
  DemoteIneligibleBestInstructionData
> {
  return combineCodec(
    getDemoteIneligibleBestInstructionDataEncoder(),
    getDemoteIneligibleBestInstructionDataDecoder(),
  );
}

export type DemoteIneligibleBestAsyncInput<
  TAccountProtocolConfig extends string = string,
  TAccountTask extends string = string,
  TAccountBidBook extends string = string,
  TAccountBid extends string = string,
  TAccountBidder extends string = string,
  TAccountAuthority extends string = string,
> = {
  protocolConfig?: Address<TAccountProtocolConfig>;
  task: Address<TAccountTask>;
  bidBook?: Address<TAccountBidBook>;
  bid?: Address<TAccountBid>;
  bidder: Address<TAccountBidder>;
  authority: TransactionSigner<TAccountAuthority>;
};

export async function getDemoteIneligibleBestInstructionAsync<
  TAccountProtocolConfig extends string,
  TAccountTask extends string,
  TAccountBidBook extends string,
  TAccountBid extends string,
  TAccountBidder extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: DemoteIneligibleBestAsyncInput<
    TAccountProtocolConfig,
    TAccountTask,
    TAccountBidBook,
    TAccountBid,
    TAccountBidder,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DemoteIneligibleBestInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountTask,
    TAccountBidBook,
    TAccountBid,
    TAccountBidder,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    task: { value: input.task ?? null, isWritable: false },
    bidBook: { value: input.bidBook ?? null, isWritable: true },
    bid: { value: input.bid ?? null, isWritable: false },
    bidder: { value: input.bidder ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.bidBook.value) {
    accounts.bidBook.value = await findBidBookPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }
  if (!accounts.bid.value) {
    accounts.bid.value = await findBidPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
      bidder: getAddressFromResolvedInstructionAccount(
        "bidder",
        accounts.bidder.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("task", accounts.task),
      getAccountMeta("bidBook", accounts.bidBook),
      getAccountMeta("bid", accounts.bid),
      getAccountMeta("bidder", accounts.bidder),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getDemoteIneligibleBestInstructionDataEncoder().encode({}),
    programAddress,
  } as DemoteIneligibleBestInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountTask,
    TAccountBidBook,
    TAccountBid,
    TAccountBidder,
    TAccountAuthority
  >);
}

export type DemoteIneligibleBestInput<
  TAccountProtocolConfig extends string = string,
  TAccountTask extends string = string,
  TAccountBidBook extends string = string,
  TAccountBid extends string = string,
  TAccountBidder extends string = string,
  TAccountAuthority extends string = string,
> = {
  protocolConfig: Address<TAccountProtocolConfig>;
  task: Address<TAccountTask>;
  bidBook: Address<TAccountBidBook>;
  bid: Address<TAccountBid>;
  bidder: Address<TAccountBidder>;
  authority: TransactionSigner<TAccountAuthority>;
};

export function getDemoteIneligibleBestInstruction<
  TAccountProtocolConfig extends string,
  TAccountTask extends string,
  TAccountBidBook extends string,
  TAccountBid extends string,
  TAccountBidder extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: DemoteIneligibleBestInput<
    TAccountProtocolConfig,
    TAccountTask,
    TAccountBidBook,
    TAccountBid,
    TAccountBidder,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): DemoteIneligibleBestInstruction<
  TProgramAddress,
  TAccountProtocolConfig,
  TAccountTask,
  TAccountBidBook,
  TAccountBid,
  TAccountBidder,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    task: { value: input.task ?? null, isWritable: false },
    bidBook: { value: input.bidBook ?? null, isWritable: true },
    bid: { value: input.bid ?? null, isWritable: false },
    bidder: { value: input.bidder ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("task", accounts.task),
      getAccountMeta("bidBook", accounts.bidBook),
      getAccountMeta("bid", accounts.bid),
      getAccountMeta("bidder", accounts.bidder),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getDemoteIneligibleBestInstructionDataEncoder().encode({}),
    programAddress,
  } as DemoteIneligibleBestInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountTask,
    TAccountBidBook,
    TAccountBid,
    TAccountBidder,
    TAccountAuthority
  >);
}

export type ParsedDemoteIneligibleBestInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    protocolConfig: TAccountMetas[0];
    task: TAccountMetas[1];
    bidBook: TAccountMetas[2];
    bid: TAccountMetas[3];
    bidder: TAccountMetas[4];
    authority: TAccountMetas[5];
  };
  data: DemoteIneligibleBestInstructionData;
};

export function parseDemoteIneligibleBestInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDemoteIneligibleBestInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      protocolConfig: getNextAccount(),
      task: getNextAccount(),
      bidBook: getNextAccount(),
      bid: getNextAccount(),
      bidder: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getDemoteIneligibleBestInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
