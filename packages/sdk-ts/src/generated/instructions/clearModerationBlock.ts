/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProtocolConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const CLEAR_MODERATION_BLOCK_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([210, 192, 192, 105, 106, 74, 147, 252]);

export function getClearModerationBlockDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLEAR_MODERATION_BLOCK_DISCRIMINATOR,
  );
}

export type ClearModerationBlockInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountModerationBlock extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountModerationBlock extends string
        ? WritableAccount<TAccountModerationBlock>
        : TAccountModerationBlock,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type ClearModerationBlockInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ClearModerationBlockInstructionDataArgs = {};

export function getClearModerationBlockInstructionDataEncoder(): FixedSizeEncoder<ClearModerationBlockInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CLEAR_MODERATION_BLOCK_DISCRIMINATOR,
    }),
  );
}

export function getClearModerationBlockInstructionDataDecoder(): FixedSizeDecoder<ClearModerationBlockInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClearModerationBlockInstructionDataCodec(): FixedSizeCodec<
  ClearModerationBlockInstructionDataArgs,
  ClearModerationBlockInstructionData
> {
  return combineCodec(
    getClearModerationBlockInstructionDataEncoder(),
    getClearModerationBlockInstructionDataDecoder(),
  );
}

export type ClearModerationBlockAsyncInput<
  TAccountProtocolConfig extends string = string,
  TAccountModerationBlock extends string = string,
  TAccountAuthority extends string = string,
> = {
  protocolConfig?: Address<TAccountProtocolConfig>;
  /**
   * Seeded by its own stored `content_hash` (canonical PDA). Stays open after the
   * clear — the on-chain audit trail of the takedown.
   */
  moderationBlock: Address<TAccountModerationBlock>;
  authority: TransactionSigner<TAccountAuthority>;
};

export async function getClearModerationBlockInstructionAsync<
  TAccountProtocolConfig extends string,
  TAccountModerationBlock extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: ClearModerationBlockAsyncInput<
    TAccountProtocolConfig,
    TAccountModerationBlock,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ClearModerationBlockInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountModerationBlock,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    moderationBlock: { value: input.moderationBlock ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("moderationBlock", accounts.moderationBlock),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getClearModerationBlockInstructionDataEncoder().encode({}),
    programAddress,
  } as ClearModerationBlockInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountModerationBlock,
    TAccountAuthority
  >);
}

export type ClearModerationBlockInput<
  TAccountProtocolConfig extends string = string,
  TAccountModerationBlock extends string = string,
  TAccountAuthority extends string = string,
> = {
  protocolConfig: Address<TAccountProtocolConfig>;
  /**
   * Seeded by its own stored `content_hash` (canonical PDA). Stays open after the
   * clear — the on-chain audit trail of the takedown.
   */
  moderationBlock: Address<TAccountModerationBlock>;
  authority: TransactionSigner<TAccountAuthority>;
};

export function getClearModerationBlockInstruction<
  TAccountProtocolConfig extends string,
  TAccountModerationBlock extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: ClearModerationBlockInput<
    TAccountProtocolConfig,
    TAccountModerationBlock,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): ClearModerationBlockInstruction<
  TProgramAddress,
  TAccountProtocolConfig,
  TAccountModerationBlock,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    moderationBlock: { value: input.moderationBlock ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("moderationBlock", accounts.moderationBlock),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getClearModerationBlockInstructionDataEncoder().encode({}),
    programAddress,
  } as ClearModerationBlockInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountModerationBlock,
    TAccountAuthority
  >);
}

export type ParsedClearModerationBlockInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    protocolConfig: TAccountMetas[0];
    /**
     * Seeded by its own stored `content_hash` (canonical PDA). Stays open after the
     * clear — the on-chain audit trail of the takedown.
     */
    moderationBlock: TAccountMetas[1];
    authority: TAccountMetas[2];
  };
  data: ClearModerationBlockInstructionData;
};

export function parseClearModerationBlockInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClearModerationBlockInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      protocolConfig: getNextAccount(),
      moderationBlock: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getClearModerationBlockInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
