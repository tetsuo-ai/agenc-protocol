/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findAcceptTaskResultClaimPda,
  findProtocolConfigPda,
  findTaskSubmissionPda,
  findTaskValidationConfigPda,
} from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const SUBMIT_TASK_RESULT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([39, 108, 74, 4, 66, 125, 157, 7]);

export function getSubmitTaskResultDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SUBMIT_TASK_RESULT_DISCRIMINATOR,
  );
}

export type SubmitTaskResultInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountClaim extends string | AccountMeta<string> = string,
  TAccountTaskValidationConfig extends string | AccountMeta<string> = string,
  TAccountTaskSubmission extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountWorker extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTask extends string
        ? WritableAccount<TAccountTask>
        : TAccountTask,
      TAccountClaim extends string
        ? WritableAccount<TAccountClaim>
        : TAccountClaim,
      TAccountTaskValidationConfig extends string
        ? WritableAccount<TAccountTaskValidationConfig>
        : TAccountTaskValidationConfig,
      TAccountTaskSubmission extends string
        ? WritableAccount<TAccountTaskSubmission>
        : TAccountTaskSubmission,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountWorker extends string
        ? ReadonlyAccount<TAccountWorker>
        : TAccountWorker,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SubmitTaskResultInstructionData = {
  discriminator: ReadonlyUint8Array;
  proofHash: ReadonlyUint8Array;
  resultData: Option<ReadonlyUint8Array>;
};

export type SubmitTaskResultInstructionDataArgs = {
  proofHash: ReadonlyUint8Array;
  resultData: OptionOrNullable<ReadonlyUint8Array>;
};

export function getSubmitTaskResultInstructionDataEncoder(): Encoder<SubmitTaskResultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["proofHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["resultData", getOptionEncoder(fixEncoderSize(getBytesEncoder(), 64))],
    ]),
    (value) => ({ ...value, discriminator: SUBMIT_TASK_RESULT_DISCRIMINATOR }),
  );
}

export function getSubmitTaskResultInstructionDataDecoder(): Decoder<SubmitTaskResultInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["proofHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["resultData", getOptionDecoder(fixDecoderSize(getBytesDecoder(), 64))],
  ]);
}

export function getSubmitTaskResultInstructionDataCodec(): Codec<
  SubmitTaskResultInstructionDataArgs,
  SubmitTaskResultInstructionData
> {
  return combineCodec(
    getSubmitTaskResultInstructionDataEncoder(),
    getSubmitTaskResultInstructionDataDecoder(),
  );
}

export type SubmitTaskResultAsyncInput<
  TAccountTask extends string = string,
  TAccountClaim extends string = string,
  TAccountTaskValidationConfig extends string = string,
  TAccountTaskSubmission extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountWorker extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  task: Address<TAccountTask>;
  claim?: Address<TAccountClaim>;
  taskValidationConfig?: Address<TAccountTaskValidationConfig>;
  taskSubmission?: Address<TAccountTaskSubmission>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  worker: Address<TAccountWorker>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  proofHash: SubmitTaskResultInstructionDataArgs["proofHash"];
  resultData: SubmitTaskResultInstructionDataArgs["resultData"];
};

export async function getSubmitTaskResultInstructionAsync<
  TAccountTask extends string,
  TAccountClaim extends string,
  TAccountTaskValidationConfig extends string,
  TAccountTaskSubmission extends string,
  TAccountProtocolConfig extends string,
  TAccountWorker extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: SubmitTaskResultAsyncInput<
    TAccountTask,
    TAccountClaim,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountProtocolConfig,
    TAccountWorker,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SubmitTaskResultInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountClaim,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountProtocolConfig,
    TAccountWorker,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: true },
    claim: { value: input.claim ?? null, isWritable: true },
    taskValidationConfig: {
      value: input.taskValidationConfig ?? null,
      isWritable: true,
    },
    taskSubmission: { value: input.taskSubmission ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    worker: { value: input.worker ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.claim.value) {
    accounts.claim.value = await findAcceptTaskResultClaimPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
      worker: getAddressFromResolvedInstructionAccount(
        "worker",
        accounts.worker.value,
      ),
    });
  }
  if (!accounts.taskValidationConfig.value) {
    accounts.taskValidationConfig.value = await findTaskValidationConfigPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }
  if (!accounts.taskSubmission.value) {
    accounts.taskSubmission.value = await findTaskSubmissionPda({
      claim: getAddressFromResolvedInstructionAccount(
        "claim",
        accounts.claim.value,
      ),
    });
  }
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("claim", accounts.claim),
      getAccountMeta("taskValidationConfig", accounts.taskValidationConfig),
      getAccountMeta("taskSubmission", accounts.taskSubmission),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("worker", accounts.worker),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSubmitTaskResultInstructionDataEncoder().encode(
      args as SubmitTaskResultInstructionDataArgs,
    ),
    programAddress,
  } as SubmitTaskResultInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountClaim,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountProtocolConfig,
    TAccountWorker,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type SubmitTaskResultInput<
  TAccountTask extends string = string,
  TAccountClaim extends string = string,
  TAccountTaskValidationConfig extends string = string,
  TAccountTaskSubmission extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountWorker extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  task: Address<TAccountTask>;
  claim: Address<TAccountClaim>;
  taskValidationConfig: Address<TAccountTaskValidationConfig>;
  taskSubmission: Address<TAccountTaskSubmission>;
  protocolConfig: Address<TAccountProtocolConfig>;
  worker: Address<TAccountWorker>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  proofHash: SubmitTaskResultInstructionDataArgs["proofHash"];
  resultData: SubmitTaskResultInstructionDataArgs["resultData"];
};

export function getSubmitTaskResultInstruction<
  TAccountTask extends string,
  TAccountClaim extends string,
  TAccountTaskValidationConfig extends string,
  TAccountTaskSubmission extends string,
  TAccountProtocolConfig extends string,
  TAccountWorker extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: SubmitTaskResultInput<
    TAccountTask,
    TAccountClaim,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountProtocolConfig,
    TAccountWorker,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SubmitTaskResultInstruction<
  TProgramAddress,
  TAccountTask,
  TAccountClaim,
  TAccountTaskValidationConfig,
  TAccountTaskSubmission,
  TAccountProtocolConfig,
  TAccountWorker,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: true },
    claim: { value: input.claim ?? null, isWritable: true },
    taskValidationConfig: {
      value: input.taskValidationConfig ?? null,
      isWritable: true,
    },
    taskSubmission: { value: input.taskSubmission ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    worker: { value: input.worker ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("claim", accounts.claim),
      getAccountMeta("taskValidationConfig", accounts.taskValidationConfig),
      getAccountMeta("taskSubmission", accounts.taskSubmission),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("worker", accounts.worker),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSubmitTaskResultInstructionDataEncoder().encode(
      args as SubmitTaskResultInstructionDataArgs,
    ),
    programAddress,
  } as SubmitTaskResultInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountClaim,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountProtocolConfig,
    TAccountWorker,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedSubmitTaskResultInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    task: TAccountMetas[0];
    claim: TAccountMetas[1];
    taskValidationConfig: TAccountMetas[2];
    taskSubmission: TAccountMetas[3];
    protocolConfig: TAccountMetas[4];
    worker: TAccountMetas[5];
    authority: TAccountMetas[6];
    systemProgram: TAccountMetas[7];
  };
  data: SubmitTaskResultInstructionData;
};

export function parseSubmitTaskResultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSubmitTaskResultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 8,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      task: getNextAccount(),
      claim: getNextAccount(),
      taskValidationConfig: getNextAccount(),
      taskSubmission: getNextAccount(),
      protocolConfig: getNextAccount(),
      worker: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSubmitTaskResultInstructionDataDecoder().decode(instruction.data),
  };
}
