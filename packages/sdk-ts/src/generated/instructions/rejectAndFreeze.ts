/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findProtocolConfigPda,
  findTaskSubmissionPda,
  findTaskValidationConfigPda,
} from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const REJECT_AND_FREEZE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([114, 109, 131, 41, 79, 109, 135, 18]);

export function getRejectAndFreezeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REJECT_AND_FREEZE_DISCRIMINATOR,
  );
}

export type RejectAndFreezeInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountClaim extends string | AccountMeta<string> = string,
  TAccountTaskValidationConfig extends string | AccountMeta<string> = string,
  TAccountTaskSubmission extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountAgentStats extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTask extends string
        ? WritableAccount<TAccountTask>
        : TAccountTask,
      TAccountClaim extends string
        ? ReadonlyAccount<TAccountClaim>
        : TAccountClaim,
      TAccountTaskValidationConfig extends string
        ? WritableAccount<TAccountTaskValidationConfig>
        : TAccountTaskValidationConfig,
      TAccountTaskSubmission extends string
        ? WritableAccount<TAccountTaskSubmission>
        : TAccountTaskSubmission,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountCreator extends string
        ? WritableSignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      TAccountAgentStats extends string
        ? WritableAccount<TAccountAgentStats>
        : TAccountAgentStats,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RejectAndFreezeInstructionData = {
  discriminator: ReadonlyUint8Array;
  rejectionHash: ReadonlyUint8Array;
};

export type RejectAndFreezeInstructionDataArgs = {
  rejectionHash: ReadonlyUint8Array;
};

export function getRejectAndFreezeInstructionDataEncoder(): FixedSizeEncoder<RejectAndFreezeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["rejectionHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: REJECT_AND_FREEZE_DISCRIMINATOR }),
  );
}

export function getRejectAndFreezeInstructionDataDecoder(): FixedSizeDecoder<RejectAndFreezeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["rejectionHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getRejectAndFreezeInstructionDataCodec(): FixedSizeCodec<
  RejectAndFreezeInstructionDataArgs,
  RejectAndFreezeInstructionData
> {
  return combineCodec(
    getRejectAndFreezeInstructionDataEncoder(),
    getRejectAndFreezeInstructionDataDecoder(),
  );
}

export type RejectAndFreezeAsyncInput<
  TAccountTask extends string = string,
  TAccountClaim extends string = string,
  TAccountTaskValidationConfig extends string = string,
  TAccountTaskSubmission extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountCreator extends string = string,
  TAccountAgentStats extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  task: Address<TAccountTask>;
  claim: Address<TAccountClaim>;
  taskValidationConfig?: Address<TAccountTaskValidationConfig>;
  taskSubmission?: Address<TAccountTaskSubmission>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  creator: TransactionSigner<TAccountCreator>;
  /**
   * OPTIONAL (P6.6): the worker agent's track-record aggregate. When supplied, this
   * freeze-rejection bumps `tasks_rejected`. Bound to `["agent_stats", claim.worker]`
   * (the claim's worker is the worker AgentRegistration PDA), created lazily on first
   * write. Telemetry only — never gates the freeze above.
   */
  agentStats?: Address<TAccountAgentStats>;
  /** Required only when `agent_stats` is supplied (for `init_if_needed`). */
  systemProgram?: Address<TAccountSystemProgram>;
  rejectionHash: RejectAndFreezeInstructionDataArgs["rejectionHash"];
};

export async function getRejectAndFreezeInstructionAsync<
  TAccountTask extends string,
  TAccountClaim extends string,
  TAccountTaskValidationConfig extends string,
  TAccountTaskSubmission extends string,
  TAccountProtocolConfig extends string,
  TAccountCreator extends string,
  TAccountAgentStats extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RejectAndFreezeAsyncInput<
    TAccountTask,
    TAccountClaim,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountProtocolConfig,
    TAccountCreator,
    TAccountAgentStats,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RejectAndFreezeInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountClaim,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountProtocolConfig,
    TAccountCreator,
    TAccountAgentStats,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: true },
    claim: { value: input.claim ?? null, isWritable: false },
    taskValidationConfig: {
      value: input.taskValidationConfig ?? null,
      isWritable: true,
    },
    taskSubmission: { value: input.taskSubmission ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    creator: { value: input.creator ?? null, isWritable: true },
    agentStats: { value: input.agentStats ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.taskValidationConfig.value) {
    accounts.taskValidationConfig.value = await findTaskValidationConfigPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }
  if (!accounts.taskSubmission.value) {
    accounts.taskSubmission.value = await findTaskSubmissionPda({
      claim: getAddressFromResolvedInstructionAccount(
        "claim",
        accounts.claim.value,
      ),
    });
  }
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("claim", accounts.claim),
      getAccountMeta("taskValidationConfig", accounts.taskValidationConfig),
      getAccountMeta("taskSubmission", accounts.taskSubmission),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("agentStats", accounts.agentStats),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRejectAndFreezeInstructionDataEncoder().encode(
      args as RejectAndFreezeInstructionDataArgs,
    ),
    programAddress,
  } as RejectAndFreezeInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountClaim,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountProtocolConfig,
    TAccountCreator,
    TAccountAgentStats,
    TAccountSystemProgram
  >);
}

export type RejectAndFreezeInput<
  TAccountTask extends string = string,
  TAccountClaim extends string = string,
  TAccountTaskValidationConfig extends string = string,
  TAccountTaskSubmission extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountCreator extends string = string,
  TAccountAgentStats extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  task: Address<TAccountTask>;
  claim: Address<TAccountClaim>;
  taskValidationConfig: Address<TAccountTaskValidationConfig>;
  taskSubmission: Address<TAccountTaskSubmission>;
  protocolConfig: Address<TAccountProtocolConfig>;
  creator: TransactionSigner<TAccountCreator>;
  /**
   * OPTIONAL (P6.6): the worker agent's track-record aggregate. When supplied, this
   * freeze-rejection bumps `tasks_rejected`. Bound to `["agent_stats", claim.worker]`
   * (the claim's worker is the worker AgentRegistration PDA), created lazily on first
   * write. Telemetry only — never gates the freeze above.
   */
  agentStats?: Address<TAccountAgentStats>;
  /** Required only when `agent_stats` is supplied (for `init_if_needed`). */
  systemProgram?: Address<TAccountSystemProgram>;
  rejectionHash: RejectAndFreezeInstructionDataArgs["rejectionHash"];
};

export function getRejectAndFreezeInstruction<
  TAccountTask extends string,
  TAccountClaim extends string,
  TAccountTaskValidationConfig extends string,
  TAccountTaskSubmission extends string,
  TAccountProtocolConfig extends string,
  TAccountCreator extends string,
  TAccountAgentStats extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RejectAndFreezeInput<
    TAccountTask,
    TAccountClaim,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountProtocolConfig,
    TAccountCreator,
    TAccountAgentStats,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RejectAndFreezeInstruction<
  TProgramAddress,
  TAccountTask,
  TAccountClaim,
  TAccountTaskValidationConfig,
  TAccountTaskSubmission,
  TAccountProtocolConfig,
  TAccountCreator,
  TAccountAgentStats,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: true },
    claim: { value: input.claim ?? null, isWritable: false },
    taskValidationConfig: {
      value: input.taskValidationConfig ?? null,
      isWritable: true,
    },
    taskSubmission: { value: input.taskSubmission ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    creator: { value: input.creator ?? null, isWritable: true },
    agentStats: { value: input.agentStats ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("claim", accounts.claim),
      getAccountMeta("taskValidationConfig", accounts.taskValidationConfig),
      getAccountMeta("taskSubmission", accounts.taskSubmission),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("agentStats", accounts.agentStats),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRejectAndFreezeInstructionDataEncoder().encode(
      args as RejectAndFreezeInstructionDataArgs,
    ),
    programAddress,
  } as RejectAndFreezeInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountClaim,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountProtocolConfig,
    TAccountCreator,
    TAccountAgentStats,
    TAccountSystemProgram
  >);
}

export type ParsedRejectAndFreezeInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    task: TAccountMetas[0];
    claim: TAccountMetas[1];
    taskValidationConfig: TAccountMetas[2];
    taskSubmission: TAccountMetas[3];
    protocolConfig: TAccountMetas[4];
    creator: TAccountMetas[5];
    /**
     * OPTIONAL (P6.6): the worker agent's track-record aggregate. When supplied, this
     * freeze-rejection bumps `tasks_rejected`. Bound to `["agent_stats", claim.worker]`
     * (the claim's worker is the worker AgentRegistration PDA), created lazily on first
     * write. Telemetry only — never gates the freeze above.
     */
    agentStats?: TAccountMetas[6] | undefined;
    /** Required only when `agent_stats` is supplied (for `init_if_needed`). */
    systemProgram?: TAccountMetas[7] | undefined;
  };
  data: RejectAndFreezeInstructionData;
};

export function parseRejectAndFreezeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRejectAndFreezeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 8,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === AGENC_COORDINATION_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      task: getNextAccount(),
      claim: getNextAccount(),
      taskValidationConfig: getNextAccount(),
      taskSubmission: getNextAccount(),
      protocolConfig: getNextAccount(),
      creator: getNextAccount(),
      agentStats: getNextOptionalAccount(),
      systemProgram: getNextOptionalAccount(),
    },
    data: getRejectAndFreezeInstructionDataDecoder().decode(instruction.data),
  };
}
