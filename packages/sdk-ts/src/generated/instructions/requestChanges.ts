/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findProtocolConfigPda,
  findTaskSubmissionPda,
  findTaskValidationConfigPda,
} from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const REQUEST_CHANGES_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [136, 84, 241, 1, 189, 89, 226, 187],
);

export function getRequestChangesDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REQUEST_CHANGES_DISCRIMINATOR,
  );
}

export type RequestChangesInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountClaim extends string | AccountMeta<string> = string,
  TAccountTaskValidationConfig extends string | AccountMeta<string> = string,
  TAccountTaskSubmission extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTask extends string
        ? WritableAccount<TAccountTask>
        : TAccountTask,
      TAccountClaim extends string
        ? WritableAccount<TAccountClaim>
        : TAccountClaim,
      TAccountTaskValidationConfig extends string
        ? WritableAccount<TAccountTaskValidationConfig>
        : TAccountTaskValidationConfig,
      TAccountTaskSubmission extends string
        ? WritableAccount<TAccountTaskSubmission>
        : TAccountTaskSubmission,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountCreator extends string
        ? WritableSignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      ...TRemainingAccounts,
    ]
  >;

export type RequestChangesInstructionData = {
  discriminator: ReadonlyUint8Array;
  changesHash: ReadonlyUint8Array;
};

export type RequestChangesInstructionDataArgs = {
  changesHash: ReadonlyUint8Array;
};

export function getRequestChangesInstructionDataEncoder(): FixedSizeEncoder<RequestChangesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["changesHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: REQUEST_CHANGES_DISCRIMINATOR }),
  );
}

export function getRequestChangesInstructionDataDecoder(): FixedSizeDecoder<RequestChangesInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["changesHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getRequestChangesInstructionDataCodec(): FixedSizeCodec<
  RequestChangesInstructionDataArgs,
  RequestChangesInstructionData
> {
  return combineCodec(
    getRequestChangesInstructionDataEncoder(),
    getRequestChangesInstructionDataDecoder(),
  );
}

export type RequestChangesAsyncInput<
  TAccountTask extends string = string,
  TAccountClaim extends string = string,
  TAccountTaskValidationConfig extends string = string,
  TAccountTaskSubmission extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountCreator extends string = string,
> = {
  task: Address<TAccountTask>;
  claim: Address<TAccountClaim>;
  taskValidationConfig?: Address<TAccountTaskValidationConfig>;
  taskSubmission?: Address<TAccountTaskSubmission>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  creator: TransactionSigner<TAccountCreator>;
  changesHash: RequestChangesInstructionDataArgs["changesHash"];
};

export async function getRequestChangesInstructionAsync<
  TAccountTask extends string,
  TAccountClaim extends string,
  TAccountTaskValidationConfig extends string,
  TAccountTaskSubmission extends string,
  TAccountProtocolConfig extends string,
  TAccountCreator extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RequestChangesAsyncInput<
    TAccountTask,
    TAccountClaim,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountProtocolConfig,
    TAccountCreator
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RequestChangesInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountClaim,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountProtocolConfig,
    TAccountCreator
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: true },
    claim: { value: input.claim ?? null, isWritable: true },
    taskValidationConfig: {
      value: input.taskValidationConfig ?? null,
      isWritable: true,
    },
    taskSubmission: { value: input.taskSubmission ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    creator: { value: input.creator ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.taskValidationConfig.value) {
    accounts.taskValidationConfig.value = await findTaskValidationConfigPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }
  if (!accounts.taskSubmission.value) {
    accounts.taskSubmission.value = await findTaskSubmissionPda({
      claim: getAddressFromResolvedInstructionAccount(
        "claim",
        accounts.claim.value,
      ),
    });
  }
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("claim", accounts.claim),
      getAccountMeta("taskValidationConfig", accounts.taskValidationConfig),
      getAccountMeta("taskSubmission", accounts.taskSubmission),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("creator", accounts.creator),
    ],
    data: getRequestChangesInstructionDataEncoder().encode(
      args as RequestChangesInstructionDataArgs,
    ),
    programAddress,
  } as RequestChangesInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountClaim,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountProtocolConfig,
    TAccountCreator
  >);
}

export type RequestChangesInput<
  TAccountTask extends string = string,
  TAccountClaim extends string = string,
  TAccountTaskValidationConfig extends string = string,
  TAccountTaskSubmission extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountCreator extends string = string,
> = {
  task: Address<TAccountTask>;
  claim: Address<TAccountClaim>;
  taskValidationConfig: Address<TAccountTaskValidationConfig>;
  taskSubmission: Address<TAccountTaskSubmission>;
  protocolConfig: Address<TAccountProtocolConfig>;
  creator: TransactionSigner<TAccountCreator>;
  changesHash: RequestChangesInstructionDataArgs["changesHash"];
};

export function getRequestChangesInstruction<
  TAccountTask extends string,
  TAccountClaim extends string,
  TAccountTaskValidationConfig extends string,
  TAccountTaskSubmission extends string,
  TAccountProtocolConfig extends string,
  TAccountCreator extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RequestChangesInput<
    TAccountTask,
    TAccountClaim,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountProtocolConfig,
    TAccountCreator
  >,
  config?: { programAddress?: TProgramAddress },
): RequestChangesInstruction<
  TProgramAddress,
  TAccountTask,
  TAccountClaim,
  TAccountTaskValidationConfig,
  TAccountTaskSubmission,
  TAccountProtocolConfig,
  TAccountCreator
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: true },
    claim: { value: input.claim ?? null, isWritable: true },
    taskValidationConfig: {
      value: input.taskValidationConfig ?? null,
      isWritable: true,
    },
    taskSubmission: { value: input.taskSubmission ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    creator: { value: input.creator ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("claim", accounts.claim),
      getAccountMeta("taskValidationConfig", accounts.taskValidationConfig),
      getAccountMeta("taskSubmission", accounts.taskSubmission),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("creator", accounts.creator),
    ],
    data: getRequestChangesInstructionDataEncoder().encode(
      args as RequestChangesInstructionDataArgs,
    ),
    programAddress,
  } as RequestChangesInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountClaim,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountProtocolConfig,
    TAccountCreator
  >);
}

export type ParsedRequestChangesInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    task: TAccountMetas[0];
    claim: TAccountMetas[1];
    taskValidationConfig: TAccountMetas[2];
    taskSubmission: TAccountMetas[3];
    protocolConfig: TAccountMetas[4];
    creator: TAccountMetas[5];
  };
  data: RequestChangesInstructionData;
};

export function parseRequestChangesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRequestChangesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      task: getNextAccount(),
      claim: getNextAccount(),
      taskValidationConfig: getNextAccount(),
      taskSubmission: getNextAccount(),
      protocolConfig: getNextAccount(),
      creator: getNextAccount(),
    },
    data: getRequestChangesInstructionDataDecoder().decode(instruction.data),
  };
}
