/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findStorePda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const REGISTER_STORE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  63, 55, 152, 6, 167, 127, 89, 129,
]);

export function getRegisterStoreDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_STORE_DISCRIMINATOR,
  );
}

export type RegisterStoreInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountStore extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountStore extends string
        ? WritableAccount<TAccountStore>
        : TAccountStore,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterStoreInstructionData = {
  discriminator: ReadonlyUint8Array;
  handle: ReadonlyUint8Array;
  metadataHash: ReadonlyUint8Array;
  metadataUri: string;
  referrerFeeBps: number;
  operator: Address;
  operatorFeeBps: number;
  domain: string;
};

export type RegisterStoreInstructionDataArgs = {
  handle: ReadonlyUint8Array;
  metadataHash: ReadonlyUint8Array;
  metadataUri: string;
  referrerFeeBps: number;
  operator: Address;
  operatorFeeBps: number;
  domain: string;
};

export function getRegisterStoreInstructionDataEncoder(): Encoder<RegisterStoreInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["handle", fixEncoderSize(getBytesEncoder(), 32)],
      ["metadataHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["metadataUri", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["referrerFeeBps", getU16Encoder()],
      ["operator", getAddressEncoder()],
      ["operatorFeeBps", getU16Encoder()],
      ["domain", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: REGISTER_STORE_DISCRIMINATOR }),
  );
}

export function getRegisterStoreInstructionDataDecoder(): Decoder<RegisterStoreInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["handle", fixDecoderSize(getBytesDecoder(), 32)],
    ["metadataHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["metadataUri", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["referrerFeeBps", getU16Decoder()],
    ["operator", getAddressDecoder()],
    ["operatorFeeBps", getU16Decoder()],
    ["domain", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getRegisterStoreInstructionDataCodec(): Codec<
  RegisterStoreInstructionDataArgs,
  RegisterStoreInstructionData
> {
  return combineCodec(
    getRegisterStoreInstructionDataEncoder(),
    getRegisterStoreInstructionDataDecoder(),
  );
}

export type RegisterStoreAsyncInput<
  TAccountStore extends string = string,
  TAccountOwner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * `init` ⇒ one store per wallet (the live product invariant); registering
   * twice fails at account creation, and a re-register after close re-inits a
   * fresh entry.
   */
  store?: Address<TAccountStore>;
  /**
   * The self-registering store owner. No authority constraint — this is the
   * permissionless path. Pays rent AND the registration bond.
   */
  owner: TransactionSigner<TAccountOwner>;
  systemProgram?: Address<TAccountSystemProgram>;
  handle: RegisterStoreInstructionDataArgs["handle"];
  metadataHash: RegisterStoreInstructionDataArgs["metadataHash"];
  metadataUri: RegisterStoreInstructionDataArgs["metadataUri"];
  referrerFeeBps: RegisterStoreInstructionDataArgs["referrerFeeBps"];
  operator: RegisterStoreInstructionDataArgs["operator"];
  operatorFeeBps: RegisterStoreInstructionDataArgs["operatorFeeBps"];
  domain: RegisterStoreInstructionDataArgs["domain"];
};

export async function getRegisterStoreInstructionAsync<
  TAccountStore extends string,
  TAccountOwner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RegisterStoreAsyncInput<
    TAccountStore,
    TAccountOwner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterStoreInstruction<
    TProgramAddress,
    TAccountStore,
    TAccountOwner,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    store: { value: input.store ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.store.value) {
    accounts.store.value = await findStorePda({
      owner: getAddressFromResolvedInstructionAccount(
        "owner",
        accounts.owner.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("store", accounts.store),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterStoreInstructionDataEncoder().encode(
      args as RegisterStoreInstructionDataArgs,
    ),
    programAddress,
  } as RegisterStoreInstruction<
    TProgramAddress,
    TAccountStore,
    TAccountOwner,
    TAccountSystemProgram
  >);
}

export type RegisterStoreInput<
  TAccountStore extends string = string,
  TAccountOwner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * `init` ⇒ one store per wallet (the live product invariant); registering
   * twice fails at account creation, and a re-register after close re-inits a
   * fresh entry.
   */
  store: Address<TAccountStore>;
  /**
   * The self-registering store owner. No authority constraint — this is the
   * permissionless path. Pays rent AND the registration bond.
   */
  owner: TransactionSigner<TAccountOwner>;
  systemProgram?: Address<TAccountSystemProgram>;
  handle: RegisterStoreInstructionDataArgs["handle"];
  metadataHash: RegisterStoreInstructionDataArgs["metadataHash"];
  metadataUri: RegisterStoreInstructionDataArgs["metadataUri"];
  referrerFeeBps: RegisterStoreInstructionDataArgs["referrerFeeBps"];
  operator: RegisterStoreInstructionDataArgs["operator"];
  operatorFeeBps: RegisterStoreInstructionDataArgs["operatorFeeBps"];
  domain: RegisterStoreInstructionDataArgs["domain"];
};

export function getRegisterStoreInstruction<
  TAccountStore extends string,
  TAccountOwner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RegisterStoreInput<
    TAccountStore,
    TAccountOwner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterStoreInstruction<
  TProgramAddress,
  TAccountStore,
  TAccountOwner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    store: { value: input.store ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("store", accounts.store),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterStoreInstructionDataEncoder().encode(
      args as RegisterStoreInstructionDataArgs,
    ),
    programAddress,
  } as RegisterStoreInstruction<
    TProgramAddress,
    TAccountStore,
    TAccountOwner,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterStoreInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * `init` ⇒ one store per wallet (the live product invariant); registering
     * twice fails at account creation, and a re-register after close re-inits a
     * fresh entry.
     */
    store: TAccountMetas[0];
    /**
     * The self-registering store owner. No authority constraint — this is the
     * permissionless path. Pays rent AND the registration bond.
     */
    owner: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: RegisterStoreInstructionData;
};

export function parseRegisterStoreInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterStoreInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      store: getNextAccount(),
      owner: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterStoreInstructionDataDecoder().decode(instruction.data),
  };
}
