/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findDefaultTrustListPda, findProtocolConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const SET_DEFAULT_TRUST_LIST_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([134, 54, 111, 97, 148, 185, 102, 131]);

export function getSetDefaultTrustListDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_DEFAULT_TRUST_LIST_DISCRIMINATOR,
  );
}

export type SetDefaultTrustListInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountDefaultTrustList extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountDefaultTrustList extends string
        ? WritableAccount<TAccountDefaultTrustList>
        : TAccountDefaultTrustList,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SetDefaultTrustListInstructionData = {
  discriminator: ReadonlyUint8Array;
  listHash: ReadonlyUint8Array;
  listUri: string;
};

export type SetDefaultTrustListInstructionDataArgs = {
  listHash: ReadonlyUint8Array;
  listUri: string;
};

export function getSetDefaultTrustListInstructionDataEncoder(): Encoder<SetDefaultTrustListInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["listHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["listUri", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_DEFAULT_TRUST_LIST_DISCRIMINATOR,
    }),
  );
}

export function getSetDefaultTrustListInstructionDataDecoder(): Decoder<SetDefaultTrustListInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["listHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["listUri", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getSetDefaultTrustListInstructionDataCodec(): Codec<
  SetDefaultTrustListInstructionDataArgs,
  SetDefaultTrustListInstructionData
> {
  return combineCodec(
    getSetDefaultTrustListInstructionDataEncoder(),
    getSetDefaultTrustListInstructionDataDecoder(),
  );
}

export type SetDefaultTrustListAsyncInput<
  TAccountProtocolConfig extends string = string,
  TAccountDefaultTrustList extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  protocolConfig?: Address<TAccountProtocolConfig>;
  /** Singleton pointer PDA; `init_if_needed` so the first update creates it. */
  defaultTrustList?: Address<TAccountDefaultTrustList>;
  /**
   * Fee payer / tx assembler. Approval authority is the multisig threshold over
   * `remaining_accounts`, exactly like `update_protocol_fee`.
   */
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  listHash: SetDefaultTrustListInstructionDataArgs["listHash"];
  listUri: SetDefaultTrustListInstructionDataArgs["listUri"];
};

export async function getSetDefaultTrustListInstructionAsync<
  TAccountProtocolConfig extends string,
  TAccountDefaultTrustList extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: SetDefaultTrustListAsyncInput<
    TAccountProtocolConfig,
    TAccountDefaultTrustList,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetDefaultTrustListInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountDefaultTrustList,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    defaultTrustList: {
      value: input.defaultTrustList ?? null,
      isWritable: true,
    },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.defaultTrustList.value) {
    accounts.defaultTrustList.value = await findDefaultTrustListPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("defaultTrustList", accounts.defaultTrustList),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSetDefaultTrustListInstructionDataEncoder().encode(
      args as SetDefaultTrustListInstructionDataArgs,
    ),
    programAddress,
  } as SetDefaultTrustListInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountDefaultTrustList,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type SetDefaultTrustListInput<
  TAccountProtocolConfig extends string = string,
  TAccountDefaultTrustList extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  protocolConfig: Address<TAccountProtocolConfig>;
  /** Singleton pointer PDA; `init_if_needed` so the first update creates it. */
  defaultTrustList: Address<TAccountDefaultTrustList>;
  /**
   * Fee payer / tx assembler. Approval authority is the multisig threshold over
   * `remaining_accounts`, exactly like `update_protocol_fee`.
   */
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  listHash: SetDefaultTrustListInstructionDataArgs["listHash"];
  listUri: SetDefaultTrustListInstructionDataArgs["listUri"];
};

export function getSetDefaultTrustListInstruction<
  TAccountProtocolConfig extends string,
  TAccountDefaultTrustList extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: SetDefaultTrustListInput<
    TAccountProtocolConfig,
    TAccountDefaultTrustList,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SetDefaultTrustListInstruction<
  TProgramAddress,
  TAccountProtocolConfig,
  TAccountDefaultTrustList,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    defaultTrustList: {
      value: input.defaultTrustList ?? null,
      isWritable: true,
    },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("defaultTrustList", accounts.defaultTrustList),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSetDefaultTrustListInstructionDataEncoder().encode(
      args as SetDefaultTrustListInstructionDataArgs,
    ),
    programAddress,
  } as SetDefaultTrustListInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountDefaultTrustList,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedSetDefaultTrustListInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    protocolConfig: TAccountMetas[0];
    /** Singleton pointer PDA; `init_if_needed` so the first update creates it. */
    defaultTrustList: TAccountMetas[1];
    /**
     * Fee payer / tx assembler. Approval authority is the multisig threshold over
     * `remaining_accounts`, exactly like `update_protocol_fee`.
     */
    authority: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: SetDefaultTrustListInstructionData;
};

export function parseSetDefaultTrustListInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetDefaultTrustListInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      protocolConfig: getNextAccount(),
      defaultTrustList: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSetDefaultTrustListInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
