/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";

import {
  getBorshStringDecoder,
  getBorshStringEncoder,
} from "../codecs/borshString";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findStorePda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_STORE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  169, 49, 137, 251, 233, 234, 172, 103,
]);

export function getUpdateStoreDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(UPDATE_STORE_DISCRIMINATOR);
}

export type UpdateStoreInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountStore extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountStore extends string
        ? WritableAccount<TAccountStore>
        : TAccountStore,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateStoreInstructionData = {
  discriminator: ReadonlyUint8Array;
  handle: ReadonlyUint8Array;
  metadataHash: ReadonlyUint8Array;
  metadataUri: string;
  referrerFeeBps: number;
  operator: Address;
  operatorFeeBps: number;
  domain: string;
};

export type UpdateStoreInstructionDataArgs = {
  handle: ReadonlyUint8Array;
  metadataHash: ReadonlyUint8Array;
  metadataUri: string;
  referrerFeeBps: number;
  operator: Address;
  operatorFeeBps: number;
  domain: string;
};

export function getUpdateStoreInstructionDataEncoder(): Encoder<UpdateStoreInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["handle", getFixedBytesEncoder(32, "handle")],
      ["metadataHash", getFixedBytesEncoder(32, "metadataHash")],
      [
        "metadataUri",
        addEncoderSizePrefix(getBorshStringEncoder(), getU32Encoder()),
      ],
      ["referrerFeeBps", getU16Encoder()],
      ["operator", getAddressEncoder()],
      ["operatorFeeBps", getU16Encoder()],
      [
        "domain",
        addEncoderSizePrefix(getBorshStringEncoder(), getU32Encoder()),
      ],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_STORE_DISCRIMINATOR }),
  );
}

export function getUpdateStoreInstructionDataDecoder(): Decoder<UpdateStoreInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["handle", fixDecoderSize(getBytesDecoder(), 32)],
    ["metadataHash", fixDecoderSize(getBytesDecoder(), 32)],
    [
      "metadataUri",
      addDecoderSizePrefix(getBorshStringDecoder(), getU32Decoder()),
    ],
    ["referrerFeeBps", getU16Decoder()],
    ["operator", getAddressDecoder()],
    ["operatorFeeBps", getU16Decoder()],
    ["domain", addDecoderSizePrefix(getBorshStringDecoder(), getU32Decoder())],
  ]);
}

export function getUpdateStoreInstructionDataCodec(): Codec<
  UpdateStoreInstructionDataArgs,
  UpdateStoreInstructionData
> {
  return combineCodec(
    getUpdateStoreInstructionDataEncoder(),
    getUpdateStoreInstructionDataDecoder(),
  );
}

export type UpdateStoreAsyncInput<
  TAccountStore extends string = string,
  TAccountOwner extends string = string,
> = {
  store?: Address<TAccountStore>;
  owner: TransactionSigner<TAccountOwner>;
  handle: UpdateStoreInstructionDataArgs["handle"];
  metadataHash: UpdateStoreInstructionDataArgs["metadataHash"];
  metadataUri: UpdateStoreInstructionDataArgs["metadataUri"];
  referrerFeeBps: UpdateStoreInstructionDataArgs["referrerFeeBps"];
  operator: UpdateStoreInstructionDataArgs["operator"];
  operatorFeeBps: UpdateStoreInstructionDataArgs["operatorFeeBps"];
  domain: UpdateStoreInstructionDataArgs["domain"];
};

export async function getUpdateStoreInstructionAsync<
  TAccountStore extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: UpdateStoreAsyncInput<TAccountStore, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateStoreInstruction<TProgramAddress, TAccountStore, TAccountOwner>
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    store: { value: input.store ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.store.value) {
    accounts.store.value = await findStorePda({
      owner: getAddressFromResolvedInstructionAccount(
        "owner",
        accounts.owner.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("store", accounts.store),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getUpdateStoreInstructionDataEncoder().encode(
      args as UpdateStoreInstructionDataArgs,
    ),
    programAddress,
  } as UpdateStoreInstruction<TProgramAddress, TAccountStore, TAccountOwner>);
}

export type UpdateStoreInput<
  TAccountStore extends string = string,
  TAccountOwner extends string = string,
> = {
  store: Address<TAccountStore>;
  owner: TransactionSigner<TAccountOwner>;
  handle: UpdateStoreInstructionDataArgs["handle"];
  metadataHash: UpdateStoreInstructionDataArgs["metadataHash"];
  metadataUri: UpdateStoreInstructionDataArgs["metadataUri"];
  referrerFeeBps: UpdateStoreInstructionDataArgs["referrerFeeBps"];
  operator: UpdateStoreInstructionDataArgs["operator"];
  operatorFeeBps: UpdateStoreInstructionDataArgs["operatorFeeBps"];
  domain: UpdateStoreInstructionDataArgs["domain"];
};

export function getUpdateStoreInstruction<
  TAccountStore extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: UpdateStoreInput<TAccountStore, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): UpdateStoreInstruction<TProgramAddress, TAccountStore, TAccountOwner> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    store: { value: input.store ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("store", accounts.store),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getUpdateStoreInstructionDataEncoder().encode(
      args as UpdateStoreInstructionDataArgs,
    ),
    programAddress,
  } as UpdateStoreInstruction<TProgramAddress, TAccountStore, TAccountOwner>);
}

export type ParsedUpdateStoreInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    store: TAccountMetas[0];
    owner: TAccountMetas[1];
  };
  data: UpdateStoreInstructionData;
};

export function parseUpdateStoreInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateStoreInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { store: getNextAccount(), owner: getNextAccount() },
    data: getUpdateStoreInstructionDataDecoder().decode(instruction.data),
  };
}
