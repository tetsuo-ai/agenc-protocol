/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const FINALIZE_ATTESTOR_EXIT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([64, 78, 208, 116, 104, 235, 209, 53]);

export function getFinalizeAttestorExitDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(FINALIZE_ATTESTOR_EXIT_DISCRIMINATOR);
}

export type FinalizeAttestorExitInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountModerationAttestor extends string | AccountMeta<string> = string,
  TAccountAttestor extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountModerationAttestor extends string
        ? WritableAccount<TAccountModerationAttestor>
        : TAccountModerationAttestor,
      TAccountAttestor extends string
        ? WritableSignerAccount<TAccountAttestor> &
            AccountSignerMeta<TAccountAttestor>
        : TAccountAttestor,
      ...TRemainingAccounts,
    ]
  >;

export type FinalizeAttestorExitInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type FinalizeAttestorExitInstructionDataArgs = {};

export function getFinalizeAttestorExitInstructionDataEncoder(): FixedSizeEncoder<FinalizeAttestorExitInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
    ]),
    (value) => ({
      ...value,
      discriminator: FINALIZE_ATTESTOR_EXIT_DISCRIMINATOR,
    }),
  );
}

export function getFinalizeAttestorExitInstructionDataDecoder(): FixedSizeDecoder<FinalizeAttestorExitInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getFinalizeAttestorExitInstructionDataCodec(): FixedSizeCodec<
  FinalizeAttestorExitInstructionDataArgs,
  FinalizeAttestorExitInstructionData
> {
  return combineCodec(
    getFinalizeAttestorExitInstructionDataEncoder(),
    getFinalizeAttestorExitInstructionDataDecoder(),
  );
}

export type FinalizeAttestorExitInput<
  TAccountModerationAttestor extends string = string,
  TAccountAttestor extends string = string,
> = {
  /**
   * Roster entry to close. `close = attestor` refunds ALL lamports on the PDA
   * (rent + registration bond) to the attestor — the full, non-confiscatable refund.
   * SELF-REGISTERED entries only (`assigned_by == attestor`): the refund is the
   * attestor's own bond + rent. A deputized entry's rent belongs to the authority
   * and is returned to it via `revoke_moderation_attestor`, not drained here.
   */
  moderationAttestor: Address<TAccountModerationAttestor>;
  /** Only the attestor itself may finalize; it receives the refund. */
  attestor: TransactionSigner<TAccountAttestor>;
};

export function getFinalizeAttestorExitInstruction<
  TAccountModerationAttestor extends string,
  TAccountAttestor extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: FinalizeAttestorExitInput<
    TAccountModerationAttestor,
    TAccountAttestor
  >,
  config?: { programAddress?: TProgramAddress },
): FinalizeAttestorExitInstruction<
  TProgramAddress,
  TAccountModerationAttestor,
  TAccountAttestor
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    moderationAttestor: {
      value: input.moderationAttestor ?? null,
      isWritable: true,
    },
    attestor: { value: input.attestor ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("moderationAttestor", accounts.moderationAttestor),
      getAccountMeta("attestor", accounts.attestor),
    ],
    data: getFinalizeAttestorExitInstructionDataEncoder().encode({}),
    programAddress,
  } as FinalizeAttestorExitInstruction<
    TProgramAddress,
    TAccountModerationAttestor,
    TAccountAttestor
  >);
}

export type ParsedFinalizeAttestorExitInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Roster entry to close. `close = attestor` refunds ALL lamports on the PDA
     * (rent + registration bond) to the attestor — the full, non-confiscatable refund.
     * SELF-REGISTERED entries only (`assigned_by == attestor`): the refund is the
     * attestor's own bond + rent. A deputized entry's rent belongs to the authority
     * and is returned to it via `revoke_moderation_attestor`, not drained here.
     */
    moderationAttestor: TAccountMetas[0];
    /** Only the attestor itself may finalize; it receives the refund. */
    attestor: TAccountMetas[1];
  };
  data: FinalizeAttestorExitInstructionData;
};

export function parseFinalizeAttestorExitInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFinalizeAttestorExitInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      moderationAttestor: getNextAccount(),
      attestor: getNextAccount(),
    },
    data: getFinalizeAttestorExitInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
