/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findEscrowPda, findHireRecordPda, findTaskJobSpecPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_TASK_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  55, 234, 77, 69, 245, 208, 54, 167,
]);

export function getCloseTaskDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLOSE_TASK_DISCRIMINATOR);
}

export type CloseTaskInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountTaskJobSpec extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountHireRecord extends string | AccountMeta<string> = string,
  TAccountListing extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTask extends string
        ? WritableAccount<TAccountTask>
        : TAccountTask,
      TAccountTaskJobSpec extends string
        ? WritableAccount<TAccountTaskJobSpec>
        : TAccountTaskJobSpec,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountHireRecord extends string
        ? WritableAccount<TAccountHireRecord>
        : TAccountHireRecord,
      TAccountListing extends string
        ? WritableAccount<TAccountListing>
        : TAccountListing,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type CloseTaskInstructionData = { discriminator: ReadonlyUint8Array };

export type CloseTaskInstructionDataArgs = {};

export function getCloseTaskInstructionDataEncoder(): FixedSizeEncoder<CloseTaskInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_TASK_DISCRIMINATOR }),
  );
}

export function getCloseTaskInstructionDataDecoder(): FixedSizeDecoder<CloseTaskInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseTaskInstructionDataCodec(): FixedSizeCodec<
  CloseTaskInstructionDataArgs,
  CloseTaskInstructionData
> {
  return combineCodec(
    getCloseTaskInstructionDataEncoder(),
    getCloseTaskInstructionDataDecoder(),
  );
}

export type CloseTaskAsyncInput<
  TAccountTask extends string = string,
  TAccountTaskJobSpec extends string = string,
  TAccountEscrow extends string = string,
  TAccountHireRecord extends string = string,
  TAccountListing extends string = string,
  TAccountAuthority extends string = string,
> = {
  task: Address<TAccountTask>;
  /**
   * Optional leftover job-spec pointer for this task. When provided it is closed
   * alongside the task so its rent is reclaimed too. Bound to this task by seeds
   * + constraint so a caller cannot close another task's pointer.
   */
  taskJobSpec?: Address<TAccountTaskJobSpec>;
  /**
   * Optional still-alive escrow PDA. Only `expire_dispute` leaves the escrow
   * account open (drained, `is_closed = true`) on a terminal task; provide it
   * here to reclaim its rent. Bound to this task by seeds + constraint.
   */
  escrow?: Address<TAccountEscrow>;
  /**
   * Hire link PDA for this task. ALWAYS required — the caller passes the derived
   * ["hire", task] address even for non-hired tasks (where it is an empty system
   * account). close_task decides from the on-chain owner whether a live hire must
   * be settled, so a caller cannot dodge the capacity decrement by omitting it.
   * the handler, and a live record is deserialized + validated there.
   */
  hireRecord?: Address<TAccountHireRecord>;
  /**
   * Source listing, required when a live hire link is present, so its `open_jobs`
   * capacity counter can be decremented. Verified against `hire_record.listing`.
   */
  listing?: Address<TAccountListing>;
  /** Task creator; receives the reclaimed rent. Mutable to credit lamports. */
  authority: TransactionSigner<TAccountAuthority>;
};

export async function getCloseTaskInstructionAsync<
  TAccountTask extends string,
  TAccountTaskJobSpec extends string,
  TAccountEscrow extends string,
  TAccountHireRecord extends string,
  TAccountListing extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: CloseTaskAsyncInput<
    TAccountTask,
    TAccountTaskJobSpec,
    TAccountEscrow,
    TAccountHireRecord,
    TAccountListing,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseTaskInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountTaskJobSpec,
    TAccountEscrow,
    TAccountHireRecord,
    TAccountListing,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: true },
    taskJobSpec: { value: input.taskJobSpec ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    hireRecord: { value: input.hireRecord ?? null, isWritable: true },
    listing: { value: input.listing ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.taskJobSpec.value) {
    accounts.taskJobSpec.value = await findTaskJobSpecPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }
  if (!accounts.escrow.value) {
    accounts.escrow.value = await findEscrowPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }
  if (!accounts.hireRecord.value) {
    accounts.hireRecord.value = await findHireRecordPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("taskJobSpec", accounts.taskJobSpec),
      getAccountMeta("escrow", accounts.escrow),
      getAccountMeta("hireRecord", accounts.hireRecord),
      getAccountMeta("listing", accounts.listing),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getCloseTaskInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseTaskInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountTaskJobSpec,
    TAccountEscrow,
    TAccountHireRecord,
    TAccountListing,
    TAccountAuthority
  >);
}

export type CloseTaskInput<
  TAccountTask extends string = string,
  TAccountTaskJobSpec extends string = string,
  TAccountEscrow extends string = string,
  TAccountHireRecord extends string = string,
  TAccountListing extends string = string,
  TAccountAuthority extends string = string,
> = {
  task: Address<TAccountTask>;
  /**
   * Optional leftover job-spec pointer for this task. When provided it is closed
   * alongside the task so its rent is reclaimed too. Bound to this task by seeds
   * + constraint so a caller cannot close another task's pointer.
   */
  taskJobSpec?: Address<TAccountTaskJobSpec>;
  /**
   * Optional still-alive escrow PDA. Only `expire_dispute` leaves the escrow
   * account open (drained, `is_closed = true`) on a terminal task; provide it
   * here to reclaim its rent. Bound to this task by seeds + constraint.
   */
  escrow?: Address<TAccountEscrow>;
  /**
   * Hire link PDA for this task. ALWAYS required — the caller passes the derived
   * ["hire", task] address even for non-hired tasks (where it is an empty system
   * account). close_task decides from the on-chain owner whether a live hire must
   * be settled, so a caller cannot dodge the capacity decrement by omitting it.
   * the handler, and a live record is deserialized + validated there.
   */
  hireRecord: Address<TAccountHireRecord>;
  /**
   * Source listing, required when a live hire link is present, so its `open_jobs`
   * capacity counter can be decremented. Verified against `hire_record.listing`.
   */
  listing?: Address<TAccountListing>;
  /** Task creator; receives the reclaimed rent. Mutable to credit lamports. */
  authority: TransactionSigner<TAccountAuthority>;
};

export function getCloseTaskInstruction<
  TAccountTask extends string,
  TAccountTaskJobSpec extends string,
  TAccountEscrow extends string,
  TAccountHireRecord extends string,
  TAccountListing extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: CloseTaskInput<
    TAccountTask,
    TAccountTaskJobSpec,
    TAccountEscrow,
    TAccountHireRecord,
    TAccountListing,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): CloseTaskInstruction<
  TProgramAddress,
  TAccountTask,
  TAccountTaskJobSpec,
  TAccountEscrow,
  TAccountHireRecord,
  TAccountListing,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: true },
    taskJobSpec: { value: input.taskJobSpec ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    hireRecord: { value: input.hireRecord ?? null, isWritable: true },
    listing: { value: input.listing ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("taskJobSpec", accounts.taskJobSpec),
      getAccountMeta("escrow", accounts.escrow),
      getAccountMeta("hireRecord", accounts.hireRecord),
      getAccountMeta("listing", accounts.listing),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getCloseTaskInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseTaskInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountTaskJobSpec,
    TAccountEscrow,
    TAccountHireRecord,
    TAccountListing,
    TAccountAuthority
  >);
}

export type ParsedCloseTaskInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    task: TAccountMetas[0];
    /**
     * Optional leftover job-spec pointer for this task. When provided it is closed
     * alongside the task so its rent is reclaimed too. Bound to this task by seeds
     * + constraint so a caller cannot close another task's pointer.
     */
    taskJobSpec?: TAccountMetas[1] | undefined;
    /**
     * Optional still-alive escrow PDA. Only `expire_dispute` leaves the escrow
     * account open (drained, `is_closed = true`) on a terminal task; provide it
     * here to reclaim its rent. Bound to this task by seeds + constraint.
     */
    escrow?: TAccountMetas[2] | undefined;
    /**
     * Hire link PDA for this task. ALWAYS required — the caller passes the derived
     * ["hire", task] address even for non-hired tasks (where it is an empty system
     * account). close_task decides from the on-chain owner whether a live hire must
     * be settled, so a caller cannot dodge the capacity decrement by omitting it.
     * the handler, and a live record is deserialized + validated there.
     */
    hireRecord: TAccountMetas[3];
    /**
     * Source listing, required when a live hire link is present, so its `open_jobs`
     * capacity counter can be decremented. Verified against `hire_record.listing`.
     */
    listing?: TAccountMetas[4] | undefined;
    /** Task creator; receives the reclaimed rent. Mutable to credit lamports. */
    authority: TAccountMetas[5];
  };
  data: CloseTaskInstructionData;
};

export function parseCloseTaskInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseTaskInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === AGENC_COORDINATION_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      task: getNextAccount(),
      taskJobSpec: getNextOptionalAccount(),
      escrow: getNextOptionalAccount(),
      hireRecord: getNextAccount(),
      listing: getNextOptionalAccount(),
      authority: getNextAccount(),
    },
    data: getCloseTaskInstructionDataDecoder().decode(instruction.data),
  };
}
