/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findHireRecordPda,
  findProtocolConfigPda,
  findTaskAttestorConfigPda,
  findTaskValidationConfigPda,
} from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const CONFIGURE_TASK_VALIDATION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([11, 79, 19, 188, 13, 32, 244, 90]);

export function getConfigureTaskValidationDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CONFIGURE_TASK_VALIDATION_DISCRIMINATOR,
  );
}

export type ConfigureTaskValidationInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountTaskValidationConfig extends string | AccountMeta<string> = string,
  TAccountTaskAttestorConfig extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountHireRecord extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTask extends string
        ? WritableAccount<TAccountTask>
        : TAccountTask,
      TAccountTaskValidationConfig extends string
        ? WritableAccount<TAccountTaskValidationConfig>
        : TAccountTaskValidationConfig,
      TAccountTaskAttestorConfig extends string
        ? WritableAccount<TAccountTaskAttestorConfig>
        : TAccountTaskAttestorConfig,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountHireRecord extends string
        ? ReadonlyAccount<TAccountHireRecord>
        : TAccountHireRecord,
      TAccountCreator extends string
        ? WritableSignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ConfigureTaskValidationInstructionData = {
  discriminator: ReadonlyUint8Array;
  mode: number;
  reviewWindowSecs: bigint;
  validatorQuorum: number;
  attestor: Option<Address>;
};

export type ConfigureTaskValidationInstructionDataArgs = {
  mode: number;
  reviewWindowSecs: number | bigint;
  validatorQuorum: number;
  attestor: OptionOrNullable<Address>;
};

export function getConfigureTaskValidationInstructionDataEncoder(): Encoder<ConfigureTaskValidationInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["mode", getU8Encoder()],
      ["reviewWindowSecs", getI64Encoder()],
      ["validatorQuorum", getU8Encoder()],
      ["attestor", getOptionEncoder(getAddressEncoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: CONFIGURE_TASK_VALIDATION_DISCRIMINATOR,
    }),
  );
}

export function getConfigureTaskValidationInstructionDataDecoder(): Decoder<ConfigureTaskValidationInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["mode", getU8Decoder()],
    ["reviewWindowSecs", getI64Decoder()],
    ["validatorQuorum", getU8Decoder()],
    ["attestor", getOptionDecoder(getAddressDecoder())],
  ]);
}

export function getConfigureTaskValidationInstructionDataCodec(): Codec<
  ConfigureTaskValidationInstructionDataArgs,
  ConfigureTaskValidationInstructionData
> {
  return combineCodec(
    getConfigureTaskValidationInstructionDataEncoder(),
    getConfigureTaskValidationInstructionDataDecoder(),
  );
}

export type ConfigureTaskValidationAsyncInput<
  TAccountTask extends string = string,
  TAccountTaskValidationConfig extends string = string,
  TAccountTaskAttestorConfig extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountHireRecord extends string = string,
  TAccountCreator extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  task: Address<TAccountTask>;
  taskValidationConfig?: Address<TAccountTaskValidationConfig>;
  taskAttestorConfig?: Address<TAccountTaskAttestorConfig>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  /**
   * Hire link PDA for this task. ALWAYS required — the caller passes the derived
   * ["hire", task] address even for non-hired tasks (where it is an empty system
   * account). If it is a live, program-owned HireRecord the task was hired from a
   * listing, and reconfiguring it for manual validation would route settlement
   * through accept_task_result, which does not pay the operator leg (the operator
   * fee is only settled on the hire/complete_task path) — so the handler rejects
   * it. Making it required (not optional) means the gate cannot be skipped.
   */
  hireRecord?: Address<TAccountHireRecord>;
  creator: TransactionSigner<TAccountCreator>;
  systemProgram?: Address<TAccountSystemProgram>;
  mode: ConfigureTaskValidationInstructionDataArgs["mode"];
  reviewWindowSecs: ConfigureTaskValidationInstructionDataArgs["reviewWindowSecs"];
  validatorQuorum: ConfigureTaskValidationInstructionDataArgs["validatorQuorum"];
  attestor: ConfigureTaskValidationInstructionDataArgs["attestor"];
};

export async function getConfigureTaskValidationInstructionAsync<
  TAccountTask extends string,
  TAccountTaskValidationConfig extends string,
  TAccountTaskAttestorConfig extends string,
  TAccountProtocolConfig extends string,
  TAccountHireRecord extends string,
  TAccountCreator extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: ConfigureTaskValidationAsyncInput<
    TAccountTask,
    TAccountTaskValidationConfig,
    TAccountTaskAttestorConfig,
    TAccountProtocolConfig,
    TAccountHireRecord,
    TAccountCreator,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ConfigureTaskValidationInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountTaskValidationConfig,
    TAccountTaskAttestorConfig,
    TAccountProtocolConfig,
    TAccountHireRecord,
    TAccountCreator,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: true },
    taskValidationConfig: {
      value: input.taskValidationConfig ?? null,
      isWritable: true,
    },
    taskAttestorConfig: {
      value: input.taskAttestorConfig ?? null,
      isWritable: true,
    },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    hireRecord: { value: input.hireRecord ?? null, isWritable: false },
    creator: { value: input.creator ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.taskValidationConfig.value) {
    accounts.taskValidationConfig.value = await findTaskValidationConfigPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }
  if (!accounts.taskAttestorConfig.value) {
    accounts.taskAttestorConfig.value = await findTaskAttestorConfigPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.hireRecord.value) {
    accounts.hireRecord.value = await findHireRecordPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("taskValidationConfig", accounts.taskValidationConfig),
      getAccountMeta("taskAttestorConfig", accounts.taskAttestorConfig),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("hireRecord", accounts.hireRecord),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getConfigureTaskValidationInstructionDataEncoder().encode(
      args as ConfigureTaskValidationInstructionDataArgs,
    ),
    programAddress,
  } as ConfigureTaskValidationInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountTaskValidationConfig,
    TAccountTaskAttestorConfig,
    TAccountProtocolConfig,
    TAccountHireRecord,
    TAccountCreator,
    TAccountSystemProgram
  >);
}

export type ConfigureTaskValidationInput<
  TAccountTask extends string = string,
  TAccountTaskValidationConfig extends string = string,
  TAccountTaskAttestorConfig extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountHireRecord extends string = string,
  TAccountCreator extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  task: Address<TAccountTask>;
  taskValidationConfig: Address<TAccountTaskValidationConfig>;
  taskAttestorConfig: Address<TAccountTaskAttestorConfig>;
  protocolConfig: Address<TAccountProtocolConfig>;
  /**
   * Hire link PDA for this task. ALWAYS required — the caller passes the derived
   * ["hire", task] address even for non-hired tasks (where it is an empty system
   * account). If it is a live, program-owned HireRecord the task was hired from a
   * listing, and reconfiguring it for manual validation would route settlement
   * through accept_task_result, which does not pay the operator leg (the operator
   * fee is only settled on the hire/complete_task path) — so the handler rejects
   * it. Making it required (not optional) means the gate cannot be skipped.
   */
  hireRecord: Address<TAccountHireRecord>;
  creator: TransactionSigner<TAccountCreator>;
  systemProgram?: Address<TAccountSystemProgram>;
  mode: ConfigureTaskValidationInstructionDataArgs["mode"];
  reviewWindowSecs: ConfigureTaskValidationInstructionDataArgs["reviewWindowSecs"];
  validatorQuorum: ConfigureTaskValidationInstructionDataArgs["validatorQuorum"];
  attestor: ConfigureTaskValidationInstructionDataArgs["attestor"];
};

export function getConfigureTaskValidationInstruction<
  TAccountTask extends string,
  TAccountTaskValidationConfig extends string,
  TAccountTaskAttestorConfig extends string,
  TAccountProtocolConfig extends string,
  TAccountHireRecord extends string,
  TAccountCreator extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: ConfigureTaskValidationInput<
    TAccountTask,
    TAccountTaskValidationConfig,
    TAccountTaskAttestorConfig,
    TAccountProtocolConfig,
    TAccountHireRecord,
    TAccountCreator,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ConfigureTaskValidationInstruction<
  TProgramAddress,
  TAccountTask,
  TAccountTaskValidationConfig,
  TAccountTaskAttestorConfig,
  TAccountProtocolConfig,
  TAccountHireRecord,
  TAccountCreator,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: true },
    taskValidationConfig: {
      value: input.taskValidationConfig ?? null,
      isWritable: true,
    },
    taskAttestorConfig: {
      value: input.taskAttestorConfig ?? null,
      isWritable: true,
    },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    hireRecord: { value: input.hireRecord ?? null, isWritable: false },
    creator: { value: input.creator ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("taskValidationConfig", accounts.taskValidationConfig),
      getAccountMeta("taskAttestorConfig", accounts.taskAttestorConfig),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("hireRecord", accounts.hireRecord),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getConfigureTaskValidationInstructionDataEncoder().encode(
      args as ConfigureTaskValidationInstructionDataArgs,
    ),
    programAddress,
  } as ConfigureTaskValidationInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountTaskValidationConfig,
    TAccountTaskAttestorConfig,
    TAccountProtocolConfig,
    TAccountHireRecord,
    TAccountCreator,
    TAccountSystemProgram
  >);
}

export type ParsedConfigureTaskValidationInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    task: TAccountMetas[0];
    taskValidationConfig: TAccountMetas[1];
    taskAttestorConfig: TAccountMetas[2];
    protocolConfig: TAccountMetas[3];
    /**
     * Hire link PDA for this task. ALWAYS required — the caller passes the derived
     * ["hire", task] address even for non-hired tasks (where it is an empty system
     * account). If it is a live, program-owned HireRecord the task was hired from a
     * listing, and reconfiguring it for manual validation would route settlement
     * through accept_task_result, which does not pay the operator leg (the operator
     * fee is only settled on the hire/complete_task path) — so the handler rejects
     * it. Making it required (not optional) means the gate cannot be skipped.
     */
    hireRecord: TAccountMetas[4];
    creator: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
  };
  data: ConfigureTaskValidationInstructionData;
};

export function parseConfigureTaskValidationInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedConfigureTaskValidationInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      task: getNextAccount(),
      taskValidationConfig: getNextAccount(),
      taskAttestorConfig: getNextAccount(),
      protocolConfig: getNextAccount(),
      hireRecord: getNextAccount(),
      creator: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getConfigureTaskValidationInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
