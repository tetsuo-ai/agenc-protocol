/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findAcceptTaskResultClaimPda,
  findEscrowPda,
  findProtocolConfigPda,
  findTaskSubmissionPda,
  findTaskValidationConfigPda,
} from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const DISTRIBUTE_GHOST_SHARE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([238, 29, 21, 234, 93, 251, 101, 47]);

export function getDistributeGhostShareDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(DISTRIBUTE_GHOST_SHARE_DISCRIMINATOR);
}

export type DistributeGhostShareInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountClaim extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountTaskValidationConfig extends string | AccountMeta<string> = string,
  TAccountTaskSubmission extends string | AccountMeta<string> = string,
  TAccountWorker extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountWorkerAuthority extends string | AccountMeta<string> = string,
  TAccountOperator extends string | AccountMeta<string> = string,
  TAccountReferrer extends string | AccountMeta<string> = string,
  TAccountCranker extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTask extends string
        ? WritableAccount<TAccountTask>
        : TAccountTask,
      TAccountClaim extends string
        ? WritableAccount<TAccountClaim>
        : TAccountClaim,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountTaskValidationConfig extends string
        ? WritableAccount<TAccountTaskValidationConfig>
        : TAccountTaskValidationConfig,
      TAccountTaskSubmission extends string
        ? WritableAccount<TAccountTaskSubmission>
        : TAccountTaskSubmission,
      TAccountWorker extends string
        ? WritableAccount<TAccountWorker>
        : TAccountWorker,
      TAccountProtocolConfig extends string
        ? WritableAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountCreator extends string
        ? WritableAccount<TAccountCreator>
        : TAccountCreator,
      TAccountWorkerAuthority extends string
        ? WritableAccount<TAccountWorkerAuthority>
        : TAccountWorkerAuthority,
      TAccountOperator extends string
        ? WritableAccount<TAccountOperator>
        : TAccountOperator,
      TAccountReferrer extends string
        ? WritableAccount<TAccountReferrer>
        : TAccountReferrer,
      TAccountCranker extends string
        ? ReadonlySignerAccount<TAccountCranker> &
            AccountSignerMeta<TAccountCranker>
        : TAccountCranker,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DistributeGhostShareInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type DistributeGhostShareInstructionDataArgs = {};

export function getDistributeGhostShareInstructionDataEncoder(): FixedSizeEncoder<DistributeGhostShareInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
    ]),
    (value) => ({
      ...value,
      discriminator: DISTRIBUTE_GHOST_SHARE_DISCRIMINATOR,
    }),
  );
}

export function getDistributeGhostShareInstructionDataDecoder(): FixedSizeDecoder<DistributeGhostShareInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getDistributeGhostShareInstructionDataCodec(): FixedSizeCodec<
  DistributeGhostShareInstructionDataArgs,
  DistributeGhostShareInstructionData
> {
  return combineCodec(
    getDistributeGhostShareInstructionDataEncoder(),
    getDistributeGhostShareInstructionDataDecoder(),
  );
}

export type DistributeGhostShareAsyncInput<
  TAccountTask extends string = string,
  TAccountClaim extends string = string,
  TAccountEscrow extends string = string,
  TAccountTaskValidationConfig extends string = string,
  TAccountTaskSubmission extends string = string,
  TAccountWorker extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountTreasury extends string = string,
  TAccountCreator extends string = string,
  TAccountWorkerAuthority extends string = string,
  TAccountOperator extends string = string,
  TAccountReferrer extends string = string,
  TAccountCranker extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  task: Address<TAccountTask>;
  claim?: Address<TAccountClaim>;
  escrow?: Address<TAccountEscrow>;
  taskValidationConfig?: Address<TAccountTaskValidationConfig>;
  taskSubmission?: Address<TAccountTaskSubmission>;
  worker: Address<TAccountWorker>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  treasury: Address<TAccountTreasury>;
  /** validated against task.creator. Never receives pool funds. */
  creator: Address<TAccountCreator>;
  /** against worker.authority (stored pubkey — spec invariant 2). */
  workerAuthority: Address<TAccountWorkerAuthority>;
  /**
   * only when the task carries a non-zero operator fee. (A contest can never be
   * a hire — configure_task_validation rejects live-HireRecord tasks — so the
   * terms come from the Task alone; no HireRecord fallback.)
   */
  operator?: Address<TAccountOperator>;
  /** split). Required only when the task carries a non-zero referrer fee. */
  referrer?: Address<TAccountReferrer>;
  /** Permissionless cranker; pays only the transaction fee. */
  cranker: TransactionSigner<TAccountCranker>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getDistributeGhostShareInstructionAsync<
  TAccountTask extends string,
  TAccountClaim extends string,
  TAccountEscrow extends string,
  TAccountTaskValidationConfig extends string,
  TAccountTaskSubmission extends string,
  TAccountWorker extends string,
  TAccountProtocolConfig extends string,
  TAccountTreasury extends string,
  TAccountCreator extends string,
  TAccountWorkerAuthority extends string,
  TAccountOperator extends string,
  TAccountReferrer extends string,
  TAccountCranker extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: DistributeGhostShareAsyncInput<
    TAccountTask,
    TAccountClaim,
    TAccountEscrow,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountWorker,
    TAccountProtocolConfig,
    TAccountTreasury,
    TAccountCreator,
    TAccountWorkerAuthority,
    TAccountOperator,
    TAccountReferrer,
    TAccountCranker,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DistributeGhostShareInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountClaim,
    TAccountEscrow,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountWorker,
    TAccountProtocolConfig,
    TAccountTreasury,
    TAccountCreator,
    TAccountWorkerAuthority,
    TAccountOperator,
    TAccountReferrer,
    TAccountCranker,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: true },
    claim: { value: input.claim ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    taskValidationConfig: {
      value: input.taskValidationConfig ?? null,
      isWritable: true,
    },
    taskSubmission: { value: input.taskSubmission ?? null, isWritable: true },
    worker: { value: input.worker ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: true },
    workerAuthority: { value: input.workerAuthority ?? null, isWritable: true },
    operator: { value: input.operator ?? null, isWritable: true },
    referrer: { value: input.referrer ?? null, isWritable: true },
    cranker: { value: input.cranker ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.claim.value) {
    accounts.claim.value = await findAcceptTaskResultClaimPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
      worker: getAddressFromResolvedInstructionAccount(
        "worker",
        accounts.worker.value,
      ),
    });
  }
  if (!accounts.escrow.value) {
    accounts.escrow.value = await findEscrowPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }
  if (!accounts.taskValidationConfig.value) {
    accounts.taskValidationConfig.value = await findTaskValidationConfigPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }
  if (!accounts.taskSubmission.value) {
    accounts.taskSubmission.value = await findTaskSubmissionPda({
      claim: getAddressFromResolvedInstructionAccount(
        "claim",
        accounts.claim.value,
      ),
    });
  }
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("claim", accounts.claim),
      getAccountMeta("escrow", accounts.escrow),
      getAccountMeta("taskValidationConfig", accounts.taskValidationConfig),
      getAccountMeta("taskSubmission", accounts.taskSubmission),
      getAccountMeta("worker", accounts.worker),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("workerAuthority", accounts.workerAuthority),
      getAccountMeta("operator", accounts.operator),
      getAccountMeta("referrer", accounts.referrer),
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getDistributeGhostShareInstructionDataEncoder().encode({}),
    programAddress,
  } as DistributeGhostShareInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountClaim,
    TAccountEscrow,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountWorker,
    TAccountProtocolConfig,
    TAccountTreasury,
    TAccountCreator,
    TAccountWorkerAuthority,
    TAccountOperator,
    TAccountReferrer,
    TAccountCranker,
    TAccountSystemProgram
  >);
}

export type DistributeGhostShareInput<
  TAccountTask extends string = string,
  TAccountClaim extends string = string,
  TAccountEscrow extends string = string,
  TAccountTaskValidationConfig extends string = string,
  TAccountTaskSubmission extends string = string,
  TAccountWorker extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountTreasury extends string = string,
  TAccountCreator extends string = string,
  TAccountWorkerAuthority extends string = string,
  TAccountOperator extends string = string,
  TAccountReferrer extends string = string,
  TAccountCranker extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  task: Address<TAccountTask>;
  claim: Address<TAccountClaim>;
  escrow: Address<TAccountEscrow>;
  taskValidationConfig: Address<TAccountTaskValidationConfig>;
  taskSubmission: Address<TAccountTaskSubmission>;
  worker: Address<TAccountWorker>;
  protocolConfig: Address<TAccountProtocolConfig>;
  treasury: Address<TAccountTreasury>;
  /** validated against task.creator. Never receives pool funds. */
  creator: Address<TAccountCreator>;
  /** against worker.authority (stored pubkey — spec invariant 2). */
  workerAuthority: Address<TAccountWorkerAuthority>;
  /**
   * only when the task carries a non-zero operator fee. (A contest can never be
   * a hire — configure_task_validation rejects live-HireRecord tasks — so the
   * terms come from the Task alone; no HireRecord fallback.)
   */
  operator?: Address<TAccountOperator>;
  /** split). Required only when the task carries a non-zero referrer fee. */
  referrer?: Address<TAccountReferrer>;
  /** Permissionless cranker; pays only the transaction fee. */
  cranker: TransactionSigner<TAccountCranker>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getDistributeGhostShareInstruction<
  TAccountTask extends string,
  TAccountClaim extends string,
  TAccountEscrow extends string,
  TAccountTaskValidationConfig extends string,
  TAccountTaskSubmission extends string,
  TAccountWorker extends string,
  TAccountProtocolConfig extends string,
  TAccountTreasury extends string,
  TAccountCreator extends string,
  TAccountWorkerAuthority extends string,
  TAccountOperator extends string,
  TAccountReferrer extends string,
  TAccountCranker extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: DistributeGhostShareInput<
    TAccountTask,
    TAccountClaim,
    TAccountEscrow,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountWorker,
    TAccountProtocolConfig,
    TAccountTreasury,
    TAccountCreator,
    TAccountWorkerAuthority,
    TAccountOperator,
    TAccountReferrer,
    TAccountCranker,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): DistributeGhostShareInstruction<
  TProgramAddress,
  TAccountTask,
  TAccountClaim,
  TAccountEscrow,
  TAccountTaskValidationConfig,
  TAccountTaskSubmission,
  TAccountWorker,
  TAccountProtocolConfig,
  TAccountTreasury,
  TAccountCreator,
  TAccountWorkerAuthority,
  TAccountOperator,
  TAccountReferrer,
  TAccountCranker,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: true },
    claim: { value: input.claim ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    taskValidationConfig: {
      value: input.taskValidationConfig ?? null,
      isWritable: true,
    },
    taskSubmission: { value: input.taskSubmission ?? null, isWritable: true },
    worker: { value: input.worker ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: true },
    workerAuthority: { value: input.workerAuthority ?? null, isWritable: true },
    operator: { value: input.operator ?? null, isWritable: true },
    referrer: { value: input.referrer ?? null, isWritable: true },
    cranker: { value: input.cranker ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("claim", accounts.claim),
      getAccountMeta("escrow", accounts.escrow),
      getAccountMeta("taskValidationConfig", accounts.taskValidationConfig),
      getAccountMeta("taskSubmission", accounts.taskSubmission),
      getAccountMeta("worker", accounts.worker),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("workerAuthority", accounts.workerAuthority),
      getAccountMeta("operator", accounts.operator),
      getAccountMeta("referrer", accounts.referrer),
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getDistributeGhostShareInstructionDataEncoder().encode({}),
    programAddress,
  } as DistributeGhostShareInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountClaim,
    TAccountEscrow,
    TAccountTaskValidationConfig,
    TAccountTaskSubmission,
    TAccountWorker,
    TAccountProtocolConfig,
    TAccountTreasury,
    TAccountCreator,
    TAccountWorkerAuthority,
    TAccountOperator,
    TAccountReferrer,
    TAccountCranker,
    TAccountSystemProgram
  >);
}

export type ParsedDistributeGhostShareInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    task: TAccountMetas[0];
    claim: TAccountMetas[1];
    escrow: TAccountMetas[2];
    taskValidationConfig: TAccountMetas[3];
    taskSubmission: TAccountMetas[4];
    worker: TAccountMetas[5];
    protocolConfig: TAccountMetas[6];
    treasury: TAccountMetas[7];
    /** validated against task.creator. Never receives pool funds. */
    creator: TAccountMetas[8];
    /** against worker.authority (stored pubkey — spec invariant 2). */
    workerAuthority: TAccountMetas[9];
    /**
     * only when the task carries a non-zero operator fee. (A contest can never be
     * a hire — configure_task_validation rejects live-HireRecord tasks — so the
     * terms come from the Task alone; no HireRecord fallback.)
     */
    operator?: TAccountMetas[10] | undefined;
    /** split). Required only when the task carries a non-zero referrer fee. */
    referrer?: TAccountMetas[11] | undefined;
    /** Permissionless cranker; pays only the transaction fee. */
    cranker: TAccountMetas[12];
    systemProgram: TAccountMetas[13];
  };
  data: DistributeGhostShareInstructionData;
};

export function parseDistributeGhostShareInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDistributeGhostShareInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 14) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 14,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === AGENC_COORDINATION_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      task: getNextAccount(),
      claim: getNextAccount(),
      escrow: getNextAccount(),
      taskValidationConfig: getNextAccount(),
      taskSubmission: getNextAccount(),
      worker: getNextAccount(),
      protocolConfig: getNextAccount(),
      treasury: getNextAccount(),
      creator: getNextAccount(),
      workerAuthority: getNextAccount(),
      operator: getNextOptionalAccount(),
      referrer: getNextOptionalAccount(),
      cranker: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDistributeGhostShareInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
