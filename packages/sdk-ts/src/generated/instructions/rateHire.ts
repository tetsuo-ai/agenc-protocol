/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findHireRatingPda,
  findHireRecordPda,
  findProtocolConfigPda,
} from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const RATE_HIRE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  31, 201, 148, 116, 69, 243, 201, 90,
]);

export function getRateHireDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(RATE_HIRE_DISCRIMINATOR);
}

export type RateHireInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountHireRecord extends string | AccountMeta<string> = string,
  TAccountListing extends string | AccountMeta<string> = string,
  TAccountHireRating extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountBuyer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTask extends string
        ? ReadonlyAccount<TAccountTask>
        : TAccountTask,
      TAccountHireRecord extends string
        ? ReadonlyAccount<TAccountHireRecord>
        : TAccountHireRecord,
      TAccountListing extends string
        ? WritableAccount<TAccountListing>
        : TAccountListing,
      TAccountHireRating extends string
        ? WritableAccount<TAccountHireRating>
        : TAccountHireRating,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountBuyer extends string
        ? WritableSignerAccount<TAccountBuyer> &
            AccountSignerMeta<TAccountBuyer>
        : TAccountBuyer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RateHireInstructionData = {
  discriminator: ReadonlyUint8Array;
  score: number;
  reviewHash: Option<ReadonlyUint8Array>;
  reviewUri: string;
};

export type RateHireInstructionDataArgs = {
  score: number;
  reviewHash: OptionOrNullable<ReadonlyUint8Array>;
  reviewUri: string;
};

export function getRateHireInstructionDataEncoder(): Encoder<RateHireInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["score", getU8Encoder()],
      ["reviewHash", getOptionEncoder(fixEncoderSize(getBytesEncoder(), 32))],
      ["reviewUri", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: RATE_HIRE_DISCRIMINATOR }),
  );
}

export function getRateHireInstructionDataDecoder(): Decoder<RateHireInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["score", getU8Decoder()],
    ["reviewHash", getOptionDecoder(fixDecoderSize(getBytesDecoder(), 32))],
    ["reviewUri", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getRateHireInstructionDataCodec(): Codec<
  RateHireInstructionDataArgs,
  RateHireInstructionData
> {
  return combineCodec(
    getRateHireInstructionDataEncoder(),
    getRateHireInstructionDataDecoder(),
  );
}

export type RateHireAsyncInput<
  TAccountTask extends string = string,
  TAccountHireRecord extends string = string,
  TAccountListing extends string = string,
  TAccountHireRating extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountBuyer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * The hired task being rated. Must be terminal `Completed` and its `creator`
   * (the recorded buyer) must equal the signer (checked in the handler).
   */
  task: Address<TAccountTask>;
  /**
   * Links the task to its source listing (PDA `["hire", task]`). Its existence
   * proves this task was minted by a listing hire; `listing` here must match
   * `hire_record.listing`.
   */
  hireRecord?: Address<TAccountHireRecord>;
  /**
   * Source service listing whose rating aggregate is updated. Bound by its own
   * canonical seeds AND matched to the hire record so it cannot be substituted.
   */
  listing: Address<TAccountListing>;
  /** One-rating-per-hire: `init` makes a second `rate_hire` on the same task fail. */
  hireRating?: Address<TAccountHireRating>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  /**
   * The buyer recorded on the task (`task.creator`). Must sign and pay rent.
   * Buyer-equality is enforced in the handler against `task.creator`.
   */
  buyer: TransactionSigner<TAccountBuyer>;
  systemProgram?: Address<TAccountSystemProgram>;
  score: RateHireInstructionDataArgs["score"];
  reviewHash: RateHireInstructionDataArgs["reviewHash"];
  reviewUri: RateHireInstructionDataArgs["reviewUri"];
};

export async function getRateHireInstructionAsync<
  TAccountTask extends string,
  TAccountHireRecord extends string,
  TAccountListing extends string,
  TAccountHireRating extends string,
  TAccountProtocolConfig extends string,
  TAccountBuyer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RateHireAsyncInput<
    TAccountTask,
    TAccountHireRecord,
    TAccountListing,
    TAccountHireRating,
    TAccountProtocolConfig,
    TAccountBuyer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RateHireInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountHireRecord,
    TAccountListing,
    TAccountHireRating,
    TAccountProtocolConfig,
    TAccountBuyer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: false },
    hireRecord: { value: input.hireRecord ?? null, isWritable: false },
    listing: { value: input.listing ?? null, isWritable: true },
    hireRating: { value: input.hireRating ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    buyer: { value: input.buyer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hireRecord.value) {
    accounts.hireRecord.value = await findHireRecordPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }
  if (!accounts.hireRating.value) {
    accounts.hireRating.value = await findHireRatingPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("hireRecord", accounts.hireRecord),
      getAccountMeta("listing", accounts.listing),
      getAccountMeta("hireRating", accounts.hireRating),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("buyer", accounts.buyer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRateHireInstructionDataEncoder().encode(
      args as RateHireInstructionDataArgs,
    ),
    programAddress,
  } as RateHireInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountHireRecord,
    TAccountListing,
    TAccountHireRating,
    TAccountProtocolConfig,
    TAccountBuyer,
    TAccountSystemProgram
  >);
}

export type RateHireInput<
  TAccountTask extends string = string,
  TAccountHireRecord extends string = string,
  TAccountListing extends string = string,
  TAccountHireRating extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountBuyer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * The hired task being rated. Must be terminal `Completed` and its `creator`
   * (the recorded buyer) must equal the signer (checked in the handler).
   */
  task: Address<TAccountTask>;
  /**
   * Links the task to its source listing (PDA `["hire", task]`). Its existence
   * proves this task was minted by a listing hire; `listing` here must match
   * `hire_record.listing`.
   */
  hireRecord: Address<TAccountHireRecord>;
  /**
   * Source service listing whose rating aggregate is updated. Bound by its own
   * canonical seeds AND matched to the hire record so it cannot be substituted.
   */
  listing: Address<TAccountListing>;
  /** One-rating-per-hire: `init` makes a second `rate_hire` on the same task fail. */
  hireRating: Address<TAccountHireRating>;
  protocolConfig: Address<TAccountProtocolConfig>;
  /**
   * The buyer recorded on the task (`task.creator`). Must sign and pay rent.
   * Buyer-equality is enforced in the handler against `task.creator`.
   */
  buyer: TransactionSigner<TAccountBuyer>;
  systemProgram?: Address<TAccountSystemProgram>;
  score: RateHireInstructionDataArgs["score"];
  reviewHash: RateHireInstructionDataArgs["reviewHash"];
  reviewUri: RateHireInstructionDataArgs["reviewUri"];
};

export function getRateHireInstruction<
  TAccountTask extends string,
  TAccountHireRecord extends string,
  TAccountListing extends string,
  TAccountHireRating extends string,
  TAccountProtocolConfig extends string,
  TAccountBuyer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RateHireInput<
    TAccountTask,
    TAccountHireRecord,
    TAccountListing,
    TAccountHireRating,
    TAccountProtocolConfig,
    TAccountBuyer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RateHireInstruction<
  TProgramAddress,
  TAccountTask,
  TAccountHireRecord,
  TAccountListing,
  TAccountHireRating,
  TAccountProtocolConfig,
  TAccountBuyer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: false },
    hireRecord: { value: input.hireRecord ?? null, isWritable: false },
    listing: { value: input.listing ?? null, isWritable: true },
    hireRating: { value: input.hireRating ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    buyer: { value: input.buyer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("hireRecord", accounts.hireRecord),
      getAccountMeta("listing", accounts.listing),
      getAccountMeta("hireRating", accounts.hireRating),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("buyer", accounts.buyer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRateHireInstructionDataEncoder().encode(
      args as RateHireInstructionDataArgs,
    ),
    programAddress,
  } as RateHireInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountHireRecord,
    TAccountListing,
    TAccountHireRating,
    TAccountProtocolConfig,
    TAccountBuyer,
    TAccountSystemProgram
  >);
}

export type ParsedRateHireInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * The hired task being rated. Must be terminal `Completed` and its `creator`
     * (the recorded buyer) must equal the signer (checked in the handler).
     */
    task: TAccountMetas[0];
    /**
     * Links the task to its source listing (PDA `["hire", task]`). Its existence
     * proves this task was minted by a listing hire; `listing` here must match
     * `hire_record.listing`.
     */
    hireRecord: TAccountMetas[1];
    /**
     * Source service listing whose rating aggregate is updated. Bound by its own
     * canonical seeds AND matched to the hire record so it cannot be substituted.
     */
    listing: TAccountMetas[2];
    /** One-rating-per-hire: `init` makes a second `rate_hire` on the same task fail. */
    hireRating: TAccountMetas[3];
    protocolConfig: TAccountMetas[4];
    /**
     * The buyer recorded on the task (`task.creator`). Must sign and pay rent.
     * Buyer-equality is enforced in the handler against `task.creator`.
     */
    buyer: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
  };
  data: RateHireInstructionData;
};

export function parseRateHireInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRateHireInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      task: getNextAccount(),
      hireRecord: getNextAccount(),
      listing: getNextAccount(),
      hireRating: getNextAccount(),
      protocolConfig: getNextAccount(),
      buyer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRateHireInstructionDataDecoder().decode(instruction.data),
  };
}
