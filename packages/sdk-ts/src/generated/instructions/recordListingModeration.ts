/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findListingModerationPda, findModerationConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const RECORD_LISTING_MODERATION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([83, 247, 2, 131, 91, 223, 49, 33]);

export function getRecordListingModerationDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECORD_LISTING_MODERATION_DISCRIMINATOR,
  );
}

export type RecordListingModerationInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountModerationConfig extends string | AccountMeta<string> = string,
  TAccountListing extends string | AccountMeta<string> = string,
  TAccountModerator extends string | AccountMeta<string> = string,
  TAccountListingModeration extends string | AccountMeta<string> = string,
  TAccountModerationAttestor extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountModerationConfig extends string
        ? ReadonlyAccount<TAccountModerationConfig>
        : TAccountModerationConfig,
      TAccountListing extends string
        ? ReadonlyAccount<TAccountListing>
        : TAccountListing,
      TAccountModerator extends string
        ? WritableSignerAccount<TAccountModerator> &
            AccountSignerMeta<TAccountModerator>
        : TAccountModerator,
      TAccountListingModeration extends string
        ? WritableAccount<TAccountListingModeration>
        : TAccountListingModeration,
      TAccountModerationAttestor extends string
        ? ReadonlyAccount<TAccountModerationAttestor>
        : TAccountModerationAttestor,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RecordListingModerationInstructionData = {
  discriminator: ReadonlyUint8Array;
  jobSpecHash: ReadonlyUint8Array;
  status: number;
  riskScore: number;
  categoryMask: bigint;
  policyHash: ReadonlyUint8Array;
  scannerHash: ReadonlyUint8Array;
  expiresAt: bigint;
};

export type RecordListingModerationInstructionDataArgs = {
  jobSpecHash: ReadonlyUint8Array;
  status: number;
  riskScore: number;
  categoryMask: number | bigint;
  policyHash: ReadonlyUint8Array;
  scannerHash: ReadonlyUint8Array;
  expiresAt: number | bigint;
};

export function getRecordListingModerationInstructionDataEncoder(): FixedSizeEncoder<RecordListingModerationInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["jobSpecHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["status", getU8Encoder()],
      ["riskScore", getU8Encoder()],
      ["categoryMask", getU64Encoder()],
      ["policyHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["scannerHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["expiresAt", getI64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: RECORD_LISTING_MODERATION_DISCRIMINATOR,
    }),
  );
}

export function getRecordListingModerationInstructionDataDecoder(): FixedSizeDecoder<RecordListingModerationInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["jobSpecHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["status", getU8Decoder()],
    ["riskScore", getU8Decoder()],
    ["categoryMask", getU64Decoder()],
    ["policyHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["scannerHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["expiresAt", getI64Decoder()],
  ]);
}

export function getRecordListingModerationInstructionDataCodec(): FixedSizeCodec<
  RecordListingModerationInstructionDataArgs,
  RecordListingModerationInstructionData
> {
  return combineCodec(
    getRecordListingModerationInstructionDataEncoder(),
    getRecordListingModerationInstructionDataDecoder(),
  );
}

export type RecordListingModerationAsyncInput<
  TAccountModerationConfig extends string = string,
  TAccountListing extends string = string,
  TAccountModerator extends string = string,
  TAccountListingModeration extends string = string,
  TAccountModerationAttestor extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  moderationConfig?: Address<TAccountModerationConfig>;
  listing: Address<TAccountListing>;
  /**
   * The recording signer. Authorization (global moderation authority OR a registered
   * attestor) is checked in the handler, not as an account constraint here. Declared
   * before `listing_moderation` so the v2 seed can reference it.
   */
  moderator: TransactionSigner<TAccountModerator>;
  /**
   * P1.2 §4.3 — v2 MODERATOR-KEYED record (the listing mirror of
   * `task_moderation_v2`): each attestor owns an exclusive slot; `init_if_needed`
   * is self-re-review only; no cross-attestor overwrites. Post-upgrade, records are
   * written ONLY under v2 seeds — legacy `["listing_moderation", …]` PDAs are frozen.
   */
  listingModeration?: Address<TAccountListingModeration>;
  /**
   * OPTIONAL (P6.8): a registered moderation-attestor roster entry. When supplied (and
   * `moderator == moderation_attestor.attestor`), authorizes a non-global-authority
   * attestor to record. Bound to `["moderation_attestor", moderator]` — Anchor enforces
   * the canonical PDA, so a forged/mismatched entry fails account resolution, and a
   * REVOKED attestor's PDA is closed and fails to load (cannot attest). This instruction
   * is full-surface only, so this field carries no canary-surface implications.
   */
  moderationAttestor?: Address<TAccountModerationAttestor>;
  systemProgram?: Address<TAccountSystemProgram>;
  jobSpecHash: RecordListingModerationInstructionDataArgs["jobSpecHash"];
  status: RecordListingModerationInstructionDataArgs["status"];
  riskScore: RecordListingModerationInstructionDataArgs["riskScore"];
  categoryMask: RecordListingModerationInstructionDataArgs["categoryMask"];
  policyHash: RecordListingModerationInstructionDataArgs["policyHash"];
  scannerHash: RecordListingModerationInstructionDataArgs["scannerHash"];
  expiresAt: RecordListingModerationInstructionDataArgs["expiresAt"];
};

export async function getRecordListingModerationInstructionAsync<
  TAccountModerationConfig extends string,
  TAccountListing extends string,
  TAccountModerator extends string,
  TAccountListingModeration extends string,
  TAccountModerationAttestor extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RecordListingModerationAsyncInput<
    TAccountModerationConfig,
    TAccountListing,
    TAccountModerator,
    TAccountListingModeration,
    TAccountModerationAttestor,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RecordListingModerationInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountListing,
    TAccountModerator,
    TAccountListingModeration,
    TAccountModerationAttestor,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    moderationConfig: {
      value: input.moderationConfig ?? null,
      isWritable: false,
    },
    listing: { value: input.listing ?? null, isWritable: false },
    moderator: { value: input.moderator ?? null, isWritable: true },
    listingModeration: {
      value: input.listingModeration ?? null,
      isWritable: true,
    },
    moderationAttestor: {
      value: input.moderationAttestor ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.moderationConfig.value) {
    accounts.moderationConfig.value = await findModerationConfigPda();
  }
  if (!accounts.listingModeration.value) {
    accounts.listingModeration.value = await findListingModerationPda({
      listing: getAddressFromResolvedInstructionAccount(
        "listing",
        accounts.listing.value,
      ),
      jobSpecHash: getNonNullResolvedInstructionInput(
        "jobSpecHash",
        args.jobSpecHash,
      ),
      moderator: getAddressFromResolvedInstructionAccount(
        "moderator",
        accounts.moderator.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("moderationConfig", accounts.moderationConfig),
      getAccountMeta("listing", accounts.listing),
      getAccountMeta("moderator", accounts.moderator),
      getAccountMeta("listingModeration", accounts.listingModeration),
      getAccountMeta("moderationAttestor", accounts.moderationAttestor),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRecordListingModerationInstructionDataEncoder().encode(
      args as RecordListingModerationInstructionDataArgs,
    ),
    programAddress,
  } as RecordListingModerationInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountListing,
    TAccountModerator,
    TAccountListingModeration,
    TAccountModerationAttestor,
    TAccountSystemProgram
  >);
}

export type RecordListingModerationInput<
  TAccountModerationConfig extends string = string,
  TAccountListing extends string = string,
  TAccountModerator extends string = string,
  TAccountListingModeration extends string = string,
  TAccountModerationAttestor extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  moderationConfig: Address<TAccountModerationConfig>;
  listing: Address<TAccountListing>;
  /**
   * The recording signer. Authorization (global moderation authority OR a registered
   * attestor) is checked in the handler, not as an account constraint here. Declared
   * before `listing_moderation` so the v2 seed can reference it.
   */
  moderator: TransactionSigner<TAccountModerator>;
  /**
   * P1.2 §4.3 — v2 MODERATOR-KEYED record (the listing mirror of
   * `task_moderation_v2`): each attestor owns an exclusive slot; `init_if_needed`
   * is self-re-review only; no cross-attestor overwrites. Post-upgrade, records are
   * written ONLY under v2 seeds — legacy `["listing_moderation", …]` PDAs are frozen.
   */
  listingModeration: Address<TAccountListingModeration>;
  /**
   * OPTIONAL (P6.8): a registered moderation-attestor roster entry. When supplied (and
   * `moderator == moderation_attestor.attestor`), authorizes a non-global-authority
   * attestor to record. Bound to `["moderation_attestor", moderator]` — Anchor enforces
   * the canonical PDA, so a forged/mismatched entry fails account resolution, and a
   * REVOKED attestor's PDA is closed and fails to load (cannot attest). This instruction
   * is full-surface only, so this field carries no canary-surface implications.
   */
  moderationAttestor?: Address<TAccountModerationAttestor>;
  systemProgram?: Address<TAccountSystemProgram>;
  jobSpecHash: RecordListingModerationInstructionDataArgs["jobSpecHash"];
  status: RecordListingModerationInstructionDataArgs["status"];
  riskScore: RecordListingModerationInstructionDataArgs["riskScore"];
  categoryMask: RecordListingModerationInstructionDataArgs["categoryMask"];
  policyHash: RecordListingModerationInstructionDataArgs["policyHash"];
  scannerHash: RecordListingModerationInstructionDataArgs["scannerHash"];
  expiresAt: RecordListingModerationInstructionDataArgs["expiresAt"];
};

export function getRecordListingModerationInstruction<
  TAccountModerationConfig extends string,
  TAccountListing extends string,
  TAccountModerator extends string,
  TAccountListingModeration extends string,
  TAccountModerationAttestor extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RecordListingModerationInput<
    TAccountModerationConfig,
    TAccountListing,
    TAccountModerator,
    TAccountListingModeration,
    TAccountModerationAttestor,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RecordListingModerationInstruction<
  TProgramAddress,
  TAccountModerationConfig,
  TAccountListing,
  TAccountModerator,
  TAccountListingModeration,
  TAccountModerationAttestor,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    moderationConfig: {
      value: input.moderationConfig ?? null,
      isWritable: false,
    },
    listing: { value: input.listing ?? null, isWritable: false },
    moderator: { value: input.moderator ?? null, isWritable: true },
    listingModeration: {
      value: input.listingModeration ?? null,
      isWritable: true,
    },
    moderationAttestor: {
      value: input.moderationAttestor ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("moderationConfig", accounts.moderationConfig),
      getAccountMeta("listing", accounts.listing),
      getAccountMeta("moderator", accounts.moderator),
      getAccountMeta("listingModeration", accounts.listingModeration),
      getAccountMeta("moderationAttestor", accounts.moderationAttestor),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRecordListingModerationInstructionDataEncoder().encode(
      args as RecordListingModerationInstructionDataArgs,
    ),
    programAddress,
  } as RecordListingModerationInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountListing,
    TAccountModerator,
    TAccountListingModeration,
    TAccountModerationAttestor,
    TAccountSystemProgram
  >);
}

export type ParsedRecordListingModerationInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    moderationConfig: TAccountMetas[0];
    listing: TAccountMetas[1];
    /**
     * The recording signer. Authorization (global moderation authority OR a registered
     * attestor) is checked in the handler, not as an account constraint here. Declared
     * before `listing_moderation` so the v2 seed can reference it.
     */
    moderator: TAccountMetas[2];
    /**
     * P1.2 §4.3 — v2 MODERATOR-KEYED record (the listing mirror of
     * `task_moderation_v2`): each attestor owns an exclusive slot; `init_if_needed`
     * is self-re-review only; no cross-attestor overwrites. Post-upgrade, records are
     * written ONLY under v2 seeds — legacy `["listing_moderation", …]` PDAs are frozen.
     */
    listingModeration: TAccountMetas[3];
    /**
     * OPTIONAL (P6.8): a registered moderation-attestor roster entry. When supplied (and
     * `moderator == moderation_attestor.attestor`), authorizes a non-global-authority
     * attestor to record. Bound to `["moderation_attestor", moderator]` — Anchor enforces
     * the canonical PDA, so a forged/mismatched entry fails account resolution, and a
     * REVOKED attestor's PDA is closed and fails to load (cannot attest). This instruction
     * is full-surface only, so this field carries no canary-surface implications.
     */
    moderationAttestor?: TAccountMetas[4] | undefined;
    systemProgram: TAccountMetas[5];
  };
  data: RecordListingModerationInstructionData;
};

export function parseRecordListingModerationInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRecordListingModerationInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === AGENC_COORDINATION_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      moderationConfig: getNextAccount(),
      listing: getNextAccount(),
      moderator: getNextAccount(),
      listingModeration: getNextAccount(),
      moderationAttestor: getNextOptionalAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRecordListingModerationInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
