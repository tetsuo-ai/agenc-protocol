/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findBidBookPda,
  findBidderMarketStatePda,
  findBidPda,
  findProtocolConfigPda,
} from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const EXPIRE_BID_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  61, 99, 189, 49, 121, 31, 41, 42,
]);

export function getExpireBidDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(EXPIRE_BID_DISCRIMINATOR);
}

export type ExpireBidInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountBidBook extends string | AccountMeta<string> = string,
  TAccountBid extends string | AccountMeta<string> = string,
  TAccountBidderMarketState extends string | AccountMeta<string> = string,
  TAccountBidder extends string | AccountMeta<string> = string,
  TAccountBidderAuthority extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountTask extends string
        ? ReadonlyAccount<TAccountTask>
        : TAccountTask,
      TAccountBidBook extends string
        ? WritableAccount<TAccountBidBook>
        : TAccountBidBook,
      TAccountBid extends string ? WritableAccount<TAccountBid> : TAccountBid,
      TAccountBidderMarketState extends string
        ? WritableAccount<TAccountBidderMarketState>
        : TAccountBidderMarketState,
      TAccountBidder extends string
        ? ReadonlyAccount<TAccountBidder>
        : TAccountBidder,
      TAccountBidderAuthority extends string
        ? WritableAccount<TAccountBidderAuthority>
        : TAccountBidderAuthority,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type ExpireBidInstructionData = { discriminator: ReadonlyUint8Array };

export type ExpireBidInstructionDataArgs = {};

export function getExpireBidInstructionDataEncoder(): FixedSizeEncoder<ExpireBidInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
    ]),
    (value) => ({ ...value, discriminator: EXPIRE_BID_DISCRIMINATOR }),
  );
}

export function getExpireBidInstructionDataDecoder(): FixedSizeDecoder<ExpireBidInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getExpireBidInstructionDataCodec(): FixedSizeCodec<
  ExpireBidInstructionDataArgs,
  ExpireBidInstructionData
> {
  return combineCodec(
    getExpireBidInstructionDataEncoder(),
    getExpireBidInstructionDataDecoder(),
  );
}

export type ExpireBidAsyncInput<
  TAccountProtocolConfig extends string = string,
  TAccountTask extends string = string,
  TAccountBidBook extends string = string,
  TAccountBid extends string = string,
  TAccountBidderMarketState extends string = string,
  TAccountBidder extends string = string,
  TAccountBidderAuthority extends string = string,
  TAccountAuthority extends string = string,
> = {
  protocolConfig?: Address<TAccountProtocolConfig>;
  task: Address<TAccountTask>;
  bidBook?: Address<TAccountBidBook>;
  bid?: Address<TAccountBid>;
  bidderMarketState?: Address<TAccountBidderMarketState>;
  bidder: Address<TAccountBidder>;
  /** and only receives lamports when the expired bid account is closed. */
  bidderAuthority: Address<TAccountBidderAuthority>;
  authority: TransactionSigner<TAccountAuthority>;
};

export async function getExpireBidInstructionAsync<
  TAccountProtocolConfig extends string,
  TAccountTask extends string,
  TAccountBidBook extends string,
  TAccountBid extends string,
  TAccountBidderMarketState extends string,
  TAccountBidder extends string,
  TAccountBidderAuthority extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: ExpireBidAsyncInput<
    TAccountProtocolConfig,
    TAccountTask,
    TAccountBidBook,
    TAccountBid,
    TAccountBidderMarketState,
    TAccountBidder,
    TAccountBidderAuthority,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ExpireBidInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountTask,
    TAccountBidBook,
    TAccountBid,
    TAccountBidderMarketState,
    TAccountBidder,
    TAccountBidderAuthority,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    task: { value: input.task ?? null, isWritable: false },
    bidBook: { value: input.bidBook ?? null, isWritable: true },
    bid: { value: input.bid ?? null, isWritable: true },
    bidderMarketState: {
      value: input.bidderMarketState ?? null,
      isWritable: true,
    },
    bidder: { value: input.bidder ?? null, isWritable: false },
    bidderAuthority: { value: input.bidderAuthority ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.bidBook.value) {
    accounts.bidBook.value = await findBidBookPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }
  if (!accounts.bid.value) {
    accounts.bid.value = await findBidPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
      bidder: getAddressFromResolvedInstructionAccount(
        "bidder",
        accounts.bidder.value,
      ),
    });
  }
  if (!accounts.bidderMarketState.value) {
    accounts.bidderMarketState.value = await findBidderMarketStatePda({
      bidder: getAddressFromResolvedInstructionAccount(
        "bidder",
        accounts.bidder.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("task", accounts.task),
      getAccountMeta("bidBook", accounts.bidBook),
      getAccountMeta("bid", accounts.bid),
      getAccountMeta("bidderMarketState", accounts.bidderMarketState),
      getAccountMeta("bidder", accounts.bidder),
      getAccountMeta("bidderAuthority", accounts.bidderAuthority),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getExpireBidInstructionDataEncoder().encode({}),
    programAddress,
  } as ExpireBidInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountTask,
    TAccountBidBook,
    TAccountBid,
    TAccountBidderMarketState,
    TAccountBidder,
    TAccountBidderAuthority,
    TAccountAuthority
  >);
}

export type ExpireBidInput<
  TAccountProtocolConfig extends string = string,
  TAccountTask extends string = string,
  TAccountBidBook extends string = string,
  TAccountBid extends string = string,
  TAccountBidderMarketState extends string = string,
  TAccountBidder extends string = string,
  TAccountBidderAuthority extends string = string,
  TAccountAuthority extends string = string,
> = {
  protocolConfig: Address<TAccountProtocolConfig>;
  task: Address<TAccountTask>;
  bidBook: Address<TAccountBidBook>;
  bid: Address<TAccountBid>;
  bidderMarketState: Address<TAccountBidderMarketState>;
  bidder: Address<TAccountBidder>;
  /** and only receives lamports when the expired bid account is closed. */
  bidderAuthority: Address<TAccountBidderAuthority>;
  authority: TransactionSigner<TAccountAuthority>;
};

export function getExpireBidInstruction<
  TAccountProtocolConfig extends string,
  TAccountTask extends string,
  TAccountBidBook extends string,
  TAccountBid extends string,
  TAccountBidderMarketState extends string,
  TAccountBidder extends string,
  TAccountBidderAuthority extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: ExpireBidInput<
    TAccountProtocolConfig,
    TAccountTask,
    TAccountBidBook,
    TAccountBid,
    TAccountBidderMarketState,
    TAccountBidder,
    TAccountBidderAuthority,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): ExpireBidInstruction<
  TProgramAddress,
  TAccountProtocolConfig,
  TAccountTask,
  TAccountBidBook,
  TAccountBid,
  TAccountBidderMarketState,
  TAccountBidder,
  TAccountBidderAuthority,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    task: { value: input.task ?? null, isWritable: false },
    bidBook: { value: input.bidBook ?? null, isWritable: true },
    bid: { value: input.bid ?? null, isWritable: true },
    bidderMarketState: {
      value: input.bidderMarketState ?? null,
      isWritable: true,
    },
    bidder: { value: input.bidder ?? null, isWritable: false },
    bidderAuthority: { value: input.bidderAuthority ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("task", accounts.task),
      getAccountMeta("bidBook", accounts.bidBook),
      getAccountMeta("bid", accounts.bid),
      getAccountMeta("bidderMarketState", accounts.bidderMarketState),
      getAccountMeta("bidder", accounts.bidder),
      getAccountMeta("bidderAuthority", accounts.bidderAuthority),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getExpireBidInstructionDataEncoder().encode({}),
    programAddress,
  } as ExpireBidInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountTask,
    TAccountBidBook,
    TAccountBid,
    TAccountBidderMarketState,
    TAccountBidder,
    TAccountBidderAuthority,
    TAccountAuthority
  >);
}

export type ParsedExpireBidInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    protocolConfig: TAccountMetas[0];
    task: TAccountMetas[1];
    bidBook: TAccountMetas[2];
    bid: TAccountMetas[3];
    bidderMarketState: TAccountMetas[4];
    bidder: TAccountMetas[5];
    /** and only receives lamports when the expired bid account is closed. */
    bidderAuthority: TAccountMetas[6];
    authority: TAccountMetas[7];
  };
  data: ExpireBidInstructionData;
};

export function parseExpireBidInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedExpireBidInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 8,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      protocolConfig: getNextAccount(),
      task: getNextAccount(),
      bidBook: getNextAccount(),
      bid: getNextAccount(),
      bidderMarketState: getNextAccount(),
      bidder: getNextAccount(),
      bidderAuthority: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getExpireBidInstructionDataDecoder().decode(instruction.data),
  };
}
