/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findModerationConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const REVOKE_MODERATION_ATTESTOR_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([192, 141, 245, 60, 48, 104, 165, 105]);

export function getRevokeModerationAttestorDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVOKE_MODERATION_ATTESTOR_DISCRIMINATOR,
  );
}

export type RevokeModerationAttestorInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountModerationConfig extends string | AccountMeta<string> = string,
  TAccountModerationAttestor extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountModerationConfig extends string
        ? ReadonlyAccount<TAccountModerationConfig>
        : TAccountModerationConfig,
      TAccountModerationAttestor extends string
        ? WritableAccount<TAccountModerationAttestor>
        : TAccountModerationAttestor,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type RevokeModerationAttestorInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RevokeModerationAttestorInstructionDataArgs = {};

export function getRevokeModerationAttestorInstructionDataEncoder(): FixedSizeEncoder<RevokeModerationAttestorInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REVOKE_MODERATION_ATTESTOR_DISCRIMINATOR,
    }),
  );
}

export function getRevokeModerationAttestorInstructionDataDecoder(): FixedSizeDecoder<RevokeModerationAttestorInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRevokeModerationAttestorInstructionDataCodec(): FixedSizeCodec<
  RevokeModerationAttestorInstructionDataArgs,
  RevokeModerationAttestorInstructionData
> {
  return combineCodec(
    getRevokeModerationAttestorInstructionDataEncoder(),
    getRevokeModerationAttestorInstructionDataDecoder(),
  );
}

export type RevokeModerationAttestorAsyncInput<
  TAccountModerationConfig extends string = string,
  TAccountModerationAttestor extends string = string,
  TAccountAuthority extends string = string,
> = {
  moderationConfig?: Address<TAccountModerationConfig>;
  /**
   * Roster entry to remove. Seeded by its own stored `attestor`, so the canonical PDA
   * is enforced; `close = authority` returns the rent to the moderation authority.
   */
  moderationAttestor: Address<TAccountModerationAttestor>;
  /** Must be the moderation authority that owns the moderation config. */
  authority: TransactionSigner<TAccountAuthority>;
};

export async function getRevokeModerationAttestorInstructionAsync<
  TAccountModerationConfig extends string,
  TAccountModerationAttestor extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RevokeModerationAttestorAsyncInput<
    TAccountModerationConfig,
    TAccountModerationAttestor,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RevokeModerationAttestorInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountModerationAttestor,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    moderationConfig: {
      value: input.moderationConfig ?? null,
      isWritable: false,
    },
    moderationAttestor: {
      value: input.moderationAttestor ?? null,
      isWritable: true,
    },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.moderationConfig.value) {
    accounts.moderationConfig.value = await findModerationConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("moderationConfig", accounts.moderationConfig),
      getAccountMeta("moderationAttestor", accounts.moderationAttestor),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getRevokeModerationAttestorInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokeModerationAttestorInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountModerationAttestor,
    TAccountAuthority
  >);
}

export type RevokeModerationAttestorInput<
  TAccountModerationConfig extends string = string,
  TAccountModerationAttestor extends string = string,
  TAccountAuthority extends string = string,
> = {
  moderationConfig: Address<TAccountModerationConfig>;
  /**
   * Roster entry to remove. Seeded by its own stored `attestor`, so the canonical PDA
   * is enforced; `close = authority` returns the rent to the moderation authority.
   */
  moderationAttestor: Address<TAccountModerationAttestor>;
  /** Must be the moderation authority that owns the moderation config. */
  authority: TransactionSigner<TAccountAuthority>;
};

export function getRevokeModerationAttestorInstruction<
  TAccountModerationConfig extends string,
  TAccountModerationAttestor extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: RevokeModerationAttestorInput<
    TAccountModerationConfig,
    TAccountModerationAttestor,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): RevokeModerationAttestorInstruction<
  TProgramAddress,
  TAccountModerationConfig,
  TAccountModerationAttestor,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    moderationConfig: {
      value: input.moderationConfig ?? null,
      isWritable: false,
    },
    moderationAttestor: {
      value: input.moderationAttestor ?? null,
      isWritable: true,
    },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("moderationConfig", accounts.moderationConfig),
      getAccountMeta("moderationAttestor", accounts.moderationAttestor),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getRevokeModerationAttestorInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokeModerationAttestorInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountModerationAttestor,
    TAccountAuthority
  >);
}

export type ParsedRevokeModerationAttestorInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    moderationConfig: TAccountMetas[0];
    /**
     * Roster entry to remove. Seeded by its own stored `attestor`, so the canonical PDA
     * is enforced; `close = authority` returns the rent to the moderation authority.
     */
    moderationAttestor: TAccountMetas[1];
    /** Must be the moderation authority that owns the moderation config. */
    authority: TAccountMetas[2];
  };
  data: RevokeModerationAttestorInstructionData;
};

export function parseRevokeModerationAttestorInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevokeModerationAttestorInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      moderationConfig: getNextAccount(),
      moderationAttestor: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getRevokeModerationAttestorInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
