/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findCompletionBondPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const RECLAIM_COMPLETION_BOND_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([199, 123, 80, 81, 92, 254, 10, 192]);

export function getReclaimCompletionBondDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(RECLAIM_COMPLETION_BOND_DISCRIMINATOR);
}

export type ReclaimCompletionBondInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountCompletionBond extends string | AccountMeta<string> = string,
  TAccountParty extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTask extends string
        ? ReadonlyAccount<TAccountTask>
        : TAccountTask,
      TAccountCompletionBond extends string
        ? WritableAccount<TAccountCompletionBond>
        : TAccountCompletionBond,
      TAccountParty extends string
        ? WritableAccount<TAccountParty>
        : TAccountParty,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ReclaimCompletionBondInstructionData = {
  discriminator: ReadonlyUint8Array;
  role: number;
};

export type ReclaimCompletionBondInstructionDataArgs = { role: number };

export function getReclaimCompletionBondInstructionDataEncoder(): FixedSizeEncoder<ReclaimCompletionBondInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["role", getU8Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: RECLAIM_COMPLETION_BOND_DISCRIMINATOR,
    }),
  );
}

export function getReclaimCompletionBondInstructionDataDecoder(): FixedSizeDecoder<ReclaimCompletionBondInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["role", getU8Decoder()],
  ]);
}

export function getReclaimCompletionBondInstructionDataCodec(): FixedSizeCodec<
  ReclaimCompletionBondInstructionDataArgs,
  ReclaimCompletionBondInstructionData
> {
  return combineCodec(
    getReclaimCompletionBondInstructionDataEncoder(),
    getReclaimCompletionBondInstructionDataDecoder(),
  );
}

export type ReclaimCompletionBondAsyncInput<
  TAccountTask extends string = string,
  TAccountCompletionBond extends string = string,
  TAccountParty extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  task: Address<TAccountTask>;
  /** validated by settle_completion_bond in the handler. */
  completionBond?: Address<TAccountCompletionBond>;
  party: Address<TAccountParty>;
  systemProgram?: Address<TAccountSystemProgram>;
  role: ReclaimCompletionBondInstructionDataArgs["role"];
};

export async function getReclaimCompletionBondInstructionAsync<
  TAccountTask extends string,
  TAccountCompletionBond extends string,
  TAccountParty extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: ReclaimCompletionBondAsyncInput<
    TAccountTask,
    TAccountCompletionBond,
    TAccountParty,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ReclaimCompletionBondInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountCompletionBond,
    TAccountParty,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: false },
    completionBond: { value: input.completionBond ?? null, isWritable: true },
    party: { value: input.party ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.completionBond.value) {
    accounts.completionBond.value = await findCompletionBondPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
      party: getAddressFromResolvedInstructionAccount(
        "party",
        accounts.party.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("completionBond", accounts.completionBond),
      getAccountMeta("party", accounts.party),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getReclaimCompletionBondInstructionDataEncoder().encode(
      args as ReclaimCompletionBondInstructionDataArgs,
    ),
    programAddress,
  } as ReclaimCompletionBondInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountCompletionBond,
    TAccountParty,
    TAccountSystemProgram
  >);
}

export type ReclaimCompletionBondInput<
  TAccountTask extends string = string,
  TAccountCompletionBond extends string = string,
  TAccountParty extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  task: Address<TAccountTask>;
  /** validated by settle_completion_bond in the handler. */
  completionBond: Address<TAccountCompletionBond>;
  party: Address<TAccountParty>;
  systemProgram?: Address<TAccountSystemProgram>;
  role: ReclaimCompletionBondInstructionDataArgs["role"];
};

export function getReclaimCompletionBondInstruction<
  TAccountTask extends string,
  TAccountCompletionBond extends string,
  TAccountParty extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: ReclaimCompletionBondInput<
    TAccountTask,
    TAccountCompletionBond,
    TAccountParty,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ReclaimCompletionBondInstruction<
  TProgramAddress,
  TAccountTask,
  TAccountCompletionBond,
  TAccountParty,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: false },
    completionBond: { value: input.completionBond ?? null, isWritable: true },
    party: { value: input.party ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("completionBond", accounts.completionBond),
      getAccountMeta("party", accounts.party),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getReclaimCompletionBondInstructionDataEncoder().encode(
      args as ReclaimCompletionBondInstructionDataArgs,
    ),
    programAddress,
  } as ReclaimCompletionBondInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountCompletionBond,
    TAccountParty,
    TAccountSystemProgram
  >);
}

export type ParsedReclaimCompletionBondInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    task: TAccountMetas[0];
    /** validated by settle_completion_bond in the handler. */
    completionBond: TAccountMetas[1];
    party: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ReclaimCompletionBondInstructionData;
};

export function parseReclaimCompletionBondInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedReclaimCompletionBondInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      task: getNextAccount(),
      completionBond: getNextAccount(),
      party: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getReclaimCompletionBondInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
