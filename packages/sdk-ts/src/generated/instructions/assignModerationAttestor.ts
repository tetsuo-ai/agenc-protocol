/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findModerationAttestorPda, findModerationConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const ASSIGN_MODERATION_ATTESTOR_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([236, 52, 254, 148, 54, 97, 130, 25]);

export function getAssignModerationAttestorDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(
    ASSIGN_MODERATION_ATTESTOR_DISCRIMINATOR,
  );
}

export type AssignModerationAttestorInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountModerationConfig extends string | AccountMeta<string> = string,
  TAccountModerationAttestor extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountModerationConfig extends string
        ? ReadonlyAccount<TAccountModerationConfig>
        : TAccountModerationConfig,
      TAccountModerationAttestor extends string
        ? WritableAccount<TAccountModerationAttestor>
        : TAccountModerationAttestor,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AssignModerationAttestorInstructionData = {
  discriminator: ReadonlyUint8Array;
  attestor: Address;
};

export type AssignModerationAttestorInstructionDataArgs = { attestor: Address };

export function getAssignModerationAttestorInstructionDataEncoder(): FixedSizeEncoder<AssignModerationAttestorInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["attestor", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: ASSIGN_MODERATION_ATTESTOR_DISCRIMINATOR,
    }),
  );
}

export function getAssignModerationAttestorInstructionDataDecoder(): FixedSizeDecoder<AssignModerationAttestorInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["attestor", getAddressDecoder()],
  ]);
}

export function getAssignModerationAttestorInstructionDataCodec(): FixedSizeCodec<
  AssignModerationAttestorInstructionDataArgs,
  AssignModerationAttestorInstructionData
> {
  return combineCodec(
    getAssignModerationAttestorInstructionDataEncoder(),
    getAssignModerationAttestorInstructionDataDecoder(),
  );
}

export type AssignModerationAttestorAsyncInput<
  TAccountModerationConfig extends string = string,
  TAccountModerationAttestor extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  moderationConfig?: Address<TAccountModerationConfig>;
  /** Roster entry for `attestor`. `init` ⇒ assigning an already-assigned wallet fails. */
  moderationAttestor?: Address<TAccountModerationAttestor>;
  /**
   * Must be the moderation authority that owns the moderation config. Unlike the
   * threshold-approved dispute-resolver roster, this moderation roster remains
   * authority-managed under its separate trust model.
   */
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  attestor: AssignModerationAttestorInstructionDataArgs["attestor"];
};

export async function getAssignModerationAttestorInstructionAsync<
  TAccountModerationConfig extends string,
  TAccountModerationAttestor extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: AssignModerationAttestorAsyncInput<
    TAccountModerationConfig,
    TAccountModerationAttestor,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AssignModerationAttestorInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountModerationAttestor,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    moderationConfig: {
      value: input.moderationConfig ?? null,
      isWritable: false,
    },
    moderationAttestor: {
      value: input.moderationAttestor ?? null,
      isWritable: true,
    },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.moderationConfig.value) {
    accounts.moderationConfig.value = await findModerationConfigPda();
  }
  if (!accounts.moderationAttestor.value) {
    accounts.moderationAttestor.value = await findModerationAttestorPda({
      attestor: getNonNullResolvedInstructionInput("attestor", args.attestor),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("moderationConfig", accounts.moderationConfig),
      getAccountMeta("moderationAttestor", accounts.moderationAttestor),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAssignModerationAttestorInstructionDataEncoder().encode(
      args as AssignModerationAttestorInstructionDataArgs,
    ),
    programAddress,
  } as AssignModerationAttestorInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountModerationAttestor,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type AssignModerationAttestorInput<
  TAccountModerationConfig extends string = string,
  TAccountModerationAttestor extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  moderationConfig: Address<TAccountModerationConfig>;
  /** Roster entry for `attestor`. `init` ⇒ assigning an already-assigned wallet fails. */
  moderationAttestor: Address<TAccountModerationAttestor>;
  /**
   * Must be the moderation authority that owns the moderation config. Unlike the
   * threshold-approved dispute-resolver roster, this moderation roster remains
   * authority-managed under its separate trust model.
   */
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  attestor: AssignModerationAttestorInstructionDataArgs["attestor"];
};

export function getAssignModerationAttestorInstruction<
  TAccountModerationConfig extends string,
  TAccountModerationAttestor extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: AssignModerationAttestorInput<
    TAccountModerationConfig,
    TAccountModerationAttestor,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AssignModerationAttestorInstruction<
  TProgramAddress,
  TAccountModerationConfig,
  TAccountModerationAttestor,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    moderationConfig: {
      value: input.moderationConfig ?? null,
      isWritable: false,
    },
    moderationAttestor: {
      value: input.moderationAttestor ?? null,
      isWritable: true,
    },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("moderationConfig", accounts.moderationConfig),
      getAccountMeta("moderationAttestor", accounts.moderationAttestor),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAssignModerationAttestorInstructionDataEncoder().encode(
      args as AssignModerationAttestorInstructionDataArgs,
    ),
    programAddress,
  } as AssignModerationAttestorInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountModerationAttestor,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedAssignModerationAttestorInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    moderationConfig: TAccountMetas[0];
    /** Roster entry for `attestor`. `init` ⇒ assigning an already-assigned wallet fails. */
    moderationAttestor: TAccountMetas[1];
    /**
     * Must be the moderation authority that owns the moderation config. Unlike the
     * threshold-approved dispute-resolver roster, this moderation roster remains
     * authority-managed under its separate trust model.
     */
    authority: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: AssignModerationAttestorInstructionData;
};

export function parseAssignModerationAttestorInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAssignModerationAttestorInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      moderationConfig: getNextAccount(),
      moderationAttestor: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAssignModerationAttestorInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
