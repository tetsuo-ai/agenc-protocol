/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findAuthorityVotePda,
  findProtocolConfigPda,
  findVoteDisputeVotePda,
} from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const VOTE_DISPUTE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  23, 190, 211, 170, 65, 223, 4, 243,
]);

export function getVoteDisputeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VOTE_DISPUTE_DISCRIMINATOR,
  );
}

export type VoteDisputeInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountDispute extends string | AccountMeta<string> = string,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountWorkerClaim extends string | AccountMeta<string> = string,
  TAccountDefendantAgent extends string | AccountMeta<string> = string,
  TAccountVote extends string | AccountMeta<string> = string,
  TAccountAuthorityVote extends string | AccountMeta<string> = string,
  TAccountArbiter extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDispute extends string
        ? WritableAccount<TAccountDispute>
        : TAccountDispute,
      TAccountTask extends string
        ? ReadonlyAccount<TAccountTask>
        : TAccountTask,
      TAccountWorkerClaim extends string
        ? ReadonlyAccount<TAccountWorkerClaim>
        : TAccountWorkerClaim,
      TAccountDefendantAgent extends string
        ? ReadonlyAccount<TAccountDefendantAgent>
        : TAccountDefendantAgent,
      TAccountVote extends string
        ? WritableAccount<TAccountVote>
        : TAccountVote,
      TAccountAuthorityVote extends string
        ? WritableAccount<TAccountAuthorityVote>
        : TAccountAuthorityVote,
      TAccountArbiter extends string
        ? WritableAccount<TAccountArbiter>
        : TAccountArbiter,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type VoteDisputeInstructionData = {
  discriminator: ReadonlyUint8Array;
  approve: boolean;
};

export type VoteDisputeInstructionDataArgs = { approve: boolean };

export function getVoteDisputeInstructionDataEncoder(): FixedSizeEncoder<VoteDisputeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["approve", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: VOTE_DISPUTE_DISCRIMINATOR }),
  );
}

export function getVoteDisputeInstructionDataDecoder(): FixedSizeDecoder<VoteDisputeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["approve", getBooleanDecoder()],
  ]);
}

export function getVoteDisputeInstructionDataCodec(): FixedSizeCodec<
  VoteDisputeInstructionDataArgs,
  VoteDisputeInstructionData
> {
  return combineCodec(
    getVoteDisputeInstructionDataEncoder(),
    getVoteDisputeInstructionDataDecoder(),
  );
}

export type VoteDisputeAsyncInput<
  TAccountDispute extends string = string,
  TAccountTask extends string = string,
  TAccountWorkerClaim extends string = string,
  TAccountDefendantAgent extends string = string,
  TAccountVote extends string = string,
  TAccountAuthorityVote extends string = string,
  TAccountArbiter extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  dispute: Address<TAccountDispute>;
  /** Task account for arbiter party validation (fix #461) */
  task: Address<TAccountTask>;
  /**
   * Optional: Worker's claim on the task (for arbiter party validation, fix #461)
   * If provided, validates arbiter is not the worker
   */
  workerClaim?: Address<TAccountWorkerClaim>;
  /**
   * Optional: Defendant's agent registration (for authority-level participant check, fix #824)
   * If provided, validates arbiter's authority is not the defendant worker's authority.
   * Must match the dispute's defendant field.
   */
  defendantAgent?: Address<TAccountDefendantAgent>;
  vote?: Address<TAccountVote>;
  /**
   * Authority-level vote tracking to prevent Sybil attacks (fix #101)
   * One authority can only vote once per dispute, regardless of how many agents they control
   */
  authorityVote?: Address<TAccountAuthorityVote>;
  arbiter: Address<TAccountArbiter>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  approve: VoteDisputeInstructionDataArgs["approve"];
};

export async function getVoteDisputeInstructionAsync<
  TAccountDispute extends string,
  TAccountTask extends string,
  TAccountWorkerClaim extends string,
  TAccountDefendantAgent extends string,
  TAccountVote extends string,
  TAccountAuthorityVote extends string,
  TAccountArbiter extends string,
  TAccountProtocolConfig extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: VoteDisputeAsyncInput<
    TAccountDispute,
    TAccountTask,
    TAccountWorkerClaim,
    TAccountDefendantAgent,
    TAccountVote,
    TAccountAuthorityVote,
    TAccountArbiter,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  VoteDisputeInstruction<
    TProgramAddress,
    TAccountDispute,
    TAccountTask,
    TAccountWorkerClaim,
    TAccountDefendantAgent,
    TAccountVote,
    TAccountAuthorityVote,
    TAccountArbiter,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    dispute: { value: input.dispute ?? null, isWritable: true },
    task: { value: input.task ?? null, isWritable: false },
    workerClaim: { value: input.workerClaim ?? null, isWritable: false },
    defendantAgent: { value: input.defendantAgent ?? null, isWritable: false },
    vote: { value: input.vote ?? null, isWritable: true },
    authorityVote: { value: input.authorityVote ?? null, isWritable: true },
    arbiter: { value: input.arbiter ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vote.value) {
    accounts.vote.value = await findVoteDisputeVotePda({
      dispute: getAddressFromResolvedInstructionAccount(
        "dispute",
        accounts.dispute.value,
      ),
      arbiter: getAddressFromResolvedInstructionAccount(
        "arbiter",
        accounts.arbiter.value,
      ),
    });
  }
  if (!accounts.authorityVote.value) {
    accounts.authorityVote.value = await findAuthorityVotePda({
      dispute: getAddressFromResolvedInstructionAccount(
        "dispute",
        accounts.dispute.value,
      ),
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value,
      ),
    });
  }
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("dispute", accounts.dispute),
      getAccountMeta("task", accounts.task),
      getAccountMeta("workerClaim", accounts.workerClaim),
      getAccountMeta("defendantAgent", accounts.defendantAgent),
      getAccountMeta("vote", accounts.vote),
      getAccountMeta("authorityVote", accounts.authorityVote),
      getAccountMeta("arbiter", accounts.arbiter),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getVoteDisputeInstructionDataEncoder().encode(
      args as VoteDisputeInstructionDataArgs,
    ),
    programAddress,
  } as VoteDisputeInstruction<
    TProgramAddress,
    TAccountDispute,
    TAccountTask,
    TAccountWorkerClaim,
    TAccountDefendantAgent,
    TAccountVote,
    TAccountAuthorityVote,
    TAccountArbiter,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type VoteDisputeInput<
  TAccountDispute extends string = string,
  TAccountTask extends string = string,
  TAccountWorkerClaim extends string = string,
  TAccountDefendantAgent extends string = string,
  TAccountVote extends string = string,
  TAccountAuthorityVote extends string = string,
  TAccountArbiter extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  dispute: Address<TAccountDispute>;
  /** Task account for arbiter party validation (fix #461) */
  task: Address<TAccountTask>;
  /**
   * Optional: Worker's claim on the task (for arbiter party validation, fix #461)
   * If provided, validates arbiter is not the worker
   */
  workerClaim?: Address<TAccountWorkerClaim>;
  /**
   * Optional: Defendant's agent registration (for authority-level participant check, fix #824)
   * If provided, validates arbiter's authority is not the defendant worker's authority.
   * Must match the dispute's defendant field.
   */
  defendantAgent?: Address<TAccountDefendantAgent>;
  vote: Address<TAccountVote>;
  /**
   * Authority-level vote tracking to prevent Sybil attacks (fix #101)
   * One authority can only vote once per dispute, regardless of how many agents they control
   */
  authorityVote: Address<TAccountAuthorityVote>;
  arbiter: Address<TAccountArbiter>;
  protocolConfig: Address<TAccountProtocolConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  approve: VoteDisputeInstructionDataArgs["approve"];
};

export function getVoteDisputeInstruction<
  TAccountDispute extends string,
  TAccountTask extends string,
  TAccountWorkerClaim extends string,
  TAccountDefendantAgent extends string,
  TAccountVote extends string,
  TAccountAuthorityVote extends string,
  TAccountArbiter extends string,
  TAccountProtocolConfig extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: VoteDisputeInput<
    TAccountDispute,
    TAccountTask,
    TAccountWorkerClaim,
    TAccountDefendantAgent,
    TAccountVote,
    TAccountAuthorityVote,
    TAccountArbiter,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): VoteDisputeInstruction<
  TProgramAddress,
  TAccountDispute,
  TAccountTask,
  TAccountWorkerClaim,
  TAccountDefendantAgent,
  TAccountVote,
  TAccountAuthorityVote,
  TAccountArbiter,
  TAccountProtocolConfig,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    dispute: { value: input.dispute ?? null, isWritable: true },
    task: { value: input.task ?? null, isWritable: false },
    workerClaim: { value: input.workerClaim ?? null, isWritable: false },
    defendantAgent: { value: input.defendantAgent ?? null, isWritable: false },
    vote: { value: input.vote ?? null, isWritable: true },
    authorityVote: { value: input.authorityVote ?? null, isWritable: true },
    arbiter: { value: input.arbiter ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("dispute", accounts.dispute),
      getAccountMeta("task", accounts.task),
      getAccountMeta("workerClaim", accounts.workerClaim),
      getAccountMeta("defendantAgent", accounts.defendantAgent),
      getAccountMeta("vote", accounts.vote),
      getAccountMeta("authorityVote", accounts.authorityVote),
      getAccountMeta("arbiter", accounts.arbiter),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getVoteDisputeInstructionDataEncoder().encode(
      args as VoteDisputeInstructionDataArgs,
    ),
    programAddress,
  } as VoteDisputeInstruction<
    TProgramAddress,
    TAccountDispute,
    TAccountTask,
    TAccountWorkerClaim,
    TAccountDefendantAgent,
    TAccountVote,
    TAccountAuthorityVote,
    TAccountArbiter,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedVoteDisputeInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    dispute: TAccountMetas[0];
    /** Task account for arbiter party validation (fix #461) */
    task: TAccountMetas[1];
    /**
     * Optional: Worker's claim on the task (for arbiter party validation, fix #461)
     * If provided, validates arbiter is not the worker
     */
    workerClaim?: TAccountMetas[2] | undefined;
    /**
     * Optional: Defendant's agent registration (for authority-level participant check, fix #824)
     * If provided, validates arbiter's authority is not the defendant worker's authority.
     * Must match the dispute's defendant field.
     */
    defendantAgent?: TAccountMetas[3] | undefined;
    vote: TAccountMetas[4];
    /**
     * Authority-level vote tracking to prevent Sybil attacks (fix #101)
     * One authority can only vote once per dispute, regardless of how many agents they control
     */
    authorityVote: TAccountMetas[5];
    arbiter: TAccountMetas[6];
    protocolConfig: TAccountMetas[7];
    authority: TAccountMetas[8];
    systemProgram: TAccountMetas[9];
  };
  data: VoteDisputeInstructionData;
};

export function parseVoteDisputeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedVoteDisputeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 10) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 10,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === AGENC_COORDINATION_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      dispute: getNextAccount(),
      task: getNextAccount(),
      workerClaim: getNextOptionalAccount(),
      defendantAgent: getNextOptionalAccount(),
      vote: getNextAccount(),
      authorityVote: getNextAccount(),
      arbiter: getNextAccount(),
      protocolConfig: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getVoteDisputeInstructionDataDecoder().decode(instruction.data),
  };
}
