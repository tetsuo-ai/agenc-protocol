/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAcceptTaskResultClaimPda, findProtocolConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const CLAIM_TASK_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  49, 222, 219, 238, 155, 68, 221, 136,
]);

export function getClaimTaskDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLAIM_TASK_DISCRIMINATOR);
}

export type ClaimTaskInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountClaim extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountWorker extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTask extends string
        ? WritableAccount<TAccountTask>
        : TAccountTask,
      TAccountClaim extends string
        ? WritableAccount<TAccountClaim>
        : TAccountClaim,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountWorker extends string
        ? WritableAccount<TAccountWorker>
        : TAccountWorker,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimTaskInstructionData = { discriminator: ReadonlyUint8Array };

export type ClaimTaskInstructionDataArgs = {};

export function getClaimTaskInstructionDataEncoder(): FixedSizeEncoder<ClaimTaskInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLAIM_TASK_DISCRIMINATOR }),
  );
}

export function getClaimTaskInstructionDataDecoder(): FixedSizeDecoder<ClaimTaskInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClaimTaskInstructionDataCodec(): FixedSizeCodec<
  ClaimTaskInstructionDataArgs,
  ClaimTaskInstructionData
> {
  return combineCodec(
    getClaimTaskInstructionDataEncoder(),
    getClaimTaskInstructionDataDecoder(),
  );
}

export type ClaimTaskAsyncInput<
  TAccountTask extends string = string,
  TAccountClaim extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountWorker extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  task: Address<TAccountTask>;
  claim?: Address<TAccountClaim>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  worker: Address<TAccountWorker>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getClaimTaskInstructionAsync<
  TAccountTask extends string,
  TAccountClaim extends string,
  TAccountProtocolConfig extends string,
  TAccountWorker extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: ClaimTaskAsyncInput<
    TAccountTask,
    TAccountClaim,
    TAccountProtocolConfig,
    TAccountWorker,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ClaimTaskInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountClaim,
    TAccountProtocolConfig,
    TAccountWorker,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: true },
    claim: { value: input.claim ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    worker: { value: input.worker ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.claim.value) {
    accounts.claim.value = await findAcceptTaskResultClaimPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
      worker: getAddressFromResolvedInstructionAccount(
        "worker",
        accounts.worker.value,
      ),
    });
  }
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("claim", accounts.claim),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("worker", accounts.worker),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getClaimTaskInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimTaskInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountClaim,
    TAccountProtocolConfig,
    TAccountWorker,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ClaimTaskInput<
  TAccountTask extends string = string,
  TAccountClaim extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountWorker extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  task: Address<TAccountTask>;
  claim: Address<TAccountClaim>;
  protocolConfig: Address<TAccountProtocolConfig>;
  worker: Address<TAccountWorker>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getClaimTaskInstruction<
  TAccountTask extends string,
  TAccountClaim extends string,
  TAccountProtocolConfig extends string,
  TAccountWorker extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: ClaimTaskInput<
    TAccountTask,
    TAccountClaim,
    TAccountProtocolConfig,
    TAccountWorker,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ClaimTaskInstruction<
  TProgramAddress,
  TAccountTask,
  TAccountClaim,
  TAccountProtocolConfig,
  TAccountWorker,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: true },
    claim: { value: input.claim ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    worker: { value: input.worker ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("claim", accounts.claim),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("worker", accounts.worker),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getClaimTaskInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimTaskInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountClaim,
    TAccountProtocolConfig,
    TAccountWorker,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedClaimTaskInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    task: TAccountMetas[0];
    claim: TAccountMetas[1];
    protocolConfig: TAccountMetas[2];
    worker: TAccountMetas[3];
    authority: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: ClaimTaskInstructionData;
};

export function parseClaimTaskInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClaimTaskInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      task: getNextAccount(),
      claim: getNextAccount(),
      protocolConfig: getNextAccount(),
      worker: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getClaimTaskInstructionDataDecoder().decode(instruction.data),
  };
}
