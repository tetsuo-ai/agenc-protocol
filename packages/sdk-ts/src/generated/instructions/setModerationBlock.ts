/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";

import {
  getBorshStringDecoder,
  getBorshStringEncoder,
} from "../codecs/borshString";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findModerationBlockPda, findProtocolConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const SET_MODERATION_BLOCK_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([212, 41, 81, 152, 230, 234, 217, 101]);

export function getSetModerationBlockDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_MODERATION_BLOCK_DISCRIMINATOR,
  );
}

export type SetModerationBlockInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountModerationBlock extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountModerationBlock extends string
        ? WritableAccount<TAccountModerationBlock>
        : TAccountModerationBlock,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SetModerationBlockInstructionData = {
  discriminator: ReadonlyUint8Array;
  contentHash: ReadonlyUint8Array;
  rationaleHash: ReadonlyUint8Array;
  rationaleUri: string;
};

export type SetModerationBlockInstructionDataArgs = {
  contentHash: ReadonlyUint8Array;
  rationaleHash: ReadonlyUint8Array;
  rationaleUri: string;
};

export function getSetModerationBlockInstructionDataEncoder(): Encoder<SetModerationBlockInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["contentHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["rationaleHash", fixEncoderSize(getBytesEncoder(), 32)],
      [
        "rationaleUri",
        addEncoderSizePrefix(getBorshStringEncoder(), getU32Encoder()),
      ],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_MODERATION_BLOCK_DISCRIMINATOR,
    }),
  );
}

export function getSetModerationBlockInstructionDataDecoder(): Decoder<SetModerationBlockInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["contentHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["rationaleHash", fixDecoderSize(getBytesDecoder(), 32)],
    [
      "rationaleUri",
      addDecoderSizePrefix(getBorshStringDecoder(), getU32Decoder()),
    ],
  ]);
}

export function getSetModerationBlockInstructionDataCodec(): Codec<
  SetModerationBlockInstructionDataArgs,
  SetModerationBlockInstructionData
> {
  return combineCodec(
    getSetModerationBlockInstructionDataEncoder(),
    getSetModerationBlockInstructionDataDecoder(),
  );
}

export type SetModerationBlockAsyncInput<
  TAccountProtocolConfig extends string = string,
  TAccountModerationBlock extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  protocolConfig?: Address<TAccountProtocolConfig>;
  /**
   * `init_if_needed`: a cleared block can be re-set (same PDA, audit trail intact)
   * and a live block's rationale can be updated.
   */
  moderationBlock?: Address<TAccountModerationBlock>;
  /**
   * Fee payer / tx assembler. Approval authority is the multisig threshold over
   * `remaining_accounts`, exactly like `update_protocol_fee`.
   */
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  contentHash: SetModerationBlockInstructionDataArgs["contentHash"];
  rationaleHash: SetModerationBlockInstructionDataArgs["rationaleHash"];
  rationaleUri: SetModerationBlockInstructionDataArgs["rationaleUri"];
};

export async function getSetModerationBlockInstructionAsync<
  TAccountProtocolConfig extends string,
  TAccountModerationBlock extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: SetModerationBlockAsyncInput<
    TAccountProtocolConfig,
    TAccountModerationBlock,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetModerationBlockInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountModerationBlock,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    moderationBlock: { value: input.moderationBlock ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.moderationBlock.value) {
    accounts.moderationBlock.value = await findModerationBlockPda({
      contentHash: getNonNullResolvedInstructionInput(
        "contentHash",
        args.contentHash,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("moderationBlock", accounts.moderationBlock),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSetModerationBlockInstructionDataEncoder().encode(
      args as SetModerationBlockInstructionDataArgs,
    ),
    programAddress,
  } as SetModerationBlockInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountModerationBlock,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type SetModerationBlockInput<
  TAccountProtocolConfig extends string = string,
  TAccountModerationBlock extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  protocolConfig: Address<TAccountProtocolConfig>;
  /**
   * `init_if_needed`: a cleared block can be re-set (same PDA, audit trail intact)
   * and a live block's rationale can be updated.
   */
  moderationBlock: Address<TAccountModerationBlock>;
  /**
   * Fee payer / tx assembler. Approval authority is the multisig threshold over
   * `remaining_accounts`, exactly like `update_protocol_fee`.
   */
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  contentHash: SetModerationBlockInstructionDataArgs["contentHash"];
  rationaleHash: SetModerationBlockInstructionDataArgs["rationaleHash"];
  rationaleUri: SetModerationBlockInstructionDataArgs["rationaleUri"];
};

export function getSetModerationBlockInstruction<
  TAccountProtocolConfig extends string,
  TAccountModerationBlock extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: SetModerationBlockInput<
    TAccountProtocolConfig,
    TAccountModerationBlock,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SetModerationBlockInstruction<
  TProgramAddress,
  TAccountProtocolConfig,
  TAccountModerationBlock,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    moderationBlock: { value: input.moderationBlock ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("moderationBlock", accounts.moderationBlock),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSetModerationBlockInstructionDataEncoder().encode(
      args as SetModerationBlockInstructionDataArgs,
    ),
    programAddress,
  } as SetModerationBlockInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountModerationBlock,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedSetModerationBlockInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    protocolConfig: TAccountMetas[0];
    /**
     * `init_if_needed`: a cleared block can be re-set (same PDA, audit trail intact)
     * and a live block's rationale can be updated.
     */
    moderationBlock: TAccountMetas[1];
    /**
     * Fee payer / tx assembler. Approval authority is the multisig threshold over
     * `remaining_accounts`, exactly like `update_protocol_fee`.
     */
    authority: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: SetModerationBlockInstructionData;
};

export function parseSetModerationBlockInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetModerationBlockInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      protocolConfig: getNextAccount(),
      moderationBlock: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSetModerationBlockInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
