/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProtocolConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const UNSUSPEND_AGENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [79, 75, 53, 57, 177, 142, 131, 149],
);

export function getUnsuspendAgentDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(UNSUSPEND_AGENT_DISCRIMINATOR);
}

export type UnsuspendAgentInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountAgent extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAgent extends string
        ? WritableAccount<TAccountAgent>
        : TAccountAgent,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UnsuspendAgentInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type UnsuspendAgentInstructionDataArgs = {};

export function getUnsuspendAgentInstructionDataEncoder(): FixedSizeEncoder<UnsuspendAgentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
    ]),
    (value) => ({ ...value, discriminator: UNSUSPEND_AGENT_DISCRIMINATOR }),
  );
}

export function getUnsuspendAgentInstructionDataDecoder(): FixedSizeDecoder<UnsuspendAgentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getUnsuspendAgentInstructionDataCodec(): FixedSizeCodec<
  UnsuspendAgentInstructionDataArgs,
  UnsuspendAgentInstructionData
> {
  return combineCodec(
    getUnsuspendAgentInstructionDataEncoder(),
    getUnsuspendAgentInstructionDataDecoder(),
  );
}

export type UnsuspendAgentAsyncInput<
  TAccountAgent extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountAuthority extends string = string,
> = {
  agent: Address<TAccountAgent>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  authority: TransactionSigner<TAccountAuthority>;
};

export async function getUnsuspendAgentInstructionAsync<
  TAccountAgent extends string,
  TAccountProtocolConfig extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: UnsuspendAgentAsyncInput<
    TAccountAgent,
    TAccountProtocolConfig,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UnsuspendAgentInstruction<
    TProgramAddress,
    TAccountAgent,
    TAccountProtocolConfig,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    agent: { value: input.agent ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("agent", accounts.agent),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUnsuspendAgentInstructionDataEncoder().encode({}),
    programAddress,
  } as UnsuspendAgentInstruction<
    TProgramAddress,
    TAccountAgent,
    TAccountProtocolConfig,
    TAccountAuthority
  >);
}

export type UnsuspendAgentInput<
  TAccountAgent extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountAuthority extends string = string,
> = {
  agent: Address<TAccountAgent>;
  protocolConfig: Address<TAccountProtocolConfig>;
  authority: TransactionSigner<TAccountAuthority>;
};

export function getUnsuspendAgentInstruction<
  TAccountAgent extends string,
  TAccountProtocolConfig extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: UnsuspendAgentInput<
    TAccountAgent,
    TAccountProtocolConfig,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): UnsuspendAgentInstruction<
  TProgramAddress,
  TAccountAgent,
  TAccountProtocolConfig,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    agent: { value: input.agent ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("agent", accounts.agent),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUnsuspendAgentInstructionDataEncoder().encode({}),
    programAddress,
  } as UnsuspendAgentInstruction<
    TProgramAddress,
    TAccountAgent,
    TAccountProtocolConfig,
    TAccountAuthority
  >);
}

export type ParsedUnsuspendAgentInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    agent: TAccountMetas[0];
    protocolConfig: TAccountMetas[1];
    authority: TAccountMetas[2];
  };
  data: UnsuspendAgentInstructionData;
};

export function parseUnsuspendAgentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUnsuspendAgentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      agent: getNextAccount(),
      protocolConfig: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getUnsuspendAgentInstructionDataDecoder().decode(instruction.data),
  };
}
