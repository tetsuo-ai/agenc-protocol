/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findPostPda, findProtocolConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const POST_TO_FEED_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  140, 84, 238, 165, 168, 159, 119, 128,
]);

export function getPostToFeedDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    POST_TO_FEED_DISCRIMINATOR,
  );
}

export type PostToFeedInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountPost extends string | AccountMeta<string> = string,
  TAccountAuthor extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPost extends string
        ? WritableAccount<TAccountPost>
        : TAccountPost,
      TAccountAuthor extends string
        ? ReadonlyAccount<TAccountAuthor>
        : TAccountAuthor,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PostToFeedInstructionData = {
  discriminator: ReadonlyUint8Array;
  contentHash: ReadonlyUint8Array;
  nonce: ReadonlyUint8Array;
  topic: ReadonlyUint8Array;
  parentPost: Option<Address>;
};

export type PostToFeedInstructionDataArgs = {
  contentHash: ReadonlyUint8Array;
  nonce: ReadonlyUint8Array;
  topic: ReadonlyUint8Array;
  parentPost: OptionOrNullable<Address>;
};

export function getPostToFeedInstructionDataEncoder(): Encoder<PostToFeedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["contentHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["nonce", fixEncoderSize(getBytesEncoder(), 32)],
      ["topic", fixEncoderSize(getBytesEncoder(), 32)],
      ["parentPost", getOptionEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: POST_TO_FEED_DISCRIMINATOR }),
  );
}

export function getPostToFeedInstructionDataDecoder(): Decoder<PostToFeedInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["contentHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["nonce", fixDecoderSize(getBytesDecoder(), 32)],
    ["topic", fixDecoderSize(getBytesDecoder(), 32)],
    ["parentPost", getOptionDecoder(getAddressDecoder())],
  ]);
}

export function getPostToFeedInstructionDataCodec(): Codec<
  PostToFeedInstructionDataArgs,
  PostToFeedInstructionData
> {
  return combineCodec(
    getPostToFeedInstructionDataEncoder(),
    getPostToFeedInstructionDataDecoder(),
  );
}

export type PostToFeedAsyncInput<
  TAccountPost extends string = string,
  TAccountAuthor extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  post?: Address<TAccountPost>;
  author: Address<TAccountAuthor>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  contentHash: PostToFeedInstructionDataArgs["contentHash"];
  nonce: PostToFeedInstructionDataArgs["nonce"];
  topic: PostToFeedInstructionDataArgs["topic"];
  parentPost: PostToFeedInstructionDataArgs["parentPost"];
};

export async function getPostToFeedInstructionAsync<
  TAccountPost extends string,
  TAccountAuthor extends string,
  TAccountProtocolConfig extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: PostToFeedAsyncInput<
    TAccountPost,
    TAccountAuthor,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PostToFeedInstruction<
    TProgramAddress,
    TAccountPost,
    TAccountAuthor,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    post: { value: input.post ?? null, isWritable: true },
    author: { value: input.author ?? null, isWritable: false },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.post.value) {
    accounts.post.value = await findPostPda({
      author: getAddressFromResolvedInstructionAccount(
        "author",
        accounts.author.value,
      ),
      nonce: getNonNullResolvedInstructionInput("nonce", args.nonce),
    });
  }
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("post", accounts.post),
      getAccountMeta("author", accounts.author),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getPostToFeedInstructionDataEncoder().encode(
      args as PostToFeedInstructionDataArgs,
    ),
    programAddress,
  } as PostToFeedInstruction<
    TProgramAddress,
    TAccountPost,
    TAccountAuthor,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type PostToFeedInput<
  TAccountPost extends string = string,
  TAccountAuthor extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  post: Address<TAccountPost>;
  author: Address<TAccountAuthor>;
  protocolConfig: Address<TAccountProtocolConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  contentHash: PostToFeedInstructionDataArgs["contentHash"];
  nonce: PostToFeedInstructionDataArgs["nonce"];
  topic: PostToFeedInstructionDataArgs["topic"];
  parentPost: PostToFeedInstructionDataArgs["parentPost"];
};

export function getPostToFeedInstruction<
  TAccountPost extends string,
  TAccountAuthor extends string,
  TAccountProtocolConfig extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: PostToFeedInput<
    TAccountPost,
    TAccountAuthor,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PostToFeedInstruction<
  TProgramAddress,
  TAccountPost,
  TAccountAuthor,
  TAccountProtocolConfig,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    post: { value: input.post ?? null, isWritable: true },
    author: { value: input.author ?? null, isWritable: false },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("post", accounts.post),
      getAccountMeta("author", accounts.author),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getPostToFeedInstructionDataEncoder().encode(
      args as PostToFeedInstructionDataArgs,
    ),
    programAddress,
  } as PostToFeedInstruction<
    TProgramAddress,
    TAccountPost,
    TAccountAuthor,
    TAccountProtocolConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedPostToFeedInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    post: TAccountMetas[0];
    author: TAccountMetas[1];
    protocolConfig: TAccountMetas[2];
    authority: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: PostToFeedInstructionData;
};

export function parsePostToFeedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPostToFeedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      post: getNextAccount(),
      author: getNextAccount(),
      protocolConfig: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getPostToFeedInstructionDataDecoder().decode(instruction.data),
  };
}
