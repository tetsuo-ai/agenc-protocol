/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBytesDecoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findModerationConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const MODERATION_HEARTBEAT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([215, 182, 250, 219, 71, 134, 198, 0]);

export function getModerationHeartbeatDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(MODERATION_HEARTBEAT_DISCRIMINATOR);
}

export type ModerationHeartbeatInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountModerationConfig extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountModerationConfig extends string
        ? WritableAccount<TAccountModerationConfig>
        : TAccountModerationConfig,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type ModerationHeartbeatInstructionData = {
  discriminator: ReadonlyUint8Array;
  newWindowSecs: Option<number>;
};

export type ModerationHeartbeatInstructionDataArgs = {
  newWindowSecs: OptionOrNullable<number>;
};

export function getModerationHeartbeatInstructionDataEncoder(): Encoder<ModerationHeartbeatInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["newWindowSecs", getOptionEncoder(getU32Encoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: MODERATION_HEARTBEAT_DISCRIMINATOR,
    }),
  );
}

export function getModerationHeartbeatInstructionDataDecoder(): Decoder<ModerationHeartbeatInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newWindowSecs", getOptionDecoder(getU32Decoder())],
  ]);
}

export function getModerationHeartbeatInstructionDataCodec(): Codec<
  ModerationHeartbeatInstructionDataArgs,
  ModerationHeartbeatInstructionData
> {
  return combineCodec(
    getModerationHeartbeatInstructionDataEncoder(),
    getModerationHeartbeatInstructionDataDecoder(),
  );
}

export type ModerationHeartbeatAsyncInput<
  TAccountModerationConfig extends string = string,
  TAccountAuthority extends string = string,
> = {
  moderationConfig?: Address<TAccountModerationConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  newWindowSecs: ModerationHeartbeatInstructionDataArgs["newWindowSecs"];
};

export async function getModerationHeartbeatInstructionAsync<
  TAccountModerationConfig extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: ModerationHeartbeatAsyncInput<
    TAccountModerationConfig,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ModerationHeartbeatInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    moderationConfig: {
      value: input.moderationConfig ?? null,
      isWritable: true,
    },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.moderationConfig.value) {
    accounts.moderationConfig.value = await findModerationConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("moderationConfig", accounts.moderationConfig),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getModerationHeartbeatInstructionDataEncoder().encode(
      args as ModerationHeartbeatInstructionDataArgs,
    ),
    programAddress,
  } as ModerationHeartbeatInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountAuthority
  >);
}

export type ModerationHeartbeatInput<
  TAccountModerationConfig extends string = string,
  TAccountAuthority extends string = string,
> = {
  moderationConfig: Address<TAccountModerationConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  newWindowSecs: ModerationHeartbeatInstructionDataArgs["newWindowSecs"];
};

export function getModerationHeartbeatInstruction<
  TAccountModerationConfig extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: ModerationHeartbeatInput<TAccountModerationConfig, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): ModerationHeartbeatInstruction<
  TProgramAddress,
  TAccountModerationConfig,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    moderationConfig: {
      value: input.moderationConfig ?? null,
      isWritable: true,
    },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("moderationConfig", accounts.moderationConfig),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getModerationHeartbeatInstructionDataEncoder().encode(
      args as ModerationHeartbeatInstructionDataArgs,
    ),
    programAddress,
  } as ModerationHeartbeatInstruction<
    TProgramAddress,
    TAccountModerationConfig,
    TAccountAuthority
  >);
}

export type ParsedModerationHeartbeatInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    moderationConfig: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: ModerationHeartbeatInstructionData;
};

export function parseModerationHeartbeatInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedModerationHeartbeatInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      moderationConfig: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getModerationHeartbeatInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
