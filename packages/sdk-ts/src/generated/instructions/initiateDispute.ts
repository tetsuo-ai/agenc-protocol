/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findAuthorityRateLimitPda,
  findDisputePda,
  findInitiatorClaimPda,
  findProtocolConfigPda,
} from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const INITIATE_DISPUTE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([128, 242, 160, 23, 44, 61, 171, 37]);

export function getInitiateDisputeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIATE_DISPUTE_DISCRIMINATOR,
  );
}

export type InitiateDisputeInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountDispute extends string | AccountMeta<string> = string,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountAgent extends string | AccountMeta<string> = string,
  TAccountAuthorityRateLimit extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountInitiatorClaim extends string | AccountMeta<string> = string,
  TAccountWorkerAgent extends string | AccountMeta<string> = string,
  TAccountWorkerClaim extends string | AccountMeta<string> = string,
  TAccountTaskSubmission extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDispute extends string
        ? WritableAccount<TAccountDispute>
        : TAccountDispute,
      TAccountTask extends string
        ? WritableAccount<TAccountTask>
        : TAccountTask,
      TAccountAgent extends string
        ? WritableAccount<TAccountAgent>
        : TAccountAgent,
      TAccountAuthorityRateLimit extends string
        ? WritableAccount<TAccountAuthorityRateLimit>
        : TAccountAuthorityRateLimit,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountInitiatorClaim extends string
        ? ReadonlyAccount<TAccountInitiatorClaim>
        : TAccountInitiatorClaim,
      TAccountWorkerAgent extends string
        ? WritableAccount<TAccountWorkerAgent>
        : TAccountWorkerAgent,
      TAccountWorkerClaim extends string
        ? ReadonlyAccount<TAccountWorkerClaim>
        : TAccountWorkerClaim,
      TAccountTaskSubmission extends string
        ? ReadonlyAccount<TAccountTaskSubmission>
        : TAccountTaskSubmission,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitiateDisputeInstructionData = {
  discriminator: ReadonlyUint8Array;
  disputeId: ReadonlyUint8Array;
  taskId: ReadonlyUint8Array;
  evidenceHash: ReadonlyUint8Array;
  resolutionType: number;
  evidence: string;
};

export type InitiateDisputeInstructionDataArgs = {
  disputeId: ReadonlyUint8Array;
  taskId: ReadonlyUint8Array;
  evidenceHash: ReadonlyUint8Array;
  resolutionType: number;
  evidence: string;
};

export function getInitiateDisputeInstructionDataEncoder(): Encoder<InitiateDisputeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["disputeId", fixEncoderSize(getBytesEncoder(), 32)],
      ["taskId", fixEncoderSize(getBytesEncoder(), 32)],
      ["evidenceHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["resolutionType", getU8Encoder()],
      ["evidence", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: INITIATE_DISPUTE_DISCRIMINATOR }),
  );
}

export function getInitiateDisputeInstructionDataDecoder(): Decoder<InitiateDisputeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["disputeId", fixDecoderSize(getBytesDecoder(), 32)],
    ["taskId", fixDecoderSize(getBytesDecoder(), 32)],
    ["evidenceHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["resolutionType", getU8Decoder()],
    ["evidence", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getInitiateDisputeInstructionDataCodec(): Codec<
  InitiateDisputeInstructionDataArgs,
  InitiateDisputeInstructionData
> {
  return combineCodec(
    getInitiateDisputeInstructionDataEncoder(),
    getInitiateDisputeInstructionDataDecoder(),
  );
}

export type InitiateDisputeAsyncInput<
  TAccountDispute extends string = string,
  TAccountTask extends string = string,
  TAccountAgent extends string = string,
  TAccountAuthorityRateLimit extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountInitiatorClaim extends string = string,
  TAccountWorkerAgent extends string = string,
  TAccountWorkerClaim extends string = string,
  TAccountTaskSubmission extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  dispute?: Address<TAccountDispute>;
  task: Address<TAccountTask>;
  agent: Address<TAccountAgent>;
  /** Wallet-scoped task/dispute rate limit state shared across all agents */
  authorityRateLimit?: Address<TAccountAuthorityRateLimit>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  /** Optional: Initiator's claim if they are a worker (not the creator) */
  initiatorClaim?: Address<TAccountInitiatorClaim>;
  /** Optional: Worker agent to be disputed (required when initiator is task creator) */
  workerAgent?: Address<TAccountWorkerAgent>;
  /** Optional: Worker's claim (required when worker_agent is provided) */
  workerClaim?: Address<TAccountWorkerClaim>;
  /** Optional durable submission record used once the claim slot has been released. */
  taskSubmission?: Address<TAccountTaskSubmission>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  disputeId: InitiateDisputeInstructionDataArgs["disputeId"];
  taskId: InitiateDisputeInstructionDataArgs["taskId"];
  evidenceHash: InitiateDisputeInstructionDataArgs["evidenceHash"];
  resolutionType: InitiateDisputeInstructionDataArgs["resolutionType"];
  evidence: InitiateDisputeInstructionDataArgs["evidence"];
};

export async function getInitiateDisputeInstructionAsync<
  TAccountDispute extends string,
  TAccountTask extends string,
  TAccountAgent extends string,
  TAccountAuthorityRateLimit extends string,
  TAccountProtocolConfig extends string,
  TAccountInitiatorClaim extends string,
  TAccountWorkerAgent extends string,
  TAccountWorkerClaim extends string,
  TAccountTaskSubmission extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: InitiateDisputeAsyncInput<
    TAccountDispute,
    TAccountTask,
    TAccountAgent,
    TAccountAuthorityRateLimit,
    TAccountProtocolConfig,
    TAccountInitiatorClaim,
    TAccountWorkerAgent,
    TAccountWorkerClaim,
    TAccountTaskSubmission,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitiateDisputeInstruction<
    TProgramAddress,
    TAccountDispute,
    TAccountTask,
    TAccountAgent,
    TAccountAuthorityRateLimit,
    TAccountProtocolConfig,
    TAccountInitiatorClaim,
    TAccountWorkerAgent,
    TAccountWorkerClaim,
    TAccountTaskSubmission,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    dispute: { value: input.dispute ?? null, isWritable: true },
    task: { value: input.task ?? null, isWritable: true },
    agent: { value: input.agent ?? null, isWritable: true },
    authorityRateLimit: {
      value: input.authorityRateLimit ?? null,
      isWritable: true,
    },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    initiatorClaim: { value: input.initiatorClaim ?? null, isWritable: false },
    workerAgent: { value: input.workerAgent ?? null, isWritable: true },
    workerClaim: { value: input.workerClaim ?? null, isWritable: false },
    taskSubmission: { value: input.taskSubmission ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.dispute.value) {
    accounts.dispute.value = await findDisputePda({
      disputeId: getNonNullResolvedInstructionInput(
        "disputeId",
        args.disputeId,
      ),
    });
  }
  if (!accounts.authorityRateLimit.value) {
    accounts.authorityRateLimit.value = await findAuthorityRateLimitPda({
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value,
      ),
    });
  }
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.initiatorClaim.value) {
    accounts.initiatorClaim.value = await findInitiatorClaimPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
      agent: getAddressFromResolvedInstructionAccount(
        "agent",
        accounts.agent.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("dispute", accounts.dispute),
      getAccountMeta("task", accounts.task),
      getAccountMeta("agent", accounts.agent),
      getAccountMeta("authorityRateLimit", accounts.authorityRateLimit),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("initiatorClaim", accounts.initiatorClaim),
      getAccountMeta("workerAgent", accounts.workerAgent),
      getAccountMeta("workerClaim", accounts.workerClaim),
      getAccountMeta("taskSubmission", accounts.taskSubmission),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitiateDisputeInstructionDataEncoder().encode(
      args as InitiateDisputeInstructionDataArgs,
    ),
    programAddress,
  } as InitiateDisputeInstruction<
    TProgramAddress,
    TAccountDispute,
    TAccountTask,
    TAccountAgent,
    TAccountAuthorityRateLimit,
    TAccountProtocolConfig,
    TAccountInitiatorClaim,
    TAccountWorkerAgent,
    TAccountWorkerClaim,
    TAccountTaskSubmission,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type InitiateDisputeInput<
  TAccountDispute extends string = string,
  TAccountTask extends string = string,
  TAccountAgent extends string = string,
  TAccountAuthorityRateLimit extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountInitiatorClaim extends string = string,
  TAccountWorkerAgent extends string = string,
  TAccountWorkerClaim extends string = string,
  TAccountTaskSubmission extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  dispute: Address<TAccountDispute>;
  task: Address<TAccountTask>;
  agent: Address<TAccountAgent>;
  /** Wallet-scoped task/dispute rate limit state shared across all agents */
  authorityRateLimit: Address<TAccountAuthorityRateLimit>;
  protocolConfig: Address<TAccountProtocolConfig>;
  /** Optional: Initiator's claim if they are a worker (not the creator) */
  initiatorClaim?: Address<TAccountInitiatorClaim>;
  /** Optional: Worker agent to be disputed (required when initiator is task creator) */
  workerAgent?: Address<TAccountWorkerAgent>;
  /** Optional: Worker's claim (required when worker_agent is provided) */
  workerClaim?: Address<TAccountWorkerClaim>;
  /** Optional durable submission record used once the claim slot has been released. */
  taskSubmission?: Address<TAccountTaskSubmission>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  disputeId: InitiateDisputeInstructionDataArgs["disputeId"];
  taskId: InitiateDisputeInstructionDataArgs["taskId"];
  evidenceHash: InitiateDisputeInstructionDataArgs["evidenceHash"];
  resolutionType: InitiateDisputeInstructionDataArgs["resolutionType"];
  evidence: InitiateDisputeInstructionDataArgs["evidence"];
};

export function getInitiateDisputeInstruction<
  TAccountDispute extends string,
  TAccountTask extends string,
  TAccountAgent extends string,
  TAccountAuthorityRateLimit extends string,
  TAccountProtocolConfig extends string,
  TAccountInitiatorClaim extends string,
  TAccountWorkerAgent extends string,
  TAccountWorkerClaim extends string,
  TAccountTaskSubmission extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: InitiateDisputeInput<
    TAccountDispute,
    TAccountTask,
    TAccountAgent,
    TAccountAuthorityRateLimit,
    TAccountProtocolConfig,
    TAccountInitiatorClaim,
    TAccountWorkerAgent,
    TAccountWorkerClaim,
    TAccountTaskSubmission,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitiateDisputeInstruction<
  TProgramAddress,
  TAccountDispute,
  TAccountTask,
  TAccountAgent,
  TAccountAuthorityRateLimit,
  TAccountProtocolConfig,
  TAccountInitiatorClaim,
  TAccountWorkerAgent,
  TAccountWorkerClaim,
  TAccountTaskSubmission,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    dispute: { value: input.dispute ?? null, isWritable: true },
    task: { value: input.task ?? null, isWritable: true },
    agent: { value: input.agent ?? null, isWritable: true },
    authorityRateLimit: {
      value: input.authorityRateLimit ?? null,
      isWritable: true,
    },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    initiatorClaim: { value: input.initiatorClaim ?? null, isWritable: false },
    workerAgent: { value: input.workerAgent ?? null, isWritable: true },
    workerClaim: { value: input.workerClaim ?? null, isWritable: false },
    taskSubmission: { value: input.taskSubmission ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("dispute", accounts.dispute),
      getAccountMeta("task", accounts.task),
      getAccountMeta("agent", accounts.agent),
      getAccountMeta("authorityRateLimit", accounts.authorityRateLimit),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("initiatorClaim", accounts.initiatorClaim),
      getAccountMeta("workerAgent", accounts.workerAgent),
      getAccountMeta("workerClaim", accounts.workerClaim),
      getAccountMeta("taskSubmission", accounts.taskSubmission),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitiateDisputeInstructionDataEncoder().encode(
      args as InitiateDisputeInstructionDataArgs,
    ),
    programAddress,
  } as InitiateDisputeInstruction<
    TProgramAddress,
    TAccountDispute,
    TAccountTask,
    TAccountAgent,
    TAccountAuthorityRateLimit,
    TAccountProtocolConfig,
    TAccountInitiatorClaim,
    TAccountWorkerAgent,
    TAccountWorkerClaim,
    TAccountTaskSubmission,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedInitiateDisputeInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    dispute: TAccountMetas[0];
    task: TAccountMetas[1];
    agent: TAccountMetas[2];
    /** Wallet-scoped task/dispute rate limit state shared across all agents */
    authorityRateLimit: TAccountMetas[3];
    protocolConfig: TAccountMetas[4];
    /** Optional: Initiator's claim if they are a worker (not the creator) */
    initiatorClaim?: TAccountMetas[5] | undefined;
    /** Optional: Worker agent to be disputed (required when initiator is task creator) */
    workerAgent?: TAccountMetas[6] | undefined;
    /** Optional: Worker's claim (required when worker_agent is provided) */
    workerClaim?: TAccountMetas[7] | undefined;
    /** Optional durable submission record used once the claim slot has been released. */
    taskSubmission?: TAccountMetas[8] | undefined;
    authority: TAccountMetas[9];
    systemProgram: TAccountMetas[10];
  };
  data: InitiateDisputeInstructionData;
};

export function parseInitiateDisputeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitiateDisputeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 11) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 11,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === AGENC_COORDINATION_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      dispute: getNextAccount(),
      task: getNextAccount(),
      agent: getNextAccount(),
      authorityRateLimit: getNextAccount(),
      protocolConfig: getNextAccount(),
      initiatorClaim: getNextOptionalAccount(),
      workerAgent: getNextOptionalAccount(),
      workerClaim: getNextOptionalAccount(),
      taskSubmission: getNextOptionalAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitiateDisputeInstructionDataDecoder().decode(instruction.data),
  };
}
