/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProtocolConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_TREASURY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [60, 16, 243, 66, 96, 59, 254, 131],
);

export function getUpdateTreasuryDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(UPDATE_TREASURY_DISCRIMINATOR);
}

export type UpdateTreasuryInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountNewTreasury extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProtocolConfig extends string
        ? WritableAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountNewTreasury extends string
        ? ReadonlySignerAccount<TAccountNewTreasury> &
            AccountSignerMeta<TAccountNewTreasury>
        : TAccountNewTreasury,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateTreasuryInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type UpdateTreasuryInstructionDataArgs = {};

export function getUpdateTreasuryInstructionDataEncoder(): FixedSizeEncoder<UpdateTreasuryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_TREASURY_DISCRIMINATOR }),
  );
}

export function getUpdateTreasuryInstructionDataDecoder(): FixedSizeDecoder<UpdateTreasuryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getUpdateTreasuryInstructionDataCodec(): FixedSizeCodec<
  UpdateTreasuryInstructionDataArgs,
  UpdateTreasuryInstructionData
> {
  return combineCodec(
    getUpdateTreasuryInstructionDataEncoder(),
    getUpdateTreasuryInstructionDataDecoder(),
  );
}

export type UpdateTreasuryAsyncInput<
  TAccountProtocolConfig extends string = string,
  TAccountNewTreasury extends string = string,
  TAccountAuthority extends string = string,
> = {
  protocolConfig?: Address<TAccountProtocolConfig>;
  /**
   * Must be a system-owned signer. Production clients request the new custody
   * key's signature through this typed account in generated account metas.
   */
  newTreasury: TransactionSigner<TAccountNewTreasury>;
  authority: TransactionSigner<TAccountAuthority>;
};

export async function getUpdateTreasuryInstructionAsync<
  TAccountProtocolConfig extends string,
  TAccountNewTreasury extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: UpdateTreasuryAsyncInput<
    TAccountProtocolConfig,
    TAccountNewTreasury,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateTreasuryInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountNewTreasury,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: true },
    newTreasury: { value: input.newTreasury ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("newTreasury", accounts.newTreasury),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUpdateTreasuryInstructionDataEncoder().encode({}),
    programAddress,
  } as UpdateTreasuryInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountNewTreasury,
    TAccountAuthority
  >);
}

export type UpdateTreasuryInput<
  TAccountProtocolConfig extends string = string,
  TAccountNewTreasury extends string = string,
  TAccountAuthority extends string = string,
> = {
  protocolConfig: Address<TAccountProtocolConfig>;
  /**
   * Must be a system-owned signer. Production clients request the new custody
   * key's signature through this typed account in generated account metas.
   */
  newTreasury: TransactionSigner<TAccountNewTreasury>;
  authority: TransactionSigner<TAccountAuthority>;
};

export function getUpdateTreasuryInstruction<
  TAccountProtocolConfig extends string,
  TAccountNewTreasury extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: UpdateTreasuryInput<
    TAccountProtocolConfig,
    TAccountNewTreasury,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateTreasuryInstruction<
  TProgramAddress,
  TAccountProtocolConfig,
  TAccountNewTreasury,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: true },
    newTreasury: { value: input.newTreasury ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("newTreasury", accounts.newTreasury),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUpdateTreasuryInstructionDataEncoder().encode({}),
    programAddress,
  } as UpdateTreasuryInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountNewTreasury,
    TAccountAuthority
  >);
}

export type ParsedUpdateTreasuryInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    protocolConfig: TAccountMetas[0];
    /**
     * Must be a system-owned signer. Production clients request the new custody
     * key's signature through this typed account in generated account metas.
     */
    newTreasury: TAccountMetas[1];
    authority: TAccountMetas[2];
  };
  data: UpdateTreasuryInstructionData;
};

export function parseUpdateTreasuryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateTreasuryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      protocolConfig: getNextAccount(),
      newTreasury: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getUpdateTreasuryInstructionDataDecoder().decode(instruction.data),
  };
}
