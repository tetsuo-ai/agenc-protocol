/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  getBytesDecoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findBidBookPda, findBidderMarketStatePda, findBidPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const CANCEL_BID_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  40, 243, 190, 217, 208, 253, 86, 206,
]);

export function getCancelBidDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(CANCEL_BID_DISCRIMINATOR);
}

export type CancelBidInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountBidBook extends string | AccountMeta<string> = string,
  TAccountBid extends string | AccountMeta<string> = string,
  TAccountBidderMarketState extends string | AccountMeta<string> = string,
  TAccountBidder extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTask extends string
        ? ReadonlyAccount<TAccountTask>
        : TAccountTask,
      TAccountBidBook extends string
        ? WritableAccount<TAccountBidBook>
        : TAccountBidBook,
      TAccountBid extends string ? WritableAccount<TAccountBid> : TAccountBid,
      TAccountBidderMarketState extends string
        ? WritableAccount<TAccountBidderMarketState>
        : TAccountBidderMarketState,
      TAccountBidder extends string
        ? ReadonlyAccount<TAccountBidder>
        : TAccountBidder,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type CancelBidInstructionData = { discriminator: ReadonlyUint8Array };

export type CancelBidInstructionDataArgs = {};

export function getCancelBidInstructionDataEncoder(): FixedSizeEncoder<CancelBidInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
    ]),
    (value) => ({ ...value, discriminator: CANCEL_BID_DISCRIMINATOR }),
  );
}

export function getCancelBidInstructionDataDecoder(): FixedSizeDecoder<CancelBidInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelBidInstructionDataCodec(): FixedSizeCodec<
  CancelBidInstructionDataArgs,
  CancelBidInstructionData
> {
  return combineCodec(
    getCancelBidInstructionDataEncoder(),
    getCancelBidInstructionDataDecoder(),
  );
}

export type CancelBidAsyncInput<
  TAccountTask extends string = string,
  TAccountBidBook extends string = string,
  TAccountBid extends string = string,
  TAccountBidderMarketState extends string = string,
  TAccountBidder extends string = string,
  TAccountAuthority extends string = string,
> = {
  task: Address<TAccountTask>;
  bidBook?: Address<TAccountBidBook>;
  bid?: Address<TAccountBid>;
  bidderMarketState?: Address<TAccountBidderMarketState>;
  bidder: Address<TAccountBidder>;
  authority: TransactionSigner<TAccountAuthority>;
};

export async function getCancelBidInstructionAsync<
  TAccountTask extends string,
  TAccountBidBook extends string,
  TAccountBid extends string,
  TAccountBidderMarketState extends string,
  TAccountBidder extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: CancelBidAsyncInput<
    TAccountTask,
    TAccountBidBook,
    TAccountBid,
    TAccountBidderMarketState,
    TAccountBidder,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CancelBidInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountBidBook,
    TAccountBid,
    TAccountBidderMarketState,
    TAccountBidder,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: false },
    bidBook: { value: input.bidBook ?? null, isWritable: true },
    bid: { value: input.bid ?? null, isWritable: true },
    bidderMarketState: {
      value: input.bidderMarketState ?? null,
      isWritable: true,
    },
    bidder: { value: input.bidder ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.bidBook.value) {
    accounts.bidBook.value = await findBidBookPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }
  if (!accounts.bid.value) {
    accounts.bid.value = await findBidPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
      bidder: getAddressFromResolvedInstructionAccount(
        "bidder",
        accounts.bidder.value,
      ),
    });
  }
  if (!accounts.bidderMarketState.value) {
    accounts.bidderMarketState.value = await findBidderMarketStatePda({
      bidder: getAddressFromResolvedInstructionAccount(
        "bidder",
        accounts.bidder.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("bidBook", accounts.bidBook),
      getAccountMeta("bid", accounts.bid),
      getAccountMeta("bidderMarketState", accounts.bidderMarketState),
      getAccountMeta("bidder", accounts.bidder),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getCancelBidInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelBidInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountBidBook,
    TAccountBid,
    TAccountBidderMarketState,
    TAccountBidder,
    TAccountAuthority
  >);
}

export type CancelBidInput<
  TAccountTask extends string = string,
  TAccountBidBook extends string = string,
  TAccountBid extends string = string,
  TAccountBidderMarketState extends string = string,
  TAccountBidder extends string = string,
  TAccountAuthority extends string = string,
> = {
  task: Address<TAccountTask>;
  bidBook: Address<TAccountBidBook>;
  bid: Address<TAccountBid>;
  bidderMarketState: Address<TAccountBidderMarketState>;
  bidder: Address<TAccountBidder>;
  authority: TransactionSigner<TAccountAuthority>;
};

export function getCancelBidInstruction<
  TAccountTask extends string,
  TAccountBidBook extends string,
  TAccountBid extends string,
  TAccountBidderMarketState extends string,
  TAccountBidder extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: CancelBidInput<
    TAccountTask,
    TAccountBidBook,
    TAccountBid,
    TAccountBidderMarketState,
    TAccountBidder,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): CancelBidInstruction<
  TProgramAddress,
  TAccountTask,
  TAccountBidBook,
  TAccountBid,
  TAccountBidderMarketState,
  TAccountBidder,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: false },
    bidBook: { value: input.bidBook ?? null, isWritable: true },
    bid: { value: input.bid ?? null, isWritable: true },
    bidderMarketState: {
      value: input.bidderMarketState ?? null,
      isWritable: true,
    },
    bidder: { value: input.bidder ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("bidBook", accounts.bidBook),
      getAccountMeta("bid", accounts.bid),
      getAccountMeta("bidderMarketState", accounts.bidderMarketState),
      getAccountMeta("bidder", accounts.bidder),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getCancelBidInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelBidInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountBidBook,
    TAccountBid,
    TAccountBidderMarketState,
    TAccountBidder,
    TAccountAuthority
  >);
}

export type ParsedCancelBidInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    task: TAccountMetas[0];
    bidBook: TAccountMetas[1];
    bid: TAccountMetas[2];
    bidderMarketState: TAccountMetas[3];
    bidder: TAccountMetas[4];
    authority: TAccountMetas[5];
  };
  data: CancelBidInstructionData;
};

export function parseCancelBidInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelBidInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      task: getNextAccount(),
      bidBook: getNextAccount(),
      bid: getNextAccount(),
      bidderMarketState: getNextAccount(),
      bidder: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getCancelBidInstructionDataDecoder().decode(instruction.data),
  };
}
