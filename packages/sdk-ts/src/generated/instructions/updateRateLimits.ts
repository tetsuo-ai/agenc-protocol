/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProtocolConfigPda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_RATE_LIMITS_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([247, 36, 121, 254, 22, 16, 226, 1]);

export function getUpdateRateLimitsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_RATE_LIMITS_DISCRIMINATOR,
  );
}

export type UpdateRateLimitsInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProtocolConfig extends string
        ? WritableAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateRateLimitsInstructionData = {
  discriminator: ReadonlyUint8Array;
  taskCreationCooldown: bigint;
  maxTasksPer24h: number;
  disputeInitiationCooldown: bigint;
  maxDisputesPer24h: number;
  minStakeForDispute: bigint;
};

export type UpdateRateLimitsInstructionDataArgs = {
  taskCreationCooldown: number | bigint;
  maxTasksPer24h: number;
  disputeInitiationCooldown: number | bigint;
  maxDisputesPer24h: number;
  minStakeForDispute: number | bigint;
};

export function getUpdateRateLimitsInstructionDataEncoder(): FixedSizeEncoder<UpdateRateLimitsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["taskCreationCooldown", getI64Encoder()],
      ["maxTasksPer24h", getU8Encoder()],
      ["disputeInitiationCooldown", getI64Encoder()],
      ["maxDisputesPer24h", getU8Encoder()],
      ["minStakeForDispute", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_RATE_LIMITS_DISCRIMINATOR }),
  );
}

export function getUpdateRateLimitsInstructionDataDecoder(): FixedSizeDecoder<UpdateRateLimitsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["taskCreationCooldown", getI64Decoder()],
    ["maxTasksPer24h", getU8Decoder()],
    ["disputeInitiationCooldown", getI64Decoder()],
    ["maxDisputesPer24h", getU8Decoder()],
    ["minStakeForDispute", getU64Decoder()],
  ]);
}

export function getUpdateRateLimitsInstructionDataCodec(): FixedSizeCodec<
  UpdateRateLimitsInstructionDataArgs,
  UpdateRateLimitsInstructionData
> {
  return combineCodec(
    getUpdateRateLimitsInstructionDataEncoder(),
    getUpdateRateLimitsInstructionDataDecoder(),
  );
}

export type UpdateRateLimitsAsyncInput<
  TAccountProtocolConfig extends string = string,
  TAccountAuthority extends string = string,
> = {
  protocolConfig?: Address<TAccountProtocolConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  taskCreationCooldown: UpdateRateLimitsInstructionDataArgs["taskCreationCooldown"];
  maxTasksPer24h: UpdateRateLimitsInstructionDataArgs["maxTasksPer24h"];
  disputeInitiationCooldown: UpdateRateLimitsInstructionDataArgs["disputeInitiationCooldown"];
  maxDisputesPer24h: UpdateRateLimitsInstructionDataArgs["maxDisputesPer24h"];
  minStakeForDispute: UpdateRateLimitsInstructionDataArgs["minStakeForDispute"];
};

export async function getUpdateRateLimitsInstructionAsync<
  TAccountProtocolConfig extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: UpdateRateLimitsAsyncInput<TAccountProtocolConfig, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateRateLimitsInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUpdateRateLimitsInstructionDataEncoder().encode(
      args as UpdateRateLimitsInstructionDataArgs,
    ),
    programAddress,
  } as UpdateRateLimitsInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountAuthority
  >);
}

export type UpdateRateLimitsInput<
  TAccountProtocolConfig extends string = string,
  TAccountAuthority extends string = string,
> = {
  protocolConfig: Address<TAccountProtocolConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  taskCreationCooldown: UpdateRateLimitsInstructionDataArgs["taskCreationCooldown"];
  maxTasksPer24h: UpdateRateLimitsInstructionDataArgs["maxTasksPer24h"];
  disputeInitiationCooldown: UpdateRateLimitsInstructionDataArgs["disputeInitiationCooldown"];
  maxDisputesPer24h: UpdateRateLimitsInstructionDataArgs["maxDisputesPer24h"];
  minStakeForDispute: UpdateRateLimitsInstructionDataArgs["minStakeForDispute"];
};

export function getUpdateRateLimitsInstruction<
  TAccountProtocolConfig extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: UpdateRateLimitsInput<TAccountProtocolConfig, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): UpdateRateLimitsInstruction<
  TProgramAddress,
  TAccountProtocolConfig,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUpdateRateLimitsInstructionDataEncoder().encode(
      args as UpdateRateLimitsInstructionDataArgs,
    ),
    programAddress,
  } as UpdateRateLimitsInstruction<
    TProgramAddress,
    TAccountProtocolConfig,
    TAccountAuthority
  >);
}

export type ParsedUpdateRateLimitsInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    protocolConfig: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: UpdateRateLimitsInstructionData;
};

export function parseUpdateRateLimitsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateRateLimitsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { protocolConfig: getNextAccount(), authority: getNextAccount() },
    data: getUpdateRateLimitsInstructionDataDecoder().decode(instruction.data),
  };
}
