/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProtocolConfigPda, findReputationStakePda } from "../pdas";
import { AGENC_COORDINATION_PROGRAM_ADDRESS } from "../programs";

export const STAKE_REPUTATION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([104, 250, 157, 87, 16, 190, 180, 238]);

export function getStakeReputationDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    STAKE_REPUTATION_DISCRIMINATOR,
  );
}

export type StakeReputationInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAgent extends string | AccountMeta<string> = string,
  TAccountReputationStake extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAgent extends string
        ? ReadonlyAccount<TAccountAgent>
        : TAccountAgent,
      TAccountReputationStake extends string
        ? WritableAccount<TAccountReputationStake>
        : TAccountReputationStake,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type StakeReputationInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type StakeReputationInstructionDataArgs = { amount: number | bigint };

export function getStakeReputationInstructionDataEncoder(): FixedSizeEncoder<StakeReputationInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: STAKE_REPUTATION_DISCRIMINATOR }),
  );
}

export function getStakeReputationInstructionDataDecoder(): FixedSizeDecoder<StakeReputationInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getStakeReputationInstructionDataCodec(): FixedSizeCodec<
  StakeReputationInstructionDataArgs,
  StakeReputationInstructionData
> {
  return combineCodec(
    getStakeReputationInstructionDataEncoder(),
    getStakeReputationInstructionDataDecoder(),
  );
}

export type StakeReputationAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAgent extends string = string,
  TAccountReputationStake extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  agent: Address<TAccountAgent>;
  reputationStake?: Address<TAccountReputationStake>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: StakeReputationInstructionDataArgs["amount"];
};

export async function getStakeReputationInstructionAsync<
  TAccountAuthority extends string,
  TAccountAgent extends string,
  TAccountReputationStake extends string,
  TAccountProtocolConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: StakeReputationAsyncInput<
    TAccountAuthority,
    TAccountAgent,
    TAccountReputationStake,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  StakeReputationInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAgent,
    TAccountReputationStake,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    agent: { value: input.agent ?? null, isWritable: false },
    reputationStake: { value: input.reputationStake ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.reputationStake.value) {
    accounts.reputationStake.value = await findReputationStakePda({
      agent: getAddressFromResolvedInstructionAccount(
        "agent",
        accounts.agent.value,
      ),
    });
  }
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("agent", accounts.agent),
      getAccountMeta("reputationStake", accounts.reputationStake),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getStakeReputationInstructionDataEncoder().encode(
      args as StakeReputationInstructionDataArgs,
    ),
    programAddress,
  } as StakeReputationInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAgent,
    TAccountReputationStake,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >);
}

export type StakeReputationInput<
  TAccountAuthority extends string = string,
  TAccountAgent extends string = string,
  TAccountReputationStake extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  agent: Address<TAccountAgent>;
  reputationStake: Address<TAccountReputationStake>;
  protocolConfig: Address<TAccountProtocolConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: StakeReputationInstructionDataArgs["amount"];
};

export function getStakeReputationInstruction<
  TAccountAuthority extends string,
  TAccountAgent extends string,
  TAccountReputationStake extends string,
  TAccountProtocolConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
>(
  input: StakeReputationInput<
    TAccountAuthority,
    TAccountAgent,
    TAccountReputationStake,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): StakeReputationInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAgent,
  TAccountReputationStake,
  TAccountProtocolConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENC_COORDINATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    agent: { value: input.agent ?? null, isWritable: false },
    reputationStake: { value: input.reputationStake ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("agent", accounts.agent),
      getAccountMeta("reputationStake", accounts.reputationStake),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getStakeReputationInstructionDataEncoder().encode(
      args as StakeReputationInstructionDataArgs,
    ),
    programAddress,
  } as StakeReputationInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAgent,
    TAccountReputationStake,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >);
}

export type ParsedStakeReputationInstruction<
  TProgram extends string = typeof AGENC_COORDINATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    agent: TAccountMetas[1];
    reputationStake: TAccountMetas[2];
    protocolConfig: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: StakeReputationInstructionData;
};

export function parseStakeReputationInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedStakeReputationInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      agent: getNextAccount(),
      reputationStake: getNextAccount(),
      protocolConfig: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getStakeReputationInstructionDataDecoder().decode(instruction.data),
  };
}
