/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  getAddressDecoder,
  getI64Decoder,
  getOptionDecoder,
  getStructDecoder,
  getU16Decoder,
  getU64Decoder,
  type Address,
  type Decoder,
  type Option,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `ServiceListingHired` (sha256("event:ServiceListingHired")[0..8]). */
export const SERVICE_LISTING_HIRED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  225, 53, 144, 0, 179, 119, 21, 125,
]);

/**
 * Emitted when a buyer hires a provider from a standing service listing,
 * minting a one-shot task. Links the source listing to the new task.
 */
export type ServiceListingHiredEventData = {
  listing: Address;
  task: Address;
  providerAgent: Address;
  buyer: Address;
  price: bigint;
  totalHires: bigint;
  /** Concurrent open hires after this one (capacity counter). */
  openJobs: number;
  /**
   * WP-A1 roster telemetry: the registered `ModerationAttestor` wallet whose
   * listing attestation unlocked this hire, or `None` when the listing-moderation
   * was authored by the global `ModerationConfig.moderation_authority` (or when
   * moderation is disabled).
   */
  moderationAttestor: Option<Address>;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link ServiceListingHiredEventData} event payload — the bytes
 * AFTER the 8-byte `SERVICE_LISTING_HIRED_EVENT_DISCRIMINATOR`.
 */
export function getServiceListingHiredEventDecoder(): Decoder<ServiceListingHiredEventData> {
  return getStructDecoder([
    ["listing", getAddressDecoder()],
    ["task", getAddressDecoder()],
    ["providerAgent", getAddressDecoder()],
    ["buyer", getAddressDecoder()],
    ["price", getU64Decoder()],
    ["totalHires", getU64Decoder()],
    ["openJobs", getU16Decoder()],
    ["moderationAttestor", getOptionDecoder(getAddressDecoder())],
    ["timestamp", getI64Decoder()],
  ]);
}
