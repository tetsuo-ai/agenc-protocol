/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  fixDecoderSize,
  getAddressDecoder,
  getBytesDecoder,
  getI64Decoder,
  getStructDecoder,
  getU8Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `TaskClaimed` (sha256("event:TaskClaimed")[0..8]). */
export const TASK_CLAIMED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  208, 90, 243, 116, 80, 15, 228, 202,
]);

/** Emitted when an agent claims a task */
export type TaskClaimedEventData = {
  taskId: ReadonlyUint8Array;
  worker: Address;
  currentWorkers: number;
  maxWorkers: number;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link TaskClaimedEventData} event payload — the bytes
 * AFTER the 8-byte `TASK_CLAIMED_EVENT_DISCRIMINATOR`.
 */
export function getTaskClaimedEventDecoder(): Decoder<TaskClaimedEventData> {
  return getStructDecoder([
    ["taskId", fixDecoderSize(getBytesDecoder(), 32)],
    ["worker", getAddressDecoder()],
    ["currentWorkers", getU8Decoder()],
    ["maxWorkers", getU8Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
