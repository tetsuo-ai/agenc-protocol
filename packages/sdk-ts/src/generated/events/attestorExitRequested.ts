/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  getAddressDecoder,
  getI64Decoder,
  getStructDecoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `AttestorExitRequested` (sha256("event:AttestorExitRequested")[0..8]). */
export const ATTESTOR_EXIT_REQUESTED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  24, 232, 80, 8, 224, 107, 213, 86,
]);

/**
 * Emitted when an attestor starts its two-step exit (P1.2). From this moment the
 * attestor is rejected at the record and consumption gates; the bond refunds at
 * finalize after the cooldown.
 */
export type AttestorExitRequestedEventData = {
  attestor: Address;
  exitAt: bigint;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link AttestorExitRequestedEventData} event payload — the bytes
 * AFTER the 8-byte `ATTESTOR_EXIT_REQUESTED_EVENT_DISCRIMINATOR`.
 */
export function getAttestorExitRequestedEventDecoder(): Decoder<AttestorExitRequestedEventData> {
  return getStructDecoder([
    ["attestor", getAddressDecoder()],
    ["exitAt", getI64Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
