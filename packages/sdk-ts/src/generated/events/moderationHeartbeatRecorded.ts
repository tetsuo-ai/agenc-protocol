/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  getAddressDecoder,
  getI64Decoder,
  getStructDecoder,
  getU32Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `ModerationHeartbeatRecorded` (sha256("event:ModerationHeartbeatRecorded")[0..8]). */
export const MODERATION_HEARTBEAT_RECORDED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  25, 255, 220, 114, 183, 234, 227, 204,
]);

/**
 * Emitted on every `moderation_heartbeat` (P1.3 liveness deadman). Surfaces and
 * indexers can watch this to display gate-armed status; silence past the window
 * relaxes the consumption gates (docs/MODERATION_LIVENESS.md).
 */
export type ModerationHeartbeatRecordedEventData = {
  /** The signer that heartbeated (config authority or moderation authority). */
  by: Address;
  /**
   * The EFFECTIVE liveness window after this call (seconds; the default is
   * substituted when the stored value is 0).
   */
  windowSecs: number;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link ModerationHeartbeatRecordedEventData} event payload — the bytes
 * AFTER the 8-byte `MODERATION_HEARTBEAT_RECORDED_EVENT_DISCRIMINATOR`.
 */
export function getModerationHeartbeatRecordedEventDecoder(): Decoder<ModerationHeartbeatRecordedEventData> {
  return getStructDecoder([
    ["by", getAddressDecoder()],
    ["windowSecs", getU32Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
