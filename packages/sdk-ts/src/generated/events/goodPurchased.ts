/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  fixDecoderSize,
  getAddressDecoder,
  getBytesDecoder,
  getI64Decoder,
  getStructDecoder,
  getU64Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `GoodPurchased` (sha256("event:GoodPurchased")[0..8]). */
export const GOOD_PURCHASED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  232, 189, 14, 234, 45, 50, 89, 68,
]);

/**
 * Emitted on every unit sold — the indexer-facing provenance signal.
 * Carries the full fee split so the protocol cut is auditable per sale.
 */
export type GoodPurchasedEventData = {
  listing: Address;
  /** Buyer WALLET (bare signer). */
  buyer: Address;
  /** Seller's agent PDA. */
  seller: Address;
  /** This unit's receipt serial (== `sold_count` before the increment). */
  serial: bigint;
  /** Snapshot of the listing's metadata hash at sale time. */
  metadataHash: ReadonlyUint8Array;
  pricePaid: bigint;
  protocolFee: bigint;
  operatorFee: bigint;
  /** Units still available after this sale (`total_supply - sold_count`). */
  remainingSupply: bigint;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link GoodPurchasedEventData} event payload — the bytes
 * AFTER the 8-byte `GOOD_PURCHASED_EVENT_DISCRIMINATOR`.
 */
export function getGoodPurchasedEventDecoder(): Decoder<GoodPurchasedEventData> {
  return getStructDecoder([
    ["listing", getAddressDecoder()],
    ["buyer", getAddressDecoder()],
    ["seller", getAddressDecoder()],
    ["serial", getU64Decoder()],
    ["metadataHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["pricePaid", getU64Decoder()],
    ["protocolFee", getU64Decoder()],
    ["operatorFee", getU64Decoder()],
    ["remainingSupply", getU64Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
