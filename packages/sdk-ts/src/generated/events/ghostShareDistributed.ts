/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  getAddressDecoder,
  getStructDecoder,
  getU64Decoder,
  getU8Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `GhostShareDistributed` (sha256("event:GhostShareDistributed")[0..8]). */
export const GHOST_SHARE_DISTRIBUTED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  196, 143, 138, 26, 168, 29, 111, 98,
]);

/**
 * Emitted by each `distribute_ghost_share` crank: an unjudged contest
 * (schema-1 Competitive) submission was paid its equal slice of the remaining
 * escrow worker pool after the selection window elapsed (`ghost_at`). The paid
 * submission + claim are closed to the worker in the same instruction, so a
 * submission can be ghost-paid at most once.
 */
export type GhostShareDistributedEventData = {
  /** The contest task being ghost-split. */
  task: Address;
  /** The paid submitter's `AgentRegistration` PDA. */
  workerAgent: Address;
  /** Lamports paid to the worker for this slice (net of the fee legs). */
  lamports: bigint;
  /**
   * Live submissions REMAINING after this slice (0 = the contest is fully
   * distributed and the task is Completed).
   */
  remaining: number;
};

/**
 * Gets the decoder for the {@link GhostShareDistributedEventData} event payload — the bytes
 * AFTER the 8-byte `GHOST_SHARE_DISTRIBUTED_EVENT_DISCRIMINATOR`.
 */
export function getGhostShareDistributedEventDecoder(): Decoder<GhostShareDistributedEventData> {
  return getStructDecoder([
    ["task", getAddressDecoder()],
    ["workerAgent", getAddressDecoder()],
    ["lamports", getU64Decoder()],
    ["remaining", getU8Decoder()],
  ]);
}
