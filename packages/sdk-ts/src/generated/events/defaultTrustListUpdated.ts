/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  addDecoderSizePrefix,
  fixDecoderSize,
  getAddressDecoder,
  getBytesDecoder,
  getI64Decoder,
  getStructDecoder,
  getU32Decoder,
  getU64Decoder,
  getUtf8Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `DefaultTrustListUpdated` (sha256("event:DefaultTrustListUpdated")[0..8]). */
export const DEFAULT_TRUST_LIST_UPDATED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  92, 58, 14, 174, 134, 120, 43, 80,
]);

/**
 * Emitted when the multisig updates the on-chain default trusted-attestor list
 * pointer (P1.2 §5.1). `version` is monotonic; `timestamp` doubles as the deadman.
 */
export type DefaultTrustListUpdatedEventData = {
  listHash: ReadonlyUint8Array;
  listUri: string;
  version: bigint;
  updatedBy: Address;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link DefaultTrustListUpdatedEventData} event payload — the bytes
 * AFTER the 8-byte `DEFAULT_TRUST_LIST_UPDATED_EVENT_DISCRIMINATOR`.
 */
export function getDefaultTrustListUpdatedEventDecoder(): Decoder<DefaultTrustListUpdatedEventData> {
  return getStructDecoder([
    ["listHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["listUri", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["version", getU64Decoder()],
    ["updatedBy", getAddressDecoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
