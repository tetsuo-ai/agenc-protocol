/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  fixDecoderSize,
  getAddressDecoder,
  getBytesDecoder,
  getI64Decoder,
  getStructDecoder,
  getU16Decoder,
  getU64Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `OperatorFeePaid` (sha256("event:OperatorFeePaid")[0..8]). */
export const OPERATOR_FEE_PAID_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  206, 102, 195, 120, 182, 242, 156, 32,
]);

/**
 * Emitted when an operator (embedding-site) fee leg is paid out of a settlement
 * (spec §4 3-way split). Only emitted when the operator leg is non-zero.
 */
export type OperatorFeePaidEventData = {
  taskId: ReadonlyUint8Array;
  operator: Address;
  amount: bigint;
  operatorFeeBps: number;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link OperatorFeePaidEventData} event payload — the bytes
 * AFTER the 8-byte `OPERATOR_FEE_PAID_EVENT_DISCRIMINATOR`.
 */
export function getOperatorFeePaidEventDecoder(): Decoder<OperatorFeePaidEventData> {
  return getStructDecoder([
    ["taskId", fixDecoderSize(getBytesDecoder(), 32)],
    ["operator", getAddressDecoder()],
    ["amount", getU64Decoder()],
    ["operatorFeeBps", getU16Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
