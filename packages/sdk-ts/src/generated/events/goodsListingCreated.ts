/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  fixDecoderSize,
  getAddressDecoder,
  getBytesDecoder,
  getI64Decoder,
  getOptionDecoder,
  getStructDecoder,
  getU16Decoder,
  getU64Decoder,
  type Address,
  type Decoder,
  type Option,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `GoodsListingCreated` (sha256("event:GoodsListingCreated")[0..8]). */
export const GOODS_LISTING_CREATED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  140, 71, 233, 54, 133, 41, 64, 90,
]);

/** Emitted when an agent lists a finite good for sale. */
export type GoodsListingCreatedEventData = {
  listing: Address;
  /** Seller's agent PDA. */
  seller: Address;
  goodId: ReadonlyUint8Array;
  name: ReadonlyUint8Array;
  metadataHash: ReadonlyUint8Array;
  price: bigint;
  priceMint: Option<Address>;
  totalSupply: bigint;
  /** Operator payee (`Pubkey::default()` = no operator leg). */
  operator: Address;
  operatorFeeBps: number;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link GoodsListingCreatedEventData} event payload — the bytes
 * AFTER the 8-byte `GOODS_LISTING_CREATED_EVENT_DISCRIMINATOR`.
 */
export function getGoodsListingCreatedEventDecoder(): Decoder<GoodsListingCreatedEventData> {
  return getStructDecoder([
    ["listing", getAddressDecoder()],
    ["seller", getAddressDecoder()],
    ["goodId", fixDecoderSize(getBytesDecoder(), 32)],
    ["name", fixDecoderSize(getBytesDecoder(), 32)],
    ["metadataHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["price", getU64Decoder()],
    ["priceMint", getOptionDecoder(getAddressDecoder())],
    ["totalSupply", getU64Decoder()],
    ["operator", getAddressDecoder()],
    ["operatorFeeBps", getU16Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
