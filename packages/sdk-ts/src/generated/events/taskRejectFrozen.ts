/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  fixDecoderSize,
  getAddressDecoder,
  getBytesDecoder,
  getI64Decoder,
  getStructDecoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `TaskRejectFrozen` (sha256("event:TaskRejectFrozen")[0..8]). */
export const TASK_REJECT_FROZEN_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  67, 18, 32, 203, 147, 38, 42, 59,
]);

/** Emitted when a submission is terminally rejected and frozen for review. */
export type TaskRejectFrozenEventData = {
  task: Address;
  claim: Address;
  worker: Address;
  rejectionHash: ReadonlyUint8Array;
  reviewDeadlineAt: bigint;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link TaskRejectFrozenEventData} event payload — the bytes
 * AFTER the 8-byte `TASK_REJECT_FROZEN_EVENT_DISCRIMINATOR`.
 */
export function getTaskRejectFrozenEventDecoder(): Decoder<TaskRejectFrozenEventData> {
  return getStructDecoder([
    ["task", getAddressDecoder()],
    ["claim", getAddressDecoder()],
    ["worker", getAddressDecoder()],
    ["rejectionHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["reviewDeadlineAt", getI64Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
