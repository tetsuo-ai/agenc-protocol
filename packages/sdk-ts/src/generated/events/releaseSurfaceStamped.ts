/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  fixDecoderSize,
  getAddressDecoder,
  getBytesDecoder,
  getI64Decoder,
  getStructDecoder,
  getU16Decoder,
  getU64Decoder,
  getU8Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `ReleaseSurfaceStamped` (sha256("event:ReleaseSurfaceStamped")[0..8]). */
export const RELEASE_SURFACE_STAMPED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  116, 104, 144, 233, 104, 150, 184, 17,
]);

/**
 * Emitted only after the atomic release-boundary instruction has locked and
 * verified the reviewed executable metadata, IDL, singleton policy, and
 * upgrade-custody images in the same transaction as the surface stamp.
 */
export type ReleaseSurfaceStampedEventData = {
  authority: Address;
  surfaceRevision: number;
  disabledTaskTypeMask: number;
  programDataSlot: bigint;
  /** Full pre-stamp ProtocolConfig account-data hash. */
  protocolConfigHash: ReadonlyUint8Array;
  bidConfigHash: ReadonlyUint8Array;
  moderationConfigHash: ReadonlyUint8Array;
  idlAccountHash: ReadonlyUint8Array;
  custodyAccountHash: ReadonlyUint8Array;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link ReleaseSurfaceStampedEventData} event payload — the bytes
 * AFTER the 8-byte `RELEASE_SURFACE_STAMPED_EVENT_DISCRIMINATOR`.
 */
export function getReleaseSurfaceStampedEventDecoder(): Decoder<ReleaseSurfaceStampedEventData> {
  return getStructDecoder([
    ["authority", getAddressDecoder()],
    ["surfaceRevision", getU16Decoder()],
    ["disabledTaskTypeMask", getU8Decoder()],
    ["programDataSlot", getU64Decoder()],
    ["protocolConfigHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["bidConfigHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["moderationConfigHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["idlAccountHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["custodyAccountHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["timestamp", getI64Decoder()],
  ]);
}
