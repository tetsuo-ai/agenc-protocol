/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  fixDecoderSize,
  getAddressDecoder,
  getBytesDecoder,
  getI64Decoder,
  getStructDecoder,
  getU8Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `DisputePeerClaimSettled` (sha256("event:DisputePeerClaimSettled")[0..8]). */
export const DISPUTE_PEER_CLAIM_SETTLED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  112, 40, 20, 152, 129, 236, 218, 127,
]);

/**
 * Emitted when the permissionless chunked-settlement crank sweeps one
 * deferred collaborative peer claim after a dispute ruling. When
 * `peers_remaining` reaches zero the dispute has transitioned to its
 * recorded terminal status.
 */
export type DisputePeerClaimSettledEventData = {
  disputeId: ReadonlyUint8Array;
  taskId: ReadonlyUint8Array;
  worker: Address;
  peersRemaining: number;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link DisputePeerClaimSettledEventData} event payload — the bytes
 * AFTER the 8-byte `DISPUTE_PEER_CLAIM_SETTLED_EVENT_DISCRIMINATOR`.
 */
export function getDisputePeerClaimSettledEventDecoder(): Decoder<DisputePeerClaimSettledEventData> {
  return getStructDecoder([
    ["disputeId", fixDecoderSize(getBytesDecoder(), 32)],
    ["taskId", fixDecoderSize(getBytesDecoder(), 32)],
    ["worker", getAddressDecoder()],
    ["peersRemaining", getU8Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
