/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  getAddressDecoder,
  getI64Decoder,
  getStructDecoder,
  getU64Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getTrackRecordCounterDecoder,
  type TrackRecordCounter,
} from "../types";

/** 8-byte Anchor event discriminator for `AgentTrackRecordUpdated` (sha256("event:AgentTrackRecordUpdated")[0..8]). */
export const AGENT_TRACK_RECORD_UPDATED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  145, 32, 166, 83, 3, 162, 237, 142,
]);

/**
 * Emitted whenever a track-record counter on `AgentStats` is incremented (P6.6).
 * `new_value` is the post-increment counter so indexers can build a slash/outcome
 * history from the event stream alone (feeds the SDK `getAgentTrackRecord`).
 */
export type AgentTrackRecordUpdatedEventData = {
  /** The `AgentRegistration` PDA whose track record changed. */
  agent: Address;
  /** The `AgentStats` PDA that was written. */
  agentStats: Address;
  /** Which counter was incremented. */
  counter: TrackRecordCounter;
  /** The counter's value AFTER this increment. */
  newValue: bigint;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link AgentTrackRecordUpdatedEventData} event payload — the bytes
 * AFTER the 8-byte `AGENT_TRACK_RECORD_UPDATED_EVENT_DISCRIMINATOR`.
 */
export function getAgentTrackRecordUpdatedEventDecoder(): Decoder<AgentTrackRecordUpdatedEventData> {
  return getStructDecoder([
    ["agent", getAddressDecoder()],
    ["agentStats", getAddressDecoder()],
    ["counter", getTrackRecordCounterDecoder()],
    ["newValue", getU64Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
