/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  getAddressDecoder,
  getI64Decoder,
  getStructDecoder,
  getU64Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `ContestDepositForfeited` (sha256("event:ContestDepositForfeited")[0..8]). */
export const CONTEST_DEPOSIT_FORFEITED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  30, 223, 26, 66, 255, 92, 100, 233,
]);

/**
 * Emitted when a contest entry deposit is FORFEITED to the protocol treasury on
 * a no-show exit (`expire_claim` with a provably-absent submission PDA, or
 * `reclaim_terminal_claim`). Workers who submitted are always refunded in full
 * (their claim closes with all lamports — deposit included — to them).
 */
export type ContestDepositForfeitedEventData = {
  task: Address;
  claim: Address;
  /** The no-show worker's `AgentRegistration` PDA. */
  workerAgent: Address;
  /** Lamports forfeited to the treasury (the surplus above the claim's rent). */
  amount: bigint;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link ContestDepositForfeitedEventData} event payload — the bytes
 * AFTER the 8-byte `CONTEST_DEPOSIT_FORFEITED_EVENT_DISCRIMINATOR`.
 */
export function getContestDepositForfeitedEventDecoder(): Decoder<ContestDepositForfeitedEventData> {
  return getStructDecoder([
    ["task", getAddressDecoder()],
    ["claim", getAddressDecoder()],
    ["workerAgent", getAddressDecoder()],
    ["amount", getU64Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
