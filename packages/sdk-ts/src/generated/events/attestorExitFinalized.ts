/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  getAddressDecoder,
  getI64Decoder,
  getStructDecoder,
  getU64Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `AttestorExitFinalized` (sha256("event:AttestorExitFinalized")[0..8]). */
export const ATTESTOR_EXIT_FINALIZED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  22, 204, 34, 12, 13, 118, 232, 118,
]);

/**
 * Emitted when an attestor exit finalizes and the roster PDA closes, refunding
 * bond + rent to the attestor in full (P1.2 — never confiscatable).
 */
export type AttestorExitFinalizedEventData = {
  attestor: Address;
  refundedLamports: bigint;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link AttestorExitFinalizedEventData} event payload — the bytes
 * AFTER the 8-byte `ATTESTOR_EXIT_FINALIZED_EVENT_DISCRIMINATOR`.
 */
export function getAttestorExitFinalizedEventDecoder(): Decoder<AttestorExitFinalizedEventData> {
  return getStructDecoder([
    ["attestor", getAddressDecoder()],
    ["refundedLamports", getU64Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
