/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  getAddressDecoder,
  getI64Decoder,
  getStructDecoder,
  getU32Decoder,
  getU64Decoder,
  getU8Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `ListingRated` (sha256("event:ListingRated")[0..8]). */
export const LISTING_RATED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  177, 231, 150, 200, 14, 103, 122, 151,
]);

/**
 * Emitted when a buyer rates a completed listing hire (P6.1). Carries the new
 * listing aggregate so indexers can recompute the average without re-reading the
 * account. The provider-agent rating aggregate is deferred to P6.6's `AgentStats`.
 */
export type ListingRatedEventData = {
  /** Service listing whose aggregate was updated. */
  listing: Address;
  /** The hired task that was rated. */
  task: Address;
  /** Provider agent of the listing. */
  providerAgent: Address;
  /** The buyer (task creator) that authored the rating. */
  buyer: Address;
  /** Score in [1, 5]. */
  score: number;
  /** `listing.total_rating` after this rating. */
  newTotalRating: bigint;
  /** `listing.rating_count` after this rating. */
  newRatingCount: number;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link ListingRatedEventData} event payload — the bytes
 * AFTER the 8-byte `LISTING_RATED_EVENT_DISCRIMINATOR`.
 */
export function getListingRatedEventDecoder(): Decoder<ListingRatedEventData> {
  return getStructDecoder([
    ["listing", getAddressDecoder()],
    ["task", getAddressDecoder()],
    ["providerAgent", getAddressDecoder()],
    ["buyer", getAddressDecoder()],
    ["score", getU8Decoder()],
    ["newTotalRating", getU64Decoder()],
    ["newRatingCount", getU32Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
