/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  fixDecoderSize,
  getAddressDecoder,
  getBooleanDecoder,
  getBytesDecoder,
  getI64Decoder,
  getStructDecoder,
  getU8Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `TaskClosed` (sha256("event:TaskClosed")[0..8]). */
export const TASK_CLOSED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  158, 183, 233, 176, 74, 66, 243, 77,
]);

/** Emitted when a terminal task's account rent is reclaimed via close_task. */
export type TaskClosedEventData = {
  taskId: ReadonlyUint8Array;
  creator: Address;
  /** Terminal status at close time (`TaskStatus` repr: 3=Completed, 4=Cancelled). */
  status: number;
  /** Whether a leftover job-spec pointer was closed in the same transaction. */
  jobSpecClosed: boolean;
  /** Whether a still-alive (expire_dispute) escrow PDA was closed in the same tx. */
  escrowClosed: boolean;
  /** Whether a hire link was closed (and its listing's capacity slot freed). */
  hireRecordClosed: boolean;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link TaskClosedEventData} event payload — the bytes
 * AFTER the 8-byte `TASK_CLOSED_EVENT_DISCRIMINATOR`.
 */
export function getTaskClosedEventDecoder(): Decoder<TaskClosedEventData> {
  return getStructDecoder([
    ["taskId", fixDecoderSize(getBytesDecoder(), 32)],
    ["creator", getAddressDecoder()],
    ["status", getU8Decoder()],
    ["jobSpecClosed", getBooleanDecoder()],
    ["escrowClosed", getBooleanDecoder()],
    ["hireRecordClosed", getBooleanDecoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
