/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  getAddressDecoder,
  getBooleanDecoder,
  getI64Decoder,
  getStructDecoder,
  getU64Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `TaskClaimExpired` (sha256("event:TaskClaimExpired")[0..8]). */
export const TASK_CLAIM_EXPIRED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  93, 241, 152, 103, 72, 253, 68, 40,
]);

/**
 * Emitted when a stale claim is expired via `expire_claim` (issue #82).
 * Closes the indexer blind spot on claim-timeout fallback transitions.
 */
export type TaskClaimExpiredEventData = {
  task: Address;
  /** The expired claim's worker `AgentRegistration` PDA. */
  worker: Address;
  /** Caller who triggered expiry (worker during grace, anyone after). */
  expiredBy: Address;
  cleanupReward: bigint;
  /** True when `current_workers` hit zero and the task returned to `Open`. */
  taskReopened: boolean;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link TaskClaimExpiredEventData} event payload — the bytes
 * AFTER the 8-byte `TASK_CLAIM_EXPIRED_EVENT_DISCRIMINATOR`.
 */
export function getTaskClaimExpiredEventDecoder(): Decoder<TaskClaimExpiredEventData> {
  return getStructDecoder([
    ["task", getAddressDecoder()],
    ["worker", getAddressDecoder()],
    ["expiredBy", getAddressDecoder()],
    ["cleanupReward", getU64Decoder()],
    ["taskReopened", getBooleanDecoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
