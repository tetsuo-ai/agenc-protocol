/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  addDecoderSizePrefix,
  getAddressDecoder,
  getI64Decoder,
  getStructDecoder,
  getU32Decoder,
  getU8Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import { getBorshStringDecoder } from "../codecs/borshString";

/** 8-byte Anchor event discriminator for `AgentVerified` (sha256("event:AgentVerified")[0..8]). */
export const AGENT_VERIFIED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([56, 159, 70, 162, 200, 46, 208, 1]);

/**
 * Emitted when a trusted attestor records a domain-verification attestation for an
 * agent (P7.3). `verified_by` is the recording attestor/authority.
 */
export type AgentVerifiedEventData = {
  agent: Address;
  verifiedDomain: string;
  method: number;
  verifiedBy: Address;
  verifiedAt: bigint;
  expiresAt: bigint;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link AgentVerifiedEventData} event payload — the bytes
 * AFTER the 8-byte `AGENT_VERIFIED_EVENT_DISCRIMINATOR`.
 */
export function getAgentVerifiedEventDecoder(): Decoder<AgentVerifiedEventData> {
  return getStructDecoder([
    ["agent", getAddressDecoder()],
    [
      "verifiedDomain",
      addDecoderSizePrefix(getBorshStringDecoder(), getU32Decoder()),
    ],
    ["method", getU8Decoder()],
    ["verifiedBy", getAddressDecoder()],
    ["verifiedAt", getI64Decoder()],
    ["expiresAt", getI64Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
