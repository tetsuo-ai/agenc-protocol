/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  fixDecoderSize,
  getAddressDecoder,
  getBytesDecoder,
  getI64Decoder,
  getStructDecoder,
  getU64Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `StoreRegistered` (sha256("event:StoreRegistered")[0..8]). */
export const STORE_REGISTERED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  8, 21, 234, 141, 147, 227, 16, 145,
]);

/** Emitted when a store registers its on-chain identity (P5.2). */
export type StoreRegisteredEventData = {
  /** The `["store", owner]` PDA. */
  store: Address;
  /** Owner wallet (the identity key). */
  owner: Address;
  /** Display handle (zero-padded; NOT unique on-chain). */
  handle: ReadonlyUint8Array;
  /** Registration bond deposited on the PDA. */
  bondLamports: bigint;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link StoreRegisteredEventData} event payload — the bytes
 * AFTER the 8-byte `STORE_REGISTERED_EVENT_DISCRIMINATOR`.
 */
export function getStoreRegisteredEventDecoder(): Decoder<StoreRegisteredEventData> {
  return getStructDecoder([
    ["store", getAddressDecoder()],
    ["owner", getAddressDecoder()],
    ["handle", fixDecoderSize(getBytesDecoder(), 32)],
    ["bondLamports", getU64Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
