/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  fixDecoderSize,
  getAddressDecoder,
  getBytesDecoder,
  getI64Decoder,
  getStructDecoder,
  getU16Decoder,
  getU64Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `ReferrerFeePaid` (sha256("event:ReferrerFeePaid")[0..8]). */
export const REFERRER_FEE_PAID_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  127, 186, 248, 231, 123, 86, 255, 130,
]);

/**
 * Emitted when a referrer (demand-side embedder) fee leg is paid out of a
 * settlement (spec §4 4-way split, P6.2). Only emitted when the referrer leg is
 * non-zero.
 */
export type ReferrerFeePaidEventData = {
  taskId: ReadonlyUint8Array;
  referrer: Address;
  amount: bigint;
  referrerFeeBps: number;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link ReferrerFeePaidEventData} event payload — the bytes
 * AFTER the 8-byte `REFERRER_FEE_PAID_EVENT_DISCRIMINATOR`.
 */
export function getReferrerFeePaidEventDecoder(): Decoder<ReferrerFeePaidEventData> {
  return getStructDecoder([
    ["taskId", fixDecoderSize(getBytesDecoder(), 32)],
    ["referrer", getAddressDecoder()],
    ["amount", getU64Decoder()],
    ["referrerFeeBps", getU16Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
