/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  getAddressDecoder,
  getI64Decoder,
  getStructDecoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `AgentVerificationRevoked` (sha256("event:AgentVerificationRevoked")[0..8]). */
export const AGENT_VERIFICATION_REVOKED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  67, 186, 43, 174, 149, 119, 196, 190,
]);

/** Emitted when a trusted attestor revokes an agent's domain verification (P7.3). */
export type AgentVerificationRevokedEventData = {
  agent: Address;
  revokedBy: Address;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link AgentVerificationRevokedEventData} event payload — the bytes
 * AFTER the 8-byte `AGENT_VERIFICATION_REVOKED_EVENT_DISCRIMINATOR`.
 */
export function getAgentVerificationRevokedEventDecoder(): Decoder<AgentVerificationRevokedEventData> {
  return getStructDecoder([
    ["agent", getAddressDecoder()],
    ["revokedBy", getAddressDecoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
