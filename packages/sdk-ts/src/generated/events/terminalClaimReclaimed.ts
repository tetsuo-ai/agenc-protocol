/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  getAddressDecoder,
  getI64Decoder,
  getStructDecoder,
  getU64Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `TerminalClaimReclaimed` (sha256("event:TerminalClaimReclaimed")[0..8]). */
export const TERMINAL_CLAIM_RECLAIMED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  211, 154, 175, 230, 62, 173, 108, 51,
]);

/**
 * Emitted by `reclaim_terminal_claim`: an unsettled claim on an already-terminal
 * task was reclaimed. The eligible shapes are an empty/no-submission record, a
 * Rejected submission, or a still-Submitted Collaborative straggler after
 * completion. Task/worker slot counters are freed; applicable claim surplus is
 * forfeited to the treasury while a Submitted straggler is refunded in full.
 */
export type TerminalClaimReclaimedEventData = {
  task: Address;
  claim: Address;
  /** The affected worker's `AgentRegistration` PDA. */
  workerAgent: Address;
  /**
   * Claim lamports returned to the worker authority: the rent-exempt minimum
   * for empty/Rejected cleanup, or the full balance for a Submitted
   * Collaborative straggler.
   */
  workerRefund: bigint;
  /**
   * Lamports forfeited to the protocol treasury (the contest entry-deposit
   * surplus above rent; 0 for non-contest claims).
   */
  forfeited: bigint;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link TerminalClaimReclaimedEventData} event payload — the bytes
 * AFTER the 8-byte `TERMINAL_CLAIM_RECLAIMED_EVENT_DISCRIMINATOR`.
 */
export function getTerminalClaimReclaimedEventDecoder(): Decoder<TerminalClaimReclaimedEventData> {
  return getStructDecoder([
    ["task", getAddressDecoder()],
    ["claim", getAddressDecoder()],
    ["workerAgent", getAddressDecoder()],
    ["workerRefund", getU64Decoder()],
    ["forfeited", getU64Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
