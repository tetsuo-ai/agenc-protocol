/**
 * This code was AUTOGENERATED by scripts/generate-events.mjs (event codecs that
 * the Codama renderer tree-shakes away). Please DO NOT EDIT THIS FILE, instead
 * rerun `npm run sdk:generate` to update it.
 */

import {
  getAddressDecoder,
  getI64Decoder,
  getStructDecoder,
  getU64Decoder,
  type Address,
  type Decoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** 8-byte Anchor event discriminator for `StoreClosed` (sha256("event:StoreClosed")[0..8]). */
export const STORE_CLOSED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  184, 145, 107, 98, 153, 0, 214, 211,
]);

/**
 * Emitted when a store closes its identity PDA; rent + bond are refunded in
 * full to the owner (P5.2 — no exit cooldown: nothing money-bearing reads
 * `Store` in v1).
 */
export type StoreClosedEventData = {
  store: Address;
  owner: Address;
  /** The bond principal that was held (informational). */
  bondLamports: bigint;
  /**
   * Total lamports actually refunded to the owner at close — the full PDA
   * balance (rent + bond + any lamports transferred to the PDA after
   * registration), which is what `close = owner` returns.
   */
  refundedLamports: bigint;
  timestamp: bigint;
};

/**
 * Gets the decoder for the {@link StoreClosedEventData} event payload — the bytes
 * AFTER the 8-byte `STORE_CLOSED_EVENT_DISCRIMINATOR`.
 */
export function getStoreClosedEventDecoder(): Decoder<StoreClosedEventData> {
  return getStructDecoder([
    ["store", getAddressDecoder()],
    ["owner", getAddressDecoder()],
    ["bondLamports", getU64Decoder()],
    ["refundedLamports", getU64Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}
