/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const HIRE_RECORD_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  104, 101, 55, 188, 219, 31, 76, 113,
]);

export function getHireRecordDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(HIRE_RECORD_DISCRIMINATOR);
}

export type HireRecord = {
  discriminator: ReadonlyUint8Array;
  /** The one-shot task minted by this hire. */
  task: Address;
  /** Source service listing whose `open_jobs` is decremented when the task closes. */
  listing: Address;
  /** Operator payee snapshot for the Batch 2 fee split (`Pubkey::default()` = none). */
  operator: Address;
  /** Operator fee in basis points, snapshotted at hire time. */
  operatorFeeBps: number;
  /** PDA bump. */
  bump: number;
  /**
   * The provider agent designated by the listing at hire time. Only this
   * registration may claim the resulting one-shot task. This carves the
   * original 32-byte reserved region without changing the account layout.
   */
  designatedProvider: Address;
  /**
   * Referrer (embedder) payee snapshot for the four-way split
   * (`Pubkey::default()` = none).
   */
  referrer: Address;
  /** Referrer fee in basis points, snapshotted at hire time. */
  referrerFeeBps: number;
};

export type HireRecordArgs = {
  /** The one-shot task minted by this hire. */
  task: Address;
  /** Source service listing whose `open_jobs` is decremented when the task closes. */
  listing: Address;
  /** Operator payee snapshot for the Batch 2 fee split (`Pubkey::default()` = none). */
  operator: Address;
  /** Operator fee in basis points, snapshotted at hire time. */
  operatorFeeBps: number;
  /** PDA bump. */
  bump: number;
  /**
   * The provider agent designated by the listing at hire time. Only this
   * registration may claim the resulting one-shot task. This carves the
   * original 32-byte reserved region without changing the account layout.
   */
  designatedProvider: Address;
  /**
   * Referrer (embedder) payee snapshot for the four-way split
   * (`Pubkey::default()` = none).
   */
  referrer: Address;
  /** Referrer fee in basis points, snapshotted at hire time. */
  referrerFeeBps: number;
};

/** Gets the encoder for {@link HireRecordArgs} account data. */
export function getHireRecordEncoder(): FixedSizeEncoder<HireRecordArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["task", getAddressEncoder()],
      ["listing", getAddressEncoder()],
      ["operator", getAddressEncoder()],
      ["operatorFeeBps", getU16Encoder()],
      ["bump", getU8Encoder()],
      ["designatedProvider", getAddressEncoder()],
      ["referrer", getAddressEncoder()],
      ["referrerFeeBps", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: HIRE_RECORD_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link HireRecord} account data. */
export function getHireRecordDecoder(): FixedSizeDecoder<HireRecord> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["task", getAddressDecoder()],
    ["listing", getAddressDecoder()],
    ["operator", getAddressDecoder()],
    ["operatorFeeBps", getU16Decoder()],
    ["bump", getU8Decoder()],
    ["designatedProvider", getAddressDecoder()],
    ["referrer", getAddressDecoder()],
    ["referrerFeeBps", getU16Decoder()],
  ]);
}

/** Gets the codec for {@link HireRecord} account data. */
export function getHireRecordCodec(): FixedSizeCodec<
  HireRecordArgs,
  HireRecord
> {
  return combineCodec(getHireRecordEncoder(), getHireRecordDecoder());
}

export function decodeHireRecord<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<HireRecord, TAddress>;
export function decodeHireRecord<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<HireRecord, TAddress>;
export function decodeHireRecord<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<HireRecord, TAddress> | MaybeAccount<HireRecord, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getHireRecordDecoder(),
  );
}

export async function fetchHireRecord<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<HireRecord, TAddress>> {
  const maybeAccount = await fetchMaybeHireRecord(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeHireRecord<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<HireRecord, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeHireRecord(maybeAccount);
}

export async function fetchAllHireRecord(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<HireRecord>[]> {
  const maybeAccounts = await fetchAllMaybeHireRecord(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeHireRecord(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<HireRecord>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeHireRecord(maybeAccount));
}

export function getHireRecordSize(): number {
  return 173;
}
