/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";

export const SALE_RECEIPT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  114, 79, 236, 216, 212, 117, 80, 21,
]);

export function getSaleReceiptDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(SALE_RECEIPT_DISCRIMINATOR);
}

export type SaleReceipt = {
  discriminator: ReadonlyUint8Array;
  /** The goods listing sold from */
  listing: Address;
  /** Buyer WALLET (bare signer — buyers need no agent registration) */
  buyer: Address;
  /** This unit's index in [0, total_supply) at sale time */
  serial: bigint;
  /** Snapshot of the listing's `metadata_hash` at the moment of sale */
  metadataHash: ReadonlyUint8Array;
  /** Price paid (lamports or token smallest units) */
  pricePaid: bigint;
  /** Protocol fee taken (provenance of the cut) */
  protocolFee: bigint;
  /** Operator fee taken (0 when the listing has no operator leg) */
  operatorFee: bigint;
  /** Sale timestamp */
  timestamp: bigint;
  /** Bump seed */
  bump: number;
  /** Reserved for future use */
  reserved: ReadonlyUint8Array;
};

export type SaleReceiptArgs = {
  /** The goods listing sold from */
  listing: Address;
  /** Buyer WALLET (bare signer — buyers need no agent registration) */
  buyer: Address;
  /** This unit's index in [0, total_supply) at sale time */
  serial: number | bigint;
  /** Snapshot of the listing's `metadata_hash` at the moment of sale */
  metadataHash: ReadonlyUint8Array;
  /** Price paid (lamports or token smallest units) */
  pricePaid: number | bigint;
  /** Protocol fee taken (provenance of the cut) */
  protocolFee: number | bigint;
  /** Operator fee taken (0 when the listing has no operator leg) */
  operatorFee: number | bigint;
  /** Sale timestamp */
  timestamp: number | bigint;
  /** Bump seed */
  bump: number;
  /** Reserved for future use */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link SaleReceiptArgs} account data. */
export function getSaleReceiptEncoder(): FixedSizeEncoder<SaleReceiptArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["listing", getAddressEncoder()],
      ["buyer", getAddressEncoder()],
      ["serial", getU64Encoder()],
      ["metadataHash", getFixedBytesEncoder(32, "metadataHash")],
      ["pricePaid", getU64Encoder()],
      ["protocolFee", getU64Encoder()],
      ["operatorFee", getU64Encoder()],
      ["timestamp", getI64Encoder()],
      ["bump", getU8Encoder()],
      ["reserved", getFixedBytesEncoder(8, "reserved")],
    ]),
    (value) => ({ ...value, discriminator: SALE_RECEIPT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link SaleReceipt} account data. */
export function getSaleReceiptDecoder(): FixedSizeDecoder<SaleReceipt> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["listing", getAddressDecoder()],
    ["buyer", getAddressDecoder()],
    ["serial", getU64Decoder()],
    ["metadataHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["pricePaid", getU64Decoder()],
    ["protocolFee", getU64Decoder()],
    ["operatorFee", getU64Decoder()],
    ["timestamp", getI64Decoder()],
    ["bump", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

/** Gets the codec for {@link SaleReceipt} account data. */
export function getSaleReceiptCodec(): FixedSizeCodec<
  SaleReceiptArgs,
  SaleReceipt
> {
  return combineCodec(getSaleReceiptEncoder(), getSaleReceiptDecoder());
}

export function decodeSaleReceipt<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<SaleReceipt, TAddress>;
export function decodeSaleReceipt<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<SaleReceipt, TAddress>;
export function decodeSaleReceipt<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<SaleReceipt, TAddress> | MaybeAccount<SaleReceipt, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getSaleReceiptDecoder(),
  );
}

export async function fetchSaleReceipt<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<SaleReceipt, TAddress>> {
  const maybeAccount = await fetchMaybeSaleReceipt(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeSaleReceipt<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<SaleReceipt, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeSaleReceipt(maybeAccount);
}

export async function fetchAllSaleReceipt(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<SaleReceipt>[]> {
  const maybeAccounts = await fetchAllMaybeSaleReceipt(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeSaleReceipt(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<SaleReceipt>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeSaleReceipt(maybeAccount));
}

export function getSaleReceiptSize(): number {
  return 153;
}
