/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

import {
  getBorshStringDecoder,
  getBorshStringEncoder,
} from "../codecs/borshString";

export const HIRE_RATING_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  250, 116, 242, 149, 241, 55, 164, 63,
]);

export function getHireRatingDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(HIRE_RATING_DISCRIMINATOR);
}

export type HireRating = {
  discriminator: ReadonlyUint8Array;
  /** The completed hired task this rating is for (also the dedupe seed). */
  task: Address;
  /** Source service listing whose aggregate was updated by this rating. */
  listing: Address;
  /** The buyer (task creator) that authored the rating. */
  buyer: Address;
  /** Score in [1, 5]. */
  score: number;
  /** Optional off-chain review content hash (`None` = no written review). */
  reviewHash: Option<ReadonlyUint8Array>;
  /**
   * Optional bounded pointer to the off-chain review (e.g. agenc://review/...).
   * Empty string = no URI.
   */
  reviewUri: string;
  /** When the rating was recorded. */
  ratedAt: bigint;
  /** PDA bump. */
  bump: number;
  /** Reserved for future rating metadata. MUST stay zeroed. */
  reserved: ReadonlyUint8Array;
};

export type HireRatingArgs = {
  /** The completed hired task this rating is for (also the dedupe seed). */
  task: Address;
  /** Source service listing whose aggregate was updated by this rating. */
  listing: Address;
  /** The buyer (task creator) that authored the rating. */
  buyer: Address;
  /** Score in [1, 5]. */
  score: number;
  /** Optional off-chain review content hash (`None` = no written review). */
  reviewHash: OptionOrNullable<ReadonlyUint8Array>;
  /**
   * Optional bounded pointer to the off-chain review (e.g. agenc://review/...).
   * Empty string = no URI.
   */
  reviewUri: string;
  /** When the rating was recorded. */
  ratedAt: number | bigint;
  /** PDA bump. */
  bump: number;
  /** Reserved for future rating metadata. MUST stay zeroed. */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link HireRatingArgs} account data. */
export function getHireRatingEncoder(): Encoder<HireRatingArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["task", getAddressEncoder()],
      ["listing", getAddressEncoder()],
      ["buyer", getAddressEncoder()],
      ["score", getU8Encoder()],
      ["reviewHash", getOptionEncoder(fixEncoderSize(getBytesEncoder(), 32))],
      [
        "reviewUri",
        addEncoderSizePrefix(getBorshStringEncoder(), getU32Encoder()),
      ],
      ["ratedAt", getI64Encoder()],
      ["bump", getU8Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: HIRE_RATING_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link HireRating} account data. */
export function getHireRatingDecoder(): Decoder<HireRating> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["task", getAddressDecoder()],
    ["listing", getAddressDecoder()],
    ["buyer", getAddressDecoder()],
    ["score", getU8Decoder()],
    ["reviewHash", getOptionDecoder(fixDecoderSize(getBytesDecoder(), 32))],
    [
      "reviewUri",
      addDecoderSizePrefix(getBorshStringDecoder(), getU32Decoder()),
    ],
    ["ratedAt", getI64Decoder()],
    ["bump", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

/** Gets the codec for {@link HireRating} account data. */
export function getHireRatingCodec(): Codec<HireRatingArgs, HireRating> {
  return combineCodec(getHireRatingEncoder(), getHireRatingDecoder());
}

export function decodeHireRating<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<HireRating, TAddress>;
export function decodeHireRating<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<HireRating, TAddress>;
export function decodeHireRating<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<HireRating, TAddress> | MaybeAccount<HireRating, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getHireRatingDecoder(),
  );
}

export async function fetchHireRating<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<HireRating, TAddress>> {
  const maybeAccount = await fetchMaybeHireRating(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeHireRating<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<HireRating, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeHireRating(maybeAccount);
}

export async function fetchAllHireRating(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<HireRating>[]> {
  const maybeAccounts = await fetchAllMaybeHireRating(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeHireRating(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<HireRating>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeHireRating(maybeAccount));
}
