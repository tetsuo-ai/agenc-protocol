/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const DISPUTE_RESOLVER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([68, 124, 148, 26, 177, 224, 247, 53]);

export function getDisputeResolverDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DISPUTE_RESOLVER_DISCRIMINATOR,
  );
}

export type DisputeResolver = {
  discriminator: ReadonlyUint8Array;
  /** The wallet authorized to resolve disputes. */
  resolver: Address;
  /** The protocol authority that assigned this resolver (audit trail). */
  assignedBy: Address;
  /** Unix timestamp the assignment was created. */
  assignedAt: bigint;
  /** PDA bump. */
  bump: number;
  /** Disputes this resolver has decided through `resolve_dispute`. */
  resolvedCount: bigint;
  /**
   * Rulings later vacated/overturned. Has no on-chain incrementer yet: the
   * challenge-window mechanism that would bump it (`execute_resolution` settling a
   * pending outcome unless vacated) is the design-doc-only P6.4 step (3),
   * `docs/DISPUTE_CHALLENGE_WINDOW.md`, gated `[HUMAN: approve before build]`. The
   * field is reserved now so adding that mechanism later needs NO layout change.
   */
  overturnedCount: bigint;
  /** Unix timestamp this resolver last decided a dispute (0 = never). */
  lastResolvedAt: bigint;
  /** Reserved for future metadata. MUST stay zeroed. */
  reserved: ReadonlyUint8Array;
};

export type DisputeResolverArgs = {
  /** The wallet authorized to resolve disputes. */
  resolver: Address;
  /** The protocol authority that assigned this resolver (audit trail). */
  assignedBy: Address;
  /** Unix timestamp the assignment was created. */
  assignedAt: number | bigint;
  /** PDA bump. */
  bump: number;
  /** Disputes this resolver has decided through `resolve_dispute`. */
  resolvedCount: number | bigint;
  /**
   * Rulings later vacated/overturned. Has no on-chain incrementer yet: the
   * challenge-window mechanism that would bump it (`execute_resolution` settling a
   * pending outcome unless vacated) is the design-doc-only P6.4 step (3),
   * `docs/DISPUTE_CHALLENGE_WINDOW.md`, gated `[HUMAN: approve before build]`. The
   * field is reserved now so adding that mechanism later needs NO layout change.
   */
  overturnedCount: number | bigint;
  /** Unix timestamp this resolver last decided a dispute (0 = never). */
  lastResolvedAt: number | bigint;
  /** Reserved for future metadata. MUST stay zeroed. */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link DisputeResolverArgs} account data. */
export function getDisputeResolverEncoder(): FixedSizeEncoder<DisputeResolverArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["resolver", getAddressEncoder()],
      ["assignedBy", getAddressEncoder()],
      ["assignedAt", getI64Encoder()],
      ["bump", getU8Encoder()],
      ["resolvedCount", getU64Encoder()],
      ["overturnedCount", getU64Encoder()],
      ["lastResolvedAt", getI64Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 8)],
    ]),
    (value) => ({ ...value, discriminator: DISPUTE_RESOLVER_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link DisputeResolver} account data. */
export function getDisputeResolverDecoder(): FixedSizeDecoder<DisputeResolver> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["resolver", getAddressDecoder()],
    ["assignedBy", getAddressDecoder()],
    ["assignedAt", getI64Decoder()],
    ["bump", getU8Decoder()],
    ["resolvedCount", getU64Decoder()],
    ["overturnedCount", getU64Decoder()],
    ["lastResolvedAt", getI64Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

/** Gets the codec for {@link DisputeResolver} account data. */
export function getDisputeResolverCodec(): FixedSizeCodec<
  DisputeResolverArgs,
  DisputeResolver
> {
  return combineCodec(getDisputeResolverEncoder(), getDisputeResolverDecoder());
}

export function decodeDisputeResolver<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<DisputeResolver, TAddress>;
export function decodeDisputeResolver<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<DisputeResolver, TAddress>;
export function decodeDisputeResolver<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<DisputeResolver, TAddress>
  | MaybeAccount<DisputeResolver, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getDisputeResolverDecoder(),
  );
}

export async function fetchDisputeResolver<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<DisputeResolver, TAddress>> {
  const maybeAccount = await fetchMaybeDisputeResolver(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeDisputeResolver<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<DisputeResolver, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeDisputeResolver(maybeAccount);
}

export async function fetchAllDisputeResolver(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<DisputeResolver>[]> {
  const maybeAccounts = await fetchAllMaybeDisputeResolver(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeDisputeResolver(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<DisputeResolver>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeDisputeResolver(maybeAccount),
  );
}

export function getDisputeResolverSize(): number {
  return 113;
}
