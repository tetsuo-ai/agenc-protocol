/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getSubmissionStatusDecoder,
  getSubmissionStatusEncoder,
  type SubmissionStatus,
  type SubmissionStatusArgs,
} from "../types";

export const TASK_SUBMISSION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [111, 64, 190, 132, 148, 33, 215, 63],
);

export function getTaskSubmissionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TASK_SUBMISSION_DISCRIMINATOR,
  );
}

export type TaskSubmission = {
  discriminator: ReadonlyUint8Array;
  /** Task being submitted. */
  task: Address;
  /** Claim tied to this submission. */
  claim: Address;
  /** Worker that submitted the result. */
  worker: Address;
  /** Current submission status. */
  status: SubmissionStatus;
  /** Latest proof hash supplied by the worker. */
  proofHash: ReadonlyUint8Array;
  /** Latest result payload supplied by the worker. */
  resultData: ReadonlyUint8Array;
  /** Number of times this claim has been submitted for review. */
  submissionCount: number;
  /** Timestamp of latest submission. */
  submittedAt: bigint;
  /** Timestamp after which the review window has elapsed. */
  reviewDeadlineAt: bigint;
  /** Acceptance timestamp (0 when unresolved). */
  acceptedAt: bigint;
  /** Rejection timestamp (0 when unresolved). */
  rejectedAt: bigint;
  /** Optional rejection reason hash. */
  rejectionHash: ReadonlyUint8Array;
  /** PDA bump. */
  bump: number;
  /** Reserved for future attestation metadata. */
  reserved: ReadonlyUint8Array;
};

export type TaskSubmissionArgs = {
  /** Task being submitted. */
  task: Address;
  /** Claim tied to this submission. */
  claim: Address;
  /** Worker that submitted the result. */
  worker: Address;
  /** Current submission status. */
  status: SubmissionStatusArgs;
  /** Latest proof hash supplied by the worker. */
  proofHash: ReadonlyUint8Array;
  /** Latest result payload supplied by the worker. */
  resultData: ReadonlyUint8Array;
  /** Number of times this claim has been submitted for review. */
  submissionCount: number;
  /** Timestamp of latest submission. */
  submittedAt: number | bigint;
  /** Timestamp after which the review window has elapsed. */
  reviewDeadlineAt: number | bigint;
  /** Acceptance timestamp (0 when unresolved). */
  acceptedAt: number | bigint;
  /** Rejection timestamp (0 when unresolved). */
  rejectedAt: number | bigint;
  /** Optional rejection reason hash. */
  rejectionHash: ReadonlyUint8Array;
  /** PDA bump. */
  bump: number;
  /** Reserved for future attestation metadata. */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link TaskSubmissionArgs} account data. */
export function getTaskSubmissionEncoder(): FixedSizeEncoder<TaskSubmissionArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["task", getAddressEncoder()],
      ["claim", getAddressEncoder()],
      ["worker", getAddressEncoder()],
      ["status", getSubmissionStatusEncoder()],
      ["proofHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["resultData", fixEncoderSize(getBytesEncoder(), 64)],
      ["submissionCount", getU16Encoder()],
      ["submittedAt", getI64Encoder()],
      ["reviewDeadlineAt", getI64Encoder()],
      ["acceptedAt", getI64Encoder()],
      ["rejectedAt", getI64Encoder()],
      ["rejectionHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["bump", getU8Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 5)],
    ]),
    (value) => ({ ...value, discriminator: TASK_SUBMISSION_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link TaskSubmission} account data. */
export function getTaskSubmissionDecoder(): FixedSizeDecoder<TaskSubmission> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["task", getAddressDecoder()],
    ["claim", getAddressDecoder()],
    ["worker", getAddressDecoder()],
    ["status", getSubmissionStatusDecoder()],
    ["proofHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["resultData", fixDecoderSize(getBytesDecoder(), 64)],
    ["submissionCount", getU16Decoder()],
    ["submittedAt", getI64Decoder()],
    ["reviewDeadlineAt", getI64Decoder()],
    ["acceptedAt", getI64Decoder()],
    ["rejectedAt", getI64Decoder()],
    ["rejectionHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["bump", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 5)],
  ]);
}

/** Gets the codec for {@link TaskSubmission} account data. */
export function getTaskSubmissionCodec(): FixedSizeCodec<
  TaskSubmissionArgs,
  TaskSubmission
> {
  return combineCodec(getTaskSubmissionEncoder(), getTaskSubmissionDecoder());
}

export function decodeTaskSubmission<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<TaskSubmission, TAddress>;
export function decodeTaskSubmission<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<TaskSubmission, TAddress>;
export function decodeTaskSubmission<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<TaskSubmission, TAddress> | MaybeAccount<TaskSubmission, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getTaskSubmissionDecoder(),
  );
}

export async function fetchTaskSubmission<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<TaskSubmission, TAddress>> {
  const maybeAccount = await fetchMaybeTaskSubmission(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTaskSubmission<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<TaskSubmission, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTaskSubmission(maybeAccount);
}

export async function fetchAllTaskSubmission(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<TaskSubmission>[]> {
  const maybeAccounts = await fetchAllMaybeTaskSubmission(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTaskSubmission(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<TaskSubmission>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeTaskSubmission(maybeAccount),
  );
}

export function getTaskSubmissionSize(): number {
  return 273;
}
