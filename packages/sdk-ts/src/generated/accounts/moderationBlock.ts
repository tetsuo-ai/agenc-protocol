/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";

import {
  getBorshStringDecoder,
  getBorshStringEncoder,
} from "../codecs/borshString";

export const MODERATION_BLOCK_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([90, 220, 89, 93, 67, 217, 235, 20]);

export function getModerationBlockDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(MODERATION_BLOCK_DISCRIMINATOR);
}

export type ModerationBlock = {
  discriminator: ReadonlyUint8Array;
  /** The blocked job-spec/listing-spec content hash (the PDA seed, stored for gPA). */
  contentHash: ReadonlyUint8Array;
  /** One of `moderation_block_status::*`. */
  status: number;
  /**
   * Hash of the written takedown rationale (REQUIRED — the `resolve_dispute`
   * precedent; bounds the discretionary power with an auditable record).
   */
  rationaleHash: ReadonlyUint8Array;
  /** URI of the rationale document. */
  rationaleUri: string;
  /** When the block was first set. */
  setAt: bigint;
  /** Last set/clear transition. */
  updatedAt: bigint;
  /**
   * The fee-paying signer of the last transition (the multisig approval itself is
   * checked via `require_multisig_threshold` over remaining accounts).
   */
  updatedBy: Address;
  /** PDA bump. */
  bump: number;
  /** Reserved for future takedown metadata. MUST stay zeroed. */
  reserved: ReadonlyUint8Array;
};

export type ModerationBlockArgs = {
  /** The blocked job-spec/listing-spec content hash (the PDA seed, stored for gPA). */
  contentHash: ReadonlyUint8Array;
  /** One of `moderation_block_status::*`. */
  status: number;
  /**
   * Hash of the written takedown rationale (REQUIRED — the `resolve_dispute`
   * precedent; bounds the discretionary power with an auditable record).
   */
  rationaleHash: ReadonlyUint8Array;
  /** URI of the rationale document. */
  rationaleUri: string;
  /** When the block was first set. */
  setAt: number | bigint;
  /** Last set/clear transition. */
  updatedAt: number | bigint;
  /**
   * The fee-paying signer of the last transition (the multisig approval itself is
   * checked via `require_multisig_threshold` over remaining accounts).
   */
  updatedBy: Address;
  /** PDA bump. */
  bump: number;
  /** Reserved for future takedown metadata. MUST stay zeroed. */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link ModerationBlockArgs} account data. */
export function getModerationBlockEncoder(): Encoder<ModerationBlockArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["contentHash", getFixedBytesEncoder(32, "contentHash")],
      ["status", getU8Encoder()],
      ["rationaleHash", getFixedBytesEncoder(32, "rationaleHash")],
      [
        "rationaleUri",
        addEncoderSizePrefix(getBorshStringEncoder(), getU32Encoder()),
      ],
      ["setAt", getI64Encoder()],
      ["updatedAt", getI64Encoder()],
      ["updatedBy", getAddressEncoder()],
      ["bump", getU8Encoder()],
      ["reserved", getFixedBytesEncoder(16, "reserved")],
    ]),
    (value) => ({ ...value, discriminator: MODERATION_BLOCK_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link ModerationBlock} account data. */
export function getModerationBlockDecoder(): Decoder<ModerationBlock> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["contentHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["status", getU8Decoder()],
    ["rationaleHash", fixDecoderSize(getBytesDecoder(), 32)],
    [
      "rationaleUri",
      addDecoderSizePrefix(getBorshStringDecoder(), getU32Decoder()),
    ],
    ["setAt", getI64Decoder()],
    ["updatedAt", getI64Decoder()],
    ["updatedBy", getAddressDecoder()],
    ["bump", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 16)],
  ]);
}

/** Gets the codec for {@link ModerationBlock} account data. */
export function getModerationBlockCodec(): Codec<
  ModerationBlockArgs,
  ModerationBlock
> {
  return combineCodec(getModerationBlockEncoder(), getModerationBlockDecoder());
}

export function decodeModerationBlock<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<ModerationBlock, TAddress>;
export function decodeModerationBlock<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<ModerationBlock, TAddress>;
export function decodeModerationBlock<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<ModerationBlock, TAddress>
  | MaybeAccount<ModerationBlock, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getModerationBlockDecoder(),
  );
}

export async function fetchModerationBlock<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ModerationBlock, TAddress>> {
  const maybeAccount = await fetchMaybeModerationBlock(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeModerationBlock<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ModerationBlock, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeModerationBlock(maybeAccount);
}

export async function fetchAllModerationBlock(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ModerationBlock>[]> {
  const maybeAccounts = await fetchAllMaybeModerationBlock(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeModerationBlock(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ModerationBlock>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeModerationBlock(maybeAccount),
  );
}
