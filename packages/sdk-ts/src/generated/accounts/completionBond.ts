/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";

export const COMPLETION_BOND_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [77, 209, 12, 19, 44, 65, 93, 238],
);

export function getCompletionBondDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(COMPLETION_BOND_DISCRIMINATOR);
}

export type CompletionBond = {
  discriminator: ReadonlyUint8Array;
  /** Task this bond backs. */
  task: Address;
  /**
   * Posting wallet (creator wallet for the creator bond, worker authority for the
   * worker bond). Also the seed component and the rent/refund recipient.
   */
  party: Address;
  /** 0 = creator bond, 1 = worker bond (see `ROLE_CREATOR` / `ROLE_WORKER`). */
  role: number;
  /** Bonded principal in lamports (held as excess lamports on this PDA). */
  amount: bigint;
  /** Bond denomination. `None` = SOL (v1); SPL deferred behind a feature flag. */
  bondMint: Option<Address>;
  /** Post timestamp. */
  postedAt: bigint;
  /** PDA bump. */
  bump: number;
  /** Reserved for future bond metadata. MUST stay zeroed. */
  reserved: ReadonlyUint8Array;
};

export type CompletionBondArgs = {
  /** Task this bond backs. */
  task: Address;
  /**
   * Posting wallet (creator wallet for the creator bond, worker authority for the
   * worker bond). Also the seed component and the rent/refund recipient.
   */
  party: Address;
  /** 0 = creator bond, 1 = worker bond (see `ROLE_CREATOR` / `ROLE_WORKER`). */
  role: number;
  /** Bonded principal in lamports (held as excess lamports on this PDA). */
  amount: number | bigint;
  /** Bond denomination. `None` = SOL (v1); SPL deferred behind a feature flag. */
  bondMint: OptionOrNullable<Address>;
  /** Post timestamp. */
  postedAt: number | bigint;
  /** PDA bump. */
  bump: number;
  /** Reserved for future bond metadata. MUST stay zeroed. */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link CompletionBondArgs} account data. */
export function getCompletionBondEncoder(): Encoder<CompletionBondArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["task", getAddressEncoder()],
      ["party", getAddressEncoder()],
      ["role", getU8Encoder()],
      ["amount", getU64Encoder()],
      ["bondMint", getOptionEncoder(getAddressEncoder())],
      ["postedAt", getI64Encoder()],
      ["bump", getU8Encoder()],
      ["reserved", getFixedBytesEncoder(16, "reserved")],
    ]),
    (value) => ({ ...value, discriminator: COMPLETION_BOND_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link CompletionBond} account data. */
export function getCompletionBondDecoder(): Decoder<CompletionBond> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["task", getAddressDecoder()],
    ["party", getAddressDecoder()],
    ["role", getU8Decoder()],
    ["amount", getU64Decoder()],
    ["bondMint", getOptionDecoder(getAddressDecoder())],
    ["postedAt", getI64Decoder()],
    ["bump", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 16)],
  ]);
}

/** Gets the codec for {@link CompletionBond} account data. */
export function getCompletionBondCodec(): Codec<
  CompletionBondArgs,
  CompletionBond
> {
  return combineCodec(getCompletionBondEncoder(), getCompletionBondDecoder());
}

export function decodeCompletionBond<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<CompletionBond, TAddress>;
export function decodeCompletionBond<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<CompletionBond, TAddress>;
export function decodeCompletionBond<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<CompletionBond, TAddress> | MaybeAccount<CompletionBond, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getCompletionBondDecoder(),
  );
}

export async function fetchCompletionBond<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<CompletionBond, TAddress>> {
  const maybeAccount = await fetchMaybeCompletionBond(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeCompletionBond<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<CompletionBond, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeCompletionBond(maybeAccount);
}

export async function fetchAllCompletionBond(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<CompletionBond>[]> {
  const maybeAccounts = await fetchAllMaybeCompletionBond(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeCompletionBond(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<CompletionBond>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeCompletionBond(maybeAccount),
  );
}
