/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const AGENT_STATS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  43, 192, 26, 112, 162, 176, 77, 164,
]);

export function getAgentStatsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(AGENT_STATS_DISCRIMINATOR);
}

export type AgentStats = {
  discriminator: ReadonlyUint8Array;
  /** The `AgentRegistration` PDA these counters belong to (also the seed). */
  agent: Address;
  /**
   * Times one of this agent's submissions was rejected for re-work
   * (`reject_task_result`) or frozen for review (`reject_and_freeze`).
   */
  tasksRejected: bigint;
  /** Disputes resolved in this agent's favor as the defendant worker. */
  disputesWon: bigint;
  /**
   * Disputes resolved against this agent as the defendant worker (a loss; the
   * slash-history signal).
   */
  disputesLost: bigint;
  /** Claims by this agent that expired (no-show / abandoned) via `expire_claim`. */
  claimsExpired: bigint;
  /** Tasks created by this agent (as the creator/buyer) that were cancelled. */
  totalCancelled: bigint;
  /** Last time any counter was updated (unix timestamp). */
  lastUpdated: bigint;
  /** PDA bump. */
  bump: number;
  /** Reserved for future track-record counters. MUST stay zeroed. */
  reserved: ReadonlyUint8Array;
};

export type AgentStatsArgs = {
  /** The `AgentRegistration` PDA these counters belong to (also the seed). */
  agent: Address;
  /**
   * Times one of this agent's submissions was rejected for re-work
   * (`reject_task_result`) or frozen for review (`reject_and_freeze`).
   */
  tasksRejected: number | bigint;
  /** Disputes resolved in this agent's favor as the defendant worker. */
  disputesWon: number | bigint;
  /**
   * Disputes resolved against this agent as the defendant worker (a loss; the
   * slash-history signal).
   */
  disputesLost: number | bigint;
  /** Claims by this agent that expired (no-show / abandoned) via `expire_claim`. */
  claimsExpired: number | bigint;
  /** Tasks created by this agent (as the creator/buyer) that were cancelled. */
  totalCancelled: number | bigint;
  /** Last time any counter was updated (unix timestamp). */
  lastUpdated: number | bigint;
  /** PDA bump. */
  bump: number;
  /** Reserved for future track-record counters. MUST stay zeroed. */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link AgentStatsArgs} account data. */
export function getAgentStatsEncoder(): FixedSizeEncoder<AgentStatsArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["agent", getAddressEncoder()],
      ["tasksRejected", getU64Encoder()],
      ["disputesWon", getU64Encoder()],
      ["disputesLost", getU64Encoder()],
      ["claimsExpired", getU64Encoder()],
      ["totalCancelled", getU64Encoder()],
      ["lastUpdated", getI64Encoder()],
      ["bump", getU8Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: AGENT_STATS_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link AgentStats} account data. */
export function getAgentStatsDecoder(): FixedSizeDecoder<AgentStats> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["agent", getAddressDecoder()],
    ["tasksRejected", getU64Decoder()],
    ["disputesWon", getU64Decoder()],
    ["disputesLost", getU64Decoder()],
    ["claimsExpired", getU64Decoder()],
    ["totalCancelled", getU64Decoder()],
    ["lastUpdated", getI64Decoder()],
    ["bump", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

/** Gets the codec for {@link AgentStats} account data. */
export function getAgentStatsCodec(): FixedSizeCodec<
  AgentStatsArgs,
  AgentStats
> {
  return combineCodec(getAgentStatsEncoder(), getAgentStatsDecoder());
}

export function decodeAgentStats<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<AgentStats, TAddress>;
export function decodeAgentStats<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<AgentStats, TAddress>;
export function decodeAgentStats<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<AgentStats, TAddress> | MaybeAccount<AgentStats, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getAgentStatsDecoder(),
  );
}

export async function fetchAgentStats<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<AgentStats, TAddress>> {
  const maybeAccount = await fetchMaybeAgentStats(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeAgentStats<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<AgentStats, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeAgentStats(maybeAccount);
}

export async function fetchAllAgentStats(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<AgentStats>[]> {
  const maybeAccounts = await fetchAllMaybeAgentStats(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeAgentStats(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<AgentStats>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeAgentStats(maybeAccount));
}

export function getAgentStatsSize(): number {
  return 121;
}
