/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";

export const MODERATION_CONFIG_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([20, 180, 54, 96, 191, 141, 52, 148]);

export function getModerationConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(MODERATION_CONFIG_DISCRIMINATOR);
}

export type ModerationConfig = {
  discriminator: ReadonlyUint8Array;
  /** Protocol authority that configured this moderation gate. */
  authority: Address;
  /** Signer allowed to record moderation attestations. */
  moderationAuthority: Address;
  /** Whether task job-spec publication requires moderation attestations. */
  enabled: boolean;
  /** Creation timestamp. */
  createdAt: bigint;
  /**
   * Last update timestamp. ALSO the P1.3 liveness heartbeat: bumped by
   * `configure_task_moderation` and `moderation_heartbeat`; when it goes stale
   * past the liveness window the consumption gates relax to moderation-optional
   * (`docs/MODERATION_LIVENESS.md`).
   */
  updatedAt: bigint;
  /** PDA bump. */
  bump: number;
  /**
   * Reserved bytes. `[0..4]` carry the P1.3 liveness window (LE `u32` seconds,
   * 0 = default 90 days) via the accessors below — the value-only,
   * size-identical reserved-carve precedent (ModerationAttestor P1.2). `[4..6]`
   * MUST stay zeroed.
   */
  reserved: ReadonlyUint8Array;
};

export type ModerationConfigArgs = {
  /** Protocol authority that configured this moderation gate. */
  authority: Address;
  /** Signer allowed to record moderation attestations. */
  moderationAuthority: Address;
  /** Whether task job-spec publication requires moderation attestations. */
  enabled: boolean;
  /** Creation timestamp. */
  createdAt: number | bigint;
  /**
   * Last update timestamp. ALSO the P1.3 liveness heartbeat: bumped by
   * `configure_task_moderation` and `moderation_heartbeat`; when it goes stale
   * past the liveness window the consumption gates relax to moderation-optional
   * (`docs/MODERATION_LIVENESS.md`).
   */
  updatedAt: number | bigint;
  /** PDA bump. */
  bump: number;
  /**
   * Reserved bytes. `[0..4]` carry the P1.3 liveness window (LE `u32` seconds,
   * 0 = default 90 days) via the accessors below — the value-only,
   * size-identical reserved-carve precedent (ModerationAttestor P1.2). `[4..6]`
   * MUST stay zeroed.
   */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link ModerationConfigArgs} account data. */
export function getModerationConfigEncoder(): FixedSizeEncoder<ModerationConfigArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["authority", getAddressEncoder()],
      ["moderationAuthority", getAddressEncoder()],
      ["enabled", getBooleanEncoder()],
      ["createdAt", getI64Encoder()],
      ["updatedAt", getI64Encoder()],
      ["bump", getU8Encoder()],
      ["reserved", getFixedBytesEncoder(6, "reserved")],
    ]),
    (value) => ({ ...value, discriminator: MODERATION_CONFIG_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link ModerationConfig} account data. */
export function getModerationConfigDecoder(): FixedSizeDecoder<ModerationConfig> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["moderationAuthority", getAddressDecoder()],
    ["enabled", getBooleanDecoder()],
    ["createdAt", getI64Decoder()],
    ["updatedAt", getI64Decoder()],
    ["bump", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 6)],
  ]);
}

/** Gets the codec for {@link ModerationConfig} account data. */
export function getModerationConfigCodec(): FixedSizeCodec<
  ModerationConfigArgs,
  ModerationConfig
> {
  return combineCodec(
    getModerationConfigEncoder(),
    getModerationConfigDecoder(),
  );
}

export function decodeModerationConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<ModerationConfig, TAddress>;
export function decodeModerationConfig<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<ModerationConfig, TAddress>;
export function decodeModerationConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<ModerationConfig, TAddress>
  | MaybeAccount<ModerationConfig, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getModerationConfigDecoder(),
  );
}

export async function fetchModerationConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ModerationConfig, TAddress>> {
  const maybeAccount = await fetchMaybeModerationConfig(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeModerationConfig<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ModerationConfig, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeModerationConfig(maybeAccount);
}

export async function fetchAllModerationConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ModerationConfig>[]> {
  const maybeAccounts = await fetchAllMaybeModerationConfig(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeModerationConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ModerationConfig>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeModerationConfig(maybeAccount),
  );
}

export function getModerationConfigSize(): number {
  return 96;
}
