/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";

export const LISTING_MODERATION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([157, 129, 173, 120, 123, 143, 215, 24]);

export function getListingModerationDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(LISTING_MODERATION_DISCRIMINATOR);
}

export type ListingModeration = {
  discriminator: ReadonlyUint8Array;
  /** Service listing this decision applies to. */
  listing: Address;
  /** Provider agent of the listing at decision time. */
  providerAgent: Address;
  /** Job-spec hash approved/held/rejected. */
  jobSpecHash: ReadonlyUint8Array;
  /** One of `task_moderation_status::*`. */
  status: number;
  /** Normalized 0-100 risk score. */
  riskScore: number;
  /** Bitmask of scanner categories. */
  categoryMask: bigint;
  /** Hash of the moderation policy/threshold version. */
  policyHash: ReadonlyUint8Array;
  /** Hash of the scanner/model version bundle. */
  scannerHash: ReadonlyUint8Array;
  /** When the decision was recorded. */
  recordedAt: bigint;
  /** Optional expiry timestamp. Zero means no expiry. */
  expiresAt: bigint;
  /** Signer that recorded the decision (the moderation authority). */
  moderator: Address;
  /** PDA bump. */
  bump: number;
  /** Reserved for future attestation metadata. */
  reserved: ReadonlyUint8Array;
};

export type ListingModerationArgs = {
  /** Service listing this decision applies to. */
  listing: Address;
  /** Provider agent of the listing at decision time. */
  providerAgent: Address;
  /** Job-spec hash approved/held/rejected. */
  jobSpecHash: ReadonlyUint8Array;
  /** One of `task_moderation_status::*`. */
  status: number;
  /** Normalized 0-100 risk score. */
  riskScore: number;
  /** Bitmask of scanner categories. */
  categoryMask: number | bigint;
  /** Hash of the moderation policy/threshold version. */
  policyHash: ReadonlyUint8Array;
  /** Hash of the scanner/model version bundle. */
  scannerHash: ReadonlyUint8Array;
  /** When the decision was recorded. */
  recordedAt: number | bigint;
  /** Optional expiry timestamp. Zero means no expiry. */
  expiresAt: number | bigint;
  /** Signer that recorded the decision (the moderation authority). */
  moderator: Address;
  /** PDA bump. */
  bump: number;
  /** Reserved for future attestation metadata. */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link ListingModerationArgs} account data. */
export function getListingModerationEncoder(): FixedSizeEncoder<ListingModerationArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["listing", getAddressEncoder()],
      ["providerAgent", getAddressEncoder()],
      ["jobSpecHash", getFixedBytesEncoder(32, "jobSpecHash")],
      ["status", getU8Encoder()],
      ["riskScore", getU8Encoder()],
      ["categoryMask", getU64Encoder()],
      ["policyHash", getFixedBytesEncoder(32, "policyHash")],
      ["scannerHash", getFixedBytesEncoder(32, "scannerHash")],
      ["recordedAt", getI64Encoder()],
      ["expiresAt", getI64Encoder()],
      ["moderator", getAddressEncoder()],
      ["bump", getU8Encoder()],
      ["reserved", getFixedBytesEncoder(7, "reserved")],
    ]),
    (value) => ({ ...value, discriminator: LISTING_MODERATION_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link ListingModeration} account data. */
export function getListingModerationDecoder(): FixedSizeDecoder<ListingModeration> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["listing", getAddressDecoder()],
    ["providerAgent", getAddressDecoder()],
    ["jobSpecHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["status", getU8Decoder()],
    ["riskScore", getU8Decoder()],
    ["categoryMask", getU64Decoder()],
    ["policyHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["scannerHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["recordedAt", getI64Decoder()],
    ["expiresAt", getI64Decoder()],
    ["moderator", getAddressDecoder()],
    ["bump", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 7)],
  ]);
}

/** Gets the codec for {@link ListingModeration} account data. */
export function getListingModerationCodec(): FixedSizeCodec<
  ListingModerationArgs,
  ListingModeration
> {
  return combineCodec(
    getListingModerationEncoder(),
    getListingModerationDecoder(),
  );
}

export function decodeListingModeration<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<ListingModeration, TAddress>;
export function decodeListingModeration<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<ListingModeration, TAddress>;
export function decodeListingModeration<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<ListingModeration, TAddress>
  | MaybeAccount<ListingModeration, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getListingModerationDecoder(),
  );
}

export async function fetchListingModeration<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ListingModeration, TAddress>> {
  const maybeAccount = await fetchMaybeListingModeration(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeListingModeration<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ListingModeration, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeListingModeration(maybeAccount);
}

export async function fetchAllListingModeration(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ListingModeration>[]> {
  const maybeAccounts = await fetchAllMaybeListingModeration(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeListingModeration(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ListingModeration>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeListingModeration(maybeAccount),
  );
}

export function getListingModerationSize(): number {
  return 234;
}
