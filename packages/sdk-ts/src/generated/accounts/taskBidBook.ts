/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";
import {
  getBidBookStateDecoder,
  getBidBookStateEncoder,
  getMatchingPolicyDecoder,
  getMatchingPolicyEncoder,
  getWeightedScoreWeightsDecoder,
  getWeightedScoreWeightsEncoder,
  type BidBookState,
  type BidBookStateArgs,
  type MatchingPolicy,
  type MatchingPolicyArgs,
  type WeightedScoreWeights,
  type WeightedScoreWeightsArgs,
} from "../types";

export const TASK_BID_BOOK_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  65, 139, 202, 158, 184, 110, 242, 52,
]);

export function getTaskBidBookDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(TASK_BID_BOOK_DISCRIMINATOR);
}

export type TaskBidBook = {
  discriminator: ReadonlyUint8Array;
  task: Address;
  state: BidBookState;
  policy: MatchingPolicy;
  weights: WeightedScoreWeights;
  acceptedBid: Option<Address>;
  version: bigint;
  totalBids: number;
  activeBids: number;
  createdAt: bigint;
  updatedAt: bigint;
  bump: number;
  /**
   * Cached deterministic policy winner. `Pubkey::default()` = no tracked
   * winner. Maintained incrementally by every bid mutation so `accept_bid`
   * never has to enumerate competitors (the O(1)-accept redesign,
   * docs/design/bid-accept-o1-redesign.md). Fields appended for layout
   * stability; the revision-5 cutover preflight requires zero live books.
   */
  bestBid: Address;
  /**
   * Score components of `best_bid`, snapshotted at install so comparisons
   * never need to load the incumbent's account. Scores are pure functions
   * of these values plus the frozen `score_window_secs`.
   */
  bestRewardLamports: bigint;
  bestEtaSeconds: number;
  bestConfidenceBps: number;
  bestReputationBps: number;
  /**
   * Set when the tracked winner is removed (cancel/expiry/demotion).
   * `accept_bid` is blocked until the re-promotion grace elapses so every
   * remaining bidder gets a fair window to promote before acceptance.
   * 0 = fresh.
   */
  winnerStaleSince: bigint;
  /**
   * Frozen WeightedScore eta-normalization window, recorded once at
   * `initialize_bid_book` as `task.deadline - now`. Freezing it makes all
   * policy orderings pure functions of immutable bid fields (the winner no
   * longer depends on when the creator calls accept).
   */
  scoreWindowSecs: number;
};

export type TaskBidBookArgs = {
  task: Address;
  state: BidBookStateArgs;
  policy: MatchingPolicyArgs;
  weights: WeightedScoreWeightsArgs;
  acceptedBid: OptionOrNullable<Address>;
  version: number | bigint;
  totalBids: number;
  activeBids: number;
  createdAt: number | bigint;
  updatedAt: number | bigint;
  bump: number;
  /**
   * Cached deterministic policy winner. `Pubkey::default()` = no tracked
   * winner. Maintained incrementally by every bid mutation so `accept_bid`
   * never has to enumerate competitors (the O(1)-accept redesign,
   * docs/design/bid-accept-o1-redesign.md). Fields appended for layout
   * stability; the revision-5 cutover preflight requires zero live books.
   */
  bestBid: Address;
  /**
   * Score components of `best_bid`, snapshotted at install so comparisons
   * never need to load the incumbent's account. Scores are pure functions
   * of these values plus the frozen `score_window_secs`.
   */
  bestRewardLamports: number | bigint;
  bestEtaSeconds: number;
  bestConfidenceBps: number;
  bestReputationBps: number;
  /**
   * Set when the tracked winner is removed (cancel/expiry/demotion).
   * `accept_bid` is blocked until the re-promotion grace elapses so every
   * remaining bidder gets a fair window to promote before acceptance.
   * 0 = fresh.
   */
  winnerStaleSince: number | bigint;
  /**
   * Frozen WeightedScore eta-normalization window, recorded once at
   * `initialize_bid_book` as `task.deadline - now`. Freezing it makes all
   * policy orderings pure functions of immutable bid fields (the winner no
   * longer depends on when the creator calls accept).
   */
  scoreWindowSecs: number;
};

/** Gets the encoder for {@link TaskBidBookArgs} account data. */
export function getTaskBidBookEncoder(): Encoder<TaskBidBookArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["task", getAddressEncoder()],
      ["state", getBidBookStateEncoder()],
      ["policy", getMatchingPolicyEncoder()],
      ["weights", getWeightedScoreWeightsEncoder()],
      ["acceptedBid", getOptionEncoder(getAddressEncoder())],
      ["version", getU64Encoder()],
      ["totalBids", getU32Encoder()],
      ["activeBids", getU16Encoder()],
      ["createdAt", getI64Encoder()],
      ["updatedAt", getI64Encoder()],
      ["bump", getU8Encoder()],
      ["bestBid", getAddressEncoder()],
      ["bestRewardLamports", getU64Encoder()],
      ["bestEtaSeconds", getU32Encoder()],
      ["bestConfidenceBps", getU16Encoder()],
      ["bestReputationBps", getU16Encoder()],
      ["winnerStaleSince", getI64Encoder()],
      ["scoreWindowSecs", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: TASK_BID_BOOK_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link TaskBidBook} account data. */
export function getTaskBidBookDecoder(): Decoder<TaskBidBook> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["task", getAddressDecoder()],
    ["state", getBidBookStateDecoder()],
    ["policy", getMatchingPolicyDecoder()],
    ["weights", getWeightedScoreWeightsDecoder()],
    ["acceptedBid", getOptionDecoder(getAddressDecoder())],
    ["version", getU64Decoder()],
    ["totalBids", getU32Decoder()],
    ["activeBids", getU16Decoder()],
    ["createdAt", getI64Decoder()],
    ["updatedAt", getI64Decoder()],
    ["bump", getU8Decoder()],
    ["bestBid", getAddressDecoder()],
    ["bestRewardLamports", getU64Decoder()],
    ["bestEtaSeconds", getU32Decoder()],
    ["bestConfidenceBps", getU16Decoder()],
    ["bestReputationBps", getU16Decoder()],
    ["winnerStaleSince", getI64Decoder()],
    ["scoreWindowSecs", getU32Decoder()],
  ]);
}

/** Gets the codec for {@link TaskBidBook} account data. */
export function getTaskBidBookCodec(): Codec<TaskBidBookArgs, TaskBidBook> {
  return combineCodec(getTaskBidBookEncoder(), getTaskBidBookDecoder());
}

export function decodeTaskBidBook<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<TaskBidBook, TAddress>;
export function decodeTaskBidBook<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<TaskBidBook, TAddress>;
export function decodeTaskBidBook<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<TaskBidBook, TAddress> | MaybeAccount<TaskBidBook, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getTaskBidBookDecoder(),
  );
}

export async function fetchTaskBidBook<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<TaskBidBook, TAddress>> {
  const maybeAccount = await fetchMaybeTaskBidBook(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTaskBidBook<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<TaskBidBook, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTaskBidBook(maybeAccount);
}

export async function fetchAllTaskBidBook(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<TaskBidBook>[]> {
  const maybeAccounts = await fetchAllMaybeTaskBidBook(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTaskBidBook(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<TaskBidBook>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeTaskBidBook(maybeAccount));
}
