/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";

export const MODERATION_ATTESTOR_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([52, 62, 194, 121, 157, 169, 40, 178]);

export function getModerationAttestorDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(MODERATION_ATTESTOR_DISCRIMINATOR);
}

export type ModerationAttestor = {
  discriminator: ReadonlyUint8Array;
  /** The wallet authorized to record moderation attestations. */
  attestor: Address;
  /**
   * The wallet that created this entry (audit trail). Authority-deputized entries
   * carry the moderation authority; self-registered entries (P1.2) carry the
   * attestor itself — `assigned_by == attestor` distinguishes the two.
   */
  assignedBy: Address;
  /** Unix timestamp the assignment was created. */
  assignedAt: bigint;
  /** PDA bump. */
  bump: number;
  /**
   * Registration bond deposited on the PDA (`REGISTRATION_BOND_LAMPORTS` at
   * self-registration; 0 for authority-deputized/legacy entries). Refunded in full
   * at `finalize_attestor_exit` — never confiscatable.
   */
  bondLamports: bigint;
  /** When the attestor self-registered (0 for authority-deputized/legacy entries). */
  registeredAt: bigint;
  /**
   * Exit-window start set by `request_attestor_exit` (0 = not exiting). While
   * non-zero the attestor is rejected at the record AND consumption gates; the
   * window closes at request, not finalize.
   */
  exitAt: bigint;
  /** Reserved for future metadata. MUST stay zeroed. */
  reserved: ReadonlyUint8Array;
};

export type ModerationAttestorArgs = {
  /** The wallet authorized to record moderation attestations. */
  attestor: Address;
  /**
   * The wallet that created this entry (audit trail). Authority-deputized entries
   * carry the moderation authority; self-registered entries (P1.2) carry the
   * attestor itself — `assigned_by == attestor` distinguishes the two.
   */
  assignedBy: Address;
  /** Unix timestamp the assignment was created. */
  assignedAt: number | bigint;
  /** PDA bump. */
  bump: number;
  /**
   * Registration bond deposited on the PDA (`REGISTRATION_BOND_LAMPORTS` at
   * self-registration; 0 for authority-deputized/legacy entries). Refunded in full
   * at `finalize_attestor_exit` — never confiscatable.
   */
  bondLamports: number | bigint;
  /** When the attestor self-registered (0 for authority-deputized/legacy entries). */
  registeredAt: number | bigint;
  /**
   * Exit-window start set by `request_attestor_exit` (0 = not exiting). While
   * non-zero the attestor is rejected at the record AND consumption gates; the
   * window closes at request, not finalize.
   */
  exitAt: number | bigint;
  /** Reserved for future metadata. MUST stay zeroed. */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link ModerationAttestorArgs} account data. */
export function getModerationAttestorEncoder(): FixedSizeEncoder<ModerationAttestorArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["attestor", getAddressEncoder()],
      ["assignedBy", getAddressEncoder()],
      ["assignedAt", getI64Encoder()],
      ["bump", getU8Encoder()],
      ["bondLamports", getU64Encoder()],
      ["registeredAt", getI64Encoder()],
      ["exitAt", getI64Encoder()],
      ["reserved", getFixedBytesEncoder(8, "reserved")],
    ]),
    (value) => ({ ...value, discriminator: MODERATION_ATTESTOR_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link ModerationAttestor} account data. */
export function getModerationAttestorDecoder(): FixedSizeDecoder<ModerationAttestor> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["attestor", getAddressDecoder()],
    ["assignedBy", getAddressDecoder()],
    ["assignedAt", getI64Decoder()],
    ["bump", getU8Decoder()],
    ["bondLamports", getU64Decoder()],
    ["registeredAt", getI64Decoder()],
    ["exitAt", getI64Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

/** Gets the codec for {@link ModerationAttestor} account data. */
export function getModerationAttestorCodec(): FixedSizeCodec<
  ModerationAttestorArgs,
  ModerationAttestor
> {
  return combineCodec(
    getModerationAttestorEncoder(),
    getModerationAttestorDecoder(),
  );
}

export function decodeModerationAttestor<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<ModerationAttestor, TAddress>;
export function decodeModerationAttestor<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<ModerationAttestor, TAddress>;
export function decodeModerationAttestor<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<ModerationAttestor, TAddress>
  | MaybeAccount<ModerationAttestor, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getModerationAttestorDecoder(),
  );
}

export async function fetchModerationAttestor<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ModerationAttestor, TAddress>> {
  const maybeAccount = await fetchMaybeModerationAttestor(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeModerationAttestor<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ModerationAttestor, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeModerationAttestor(maybeAccount);
}

export async function fetchAllModerationAttestor(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ModerationAttestor>[]> {
  const maybeAccounts = await fetchAllMaybeModerationAttestor(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeModerationAttestor(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ModerationAttestor>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeModerationAttestor(maybeAccount),
  );
}

export function getModerationAttestorSize(): number {
  return 113;
}
