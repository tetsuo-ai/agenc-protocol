/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";

import {
  getBorshStringDecoder,
  getBorshStringEncoder,
} from "../codecs/borshString";

export const DEFAULT_TRUST_LIST_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([142, 83, 121, 72, 66, 39, 149, 44]);

export function getDefaultTrustListDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(DEFAULT_TRUST_LIST_DISCRIMINATOR);
}

export type DefaultTrustList = {
  discriminator: ReadonlyUint8Array;
  /** Content hash of the current list artifact. */
  listHash: ReadonlyUint8Array;
  /** URI of the list artifact. */
  listUri: string;
  /** Monotonic version, bumped on every update (rollback detection). */
  version: bigint;
  /** Deadman timestamp: when the list was last updated. */
  updatedAt: bigint;
  /** The fee-paying signer of the last update. */
  updatedBy: Address;
  /** PDA bump. */
  bump: number;
  /** Reserved for future trust-list metadata. MUST stay zeroed. */
  reserved: ReadonlyUint8Array;
};

export type DefaultTrustListArgs = {
  /** Content hash of the current list artifact. */
  listHash: ReadonlyUint8Array;
  /** URI of the list artifact. */
  listUri: string;
  /** Monotonic version, bumped on every update (rollback detection). */
  version: number | bigint;
  /** Deadman timestamp: when the list was last updated. */
  updatedAt: number | bigint;
  /** The fee-paying signer of the last update. */
  updatedBy: Address;
  /** PDA bump. */
  bump: number;
  /** Reserved for future trust-list metadata. MUST stay zeroed. */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link DefaultTrustListArgs} account data. */
export function getDefaultTrustListEncoder(): Encoder<DefaultTrustListArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["listHash", getFixedBytesEncoder(32, "listHash")],
      [
        "listUri",
        addEncoderSizePrefix(getBorshStringEncoder(), getU32Encoder()),
      ],
      ["version", getU64Encoder()],
      ["updatedAt", getI64Encoder()],
      ["updatedBy", getAddressEncoder()],
      ["bump", getU8Encoder()],
      ["reserved", getFixedBytesEncoder(16, "reserved")],
    ]),
    (value) => ({ ...value, discriminator: DEFAULT_TRUST_LIST_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link DefaultTrustList} account data. */
export function getDefaultTrustListDecoder(): Decoder<DefaultTrustList> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["listHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["listUri", addDecoderSizePrefix(getBorshStringDecoder(), getU32Decoder())],
    ["version", getU64Decoder()],
    ["updatedAt", getI64Decoder()],
    ["updatedBy", getAddressDecoder()],
    ["bump", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 16)],
  ]);
}

/** Gets the codec for {@link DefaultTrustList} account data. */
export function getDefaultTrustListCodec(): Codec<
  DefaultTrustListArgs,
  DefaultTrustList
> {
  return combineCodec(
    getDefaultTrustListEncoder(),
    getDefaultTrustListDecoder(),
  );
}

export function decodeDefaultTrustList<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<DefaultTrustList, TAddress>;
export function decodeDefaultTrustList<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<DefaultTrustList, TAddress>;
export function decodeDefaultTrustList<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<DefaultTrustList, TAddress>
  | MaybeAccount<DefaultTrustList, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getDefaultTrustListDecoder(),
  );
}

export async function fetchDefaultTrustList<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<DefaultTrustList, TAddress>> {
  const maybeAccount = await fetchMaybeDefaultTrustList(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeDefaultTrustList<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<DefaultTrustList, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeDefaultTrustList(maybeAccount);
}

export async function fetchAllDefaultTrustList(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<DefaultTrustList>[]> {
  const maybeAccounts = await fetchAllMaybeDefaultTrustList(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeDefaultTrustList(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<DefaultTrustList>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeDefaultTrustList(maybeAccount),
  );
}
