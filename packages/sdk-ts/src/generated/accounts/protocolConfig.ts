/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";

export const PROTOCOL_CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [207, 91, 250, 28, 152, 179, 215, 209],
);

export function getProtocolConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(PROTOCOL_CONFIG_DISCRIMINATOR);
}

export type ProtocolConfig = {
  discriminator: ReadonlyUint8Array;
  /**
   * Protocol authority
   * Note: Cannot be updated after initialization.
   */
  authority: Address;
  /**
   * Treasury address for protocol fees
   * Can be updated via multisig-gated `update_treasury`.
   */
  treasury: Address;
  /**
   * Approval-percentage threshold used to interpret persisted dispute outcomes.
   * Current resolver rulings encode approved/rejected as 100%/0%; historical
   * disputes may contain vote totals from the retired arbiter model.
   */
  disputeThreshold: number;
  /** Protocol fee in basis points (1/100th of a percent) */
  protocolFeeBps: number;
  /**
   * Governance vote-weight basis retained under its historical field name.
   * Agent registration uses `min_agent_stake`.
   */
  minArbiterStake: bigint;
  /** Minimum stake required to register as agent */
  minAgentStake: bigint;
  /** Max duration (seconds) a claim can stay active without completion */
  maxClaimDuration: bigint;
  /** Max duration (seconds) a dispute can remain active */
  maxDisputeDuration: bigint;
  /** Total registered agents */
  totalAgents: bigint;
  /** Total tasks created */
  totalTasks: bigint;
  /** Total tasks completed (saturating telemetry) */
  completedTasks: bigint;
  /**
   * Total SOL value distributed, in lamports (excludes SPL-token base units;
   * saturating telemetry)
   */
  totalValueDistributed: bigint;
  /** Bump seed for PDA */
  bump: number;
  /** Multisig threshold */
  multisigThreshold: number;
  /** Length of configured multisig owners */
  multisigOwnersLen: number;
  /** Minimum cooldown between task creations per authority wallet (seconds, 0 = disabled) */
  taskCreationCooldown: bigint;
  /** Maximum tasks an authority wallet can create per 24h window (0 = unlimited) */
  maxTasksPer24h: number;
  /** Minimum cooldown between dispute initiations per authority wallet (seconds, 0 = disabled) */
  disputeInitiationCooldown: bigint;
  /** Maximum disputes an authority wallet can initiate per 24h window (0 = unlimited) */
  maxDisputesPer24h: number;
  /** Minimum stake required to initiate a dispute (griefing resistance) */
  minStakeForDispute: bigint;
  /** Percentage of stake slashed on losing dispute (0-100) */
  slashPercentage: number;
  /** Cooldown between state updates per agent (seconds, 0 = disabled) (fix #415) */
  stateUpdateCooldown: bigint;
  /**
   * Initial resolver-action window for disputes in seconds (default: 24 hours).
   * No dispute voting occurs on the current assigned-resolver path.
   */
  votingPeriod: bigint;
  /** Current protocol version (for upgrades) */
  protocolVersion: number;
  /** Minimum supported version for backward compatibility */
  minSupportedVersion: number;
  /** Emergency global pause. When true, version-gated mutable protocol paths fail closed. */
  protocolPaused: boolean;
  /** Bitmask of disabled task types. Bit index matches `TaskType` repr. */
  disabledTaskTypeMask: number;
  /**
   * Multisig owners for admin-gated protocol changes.
   *
   * Updated via multisig-gated `update_multisig` with strict validation:
   * - owner keys must be unique and non-default
   * - threshold must satisfy 0 < threshold < owners_len
   * - update tx must include threshold signers from the new owner set
   *
   * Only the first `multisig_owners_len` entries are valid; remaining slots
   * are always `Pubkey::default()`.
   */
  multisigOwners: Array<Address>;
  /**
   * Deployed instruction-surface revision stamp.
   *
   * APPEND-ONLY: this is the only field after `multisig_owners`, so the 349-byte
   * historical pre-P6.5 prefix stays valid. A legacy account is migrated up to
   * the new size by `migrate_protocol` (realloc +
   * zero-init), which lands this at `0` = "surface not yet stamped". An operator
   * then establishes the current revision through the atomic
   * `stamp_release_surface` release boundary. `update_launch_controls` may
   * preserve the current value or select an older conservative revision, but
   * cannot establish the current production revision. In the full production
   * build, an older/unstamped revision must remain paused; unpausing requires
   * that CURRENT was already established by the atomic stamp.
   *
   * Semantics:
   * - `0`  → surface unstamped (treat as the conservative canary surface;
   * clients should fall back to capability probing).
   * - `>0` → the operator-declared surface revision; the SDK maps it to a typed
   * capability set (`SURFACE_REVISION_FULL` = full-surface stamp;
   * historical Phase-9 count was 84 ix; live surface is later batches).
   */
  surfaceRevision: number;
};

export type ProtocolConfigArgs = {
  /**
   * Protocol authority
   * Note: Cannot be updated after initialization.
   */
  authority: Address;
  /**
   * Treasury address for protocol fees
   * Can be updated via multisig-gated `update_treasury`.
   */
  treasury: Address;
  /**
   * Approval-percentage threshold used to interpret persisted dispute outcomes.
   * Current resolver rulings encode approved/rejected as 100%/0%; historical
   * disputes may contain vote totals from the retired arbiter model.
   */
  disputeThreshold: number;
  /** Protocol fee in basis points (1/100th of a percent) */
  protocolFeeBps: number;
  /**
   * Governance vote-weight basis retained under its historical field name.
   * Agent registration uses `min_agent_stake`.
   */
  minArbiterStake: number | bigint;
  /** Minimum stake required to register as agent */
  minAgentStake: number | bigint;
  /** Max duration (seconds) a claim can stay active without completion */
  maxClaimDuration: number | bigint;
  /** Max duration (seconds) a dispute can remain active */
  maxDisputeDuration: number | bigint;
  /** Total registered agents */
  totalAgents: number | bigint;
  /** Total tasks created */
  totalTasks: number | bigint;
  /** Total tasks completed (saturating telemetry) */
  completedTasks: number | bigint;
  /**
   * Total SOL value distributed, in lamports (excludes SPL-token base units;
   * saturating telemetry)
   */
  totalValueDistributed: number | bigint;
  /** Bump seed for PDA */
  bump: number;
  /** Multisig threshold */
  multisigThreshold: number;
  /** Length of configured multisig owners */
  multisigOwnersLen: number;
  /** Minimum cooldown between task creations per authority wallet (seconds, 0 = disabled) */
  taskCreationCooldown: number | bigint;
  /** Maximum tasks an authority wallet can create per 24h window (0 = unlimited) */
  maxTasksPer24h: number;
  /** Minimum cooldown between dispute initiations per authority wallet (seconds, 0 = disabled) */
  disputeInitiationCooldown: number | bigint;
  /** Maximum disputes an authority wallet can initiate per 24h window (0 = unlimited) */
  maxDisputesPer24h: number;
  /** Minimum stake required to initiate a dispute (griefing resistance) */
  minStakeForDispute: number | bigint;
  /** Percentage of stake slashed on losing dispute (0-100) */
  slashPercentage: number;
  /** Cooldown between state updates per agent (seconds, 0 = disabled) (fix #415) */
  stateUpdateCooldown: number | bigint;
  /**
   * Initial resolver-action window for disputes in seconds (default: 24 hours).
   * No dispute voting occurs on the current assigned-resolver path.
   */
  votingPeriod: number | bigint;
  /** Current protocol version (for upgrades) */
  protocolVersion: number;
  /** Minimum supported version for backward compatibility */
  minSupportedVersion: number;
  /** Emergency global pause. When true, version-gated mutable protocol paths fail closed. */
  protocolPaused: boolean;
  /** Bitmask of disabled task types. Bit index matches `TaskType` repr. */
  disabledTaskTypeMask: number;
  /**
   * Multisig owners for admin-gated protocol changes.
   *
   * Updated via multisig-gated `update_multisig` with strict validation:
   * - owner keys must be unique and non-default
   * - threshold must satisfy 0 < threshold < owners_len
   * - update tx must include threshold signers from the new owner set
   *
   * Only the first `multisig_owners_len` entries are valid; remaining slots
   * are always `Pubkey::default()`.
   */
  multisigOwners: Array<Address>;
  /**
   * Deployed instruction-surface revision stamp.
   *
   * APPEND-ONLY: this is the only field after `multisig_owners`, so the 349-byte
   * historical pre-P6.5 prefix stays valid. A legacy account is migrated up to
   * the new size by `migrate_protocol` (realloc +
   * zero-init), which lands this at `0` = "surface not yet stamped". An operator
   * then establishes the current revision through the atomic
   * `stamp_release_surface` release boundary. `update_launch_controls` may
   * preserve the current value or select an older conservative revision, but
   * cannot establish the current production revision. In the full production
   * build, an older/unstamped revision must remain paused; unpausing requires
   * that CURRENT was already established by the atomic stamp.
   *
   * Semantics:
   * - `0`  → surface unstamped (treat as the conservative canary surface;
   * clients should fall back to capability probing).
   * - `>0` → the operator-declared surface revision; the SDK maps it to a typed
   * capability set (`SURFACE_REVISION_FULL` = full-surface stamp;
   * historical Phase-9 count was 84 ix; live surface is later batches).
   */
  surfaceRevision: number;
};

/** Gets the encoder for {@link ProtocolConfigArgs} account data. */
export function getProtocolConfigEncoder(): FixedSizeEncoder<ProtocolConfigArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["authority", getAddressEncoder()],
      ["treasury", getAddressEncoder()],
      ["disputeThreshold", getU8Encoder()],
      ["protocolFeeBps", getU16Encoder()],
      ["minArbiterStake", getU64Encoder()],
      ["minAgentStake", getU64Encoder()],
      ["maxClaimDuration", getI64Encoder()],
      ["maxDisputeDuration", getI64Encoder()],
      ["totalAgents", getU64Encoder()],
      ["totalTasks", getU64Encoder()],
      ["completedTasks", getU64Encoder()],
      ["totalValueDistributed", getU64Encoder()],
      ["bump", getU8Encoder()],
      ["multisigThreshold", getU8Encoder()],
      ["multisigOwnersLen", getU8Encoder()],
      ["taskCreationCooldown", getI64Encoder()],
      ["maxTasksPer24h", getU8Encoder()],
      ["disputeInitiationCooldown", getI64Encoder()],
      ["maxDisputesPer24h", getU8Encoder()],
      ["minStakeForDispute", getU64Encoder()],
      ["slashPercentage", getU8Encoder()],
      ["stateUpdateCooldown", getI64Encoder()],
      ["votingPeriod", getI64Encoder()],
      ["protocolVersion", getU8Encoder()],
      ["minSupportedVersion", getU8Encoder()],
      ["protocolPaused", getBooleanEncoder()],
      ["disabledTaskTypeMask", getU8Encoder()],
      ["multisigOwners", getArrayEncoder(getAddressEncoder(), { size: 5 })],
      ["surfaceRevision", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PROTOCOL_CONFIG_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link ProtocolConfig} account data. */
export function getProtocolConfigDecoder(): FixedSizeDecoder<ProtocolConfig> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["treasury", getAddressDecoder()],
    ["disputeThreshold", getU8Decoder()],
    ["protocolFeeBps", getU16Decoder()],
    ["minArbiterStake", getU64Decoder()],
    ["minAgentStake", getU64Decoder()],
    ["maxClaimDuration", getI64Decoder()],
    ["maxDisputeDuration", getI64Decoder()],
    ["totalAgents", getU64Decoder()],
    ["totalTasks", getU64Decoder()],
    ["completedTasks", getU64Decoder()],
    ["totalValueDistributed", getU64Decoder()],
    ["bump", getU8Decoder()],
    ["multisigThreshold", getU8Decoder()],
    ["multisigOwnersLen", getU8Decoder()],
    ["taskCreationCooldown", getI64Decoder()],
    ["maxTasksPer24h", getU8Decoder()],
    ["disputeInitiationCooldown", getI64Decoder()],
    ["maxDisputesPer24h", getU8Decoder()],
    ["minStakeForDispute", getU64Decoder()],
    ["slashPercentage", getU8Decoder()],
    ["stateUpdateCooldown", getI64Decoder()],
    ["votingPeriod", getI64Decoder()],
    ["protocolVersion", getU8Decoder()],
    ["minSupportedVersion", getU8Decoder()],
    ["protocolPaused", getBooleanDecoder()],
    ["disabledTaskTypeMask", getU8Decoder()],
    ["multisigOwners", getArrayDecoder(getAddressDecoder(), { size: 5 })],
    ["surfaceRevision", getU16Decoder()],
  ]);
}

/** Gets the codec for {@link ProtocolConfig} account data. */
export function getProtocolConfigCodec(): FixedSizeCodec<
  ProtocolConfigArgs,
  ProtocolConfig
> {
  return combineCodec(getProtocolConfigEncoder(), getProtocolConfigDecoder());
}

export function decodeProtocolConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<ProtocolConfig, TAddress>;
export function decodeProtocolConfig<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<ProtocolConfig, TAddress>;
export function decodeProtocolConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<ProtocolConfig, TAddress> | MaybeAccount<ProtocolConfig, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getProtocolConfigDecoder(),
  );
}

export async function fetchProtocolConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ProtocolConfig, TAddress>> {
  const maybeAccount = await fetchMaybeProtocolConfig(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeProtocolConfig<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ProtocolConfig, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeProtocolConfig(maybeAccount);
}

export async function fetchAllProtocolConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ProtocolConfig>[]> {
  const maybeAccounts = await fetchAllMaybeProtocolConfig(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeProtocolConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ProtocolConfig>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeProtocolConfig(maybeAccount),
  );
}

export function getProtocolConfigSize(): number {
  return 351;
}
