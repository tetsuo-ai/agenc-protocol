/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

import {
  getBorshStringDecoder,
  getBorshStringEncoder,
} from "../codecs/borshString";

export const GOODS_LISTING_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  120, 149, 179, 150, 220, 115, 129, 110,
]);

export function getGoodsListingDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GOODS_LISTING_DISCRIMINATOR,
  );
}

export type GoodsListing = {
  discriminator: ReadonlyUint8Array;
  /** Seller's agent PDA */
  seller: Address;
  /**
   * Seller's WALLET authority, SNAPSHOTTED at create. Payouts and updates pin
   * to THIS, not to `seller_agent.authority` at sale time — so a seller who
   * deregisters this agent_id cannot have an attacker re-register the same
   * agent_id (same PDA) and hijack the listing's future payouts / controls
   * (batch-4 adversarial review AC-2).
   */
  sellerAuthority: Address;
  /** Unique good identifier (unique per seller; PDA seed) */
  goodId: ReadonlyUint8Array;
  /** Display name */
  name: ReadonlyUint8Array;
  /**
   * Content hash of the off-chain good metadata (also the moderation
   * BLOCK-floor key — `require_content_not_blocked` gates create + purchase)
   */
  metadataHash: ReadonlyUint8Array;
  /**
   * Where the metadata pinned by `metadata_hash` can be fetched (display:
   * name/description/image). Mirrors `ServiceListing.spec_uri`.
   */
  metadataUri: string;
  /** Price per unit in lamports (SOL) or token smallest units */
  price: bigint;
  /** Optional SPL token mint for price denomination (None = SOL) */
  priceMint: Option<Address>;
  /** Tags for discovery (encoded by client) */
  tags: ReadonlyUint8Array;
  /**
   * Supply at creation (immutable — lets indexers render "initial N,
   * restocked K times" honestly)
   */
  initialSupply: bigint;
  /**
   * Current total supply ceiling; grows ONLY via the additive-delta restock
   * in `update_goods_listing` (never set absolutely — a set would permit a
   * scarcity rug and a `sold_count` underflow)
   */
  totalSupply: bigint;
  /**
   * Units sold; monotonic. Also the next sale's receipt serial.
   * Remaining supply = `total_supply - sold_count`.
   */
  soldCount: bigint;
  /** Number of restocks applied (transparency counter) */
  restockCount: number;
  /**
   * Operator payee (the embedding site/store); `Pubkey::default()` = no
   * operator leg. Mirrors `ServiceListing.operator`.
   */
  operator: Address;
  /**
   * Operator fee in basis points; must be 0 iff `operator` is default.
   * Bounded per-leg by `MAX_OPERATOR_FEE_BPS` and at purchase by the
   * combined-fee cap (`calculate_combined_fees`).
   */
  operatorFeeBps: number;
  /** Whether the listing is purchasable (soft delist toggle) */
  isActive: boolean;
  /** Creation timestamp */
  createdAt: bigint;
  /** Last update timestamp */
  updatedAt: bigint;
  /** Bump seed */
  bump: number;
  /** Reserved for future use (rating rollups, generation discriminator, …) */
  reserved: ReadonlyUint8Array;
};

export type GoodsListingArgs = {
  /** Seller's agent PDA */
  seller: Address;
  /**
   * Seller's WALLET authority, SNAPSHOTTED at create. Payouts and updates pin
   * to THIS, not to `seller_agent.authority` at sale time — so a seller who
   * deregisters this agent_id cannot have an attacker re-register the same
   * agent_id (same PDA) and hijack the listing's future payouts / controls
   * (batch-4 adversarial review AC-2).
   */
  sellerAuthority: Address;
  /** Unique good identifier (unique per seller; PDA seed) */
  goodId: ReadonlyUint8Array;
  /** Display name */
  name: ReadonlyUint8Array;
  /**
   * Content hash of the off-chain good metadata (also the moderation
   * BLOCK-floor key — `require_content_not_blocked` gates create + purchase)
   */
  metadataHash: ReadonlyUint8Array;
  /**
   * Where the metadata pinned by `metadata_hash` can be fetched (display:
   * name/description/image). Mirrors `ServiceListing.spec_uri`.
   */
  metadataUri: string;
  /** Price per unit in lamports (SOL) or token smallest units */
  price: number | bigint;
  /** Optional SPL token mint for price denomination (None = SOL) */
  priceMint: OptionOrNullable<Address>;
  /** Tags for discovery (encoded by client) */
  tags: ReadonlyUint8Array;
  /**
   * Supply at creation (immutable — lets indexers render "initial N,
   * restocked K times" honestly)
   */
  initialSupply: number | bigint;
  /**
   * Current total supply ceiling; grows ONLY via the additive-delta restock
   * in `update_goods_listing` (never set absolutely — a set would permit a
   * scarcity rug and a `sold_count` underflow)
   */
  totalSupply: number | bigint;
  /**
   * Units sold; monotonic. Also the next sale's receipt serial.
   * Remaining supply = `total_supply - sold_count`.
   */
  soldCount: number | bigint;
  /** Number of restocks applied (transparency counter) */
  restockCount: number;
  /**
   * Operator payee (the embedding site/store); `Pubkey::default()` = no
   * operator leg. Mirrors `ServiceListing.operator`.
   */
  operator: Address;
  /**
   * Operator fee in basis points; must be 0 iff `operator` is default.
   * Bounded per-leg by `MAX_OPERATOR_FEE_BPS` and at purchase by the
   * combined-fee cap (`calculate_combined_fees`).
   */
  operatorFeeBps: number;
  /** Whether the listing is purchasable (soft delist toggle) */
  isActive: boolean;
  /** Creation timestamp */
  createdAt: number | bigint;
  /** Last update timestamp */
  updatedAt: number | bigint;
  /** Bump seed */
  bump: number;
  /** Reserved for future use (rating rollups, generation discriminator, …) */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link GoodsListingArgs} account data. */
export function getGoodsListingEncoder(): Encoder<GoodsListingArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["seller", getAddressEncoder()],
      ["sellerAuthority", getAddressEncoder()],
      ["goodId", fixEncoderSize(getBytesEncoder(), 32)],
      ["name", fixEncoderSize(getBytesEncoder(), 32)],
      ["metadataHash", fixEncoderSize(getBytesEncoder(), 32)],
      [
        "metadataUri",
        addEncoderSizePrefix(getBorshStringEncoder(), getU32Encoder()),
      ],
      ["price", getU64Encoder()],
      ["priceMint", getOptionEncoder(getAddressEncoder())],
      ["tags", fixEncoderSize(getBytesEncoder(), 64)],
      ["initialSupply", getU64Encoder()],
      ["totalSupply", getU64Encoder()],
      ["soldCount", getU64Encoder()],
      ["restockCount", getU16Encoder()],
      ["operator", getAddressEncoder()],
      ["operatorFeeBps", getU16Encoder()],
      ["isActive", getBooleanEncoder()],
      ["createdAt", getI64Encoder()],
      ["updatedAt", getI64Encoder()],
      ["bump", getU8Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 16)],
    ]),
    (value) => ({ ...value, discriminator: GOODS_LISTING_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link GoodsListing} account data. */
export function getGoodsListingDecoder(): Decoder<GoodsListing> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["seller", getAddressDecoder()],
    ["sellerAuthority", getAddressDecoder()],
    ["goodId", fixDecoderSize(getBytesDecoder(), 32)],
    ["name", fixDecoderSize(getBytesDecoder(), 32)],
    ["metadataHash", fixDecoderSize(getBytesDecoder(), 32)],
    [
      "metadataUri",
      addDecoderSizePrefix(getBorshStringDecoder(), getU32Decoder()),
    ],
    ["price", getU64Decoder()],
    ["priceMint", getOptionDecoder(getAddressDecoder())],
    ["tags", fixDecoderSize(getBytesDecoder(), 64)],
    ["initialSupply", getU64Decoder()],
    ["totalSupply", getU64Decoder()],
    ["soldCount", getU64Decoder()],
    ["restockCount", getU16Decoder()],
    ["operator", getAddressDecoder()],
    ["operatorFeeBps", getU16Decoder()],
    ["isActive", getBooleanDecoder()],
    ["createdAt", getI64Decoder()],
    ["updatedAt", getI64Decoder()],
    ["bump", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 16)],
  ]);
}

/** Gets the codec for {@link GoodsListing} account data. */
export function getGoodsListingCodec(): Codec<GoodsListingArgs, GoodsListing> {
  return combineCodec(getGoodsListingEncoder(), getGoodsListingDecoder());
}

export function decodeGoodsListing<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<GoodsListing, TAddress>;
export function decodeGoodsListing<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<GoodsListing, TAddress>;
export function decodeGoodsListing<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<GoodsListing, TAddress> | MaybeAccount<GoodsListing, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getGoodsListingDecoder(),
  );
}

export async function fetchGoodsListing<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<GoodsListing, TAddress>> {
  const maybeAccount = await fetchMaybeGoodsListing(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeGoodsListing<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<GoodsListing, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeGoodsListing(maybeAccount);
}

export async function fetchAllGoodsListing(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<GoodsListing>[]> {
  const maybeAccounts = await fetchAllMaybeGoodsListing(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeGoodsListing(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<GoodsListing>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeGoodsListing(maybeAccount));
}
