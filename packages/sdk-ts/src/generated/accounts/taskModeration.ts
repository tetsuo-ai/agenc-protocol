/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

import { getFixedBytesEncoder } from "../codecs/fixedBytes";

export const TASK_MODERATION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [170, 214, 132, 159, 229, 119, 11, 43],
);

export function getTaskModerationDiscriminatorBytes(): ReadonlyUint8Array {
  return getFixedBytesEncoder(8).encode(TASK_MODERATION_DISCRIMINATOR);
}

export type TaskModeration = {
  discriminator: ReadonlyUint8Array;
  /** Task this moderation decision applies to. */
  task: Address;
  /** Task creator at the time the decision was recorded. */
  creator: Address;
  /** Job-spec hash approved/held/rejected by the scanner or reviewer. */
  jobSpecHash: ReadonlyUint8Array;
  /** One of `task_moderation_status::*`. */
  status: number;
  /** Normalized 0-100 risk score. */
  riskScore: number;
  /** Bitmask of scanner categories, interpreted by off-chain policy docs. */
  categoryMask: bigint;
  /** Hash of the moderation policy/threshold version. */
  policyHash: ReadonlyUint8Array;
  /** Hash of the scanner/model version bundle. */
  scannerHash: ReadonlyUint8Array;
  /** When the moderation decision was recorded. */
  recordedAt: bigint;
  /** Optional expiry timestamp. Zero means no expiry. */
  expiresAt: bigint;
  /** Signer that recorded the moderation decision. */
  moderator: Address;
  /** PDA bump. */
  bump: number;
  /** Reserved for future attestation metadata. */
  reserved: ReadonlyUint8Array;
};

export type TaskModerationArgs = {
  /** Task this moderation decision applies to. */
  task: Address;
  /** Task creator at the time the decision was recorded. */
  creator: Address;
  /** Job-spec hash approved/held/rejected by the scanner or reviewer. */
  jobSpecHash: ReadonlyUint8Array;
  /** One of `task_moderation_status::*`. */
  status: number;
  /** Normalized 0-100 risk score. */
  riskScore: number;
  /** Bitmask of scanner categories, interpreted by off-chain policy docs. */
  categoryMask: number | bigint;
  /** Hash of the moderation policy/threshold version. */
  policyHash: ReadonlyUint8Array;
  /** Hash of the scanner/model version bundle. */
  scannerHash: ReadonlyUint8Array;
  /** When the moderation decision was recorded. */
  recordedAt: number | bigint;
  /** Optional expiry timestamp. Zero means no expiry. */
  expiresAt: number | bigint;
  /** Signer that recorded the moderation decision. */
  moderator: Address;
  /** PDA bump. */
  bump: number;
  /** Reserved for future attestation metadata. */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link TaskModerationArgs} account data. */
export function getTaskModerationEncoder(): FixedSizeEncoder<TaskModerationArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getFixedBytesEncoder(8, "discriminator")],
      ["task", getAddressEncoder()],
      ["creator", getAddressEncoder()],
      ["jobSpecHash", getFixedBytesEncoder(32, "jobSpecHash")],
      ["status", getU8Encoder()],
      ["riskScore", getU8Encoder()],
      ["categoryMask", getU64Encoder()],
      ["policyHash", getFixedBytesEncoder(32, "policyHash")],
      ["scannerHash", getFixedBytesEncoder(32, "scannerHash")],
      ["recordedAt", getI64Encoder()],
      ["expiresAt", getI64Encoder()],
      ["moderator", getAddressEncoder()],
      ["bump", getU8Encoder()],
      ["reserved", getFixedBytesEncoder(7, "reserved")],
    ]),
    (value) => ({ ...value, discriminator: TASK_MODERATION_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link TaskModeration} account data. */
export function getTaskModerationDecoder(): FixedSizeDecoder<TaskModeration> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["task", getAddressDecoder()],
    ["creator", getAddressDecoder()],
    ["jobSpecHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["status", getU8Decoder()],
    ["riskScore", getU8Decoder()],
    ["categoryMask", getU64Decoder()],
    ["policyHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["scannerHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["recordedAt", getI64Decoder()],
    ["expiresAt", getI64Decoder()],
    ["moderator", getAddressDecoder()],
    ["bump", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 7)],
  ]);
}

/** Gets the codec for {@link TaskModeration} account data. */
export function getTaskModerationCodec(): FixedSizeCodec<
  TaskModerationArgs,
  TaskModeration
> {
  return combineCodec(getTaskModerationEncoder(), getTaskModerationDecoder());
}

export function decodeTaskModeration<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<TaskModeration, TAddress>;
export function decodeTaskModeration<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<TaskModeration, TAddress>;
export function decodeTaskModeration<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<TaskModeration, TAddress> | MaybeAccount<TaskModeration, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getTaskModerationDecoder(),
  );
}

export async function fetchTaskModeration<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<TaskModeration, TAddress>> {
  const maybeAccount = await fetchMaybeTaskModeration(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTaskModeration<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<TaskModeration, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTaskModeration(maybeAccount);
}

export async function fetchAllTaskModeration(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<TaskModeration>[]> {
  const maybeAccounts = await fetchAllMaybeTaskModeration(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTaskModeration(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<TaskModeration>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeTaskModeration(maybeAccount),
  );
}

export function getTaskModerationSize(): number {
  return 234;
}
