/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

import {
  getBorshStringDecoder,
  getBorshStringEncoder,
} from "../codecs/borshString";

export const STORE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  130, 48, 247, 244, 182, 191, 30, 26,
]);

export function getStoreDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(STORE_DISCRIMINATOR);
}

export type Store = {
  discriminator: ReadonlyUint8Array;
  /** Owner wallet (signer of register/update/close; the future P5.3 referral payee). */
  owner: Address;
  /** Display handle, lowercase `[a-z0-9-]`, zero-padded. NOT a uniqueness key. */
  handle: ReadonlyUint8Array;
  /**
   * sha256 of the canonical `agenc.storeManifest.v1` body this store points at
   * (all-zero = no manifest pinned yet).
   */
  metadataHash: ReadonlyUint8Array;
  /**
   * Manifest URI (typically `https://<domain>/.well-known/agenc-store.json`;
   * empty = no manifest pinned yet).
   */
  metadataUri: string;
  /** Advertised default referral fee (bps, <= MAX_REFERRER_FEE_BPS). */
  referrerFeeBps: number;
  /** Advertised default operator payee (`Pubkey::default()` = none). */
  operator: Address;
  /**
   * Advertised default operator fee (bps, <= MAX_OPERATOR_FEE_BPS; requires a
   * non-default `operator` when > 0 — the `create_service_listing` pairing rule).
   */
  operatorFeeBps: number;
  /**
   * Self-declared domain (empty = hosted-only store). Verified only by the
   * MUTUAL manifest check (spec §3b) — never trusted alone. Same charset floor
   * as `validate_verified_domain`.
   */
  domain: string;
  /**
   * Registration bond held as excess lamports on this PDA (P1.2 §4.1 framing:
   * an identity deposit, never confiscatable; refunded in full at `close_store`).
   */
  bondLamports: bigint;
  /** Monotonic version, bumped on every update (staleness/CAS for indexers). */
  version: bigint;
  createdAt: bigint;
  updatedAt: bigint;
  /** PDA bump. */
  bump: number;
  /**
   * Reserved (future: verification refs, P5.3 referrer bookkeeping). MUST stay
   * zeroed.
   */
  reserved: ReadonlyUint8Array;
};

export type StoreArgs = {
  /** Owner wallet (signer of register/update/close; the future P5.3 referral payee). */
  owner: Address;
  /** Display handle, lowercase `[a-z0-9-]`, zero-padded. NOT a uniqueness key. */
  handle: ReadonlyUint8Array;
  /**
   * sha256 of the canonical `agenc.storeManifest.v1` body this store points at
   * (all-zero = no manifest pinned yet).
   */
  metadataHash: ReadonlyUint8Array;
  /**
   * Manifest URI (typically `https://<domain>/.well-known/agenc-store.json`;
   * empty = no manifest pinned yet).
   */
  metadataUri: string;
  /** Advertised default referral fee (bps, <= MAX_REFERRER_FEE_BPS). */
  referrerFeeBps: number;
  /** Advertised default operator payee (`Pubkey::default()` = none). */
  operator: Address;
  /**
   * Advertised default operator fee (bps, <= MAX_OPERATOR_FEE_BPS; requires a
   * non-default `operator` when > 0 — the `create_service_listing` pairing rule).
   */
  operatorFeeBps: number;
  /**
   * Self-declared domain (empty = hosted-only store). Verified only by the
   * MUTUAL manifest check (spec §3b) — never trusted alone. Same charset floor
   * as `validate_verified_domain`.
   */
  domain: string;
  /**
   * Registration bond held as excess lamports on this PDA (P1.2 §4.1 framing:
   * an identity deposit, never confiscatable; refunded in full at `close_store`).
   */
  bondLamports: number | bigint;
  /** Monotonic version, bumped on every update (staleness/CAS for indexers). */
  version: number | bigint;
  createdAt: number | bigint;
  updatedAt: number | bigint;
  /** PDA bump. */
  bump: number;
  /**
   * Reserved (future: verification refs, P5.3 referrer bookkeeping). MUST stay
   * zeroed.
   */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link StoreArgs} account data. */
export function getStoreEncoder(): Encoder<StoreArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["owner", getAddressEncoder()],
      ["handle", fixEncoderSize(getBytesEncoder(), 32)],
      ["metadataHash", fixEncoderSize(getBytesEncoder(), 32)],
      [
        "metadataUri",
        addEncoderSizePrefix(getBorshStringEncoder(), getU32Encoder()),
      ],
      ["referrerFeeBps", getU16Encoder()],
      ["operator", getAddressEncoder()],
      ["operatorFeeBps", getU16Encoder()],
      [
        "domain",
        addEncoderSizePrefix(getBorshStringEncoder(), getU32Encoder()),
      ],
      ["bondLamports", getU64Encoder()],
      ["version", getU64Encoder()],
      ["createdAt", getI64Encoder()],
      ["updatedAt", getI64Encoder()],
      ["bump", getU8Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 64)],
    ]),
    (value) => ({ ...value, discriminator: STORE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Store} account data. */
export function getStoreDecoder(): Decoder<Store> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["owner", getAddressDecoder()],
    ["handle", fixDecoderSize(getBytesDecoder(), 32)],
    ["metadataHash", fixDecoderSize(getBytesDecoder(), 32)],
    [
      "metadataUri",
      addDecoderSizePrefix(getBorshStringDecoder(), getU32Decoder()),
    ],
    ["referrerFeeBps", getU16Decoder()],
    ["operator", getAddressDecoder()],
    ["operatorFeeBps", getU16Decoder()],
    ["domain", addDecoderSizePrefix(getBorshStringDecoder(), getU32Decoder())],
    ["bondLamports", getU64Decoder()],
    ["version", getU64Decoder()],
    ["createdAt", getI64Decoder()],
    ["updatedAt", getI64Decoder()],
    ["bump", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 64)],
  ]);
}

/** Gets the codec for {@link Store} account data. */
export function getStoreCodec(): Codec<StoreArgs, Store> {
  return combineCodec(getStoreEncoder(), getStoreDecoder());
}

export function decodeStore<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Store, TAddress>;
export function decodeStore<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Store, TAddress>;
export function decodeStore<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Store, TAddress> | MaybeAccount<Store, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getStoreDecoder(),
  );
}

export async function fetchStore<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Store, TAddress>> {
  const maybeAccount = await fetchMaybeStore(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeStore<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Store, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeStore(maybeAccount);
}

export async function fetchAllStore(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Store>[]> {
  const maybeAccounts = await fetchAllMaybeStore(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeStore(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Store>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeStore(maybeAccount));
}
