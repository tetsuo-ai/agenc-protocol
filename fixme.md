# Enterprise audit remediation ledger

This is the permanent closeout ledger for the enterprise hardening audit begun
on 2026-07-19. It preserves the complete claim history after remediation so a
green test run cannot erase a finding, a rejected hypothesis, or an external
release blocker.

## Audit contract and outcome

- Audited baseline: `1fc5896eb14d703677232eb2ba21b853f31df9ef` on local
  `main`; the baseline started clean, with one worktree and no side branch.
- Scope: on-chain Rust and compiled execution; deployment/migration rails; SDK,
  React, tools, MCP, moderation, worker, CLI, and generated fixtures; dependency,
  CI, release, supply-chain, documentation, and read-only public-state controls.
- Original closeout safety boundary: that audit did not sign, broadcast, deploy,
  publish, push, or mutate GitHub, npm, DNS, hosted schemas, security intake, or
  mainnet state. The 2026-07-20 continuation retried one failed GitHub Actions job
  but still has not signed, broadcast, deployed, published, or changed mainnet.
- Original closeout compatibility boundary: the deployed program ID, account
  layouts, instruction ABI, and published package APIs were preserved. The
  revision-5 continuation replaced three write discriminators and coordinated new
  client versions; the program was **deployed to mainnet on 2026-07-22**
  (`surface_revision = 5`, executable SHA-256 `049a66…`). The coordinated client
  packages ship with that upgrade.
- Baseline claim accounting: **78 accepted findings = 62 repository-fixed + 6
  CONDITIONAL + 10 originally EXTERNAL**. Subsequent authorized operator work
  closed two of those external findings; the remaining eight are tracked below.
  A further **22 hypotheses are REJECTED or merged**, with the rationale retained
  below.
- Assurance boundary: review and testing cannot prove that arbitrary software is
  mathematically bug-free. The original closeout had no accepted
  repository-controlled defect open; the authorized revision-5 continuation on
  2026-07-20 found additional issues recorded immediately below. Release remains
  blocked until every `OPEN` or `PENDING-VERIFY` continuation row is independently
  closed and every external item is re-verified against the final commit.

Status meanings:

- `FIXED`: the claim was independently accepted, remediated, and covered by a
  focused regression plus the applicable aggregate gates.
- `CONDITIONAL`: no current supported-production exposure was established; the
  feature or compatibility boundary remains blocked until its stated condition
  is met.
- `EXTERNAL`: repository controls are complete where possible, but closure needs
  an authorized remote, operator, hosted-service, upstream, or mainnet action.
- `REJECTED`: independent review disproved or materially merged the hypothesis.
- `OPEN`: accepted continuation finding whose repository remediation is incomplete.
- `PENDING-VERIFY`: a remediation exists locally but has not yet passed the required
  independent senior review and final aggregate/remote gates.

## Revision-5 continuation findings — 2026-07-20

These findings were discovered after the original closeout while re-auditing the
dirty revision-5 cutover candidate and its explicit consumers. They supersede the
original “no repository-controlled defect remains” state until closed.

| ID                                  | Severity | Status           | Accepted claim and required closure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----------------------------------- | -------: | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CUTOVER-EXTEND-FEATURE-001`        |     High | `FIXED`          | The former rail serialized `ExtendProgramChecked` through Squads CPI, but mainnet never activated it and current Agave rejects loader extension through CPI. The replacement top-level legacy rail pins official Agave CLI 4.1.0 source/archive/executable bytes and the twice-reproduced 2,284,496-byte SBF (SHA-256 `79f55a6847fa954ef9427fd09c0d84f4f363857e2b0c4753b03ff02802e8684d`) with its exact 101,272-byte shortfall. Anchored two-RPC recovery validates the expected instruction, matching signature/write slot, stable postimage, rent floor, nullable provider-local `blockTime`, and safe dust surplus. A fresh senior reviewer accepted the implementation after **26/26** focused and **257/257** deployment/preflight tests, syntax, Prettier, diff integrity, exact artifacts, and fresh dual-provider live preflight. No signing or broadcast occurred; the permissionless concurrent-extension race is detected but cannot be reversed.                                                                                                                                                                                                                                                                      |
| `CUTOVER-PAYER-PIN-001`             |     High | `FIXED`          | The extension rail originally derived the payer address from a mutable keypair path and later reopened that path for the irreversible transaction. It now snapshots one private, current-user-owned, bounded, single-link source through `O_NOFOLLOW`, copies it to an unlinked mode-0400 inode, passes that same descriptor as child fd 4 for both address derivation and execution, revalidates inode identity before each spawn, and never logs, hashes, or serializes secret bytes. A fresh senior reviewer accepted the fix after independently exercising real Linux fd3/fd4 mechanics under source replacement, the full execute-flow regression, **26/26** focused tests, and the **257/257** deployment aggregate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `CUTOVER-RENT-DUST-001`             |     High | `FIXED`          | Exact postflight rent equality let any observer transfer one lamport to ProgramData after extension and permanently block recovery. Postflight now requires each balance to meet the fresh rent floor, records and schema-validates exact `excessLamports`, and compares only stable postimage fields and rent across providers. Pre-existing, post-extension, independently observed dust, underfunding, and disagreement regressions are fail-safe. A fresh senior reviewer accepted the fix after **26/26** focused and **257/257** deployment/preflight tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `CUTOVER-BLOCKTIME-NULL-001`        |   Medium | `FIXED`          | Solana RPC canonically permits `blockTime: null`, but the earlier evidence schema rejected it and could make a finalized extension unrecoverable. Version-3 evidence preserves each provider's nullable value, binds exact anchored signature and transaction/ProgramData slot ordering, and validates chronology independently whenever a timestamp exists. Null/null, null/integer, signature/slot mismatch, and malformed chronology regressions pass. A fresh senior reviewer accepted the fix after **26/26** focused and **257/257** deployment/preflight tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `SDK-COMMITMENT-CODEC-001`          |     High | `FIXED`          | Public Codama-generated v2 builders silently padded 31-byte or truncated 33-byte commitments. Generation now installs a strict exact-nonzero 32-byte encoder at the three reviewed commitment sites and fails on renderer drift; direct generated regressions reject 31-, 33-, and all-zero inputs. A fresh senior reviewer accepted the implementation after deterministic regeneration, typecheck, examples, build, zero-warning TypeDoc, package smoke, and the full **741-pass/1-intentional-skip** SDK suite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `SDK-FIXED-BYTES-001`               |   Medium | `FIXED`          | Codama/Kit pad/truncate behavior affected generated fixed-array encoders for direct fields, nested options, and caller-supplied PDA seeds. The first remediation covered only 151 direct tuple sites and was correctly rejected by senior review. Generation now rewrites all 598 emitted fixed-byte encoder sites—including nested values, PDA seeds, and injected constants—and fails if any permissive renderer call survives. Seven focused regressions cover direct, commitment, nested-option, arbitrary-size, and PDA-seed inputs; a fresh senior reviewer accepted the complete fix after deterministic drift, typecheck, examples, build, package smoke, and the full **741-pass/1-intentional-skip** SDK suite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `SDK-TEST-ARTIFACT-PROFILE-001`     |   Medium | `FIXED`          | The SDK testing-asset sync copied whichever shared target SBF existed, so a temporary frozen-canary build silently replaced the production LiteSVM fixture and caused 38 misleading E2E failures. Sync now fails closed unless three independent production-only instruction markers are present, before either test or pack copies the bytes. The production and testing SBFs are byte-identical at 2,284,496 bytes, SHA-256 `79f55a6847fa954ef9427fd09c0d84f4f363857e2b0c4753b03ff02802e8684d`; three direct guard regressions and the full **741-pass/1-intentional-skip** SDK suite pass, and a fresh senior reviewer accepted the fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `SDK-SOURCE-NUL-001`                |      Low | `FIXED`          | A literal NUL byte in `values.test.ts` made Git classify the TypeScript source and its security-relevant diff as binary, obscuring line review. The test now uses the semantically identical `\u0000` escape, restoring ordinary text diffs. Its focused 44-test suite and the full **741-pass/1-intentional-skip** SDK suite pass, and a fresh senior reviewer accepted the fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `RUST-CANARY-DEADCODE-001`          |      Low | `FIXED`          | A revision-5 hired-spec helper used only by full-surface handlers remained compiled but unused in the restricted canary SBF. Its cfg now includes full-surface builds and tests while excluding non-test canary SBF compilation. A fresh senior reviewer accepted the fix after isolated production **529/529** and canary **323/323** tests, strict Clippy `-D warnings` for both profiles, warning-free non-test canary/SBF compilation, and `cargo fmt --check`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `CLI-REVISION-COMPAT-001`           |     High | `FIXED`          | CLI promotion selects a fail-closed compatibility matrix from the finalized on-chain surface revision, then verifies each installed first-party artifact's readable/versioned manifest and actual npm dependency/peer ranges against the installed SDK and other material peers. This closes the Cartesian-product gap where individually admitted revision-4 versions (for example React 0.4.1 plus SDK 0.11) were mutually incoherent. Truly absent optional packages remain allowed; present missing/malformed/non-file manifests, missing material relationships, malformed ranges, incoherent pairs, both skew directions, and unknown revisions fail closed. Revision-5 promotion also remains blocked until the actual post-upgrade identity is captured, patched, repacked, and re-audited. Independent senior source review, live npm metadata verification, and the focused 80/80 suite pass. The final CLI pass is 129/129; typecheck/build, both generated Next router builds, format/diff, pack dry-run, and production audit also pass.                                                                                                                                                                              |
| `RELEASE-TRAIN-DRIFT-001`           |     High | `OPEN`           | The previously verified nine-package train was invalidated by the accepted late React/SDK/worker/CLI remediations. Current dry packs prove at least React, worker, and CLI no longer match the pinned `release-train.json` SRIs; SDK source/changelog also changed and must be rebuilt rather than assumed unchanged. The former double-pack/smoke evidence remains historical evidence for the superseded tree, not release authority for this one. Closure requires a quiescent clean prepack/pack of all nine packages twice, byte identity plus recomputed name/version/SHA-512/SHA-1/size, exact all-local dependency smoke, release policy/workflow/state gates, updated pins/current docs, removal of all tarballs, and independent verification.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `RELEASE-TRAIN-COMPLETENESS-001`    |   Medium | `FIXED`          | Two fresh senior falsifications rejected incomplete closures. The first found dependency-section shadowing, hidden first-party `npm:` aliases, and raw-prefix gate routing; the second proved that the static nine-package gate identity list could declare a synthetic tenth package even though the workflow implemented no routed step for it. The repair validates production/optional/peer declarations independently, rejects conflicting duplicate first-party ranges and all aliases to train packages, resolves identity in a trusted prerequisite job, and routes every later package decision only from that id. More importantly, preflight derives completion ids from the workflow's actual marked gate steps and requires exact train coverage; the selected real gate writes an exclusive one-time proof only after its checks succeed, and packaging verifies that proof against the resolved id/name/directory/version/tag through one unconditional, non-ignorable barrier. A regression reproduces the tenth-package/static-declaration/no-step bypass and fails closed. Wrong-range shadowing, aliases, prefix/id permutations, resolver mismatch, workspace, route, missing-proof, wrong-gate, duplicate-proof, and tampered-proof cases pass; exact Node 22.23.1 and 24.18.0 focused policy/workflow is **20/20** and expanded release rail is **71/71** on each. Current npm 11 package-spec and GitHub Actions job-output/reusable-workflow documentation support the implementation. A fresh independent senior source review accepted closure with no blocking findings; `RELEASE-TRAIN-DRIFT-001` remains the separate package-rebuild/SRI release-readiness blocker. |
| `SDK-HIRE-INTENT-001`               |   Medium | `FIXED`          | Ambiguous hire recovery persists a stable SHA-256 logical-intent digest and adopts an occupied Task PDA only after the exact finalized revision-5 transaction proves the canonical signature, instruction bytes, accounts, and funded input terms. Reconciliation validates the immutable Task, escrow, HireRecord, and validation snapshots—including price, hashes, provider, fees/payees, capabilities, deadline structure, and review semantics—without treating a later mutable Listing or ProtocolConfig value as historical evidence. Reused IDs, swapped terms/accounts, malformed signatures, mutable caller inputs/progress, and conflicting execution snapshots fail closed; a genuine finalized hire still recovers after the listing is retired or mutated and protocol defaults change. The fresh senior review accepted the fix after 73/73 orchestration regressions, typecheck/examples/build, zero-warning TypeDoc, package smoke, and the full **741-pass/1-intentional-skip** SDK suite.                                                                                                                                                                                                                       |
| `CLI-HIRE-RECOVERY-001`             |   Medium | `FIXED`          | Generated checkout recovery now accepts only the current genuine SDK token shape: a stable hire-intent digest, canonical task address, and canonical Solana transaction signature that decodes to exactly 64 bytes where attributable, including valid shorter leading-zero encodings. It reuses the exact saved commitment and delegates reconciliation to SDK resume without a second hire. The unsafe bare-Task reconciliation and fabricated empty-signature `hireReconciled` state were removed; generic preflight errors abort and release the idempotency key rather than poisoning it. A valid SDK finalized-failure proof explicitly discards the obsolete recovery/key reservation because atomic execution committed no hire. A malformed proof preserves a prior token; without one it enters a permanent non-recovery block that retains the debit reservation for manual review. Malformed, digest-less, and legacy fabricated tokens remain locked and are never resumed. Independent senior source review, the focused 80/80 suite, the five funded dev-loop regressions, and the final 129/129 CLI pass succeed; CLI typecheck/build, both generated Next router builds, format/diff, and pack dry-run also pass. |
| `REACT-HUMANLESS-RECOVERY-001`      |   Medium | `FIXED`          | The React humanless hook had no safe post-funding resume. It now snapshots caller input, delegates funded/recovery execution to SDK orchestration, exposes `resumeHireAndActivate`, and retains durable typed recovery across unmount/remount. Ambiguous, moderation, activation, signer-identity, and explicit custom-orchestration-RPC regressions prove no second hire and no accidental network fallback. A fresh senior reviewer accepted the fix after typecheck, build, funded embedded-wallet/hook E2E, and the full **277-pass/1-intentional-skip** React suite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `REACT-STARTER-JOB-SPEC-001`        |   Medium | `FIXED`          | A clean packed consumer exposed the missing `taskJobSpecHash`; the first correction derived the future Task PDA and canonical hash but a fresh senior reviewer rejected its false immutability claim because the stored object/arrays and backend-owned hash bytes remained mutable. The revised starter keeps a detached deeply frozen local snapshot, gives the backend a separate clone, re-hashes the task-bound local payload before and after the callback, rejects different-task substitution, snapshots response accessors once, and activates/stores only fresh funded-byte copies. The same reviewer accepted the closure after starter **34/34**, hooks **59/59**, full React **280/1 intentional skip**, typecheck/build/import/diff, and clean packed-consumer typecheck/test/build/audit gates.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `REACT-TEST-ACT-001`                |      Low | `FIXED`          | The React suite was functionally green but four hook-test files emitted reproducible Testing Library `act(...)` warnings. The harnesses now await genuine React update boundaries and rethrow captured failures without console suppression. A fresh senior reviewer accepted the fix; after the final regressions the exact four-file set passes **75/75** with zero-byte stderr and the full suite passes **280 / 1 intentional environment-gated skip** with zero-byte stderr.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `REACT-SIGNER-FREEZE-001`           |   Medium | `FIXED`          | React froze caller-owned/default custom signers before the SDK installed its canonical address descriptor, breaking valid inherited or configurable-accessor signers by making them non-extensible. React now captures the canonical creator address synchronously, rejects enqueue-time address mutation before funding, and delegates descriptor locking/freezing to the SDK in the intended order. A fresh senior reviewer accepted the closure after inherited-address, configurable-accessor custom-client, and address-race regressions; hooks **59/59**, warning set **75/75**, full React **280/1 intentional skip**, typecheck/build/import/diff, and clean packed-consumer gates pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `REACT-HIRE-INPUT-TOCTOU-001`       |     High | `FIXED`           | Public `useHire` and `useTaskActivation` formerly carried shallow caller input and mutable signer/fixed-byte arrays across TanStack scheduling, moderation, and send awaits, then could derive or return identity from changed values. The public callbacks now synchronously detach and exact-validate every relevant 32-byte value, stabilize the same signer objects through the SDK identity guard, and derive/use one Task PDA from the captured creator/task bytes. Immediate-enqueue, moderation-pending, send-pending, malformed-byte, default/override signer, and returned-PDA regressions pass; current full gates are React **285 pass / one intentional skip** and SDK **743 pass / one intentional skip**, plus typecheck/build/export smoke. A fresh senior reviewer accepted the complete rail. The 2026-07-21 two-pass quiescent aggregate closed it: full workspace green twice (SDK 883 pass/1 skip, React 312/1, worker 273, CLI 146, tools 98, starter 36, scripts 451/451 twice, compiled integration 404/9-canary-skips twice, all Rust profiles plus strict Clippy and fmt).                                                                                                                                                                                                                                                                                                                                                         |
| `REACT-WRITE-HOOK-INPUT-TOCTOU-002` |     High | `OPEN`           | Six sibling public write-hook modules still pass caller-owned mutable inputs directly into TanStack and first detach/read them only inside the asynchronously scheduled mutation function: task work, submission review, dispute, completion bond, task lifecycle, and hire rating. Immediate caller mutation can therefore change committed hashes, evidence/result bytes, nested worker/child/bid collections, or override signer identity after the public call returns but before building/signing. Closure requires synchronous entry snapshots for every fixed byte, byte buffer, nested collection/object, and selected default/override signer across every public verb, revert-sensitive immediate-mutation coverage for each input shape, aggregate React/SDK gates, and fresh senior falsification.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `STARTER-ACTIVATE-URI-TOCTOU-001`   |      Low | `FIXED`           | The starter activation route formerly mutated and repeatedly read the storage dependency's returned `uri` object across the moderation await. It now snapshots the accessor exactly once into `unknown`, validates one immutable string, and uses only that primitive for moderation and the response. Frozen/accessor and retained-object await-mutation regressions prove the returned URI is the exact attested URI. A fresh senior reviewer accepted the source fix, and the 2026-07-21 quiescent aggregate closed it (starter 36/36, React 312 pass/1 skip, full workspace green twice).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `LOCALNET-PROCESS-FLAKE-001`        |   Medium | `FIXED`           | A final full-script rerun reproduced nondeterministic cleanup because a killed child could transiently be a procfs zombie with an empty command line while its Node handle had not emitted `exit`. Cleanup now awaits the exact child handle, recognizes only documented Linux zombie/dead states, rechecks identity immediately before escalation, and rejects wrapped publication failures. The former failure passes 400/400 stress plus focused **7/7**, and a fresh senior reviewer accepted the source fix, and two consecutive clean full-script aggregate passes (451/451 on 2026-07-21) closed it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `LOCALNET-PRESIGNAL-RACE-001`       |   Medium | `PENDING-VERIFY` | Localnet signalling now opens `/proc/<pid>` as a stable process reference, reads the complete recorded identity through `/proc/self/fd/<fd>`, and passes that same descriptor as fd 3 to isolated system Python for `pidfd_send_signal(2)`. Missing kernel/helper support fails closed with no numeric-PID fallback; closed references cannot expose a reusable descriptor. Failed-spawn cleanup holds one descriptor across capture, publication, TERM, and KILL. Exact Node 22/24 focused suites pass **28/28**, 50 repeated suites pass on each runtime, and deployment/preflight passes **278/278** on each. The 2026-07-21 two-pass quiescent aggregate is green; a fresh senior pidfd/CLI review remains before `FIXED`.                                                                                                                                                                                                                                                                                                                                                                                                                |
| `CLI-LOCALNET-PURGE-STOP-001`       |   Medium | `PENDING-VERIFY` | `agenc dev --purge` now delegates to `localnet-down.mjs --purge`, requires its successful child exit, and only then starts localnet-up. The CLI contains no numeric-PID signal or fixed shutdown sleep. Real child-process fixtures prove down-complete-before-up ordering, down failure blocks up, and missing purge tooling blocks up. Exact Node 22/24 full CLI suites pass **133/133** with typecheck/build; the 2026-07-21 aggregate (CLI 146/146, full workspace twice) is green; fresh senior composition review remains before `FIXED`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `LOCALNET-PURGE-UNATTESTED-001`     |   Medium | `OPEN`           | Fresh composition review found that `localnet-down.mjs --purge` treats an absent validator identity file as “nothing to stop” and recursively deletes an existing ledger. A live validator whose record was lost can therefore have its ledger removed without any stop proof; delegated CLI purge then fails later on occupied ports after the destructive action. Closure requires refusing deletion whenever a ledger exists without an accountable validator record, preserving a verified stopped record across non-purge shutdown/startup failure so ordinary later purge remains usable, regression coverage for no-record/existing-ledger versus no-ledger no-op, truthful docs, and renewed pidfd/CLI senior review.                                                                                                                                                                                                                                                                                                                                  |
| `WORKER-HIRE-DUST-DOS-001`          |   Medium | `FIXED`           | The CLI formerly discarded owner/executable metadata and decoded every existing canonical HireRecord PDA. A shared RPC adapter now snapshots/validates canonical base64, owner, and executable metadata; every shipped worker CLI/template/dev composition supplies it. Runtime accepts only absence or an exact non-executable System-owned zero-data placeholder as direct, and otherwise requires exact program owner, 173-byte HireRecord, discriminator, task, and PDA bump. Adversarial owner/classification/provider regressions pass; exact Node 24 gates pass worker **273/273** and CLI **129/129** plus typecheck/build (the core 272-test repair also passed minimum Node 22). The bytes-only embedding fallback remains source-compatible and honestly documents that it lacks provenance preflight. A fresh senior reviewer accepted the complete rail. The 2026-07-21 two-pass quiescent aggregate closed it: full workspace green twice (SDK 883 pass/1 skip, React 312/1, worker 273, CLI 146, tools 98, starter 36, scripts 451/451 twice, compiled integration 404/9-canary-skips twice, all Rust profiles plus strict Clippy and fmt).                                                                                                                                                                                                                                                                                                   |
| `WORKER-DESIGNATED-PROVIDER-001`    |   Medium | `FIXED`           | The worker decoded the immutable HireRecord provider but formerly did not compare it with the current worker before claim construction. It now returns stable `not-designated-provider` immediately after canonical task/account checks and before content fetch, funding, WAL creation, signing, or send. The regression proves fetch, balance/rent, client send, and open-claim state remain untouched; legacy default-provider records still follow the explicit re-hire path. Exact Node 24 worker **273/273** and CLI **129/129**, typecheck, and builds pass. Normal SDK preflight would ordinarily prevent broadcast, so no guaranteed fee loss is claimed. A fresh senior reviewer accepted the repair. The 2026-07-21 two-pass quiescent aggregate closed it: full workspace green twice (SDK 883 pass/1 skip, React 312/1, worker 273, CLI 146, tools 98, starter 36, scripts 451/451 twice, compiled integration 404/9-canary-skips twice, all Rust profiles plus strict Clippy and fmt).                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `WORKER-ACCOUNT-COMMITMENT-001`     |      Low | `REJECTED`       | The proposed pre-existing finalized-vs-confirmed mismatch was disproved: pinned Kit 6.9's `createSolanaRpc` already installs `DEFAULT_RPC_CONFIG.defaultCommitment = "confirmed"`, even though unmitigated raw server RPC typically defaults to `finalized`. There was no current split. The worker CLI and generated template now still pin account/balance reads to explicit `confirmed`, making the fresh-claim invariant source-visible and drift-resistant while retaining intentional finalized rent/history reads. Generated-source and executed-template assertions plus worker **273/273** / CLI **129/129** pass. A fresh senior reviewer independently verified the rejection and retained hardening.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `FOLLOW-CI-VITE-HOST-001`           |   Medium | `EXTERNAL`       | Two scheduled React checkout attempts in run `29737720809` timed out waiting for Playwright's `127.0.0.1:3200` server while Vite bound its default `localhost`. The repository now pins Vite dev/preview to exact IPv4 loopback and includes a parity regression. A fresh senior reviewer accepted the repository fix after the focused test, React typecheck/full suite, and real HTTP/listener smoke. Closure still requires the protected scheduled workflow to pass remotely on the merged commit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `DOC-LISTING-PDA-001`               |   Medium | `FIXED`          | The normative listing guide and current `ListingModeration` source/generated docs now identify revision-5 writes as `['listing_moderation_v2', listing, spec_hash, moderator]` and distinguish frozen legacy records as read-only compatibility inputs. A fresh senior reviewer accepted the exact-seed audit after repository-wide current-doc review and focused artifact/reference drift checks; remaining legacy seeds are explicitly historical or decode-only compatibility references.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `CUTOVER-COMPAT-TEST-001`           |      Low | `EXTERNAL`       | Compiled revision 5 rejects all three legacy writes, including registered hire. The exact 99-entry canonical revision-4 IDL dispatcher snapshot proves all values are unique and excludes every v2 value; the compiled frozen-canary SBF rejects all three v2 values. A fresh senior reviewer accepted the local bidirectional evidence after revision-5 old-write **3/3**, history **9/9**, dispatcher-baseline, and canary v2-rejection **3/3** gates. Because the exact deployed revision-4 SBF is not committed as a fixture, final go/no still requires three recorded pre-upgrade simulations against the live deployed 99-instruction program.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `SDK-INIT-PROGRAMDATA-001`          |     High | `FIXED`           | The public `initializeProtocol` facade emitted only the five generated named metas, while Rust requires canonical loader-v3 ProgramData at remaining-account slot zero and additional configured owner signatures after it. Every facade-built bootstrap therefore failed `UnauthorizedUpgrade`, and thresholds above two were unrepresentable. The local repair derives ProgramData from the fixed program/loader IDs, snapshots the owner and signer containers before the first await, enforces the exact bootstrap owner/threshold/signature invariants, and appends the exact wire order. Node 24 typecheck and governance pass; Node 22 focused client/governance run **100/100**; compiled-handler LiteSVM bootstrap coverage (`tests-integration/initialize-protocol.test.mjs`) ran green in both 404-pass integration aggregates; SDK aggregate passed twice (883/1). A 2026-07-21 independent senior adversarial review confirmed exact facade-to-handler wire-order parity, pre-await snapshot/signer-identity locking, and fail-closed threshold/duplicate/default-owner/edge paths (CONFIRMED-SOUND; facade-level rejection regressions recommended as follow-up coverage). |
| `SDK-V0-WIRE-SIZE-001`              |     High | `FIXED`           | `MarketplaceClient.send` signed and handed arbitrarily oversized v0 messages to transports even though legacy/v0 wire transactions are capped at 1,232 bytes. It now invokes Kit's exact compiled-message size assertion before any wallet capability and reasserts the signed transaction at the transport seam. Regressions prove exact 1,232-byte price/no-price messages send while 1,233-byte messages become structured `AgencError`s with the original Solana size cause and invoke neither fee-payer/instruction signers nor transport send. Focused Node 24 client and typecheck pass; Node 22 focused run **100/100**; SDK aggregate passed twice (883/1). The 2026-07-21 independent senior review confirmed the size assertion precedes every signer invocation on all paths including blockhash-expiry re-sign, the transport-seam reassertion holds with a single guarded send site, the 1,232/1,233 boundary is exact with and without the CU-price prepend, and rejection is a structured `AgencError` preserving the Solana size cause (CONFIRMED-SOUND; a modifying-signer oversize regression for the post-sign reassert is recommended follow-up). Address lookup table support is tracked separately because a clear failure is not execution liveness for every valid protocol shape. |
| `PROTOCOL-BID-WIRE-LIVENESS-001`    |     High | `FIXED` (deployed 2026-07-22)         | Rust permits twenty active bids, but the production v0 client cannot serialize a worst-case distinct-account `acceptBid` above eight active bids without an address lookup table. The current mainnet read-only scan found zero books/bids on one genesis-checked provider, but independent-provider confirmation is incomplete. Closure requires a safe new-book cap that preserves legacy exits/updates, current-config migration before pause, exact serialized-size regressions, ALT-backed legacy acceptance, all generated/CLI/docs/fuzz updates, and a fresh two-provider inventory immediately before cutover. The 2026-07-21 continuation landed the client half of the verified lookup-table lifecycle: the SDK RPC transport now resolves table contents from raw base64 account bytes through the official `@solana-program/address-lookup-table` codec, rejecting missing accounts, wrong owners, and uninitialized state, with client-side exact-bijection re-validation and revert-sensitive regressions (independently senior-reviewed CONFIRMED-SOUND). The 2026-07-21 continuation then landed the founder-approved program-side redesign (docs/design/bid-accept-o1-redesign.md): `TaskBidBook` incrementally tracks its deterministic policy winner (frozen `WeightedScore` eta window recorded at book init), `accept_bid` is O(1) in accounts (tracked-winner match + exact cached-component equality + re-promotion grace after winner exits) with competitor enumeration deleted, the tracked best may sweeten but not retreat, winner exits open a grace window served by permissionless `promote_bid` / `demote_ineligible_best` cranks, and `MAX_ACTIVE_BIDS_PER_TASK` is now a pure state bound with no wire consequence. Unit argmax-equivalence, cached-winner, grace, and frozen-window regressions pass; all Rust profiles + strict Clippy + fmt, compiled integration 404/9-skips, and the full SDK chain (regenerated 101-ix client, facade, 880-pass suite, drift/build/examples) are green. **Shipped in the deployed revision-5 mainnet upgrade (2026-07-22):** the redesign removes the wire-size failure mode entirely (`MAX_ACTIVE_BIDS_PER_TASK` is now a pure state bound with no wire consequence); mainnet held zero bid books/bids at cutover, so no legacy acceptance migration was needed. |
| `PROTOCOL-DISPUTE-WIRE-LIVENESS-001` |     High | `FIXED` (deployed 2026-07-22)           | Valid assigned-resolver `resolveDispute` and `expireDispute` shapes can exceed 1,232 bytes because fixed settlement metas, dependency evidence, worker peer bundles, maximal rationale/evidence, token legs, and authority approvals compose beyond the v0 envelope. A size guard makes failure explicit but does not preserve settlement liveness. Closure requires a verified lookup-table lifecycle or an ABI redesign, exact outer-Squads sizing for mainnet authority flows, successful compiled worst-case settlement tests, rollback/recovery documentation, and independent review. Future v1/4,096-byte support is not assumed: current Solana material still describes it as an unactivated upgrade and it is not a trivial v0 switch. The 2026-07-21 continuation landed chunked settlement: rulings and expiry record the outcome O(1) (peer bundles rejected outright), deferred peers are swept one-per-transaction by the permissionless exit-gated `settle_dispute_claim` crank (defendant excluded, tombstone-structural idempotency, per-peer counter conservation), `Dispute` gained append-only peer counters plus a recorded terminal status behind the new non-terminal `SettlementPending` state, and the slash finalizers still require true terminal status. The rationale URI was already optional on-chain (hash mandatory), so hash-only rulings need no further program change. Same green evidence set as the bid row. Remaining before FIXED: independent senior review, exact outer-Squads sizing regressions for maximal authority flows, and rollback/recovery documentation. The 2026-07-21 continuation landed the client half of the verified lookup-table lifecycle: the SDK RPC transport now resolves table contents from raw base64 account bytes through the official `@solana-program/address-lookup-table` codec, rejecting missing accounts, wrong owners, and uninitialized state, with client-side exact-bijection re-validation and revert-sensitive regressions (independently senior-reviewed CONFIRMED-SOUND). **Shipped in the deployed revision-5 mainnet upgrade (2026-07-22):** chunked O(1) settlement with the `settle_dispute_claim` crank removes the wire-size failure mode; mainnet held zero active disputes at cutover. |
| `PROTOCOL-EXACT-SUFFIX-001`         |   Medium | `OPEN`           | Several Rust handlers accept surplus remaining accounts after consuming a state-dependent dependency/bid/approval prefix. Facades can therefore attach signer or writable metas that the caller reasonably believes are enforced while the program silently ignores them. Affected completion/rejection/claim/expiry and ordinary multisig paths require exact state-derived suffix lengths and non-owner signer rejection, with the explicit current-union-proposed exception retained only for `updateMultisig`. Closure requires adversarial compiled surplus-meta regressions plus SDK wire-shape coverage. |
| `PROTOCOL-CLOSE-REPEAT-001`         |   Medium | `FIXED`           | The 28-meta `closeTask` cap requires repeat cleanup, but LiteSVM retains the drained program-owned HireRecord as a zero-lamport tombstone and the second pass formerly tried to deserialize it, failing with account-discriminator mismatch. The local handler now treats only the exact zero-lamport `[255; 8]` tombstone as swept and fails closed for malformed/funded program-owned images; a separate two-child integration proves two passes, a live Task anchor, and correct listing capacity. The accidentally whole-file-formatted legacy integration test was safely restored while preserving its sole prior semantic hunk. Node 22/24 targeted tests, build, Rust unit, formatting, syntax, and diff checks pass, and both 2026-07-21 integration aggregates are green (404 pass each). The independent senior review confirmed the three-way owner/lamports/discriminator classification is fail-closed — a funded image carrying the tombstone discriminator fails deserialization and cannot be skipped — with a LiteSVM probe proving pass 2 genuinely enters the tombstone branch, and verified `marketplace.test.mjs` kept only the `taskJobSpecHash` commitment fix (CONFIRMED-SOUND; a funded-tombstone integration case is recommended follow-up). |
| `SDK-STRING-BOUNDS-001`             |   Medium | `OPEN`           | Facades generally pass caller strings to generated Borsh encoders without enforcing the protocol's UTF-8 byte limits first. Large attacker-controlled input can allocate/encode before the transaction-size guard, and multi-byte strings can pass character-count assumptions but fail on-chain. Closure requires one strict UTF-8 byte-length helper and exact per-field limits/nonempty rules across agent, task/job-spec, listing/goods/store, moderation, verification, dispute, review, and update-Option paths, with boundary/multibyte/resource regressions and current Rust-limit parity checks. |
| `SDK-REVOKE-DELEGATION-WIRE-001`    |   Medium | `OPEN`           | `revokeDelegation` currently permits recovery suffixes that continuous ownership ignores, while the orphan branch directs rent to a suffix treasury without binding it to the fixed writable authority meta. Closure requires exact branch-specific suffix rejection and canonical treasury/address aliasing equivalent to `reclaimOrphanTaskChild`, with compiled continuous/orphan/adversarial tests and facade intent coverage. |

No row above may be reclassified as fixed solely because a focused test passes;
each claim requires an independent senior verdict and the applicable aggregate
release gates on the final quiescent tree.

## Repository-controlled findings — fixed

Every row in this table received an independent senior-engineer `ACCEPT/FIXED`
verdict after the implementation and focused regressions were present.

| ID                      | Severity | Accepted claim and completed remediation                                                                                                                                                                                                                                                                                                                                     | Primary evidence                                                                                                                 |
| ----------------------- | -------: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `APC-OC-001`            |     High | Resolver assignment, revocation, and direct authority rulings allowed one authority despite an M-of-N configuration. Existing ABI-compatible remaining accounts now enforce unique configured multisig approvals on every authority path.                                                                                                                                    | `assign_dispute_resolver.rs`, `revoke_dispute_resolver.rs`, `resolve_dispute.rs`; compiled `dispute-accountable-ruling.test.mjs` |
| `APC-DEP-001`           |      Low | Deployment skip flags mixed exact parsing with JavaScript truthiness. All binary flags now share one unset/`0`/`1` parser and reject every other value.                                                                                                                                                                                                                      | `scripts/env-flags.mjs`, `env-flags.test.mjs`, deployment boundary tests                                                         |
| `ROOT-MANIFEST-001`     |   Medium | The private workspace root had accumulated npm-init publishing metadata and 510 transitive packages as direct runtime dependencies, massively expanding the declared install and supply-chain surface. The accidental fields were removed, the lock was regenerated from the reviewed workspace manifests, and policy now pins the root to its exact private workspace role. | `package.json`, root lock record, `root-manifest-policy.test.mjs`, clean `npm ci`                                                |
| `APC-COV-002`           |   Medium | The frozen canary had only one compiled path. The canary surface now has a compiled semantic matrix across all 25 exposed entrypoints and separate 11-test execution.                                                                                                                                                                                                        | `tests-integration/canary-surface.test.mjs`, CI/release canary jobs                                                              |
| `APC-COV-003`           |   Medium | The property suite modeled state without invoking the processor. It now drives real processor bytes/accounts with adversarial mutations and retained seeds while keeping the 10,000-case model gate.                                                                                                                                                                         | `tests-integration/processor-fuzz.test.mjs`, fuzz/model manifests and CI                                                         |
| `APC-COV-004`           |      Low | Unmeasured compute constants were presented as client guidance. Unsupported recommendations were removed and the documentation now distinguishes measured gates from future profiling.                                                                                                                                                                                       | `compute_budget.rs`, `docs/VALIDATION.md`                                                                                        |
| `APC-COV-005`           |   Medium | Rust advisory, license, and source policy was absent. Exact-version `cargo-audit`/`cargo-deny` gates, lock-specific policy, and a narrow expiring upstream exception now run in PR/release paths.                                                                                                                                                                            | `deny.toml`, `check-rust-supply-chain-policy.mjs`, `rust-supply-chain.yml`                                                       |
| `SDK-IDX-001`           |     High | An indexer could return an arbitrary transaction across the local-signing boundary. The SDK now strictly decodes and binds the intended program, discriminator, accounts, terms, lifetime, metadata, and instruction set.                                                                                                                                                    | `sdk-ts/src/indexer/client.ts`, adversarial `indexer.test.ts`                                                                    |
| `SDK-IDX-002`           |   Medium | Indexer requests, bodies, and pagination were unbounded. Validated deadlines/abort, streamed byte ceilings, page/item ceilings, and progress checks now fail closed.                                                                                                                                                                                                         | `indexer/client.ts`, timeout/body/pagination tests                                                                               |
| `SDK-IDX-003`           |   Medium | Successful indexer envelopes were cast rather than validated. All known response fields and nested primitives now have strict runtime validation while unknown response fields remain forward-compatible.                                                                                                                                                                    | `indexer/client.ts`, malformed-envelope matrices                                                                                 |
| `SDK-WEBHOOK-001`       |   Medium | Non-finite tolerance or clock values disabled replay-window comparisons. Finite safe-integer bounds and maximum tolerance are now enforced.                                                                                                                                                                                                                                  | `webhooks/verify.ts`, `webhooks.test.ts`                                                                                         |
| `SDK-THREAD-001`        |      Low | A receipt hashed the outbound envelope but returned a host-substituted envelope. The returned content is now byte/hash-bound to the sent envelope.                                                                                                                                                                                                                           | `task-thread/client.ts`, substitution tests                                                                                      |
| `SDK-THREAD-002`        |      Low | Task-thread identifiers could normalize paths and weak inputs entered commitments. Addresses/hashes/envelopes are validated and path segments are encoded before transport.                                                                                                                                                                                                  | `task-thread/envelope.ts`, `client.ts`, task-thread tests                                                                        |
| `SDK-WATCH-001`         |   Medium | A sole-worker task reopened after claim expiry could remain permanently deduplicated. Delivery now uses bounded transition/generation identity and proves open → claim → expire → open is emitted twice.                                                                                                                                                                     | `watch/watch.ts`, structural and compiled watch tests                                                                            |
| `SDK-WATCH-002`         |   Medium | Watcher cancellation, timeouts, and backpressure were not bounded. Iterators now own abort/return cleanup, validate timeouts, and enforce bounded queues with explicit overflow behavior.                                                                                                                                                                                    | `watch/watch.ts`, cancellation/timeout/queue tests                                                                               |
| `SDK-WATCH-003`         |   Medium | Claim discovery presented an incomplete local filter as if it were the complete on-chain claimability predicate. It now enforces every gate derivable from the fetched Task plus pinned job spec and treats worker/config/moderation/dependency/hire/stake/prior-claim gates as transaction-time checks, not a claimability guarantee.                                       | `watch/watch.ts`, `queries/helpers.ts`, claimability matrices                                                                    |
| `SDK-WATCH-004`         |   Medium | Visible task state lacked a monotonic claim-generation identity. On-chain Task reserved state now carries the discriminator used internally by the watcher so reopen transitions cannot alias earlier state; public `ClaimableTask` events intentionally do not expose it.                                                                                                   | `state.rs`, `watch/watch.ts`, watch generation regressions                                                                       |
| `SDK-SANDBOX-001`       |      Low | Sandbox attestor requests and bodies were unbounded and signature shape was weak. Bounded transport, strict endpoint/response validation, and base58 signature checks were added.                                                                                                                                                                                            | `sandbox/attest.ts`, `sandbox.test.ts`                                                                                           |
| `SDK-SURFACE-001`       |   Medium | Unknown future protocol revisions were reported fully supported. Unsupported revisions now fail closed instead of inheriting the newest known capability set.                                                                                                                                                                                                                | `facade/surface.ts`, `surface.test.ts`                                                                                           |
| `SDK-ORCH-001`          |      Low | Post-hire hosting/activation errors lacked machine-readable committed progress. Typed partial-progress errors and resume state now preserve task/signature without repeating the hire.                                                                                                                                                                                       | `orchestration/hire-and-activate.ts`, phase-recovery tests                                                                       |
| `SDK-ORCH-002`          |     High | An ambiguous hire submission could repeat a funded hire instead of reconciling finalized state. Retry now reconciles the derived task and exact intent before any resubmission and refuses ambiguity.                                                                                                                                                                        | `hire-and-activate.ts`, ambiguous-send/reconcile tests                                                                           |
| `SDK-CODEC-001`         |   Medium | General-purpose JavaScript UTF-8 codecs normalized Borsh strings, accepted replacement decoding, and collapsed lone surrogates. Generated account/instruction/event codecs now preserve BOM/NUL, reject invalid UTF-8, and reject ill-formed UTF-16 like Rust Borsh.                                                                                                         | `sdk-ts/src/generated/codecs/borshString.ts`, generator drift, `generated-borsh-string.test.ts`                                  |
| `SDK-PACK-PEER-001`     |   Medium | The exported `./testing` subpath eagerly loaded its optional native `litesvm` peer, so importing the advertised surface from a clean tarball failed when the helper was not used. Runtime peer loading is now lazy with an actionable missing-peer error, while clean ESM/CJS imports and full optional-peer sandbox execution are both package-smoked.                      | `testing/litesvm-peer.ts`, testing helpers, clean/full `pack-smoke.mjs` regressions                                              |
| `DEP-JOBSPEC-CODEC-001` |     High | Active-job-spec deployment preflight decoded Borsh strings and whitespace differently from the on-chain Rust gate. It now uses fatal byte-faithful UTF-8 and the exact Rust whitespace set.                                                                                                                                                                                  | `preflight-active-job-spec-block-scan.mjs` and its codec regressions                                                             |
| `TOOLS-VALIDATION-001`  |   Medium | Advertised tool JSON Schemas were not enforced at execution boundaries. One compiled validator now gates registry, MCP, LangChain, and CrewAI invocation with stable typed errors.                                                                                                                                                                                           | `marketplace-tools/src/tools/schema.ts`, adapters/MCP/schema tests                                                               |
| `TOOLS-SCHEMA-002`      |   Medium | Production tool schemas were incomplete or permissive. All supported tool inputs now declare bounded exact runtime schemas; unsupported keywords and extra fields fail closed.                                                                                                                                                                                               | `tools/prepare.ts`, `tools/readonly.ts`, `types.ts`, `schemas.test.ts`                                                           |
| `REACT-CACHE-001`       |   Medium | Query keys collided across deployments. Every query/invalidation key now includes a stable network/program/RPC/indexer deployment namespace.                                                                                                                                                                                                                                 | `hooks/internal.ts`, provider and hook switch tests                                                                              |
| `REACT-SIGNER-001`      |   Medium | Wallet-mutated signatures were attached to the original message. Bridges now require exact returned-message equality and verify the signature over the bytes actually submitted.                                                                                                                                                                                             | `signers/wallet-account.ts`, `wallet-adapter.ts`, signer tests                                                                   |
| `REACT-SIGNER-002`      |      Low | Concurrent per-transaction wallet calls broke single-flight wallets. Wallet Standard uses one variadic request; legacy adapters serialize while preserving order.                                                                                                                                                                                                            | signer bridge tests with single-flight fakes                                                                                     |
| `REACT-TASKSTATUS-001`  |      Low | Task-event iterators leaked and retained history across source/task changes. Cleanup now aborts and calls `return()`, and identity changes reset history.                                                                                                                                                                                                                    | `hooks/useTaskStatus.ts`, hook lifecycle tests                                                                                   |
| `REACT-TASKSTATUS-002`  |      Low | Disabled/readerless status sources still started work and polling/cancellation inputs were not validated. Source activation and timing/abort options now fail closed before work begins.                                                                                                                                                                                     | `useTaskStatus.ts`, `use-task-status-api.test.ts`                                                                                |
| `REACT-MULTIMUT-001`    |      Low | Aggregate mutation state returned the first settled action rather than the latest invocation. Hooks now track a monotonic invocation identity.                                                                                                                                                                                                                               | affected mutation hooks, `multi-mutation-state.test.tsx`                                                                         |
| `REACT-WALLET-001`      |      Low | An explicitly disconnected adapter fell back to the provider signer. An explicit adapter now exclusively owns signer state; fallback occurs only when no adapter was supplied.                                                                                                                                                                                               | `useWalletSigner.ts`, hook tests                                                                                                 |
| `REACT-MODAL-001`       |   Medium | A rejected checkout confirmation created a derived unhandled rejection. Cleanup now observes the original promise without spawning an unhandled rejecting chain.                                                                                                                                                                                                             | `HireCheckoutModal.tsx`, double-submit/rejection tests                                                                           |
| `REACT-PACK-PEER-001`   |   Medium | The advertised Tailwind preset required `tailwindcss/plugin` at module load even though Tailwind was not declared, breaking clean installed-tarball imports. The preset now exports Tailwind v3's dependency-free plugin descriptor directly; generic package smoke loads the clean export and a pinned Tailwind compile exercises its utilities.                            | `agenc-tailwind-preset.cjs`, `tailwind-preset.test.ts`, generic release pack smoke                                               |
| `WORKER-WALLET-001`     |   Medium | Wallet bytes were silently coerced and unsafe files were accepted. A shared loader now requires a private owned regular file, exact 64-byte integer array, and matching derived key.                                                                                                                                                                                         | `agenc-worker/src/wallet.ts`, `wallet.test.ts`                                                                                   |
| `WORKER-UPLOAD-001`     |      Low | Upload responses and result URIs were unbounded. Streaming size/time limits and strict URI/response validation now protect submission preparation.                                                                                                                                                                                                                           | `worker/src/result.ts`, result/runtime tests                                                                                     |
| `WORKER-LOCK-001`       |      Low | PID reuse could keep a stale worker lock alive. Lock ownership now binds an exact process identity rather than PID alone.                                                                                                                                                                                                                                                    | worker state/recovery tests                                                                                                      |
| `WORKER-STATE-001`      |   Medium | State paths permitted unsafe ownership, links, or size before atomic persistence. Directory/file ownership, modes, leaf type, no-follow, bounds, fsync, and atomic generation publication are enforced.                                                                                                                                                                      | `worker/src/state.ts`, adversarial `state.test.ts`                                                                               |
| `WORKER-FUND-001`       |   Medium | Registration and recurring claims used stale/incomplete stake, rent, deposit, and fee requirements. Funding plans now derive current on-chain/configured obligations and expose exact shortfalls before signing.                                                                                                                                                             | `worker/src/runtime.ts`, runtime and recovery tests                                                                              |
| `CLI-PROMOTE-001`       |     High | Go-live audit accepted non-production chains. Promotion now binds the exact mainnet genesis, program, executable state, and release evidence.                                                                                                                                                                                                                                | `agenc-cli/src/promote.ts`, promote tests                                                                                        |
| `CLI-PURGE-001`         |   Medium | A stale localnet PID could terminate an unrelated reused process. Purge/status/down now require the exact recorded executable, argv, cwd, owner, and start identity.                                                                                                                                                                                                         | CLI localnet tests and shared identity helper                                                                                    |
| `CLI-CONFIG-001`        |   Medium | Invalid optional configuration silently defaulted and unknown keys were accepted. Exact runtime schemas now produce path-specific errors for every present-invalid or extra field.                                                                                                                                                                                           | `agenc-cli/src/config.ts`, `config.test.ts`                                                                                      |
| `CLI-WALLET-001`        |   Medium | Promotion did not validate wallet file type, ownership, mode, bytes, or address. It now uses the hardened shared wallet loader in read-only validation mode.                                                                                                                                                                                                                 | CLI promote and worker wallet tests                                                                                              |
| `LOCALNET-IDENTITY-001` |   Medium | Repository localnet lifecycle control could signal an unrelated PID-reused process. Private atomic identity records and exact re-attestation are required before status or signal operations.                                                                                                                                                                                | `localnet-process-identity.mjs`, localnet identity tests                                                                         |
| `MOD-CANON-001`         |      Low | Non-finite numbers canonicalized to JSON `null`. Canonicalization now rejects non-finite values instead of creating collisions.                                                                                                                                                                                                                                              | moderation `src/index.ts`, `canon.test.ts`                                                                                       |
| `MOD-CANON-002`         |      Low | Accessors, proxies, cycles, exotics, sparse arrays, and non-JSON values could violate determinism or execute user code. Canonicalization now admits a bounded data-only JSON domain and rejects every ambiguous shape.                                                                                                                                                       | moderation adversarial canonicalization matrix                                                                                   |
| `TEMPLATE-CHECKOUT-001` |   Medium | The funded checkout template lacked production replay, size, rate, spend, and authentication controls. Generated code now authenticates before body parsing, enforces idempotency and bounded budgets, and refuses production without an explicit policy.                                                                                                                    | `agenc-cli/src/templates.ts`, generated-template tests/builds                                                                    |
| `TEMPLATE-ROLLBACK-001` |     High | Generated funded checkout rollback/idempotency races could lose reservations or repeat spend intent. Durable state transitions now use generation/ownership checks and reconcile ambiguous funding before retry/rollback.                                                                                                                                                    | template concurrency/failure tests and generated Next build                                                                      |
| `DEP-NPM-001`           |     High | Integration/operator tooling carried the vulnerable `bigint-buffer` path. The unused SPL helper dependency was removed, required token helpers were local and bounded, all advisories are now denied at low severity, and the integration runtime audit is zero.                                                                                                             | `tests-integration/package-lock.json`, `spl-token-legacy.mjs`, `audit-tests-integration.mjs`                                     |
| `REL-STATE-001`         |   Medium | Release publication was neither resumable nor immutable. An explicit inspected state machine now verifies registry integrity/provenance and immutable asset digests before safe resume, publish, or finalization.                                                                                                                                                            | `release-state.mjs`, state/provenance/SBOM tests, release workflow                                                               |
| `CI-FIXTURES-001`       |   Medium | Next SSR and Playwright fixtures were outside CI and lacked tracked locks. Clean-install SSR/hydration/browser/checkout gates and all independent locks are now tracked in required/nightly workflows.                                                                                                                                                                       | `react-fixtures.yml`, fixture package locks and smoke configs                                                                    |
| `TOOLCHAIN-001`         |   Medium | Node/npm and Rust release toolchains floated and advertised floors were untested. Exact release versions, engine enforcement, Rust MSRV, and Node-min compatibility jobs are now declared and asserted.                                                                                                                                                                      | `.node-version`, `.nvmrc`, `rust-toolchain.toml`, compatibility workflow                                                         |
| `TOOLCHAIN-RUNNER-001`  |   Medium | CI did not pin/reproduce the release runner family or all advertised compatibility floors. Every hosted job now selects Ubuntu 24.04 and dedicated jobs exercise Rust 1.82 and Node 22.23.1.                                                                                                                                                                                 | workflow policy tests and `.github/workflows/compatibility.yml`                                                                  |
| `RELEASE-VERSION-001`   |   Medium | Moderation wire semantics changed under an unchanged candidate version. The coordinated candidate versions and changelogs now identify the new surface without claiming publication.                                                                                                                                                                                         | package manifests, changelogs, `release-train.json`                                                                              |
| `DOC-SCALE-001`         |      Low | Scale documentation described the shipped four-worker dispute cap as future work. Current invariants and historical measurements are now separated and machine-checked.                                                                                                                                                                                                      | `docs/SCALE_COST_MODEL.md`, scale documentation test                                                                             |
| `ART-DIST-001`          |      Low | Tracked tools/MCP distribution output had no drift gate. CI/release now rebuild and require a clean deterministic diff; package smoke executes the declared surface.                                                                                                                                                                                                         | workflow gates, tracked dist, package smoke                                                                                      |
| `SUPPLY-SCRIPTS-001`    |     High | npm lifecycle scripts could execute without a complete reviewed policy. All six install roots now default-deny scripts and engines; an exact name/version/registry/integrity policy covers every script-bearing package and rejects stale/broadened decisions.                                                                                                               | `.npmrc` files, `check-npm-install-scripts.mjs`, 30 focused policy tests                                                         |
| `REL-TRAIN-001`         |   Medium | Four public packages lacked a release route and dependency order was advisory. All nine candidates now form an exact acyclic release DAG with verified prerequisite version, dist-tag, integrity, and provenance gates.                                                                                                                                                      | `release-train.json`, release policy/state workflow tests                                                                        |
| `REACT-E2E-FIXTURE-001` |   Medium | Checkout fallback silently passed bootstrap failure and leaked local validator state. The opt-in gate distinguishes true skip from failure, binds ownership, and performs transactional cleanup on every setup/teardown path.                                                                                                                                                | `localnet-e2e-gate.ts`, sandbox lifecycle tests, Playwright setup/teardown                                                       |
| `CLI-IMPORT-001`        |   Medium | Importing the exported CLI subpath executed the process and could exit. The importable command surface is pure; only dedicated executables run the process, and the alias executable imports and calls `runCliProcess` from the pure `/cli` API.                                                                                                                             | `agenc-cli/src/cli.ts`, `src/bin.ts`, alias bin, `entrypoint.test.ts`                                                            |
| `REL-PACK-SMOKE-001`    |   Medium | Release smoke skipped direct-string exports and lost nested resolver conditions. It now traverses direct/nested import/require/bin routes and rejects module output, exit-code changes, and `process.exit` while permitting bin help.                                                                                                                                        | `release-pack-smoke.mjs`, 8 focused adversarial tests                                                                            |

## Post-audit follow-up findings — fixed

After the original 78-claim closeout, GitHub CodeQL and a second independent
release rehearsal found the additional rows below. They are intentionally not
folded into the historical baseline count. No alert was dismissed or suppressed:
each source finding has a bounded structural implementation and an adversarial
regression. GitHub will continue to show alerts 1–10 against the old public
`main` commit until this exact tree is pushed and a fresh CodeQL run closes them.

| ID                              | Severity | Accepted follow-up claim and completed remediation                                                                                                                                                                                                                                                                                                                                                                                      | Verification evidence                                                                                                                         |
| ------------------------------- | -------: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `CODEQL-01`                     |   Medium | Alert 1 (`js/stack-trace-exposure`) found that the public starter activation route reflected arbitrary validation exceptions. It now returns a stable generic 400 response and never exposes dependency messages or stacks.                                                                                                                                                                                                             | starter activation adversarial tests                                                                                                          |
| `CODEQL-02`                     |      Low | Alert 2 (`js/bad-code-sanitization`) found a worker test interpolating a filesystem path into `node -e` source. Executable source is now static and every path/script value is passed as an argv data argument.                                                                                                                                                                                                                         | worker executor descendant/process-group regression                                                                                           |
| `CODEQL-03`                     |   Medium | Alert 3 (`js/incomplete-url-substring-sanitization`) found the readiness gate accepting any schema string containing `json-schema.org`. Hosted schemas now require the exact supported 2020-12 dialect URI and reject lookalike, credentialed, or alternate values.                                                                                                                                                                     | readiness schema-dialect adversarial matrix                                                                                                   |
| `CODEQL-04`                     |   Medium | Alert 4 (`js/incomplete-sanitization`) found scoped npm attestation URL construction replacing a slash without proving package structure. Canonical package identity is validated first; scoped names must contain exactly one separator, which is then encoded structurally.                                                                                                                                                           | provenance malformed-scope and canonical-identity tests                                                                                       |
| `CODEQL-05`                     |   Medium | Alert 5 (`js/polynomial-redos`) found chained package-name normalization regexes on CLI-controlled input. A single linear structural scan now preserves the exact former normalization semantics and caps output at npm's 214-character limit.                                                                                                                                                                                          | million-character regression plus 20,004-case differential fuzz                                                                               |
| `CODEQL-06`                     |   Medium | Alert 6 (`js/polynomial-redos`) found trailing-punctuation regex work on uncontrolled worker diagnostics. Trailing recovery was removed: a URL candidate is parsed and fully redacted, or replaced wholesale. No ambiguous suffix is returned.                                                                                                                                                                                          | malformed, oversized, punctuation-only, Unicode, IPv6, and prose-wrapper redaction tests                                                      |
| `CODEQL-07`                     |     High | Alert 7 (`js/polynomial-redos`) found the broad URL replacement regex. It also enabled credential/path suffix leaks at quote, angle, whitespace, invalid-scheme-prefix, punctuation, and WHATWG-repaired special-scheme boundaries without literal `://`. A linear scheme scanner recognizes hierarchical schemes plus every WHATWG special scheme (file, FTP, HTTP(S), and WS(S)), consumes the ambiguous remainder, and fails closed. | worker redactor matrix, accepted-config-to-diagnostic coverage, 5 MiB linearity probe, and stable structured AgenC-URI diagnostic regressions |
| `CODEQL-08`                     |   Medium | Alert 8 (`js/polynomial-redos`) found blockhash-expiry matching against arbitrary thrown strings. Classification now caps inspected text and uses fixed-term structural matching plus one linear per-line scan.                                                                                                                                                                                                                         | SDK blockhash bounded/repeated-token/adversarial tests                                                                                        |
| `CODEQL-09`                     |   Medium | Alert 9 was the second unsafe call site of the same blockhash expression (nested error messages). Both direct strings and cause-chain messages now use the one bounded structural classifier.                                                                                                                                                                                                                                           | SDK direct, nested, multiline, and over-cap classification tests                                                                              |
| `CODEQL-10`                     |      Low | Alert 10 (`js/polynomial-redos`) found trailing-slash removal on a caller-provided receipt base URL. A reverse character loop now trims slashes in linear time.                                                                                                                                                                                                                                                                         | 100,000-slash receipt URL regression                                                                                                          |
| `FOLLOW-NODE-001`               |     High | Node 20 is EOL and the resolved dependency graph no longer clean-installs at the old 20.18 floor. Every release/install root and minimum-runtime CI gate now requires exact Node 22.23.1 or newer; release remains pinned to Node 24.18.0/npm 11.18.0.                                                                                                                                                                                  | isolated exact-Node-22 clean install, every workspace build/typecheck, audit 0, toolchain policy                                              |
| `FOLLOW-CI-BUILD-001`           |   Medium | Clean SDK/React-fixture CI typechecked the CLI before building its worker declaration dependency. Both workflows now build worker before the downstream CLI gate, with ordering locked by policy tests.                                                                                                                                                                                                                                 | clean workspace builds and release-workflow regression                                                                                        |
| `FOLLOW-CI-IDL-001`             |   Medium | IDL Drift and protocol-tag release cache-miss Anchor builds lacked native `libudev`/`pkg-config` dependencies and could fail before verification. Both jobs install them only on cache miss and assert ordering before `cargo install`.                                                                                                                                                                                                 | workflow policy regression for both isolated jobs                                                                                             |
| `FOLLOW-CI-STATE-001`           |   Medium | The worker lock-race test waited for child `exit` before pipe closure and later treated one legitimate reaper-contention outcome as an unexpected failure. It now awaits `close`, validates child status/type, and accepts exactly both safe contention outcomes.                                                                                                                                                                       | state suite stress-tested 50 consecutive times; full worker suite 253/253                                                                     |
| `FOLLOW-OPS-DOC-001`            |   Medium | Current README/security/threat/readiness/verifiable-build/ledger text still described PVR, governance, immutable releases, and tag protection as disabled after those controls were enabled. Current claims now match authenticated live evidence; historical records stay dated.                                                                                                                                                       | stale-claim scan and readiness tests                                                                                                          |
| `FOLLOW-SEC-META-001`           |   Medium | The checked-in `security.txt` remained an intentionally invalid template after GitHub PVR was enabled. It is now active RFC 9116 metadata naming only the verified private-advisory channel; the untested mailbox stays unadvertised.                                                                                                                                                                                                   | parser/readiness regressions; canonical host deployment remains `HOST-SECURITY-001`                                                           |
| `REL-PACK-JSON-001`             |     High | `npm pack --json` mixed noisy `prepack` stdout into the JSON file consumed by smoke/SBOM/state gates, so protocol/SDK/React releases would fail before publication. Release now runs reviewed prepack explicitly, then captures `npm pack --ignore-scripts --json` only.                                                                                                                                                                | exact Node-24/npm-11 rehearsal for all nine byte-identical packages; JSON/SRI/policy verification                                             |
| `FOLLOW-CI-WORKER-DEP-001`      |   Medium | The worker lock-race regression launched child processes with the `tsx` loader but did not declare `tsx` in the worker workspace. A developer machine could pass through an unrelated installation while a clean CI install failed. The worker now owns an exact development dependency, and the reviewed release-train integrity was rebound to the newly reproduced tarball.                                                          | clean exact-Node-24 install; worker 253/253; byte-identical dual pack with release-train SRI                                                  |
| `FOLLOW-CI-SSR-001`             |   Medium | The SSR proof terminated an `npx` wrapper without awaiting the actual Next server, so its descendant could retain port 3100 and block the following browser smoke. The probe now launches the local Next binary directly, waits for graceful shutdown, and fails closed through a bounded forced-stop fallback.                                                                                                                         | production fixture build followed by two consecutive SSR proofs on port 3100; protected React fixture CI                                      |
| `FOLLOW-CI-CLI-TEMP-001`        |      Low | Four CLI generator tests assumed each npm workspace owned a physical `node_modules` directory. Hoisted clean installs need not create that directory, so the protected SDK E2E job failed before exercising the generated modules. Test sandboxes now locate the nearest real dependency root and retain the same isolated nested mock graph.                                                                                           | CLI 115/115 with the package-local `node_modules` directory absent; protected SDK E2E CI                                                      |
| `FOLLOW-SDK-GPA-COMMITMENT-001` |   Medium | The raw-RPC query transport omitted commitment, so consumers that combined its provider-default gPA results with explicit confirmed child reads could construct a mixed-commitment snapshot. The transport now defaults to the SDK-wide `confirmed` commitment and exposes an explicit override for finalized callers.                                                                                                                  | request-shape regression for default and override; SDK tests/typecheck/build; byte-identical dual pack rebound in `release-train.json`        |

## Conditional findings — intentionally blocked

| ID                        | Severity | Current safe state and mandatory closure condition                                                                                                                                                                                                                                   |
| ------------------------- | -------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `APC-ZK-001`              |     High | Production excludes private-ZK entrypoints and rejects new private tasks. Do not activate until a reproducible guest proves task semantics and golden/adversarial proof, replay, journal, image, and nullifier cases pass.                                                           |
| `APC-COV-001`             |     High | Host-only private-ZK coverage cannot establish SBF verifier/router correctness. Activation requires a feature-enabled SBF build plus compiled proof/ABI/stack/compute coverage.                                                                                                      |
| `SDK-DELIVERY-001`        |      Low | Delivery-v1 ciphertext authenticity does not itself bind all caller context. Versioned manifests and strict helpers are present, but integrations must validate the signed manifest/context; any new wire version must authenticate metadata rather than silently changing v1.       |
| `MOD-SEMANTIC-001`        |   Medium | Canonicalization v2 covers creator-controlled worker-visible semantics while c14n-v1 remains interoperable. A production moderation backend must explicitly support/test v2; structured specs otherwise fail closed instead of being represented as semantically reviewed.           |
| `REACT-CHAIN-001`         |      Low | Provider-aware wallet bridges bind the requested Solana chain. Opaque custom signers cannot be introspected; applications using one must attest the provider deployment/chain contract or the flow remains unsupported.                                                              |
| `DEP-NPM-DEPRECATION-001` |      Low | The dev-only fixture retains deprecated `whatwg-encoding@3.1.1` through jsdom 25. It is absent from every published runtime tree and has no advisory. A jsdom 29 update is not yet safe: it introduces an unapproved license and belongs in a separately reviewed fixture migration. |

## External findings closed after the audit

These operator-owned controls were enabled and independently re-read from the
public repository after the original read-only audit. They are retained here so
the claim history is not lost.

| ID                     | Severity | Verified closure                                                                                                                                                                                                                                                                                                                                |
| ---------------------- | -------: | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OPS-GH-002`           |     High | Strict `main` protection, protected release-tag rules, two eligible `production-release` environment reviewers with self-review blocked, constrained Actions, CODEOWNERS enforcement, immutable releases, and administrator/force-push/deletion restrictions are configured remotely. The live readiness gate continuously rechecks this state. |
| `SUPPLY-ASSURANCE-001` |   Medium | Dependabot security updates, secret scanning, push protection, validity/non-provider checks, and code scanning are enabled. The authenticated audit found zero open Dependabot or secret-scanning alerts; current source must still earn a clean CodeQL run under `OPS-GH-001` before release.                                                  |

## External findings — release remains blocked

These rows are not repository defects left unfixed. They are independently
confirmed deployment, publication, hosted-service, upstream, or mainnet actions
whose completion must be re-verified against the exact release commit.

| ID                   | Severity | Read-only result and required authorized action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------------------- | -------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `OPS-GH-001`         |     High | Public CI/release workflows are enabled and the previously pushed remediation passed its required runs. Land the final follow-up tree and obtain fresh protected green runs, including CodeQL, before release.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OPS-SYNC-001`       |     High | The original remediation is public on `main`; the final follow-up hardening tree is still local. Land the exact senior-reviewed commit through the protected path, then require the live readiness gate to bind local and remote bytes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `HOST-SECURITY-001`  |   Medium | GitHub Private Vulnerability Reporting is enabled and the repository now carries active metadata naming that confirmed private channel. Deploy the exact `security.txt` at both canonical hosts and verify the plain-text responses.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `HOST-SCHEMA-001`    |   Medium | Advertised schema URLs return landing HTML or 404 rather than versioned JSON. Deploy the exact schemas with correct content type/cache/integrity monitoring, or keep documentation on resolvable package-local references.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `REL-CUTOVER-001`    |     High | Repository release gates now bind a stable publish to finalized revision, executable hash, verified build, and independent RPC evidence. The stable train must remain blocked until an authorized revision-5 mainnet cutover satisfies those checks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `NPM-PROVENANCE-001` |   Medium | Existing immutable public versions have registry integrity/signatures but no npm OIDC attestations. Publish the next coordinated train only through the provenance rail and verify attestations before assigning the public channel.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `CARGO-MAINT-001`    |      Low | `bincode@1.3.3` is unmaintained through the Anchor/Solana graph and has no patched compatible release. The exact lock-specific exception is owned and expiring; remove it as soon as upstream permits migration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `EXT-MAINNET-001`    |     High | **EXECUTED 2026-07-22.** The 2,285,640-byte `5112216b…` identity (and the pre-close-task-fix 2,284,496-byte `79f55a68…`) were superseded by the O(1) bid-accept / chunked-settlement redesign; the deployed revision-5 SBF is **2,303,608 bytes**, SHA-256 `049a66e30da166c1e02ee379993425c32386f774fd9ff8861153e21900b496f2`. Live payload capacity was **2,183,224 bytes**, so the top-level legacy extension was exactly **120,384 bytes**; ProgramData `E5w1ZkgC5ysWWBECHHzqsL4s6dDUoyWBnUMRptm5cEAw` grew to 2,303,653 account-data bytes. The 120,384-byte extension and the later-slot Squads v4 2-of-3 program upgrade (execute tx `5iZiPGmU5pYSGEaNBHkTR1cpGhmGtffGp8ZSufD71ActwNyTSt4cFkLoGqiucFmQ3DveSRthCK5fuZHb3NB7Smh7`) both executed on 2026-07-22. Post-upgrade: the operator-accepted completion-bond race (`AGENC_ACCEPT_BOND_RACE`) over 89 revision-4 bond-post-eligible third-party tasks live through the upload window did **not** materialize — the post-upgrade completion-bond inventory verified **zero** live bonds. |

Former 2,284,384-byte ProgramData/Buffer lengths and their rent/top-up figures
are superseded and are not transaction inputs. The final Buffer is 2,284,533
bytes with a currently agreed 15,901,240,560-lamport rent floor; its intentional
rent difference from ProgramData is 55,680 lamports. All rent, balance, and payer
figures remain fresh-preflight inputs because the rent RPC has no context slot.

## Rejected and merged hypotheses

The independent senior review retraced all 22. A rejection means the named bug
was not established; it is not a blanket guarantee about the surrounding code.

- `RJ-001` — No untrusted production escrow-redirection/account-substitution
  path survived canonical owner/PDA/ATA and positive-alias tests.
- `RJ-002` — No unchecked audited-flow payout, fee, bond, slash, goods, or skill
  arithmetic overflow survived checked/u128 and release-overflow tracing.
- `RJ-003` — No accepted-bid principal/bond leak or double settlement survived
  compiled terminal-path coverage.
- `RJ-004` — Mixed legacy/new marketplace snapshot fallback is not reachable
  through normal creation/migration; stamped active terms bind payees.
- `RJ-005` — Governance double-vote/quorum bypass was disproved by
  authority-keyed votes, immutable snapshots, distinct identities, and execution
  rechecks.
- `RJ-006` — Arbitrary migration/reallocation was disproved by owner, PDA,
  discriminator, size, threshold, idempotence, and compiled legacy-layout gates.
- `RJ-007` — Pause/exit asymmetry is intentional: pause blocks new work while
  tested settlement and credible exits remain available under version checks.
- `RJ-008` — No second deployment-signing defect survived genesis, executable,
  ProgramData authority, finalized/context-slot, snapshot, pause, multisig, and
  immutable-material controls.
- `RJ-009` — Worker job-spec SSRF was disproved by redirect, credential, private
  address, DNS rebinding, byte, and wall-clock bounds.
- `RJ-010` — Task-thread response exhaustion was disproved by its existing
  deadline and streamed-byte controls.
- `RJ-011` — The broad worker-state race claim was narrowed; the verified pieces
  became `WORKER-LOCK-001` and `WORKER-STATE-001`.
- `RJ-012` — Cross-system atomic hire was not a valid requirement; the concrete
  recovery defects were promoted to `SDK-ORCH-001` and `SDK-ORCH-002`.
- `RJ-013` — Uploader-URI integrity was merged into `WORKER-UPLOAD-001`; on-chain
  integrity derives from the proof hash/result-data commitment, not the URI.
- `RJ-014` — Initial rejection was overturned and promoted to
  `MOD-SEMANTIC-001`.
- `RJ-015` — Initial rejection was overturned and promoted to
  `REACT-SIGNER-002`.
- `RJ-016` — Alleged duplicate generated-template declarations were a truncated
  output artifact; the actual source contained no duplicates.
- `RJ-017` — No concrete export/import, HTML escaping, attribution-link,
  referrer, or generated-code-drift defect remained on the named surfaces.
- `RJ-018` — Point-in-time review found no tracked secret, artifact/codegen/dist
  drift, or active Rust vulnerability; continuous controls were still added.
- `RJ-019` — Current npm packages are registry-integrity signed; only OIDC
  provenance is absent, tracked separately as `NPM-PROVENANCE-001`.
- `RJ-020` — Checked-in Vercel MCP configuration was quarantined absent evidence
  of auto-connect, credential forwarding, or an untrusted endpoint.
- `RJ-021` — Mutable solana-verify-container concern was quarantined absent proof
  that the selected tag currently resolved nondeterministically.
- `RJ-022` — Alleged concurrent iterator `maxQueue` overflow was disproved:
  JavaScript run-to-completion plus the loop's repeated capacity check prevents
  the claimed interleaving.

## Independent senior verification

Verification happened in two explicit stages:

1. A fresh senior-review agent, separate from the three audit claim authors,
   read the full initial ledger, retraced every claim and rejection, reproduced
   adversarial cases, refined scope/severity, and authorized implementation only
   for accepted claims.
2. After the original remediation, a fresh final senior pass reviewed the
   resulting control flow and focused regressions for the then-current rows. Its
   all-nine double-pack, matching `release-train.json` SRIs, and focused **46/46**
   release/supply result are historical evidence for that superseded tree. The
   revision-5 continuation subsequently changed SDK, React, worker, CLI, and
   localnet sources and opened the `OPEN`/`PENDING-VERIFY` rows above. Those rows
   require new independent closure and the entire package train must be rebuilt
   and rebound from the eventual quiescent tree.

Compatibility rulings retained by that review: keep the on-chain ABI by using
existing remaining accounts for threshold approval; do not silently retrofit
AAD into delivery v1; keep moderation c14n-v1 stable while adding semantic v2;
allow unknown response fields only where all known fields remain strictly typed;
and never represent a remote/operator condition as locally completed.

## Original audit verification snapshot and former follow-up (superseded)

The following gates passed on the then-remediated tree. They are retained as
historical evidence, not as authority for the current revision-5 candidate. The
final continuation evidence is recorded in `docs/VALIDATION.md`; it must not be
combined arithmetically with this superseded snapshot.

- Rust: production **524/524**, `validation-timings` **524/524**, private-ZK
  host **549/549**, canary **321/321**, and model/property **77/77** with
  `PROPTEST_CASES=10000`; strict Clippy passed for all supported profiles.
- Rust compatibility/supply: Rust **1.82** default/private/canary gates passed;
  unsupported bare no-default build failed as required; exact pinned
  `cargo-audit`/`cargo-deny` policy passed for all three locks.
- Coverage: **524/524** tests; functions **48.754842%**, lines **42.518073%**,
  regions **33.902933%**, all above the committed nonzero ratchet.
- Compiled program: production wildcard **408 total = 399 pass + 9 explicit
  canary-profile skips**; separate canary suite **11/11**.
- Deployment/preflight scripts were **231/231** at the original closeout.
  The final follow-up is **239/239**; all `scripts/*.test.mjs` are **355/355**
  (the latter includes that deployment/preflight subset).
- Node **24.18.0** / npm **11.18.0**: every workspace built and typechecked;
  The original closeout was **1,431 total = 1,429 pass + 2 intentional
  environment-gated skips**. The final follow-up is **1,446 total: 1,444 pass
  and 2 skips** before the final commitment regression. The focused SDK rerun
  is now **660 = 659 pass + 1 skip**; React **272 = 271 + 1 skip**; tools
  98, MCP 35, moderation 15, worker 253, and CLI 115 all passed.
- Node **22.23.1** / npm **11.18.0** compatibility: all workspace builds,
  typechecks, SDK/MCP examples, and clean installs passed. npm 11.7 was rejected
  by the declared engine as intended.
- npm supply: clean installs passed at all six independent roots with zero
  vulnerabilities; 14 lifecycle-script records were exactly classified; the
  exact install policy and six-lock, 1,616-package license policy passed.
- Fixtures: starter **28/28** plus ten repeated UI runs without act warnings;
  checkout and Next SSR clean builds; hydration smoke **1/1**; full browser
  checkout **1/1**; enabled localnet checkout **1/1** on two clean runs with
  owned teardown and no residual `.localnet` state.
- Artifact/codegen: Anchor production build, stack gate, deterministic SDK
  generation/drift, IDL reference, protocol artifact/package sync, and built
  artifact checks passed.

Revision-5 build lineage and deployed identity:

- Production SBF: **2,284,496 bytes**, SHA-256
  `79f55a6847fa954ef9427fd09c0d84f4f363857e2b0c4753b03ff02802e8684d`.
  Two isolated quiescent production builds, the canonical deploy `.so`, and the
  SDK testing artifact are byte-identical; the Rust-input diff SHA-256 remained
  `9deb384b8e4d3f834997a3b5414c8f109ba89ff39bb2a876e2ca6a7f1df53ff8`.
  Superseded 2026-07-20/21: the close-task repeat-cleanup fix moved the
  production SBF to **2,285,640 bytes**, SHA-256
  `5112216b5ebdeeed3d83d6fe987ea847cfe44e1e1c7f8d3caa9a5169aef2a1cb`,
  reproduced byte-identically across three builds (one before and two after the
  canary-only `utils/version.rs` cfg gate, proving that gate codegen-neutral for
  the production profile). The extension rail, its tests, and the deploy docs
  were rebound to this identity on 2026-07-21.
  **Superseded again by the O(1) bid-accept / chunked-settlement redesign and
  deployed 2026-07-22 as the live revision-5 SBF: 2,303,608 bytes, SHA-256
  `049a66e30da166c1e02ee379993425c32386f774fd9ff8861153e21900b496f2`.**
- Canonical IDL (deployed): **101 instructions / 43 accounts / 102 events / 405
  errors**, SHA-256
  `8cfd094dc356f88678ba712a8a167a9fcd94cf3c33852ec1092a7a3ff491a82e` (the earlier
  98-instruction `8711c66…` candidate IDL predates the O(1) redesign).
- The SDK testing fixture is byte-identical to the reproduced production SBF.

The program is deployed to mainnet (revision 5, 2026-07-22); package publication
and the remaining `EXTERNAL` rows are release conditions with their exact closure
criteria above.
